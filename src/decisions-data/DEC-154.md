# DEC-154: Shell chrome: admin SPA gets a catch-all NotFound route; a Sign out control (POST /logout then location to /login) appears in the admin nav and in the portal SSR header

## Amendment (wave 91): sign-out is ONE exported contract in app/src/lib/api.ts — a surface may RENDER the frame's Sign out, but no surface mints a second POST /logout

ReviewerQueue.tsx:68 hand-copies App.tsx's page-private signOut() (POST /logout + x-chq-csrf + location.assign('/login')), and its own comment admits it ('flagged as a design gap for a later wave'). That copy is what fails app/src/one-api-client.scan.test.ts (DEC-013: api.ts is the only fetch wrapper). The frame is not the problem: Chautauqua Review.dc.html:442 and :821 both DRAW a Sign out in the phone dock, and ReviewerQueue.phoneFrame.render.test.tsx:158 requires it. So the contract, not the control, is what must be singular: signOut() moves into app/src/lib/api.ts, App.tsx and ReviewerQueue both call it, and the second /logout literal disappears. This also settles the standing 'later wave' note in the source rather than deferring it again.
