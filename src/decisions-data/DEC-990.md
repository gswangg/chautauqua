# DEC-990: /e/:slug/gallery is the GRID VIEW of one Speakers surface, not a nav destination

## Amendment (wave 91): the docs article's 390 H1 is the frame's 28px literal — the SPA's --chq-type-page-title-phone-drill (25px) is a different register and never substitutes for it

src/routes/docs-site.css.ts's phone block replaced .chq-docs-article-head h1's literal with var(--chq-type-page-title-phone-drill), which app/src/styles.css:88 defines as 25px. Chautauqua Docs.dc.html:175 draws font-size:28px. That is three failures at once (public-page-title-register.scan ×2, account-docs-phone-frames, and the register's own docs row of index 40/30 · article 38/28). A shared token is only shared when both surfaces are in the same register; the SSR docs site is not on the SPA shell's drill scale, and substituting the token silently moved a customer-facing heading by 3px past a register the scan exists to hold.

## Amendment (wave 92): the docs article H1 at 390 is the frame's literal 28px, not the SPA shell's phone-drill token

src/routes/docs-site.css.ts once declared `.chq-docs-article-head h1 { font-size: var(--chq-type-page-title-phone-drill) }`, which resolved to 25px, while docs/design/Chautauqua Docs.dc.html:175 draws `font-size:28px` inside the 390 frame. Fixed per this ruling: src/routes/docs-site.css.ts:274 now declares `.chq-docs-article-head h1 { font-size: 28px; }`. Three tests failed on this one substitution (account-docs-phone-frames, and both halves of public-page-title-register). A lane reached for the shell's back-linked-drill token because the docs article IS a drill-in, but the register table already records the docs article as its own row (38/28) precisely because the customer-facing docs site does not share the admin SPA's H1 register. The .dc.html line is the geometry authority: declared the literal 28px.

## Amendment (wave 97): The docs article's phone chrome is its own two-item band, and Contents is a :target disclosure over the nav that is already rendered

`Chautauqua Docs.dc.html:167-169` replaces the desktop brandrow entirely at 390 with one band — `‹ Docs` left and `Contents` right-flushed via `margin-left:auto`, both `min-height:44px` — and `:201`/`:202` draw the pager's two controls reading the generic `‹ Previous` / `Next ›`, not the article titles `docs-site.tsx:331-338` renders at every width. The docs site ships no client JS and this ruling does not add any: the `Contents` control is an anchor to the `<nav class="chq-docs-nav">` `DocsArticlePage` ALREADY renders at docs-site.tsx:271 (merely hidden at phone by docs-site.css.ts:285's `display: none;`), revealed by a `#chq-docs-nav:target { display: block; }` rule inside the sheet's terminal phone block. A second nav list would be the duplicate-vocabulary trap DEC-613 names; a JS drawer would be a new subsystem for a disclosure CSS can do. The pager keeps ONE element per direction carrying both labels, one hidden per width — never a second pager — so a phone reader and a desktop reader always follow the same link.
