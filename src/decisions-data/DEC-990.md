# DEC-990: /e/:slug/gallery is the GRID VIEW of one Speakers surface, not a nav destination

## Amendment (wave 91): the docs article's 390 H1 is the frame's 28px literal — the SPA's --chq-type-page-title-phone-drill (25px) is a different register and never substitutes for it

src/routes/docs-site.css.ts's phone block replaced .chq-docs-article-head h1's literal with var(--chq-type-page-title-phone-drill), which app/src/styles.css:88 defines as 25px. Chautauqua Docs.dc.html:175 draws font-size:28px. That is three failures at once (public-page-title-register.scan ×2, account-docs-phone-frames, and the register's own docs row of index 40/30 · article 38/28). A shared token is only shared when both surfaces are in the same register; the SSR docs site is not on the SPA shell's drill scale, and substituting the token silently moved a customer-facing heading by 3px past a register the scan exists to hold.
