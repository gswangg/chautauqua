# DEC-651: One dialog HEADER contract: title + optional subtitle + Close, primary action first, and every free-text control in a dialog carries the mock's placeholder — enforced by a source scan, not by habit

## Amendment (wave 7): ModalFrame's header is paired to its CONTAINER's inset at every width, and gains one optional back affordance

DEC-651 made ModalFrame the one dialog header. Its .chq-modal-head (app/src/components/modal-frame.css:38) cancels its container's padding with `margin:-20px -26px 0; padding:0 26px 16px` — correct against the desktop `.chq-modal`/`.chq-drawer` 20px 26px. But styles.css:2592 cuts .chq-drawer to `padding: 16px;` at phone and re-pairs only .chq-contacts-drawer-actions (:2595), so at 390 the ModalFrame header band starts 10px outside the drawer's left edge and runs 20px wider than the drawer, overhanging both sides, with its title on a 26px gutter against a 16px body. phone-page-scaffold.test.ts:228 pins drawer-to-action-bar and never drawer-to-header. RULING: any negative margin that cancels a container's padding is paired to that container AT EVERY WIDTH — the header, the action bar and the drawer move together or not at all. Second half: the frames draw the drawer's phone head as a drill head whose first line is a '‹ <collection>' return (Contacts.dc.html:409, Comms.dc.html:288/337), so ModalFrame gains ONE optional `back` prop rendering a .chq-phone-back control — the shell class, not a fourth local one — giving that orphan its first renderer and the contact drawer the 44px '‹ Contacts' affordance the campaign has listed as blocking since wave 1.
