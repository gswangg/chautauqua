# DEC-018: The plan editor at 390 is two lists, not a narrowed grid, with its save pair docked

Chautauqua Review.dc.html:594 ("Plan editor · 390", subtitled "Criteria and reviewers as lists") is one of thirteen phone frames no lane has ever owned, and the tree currently reaches it by collapsing the desktop grid: review.css:1062 and :1208 set .chq-review-criterion-row and .chq-review-reviewer-row to grid-template-columns:1fr and stop there, which is a narrowed grid, not the anatomy the frame draws. Per the governing principle in DESIGN-RULINGS.md -- mobile REFLOWS the desktop design and phone frames may use phone grammar freely because those are translations of desktop affordances -- the frame's grammar is authoritative here: a criterion is a stacked card (48px label field, 46px type select with the caret at the control's right edge per B8, an Options sub-block of handle + 46px field + Remove, a 58px weight field with its share as muted text, and a right-pushed Remove at 44px), a reviewer is a two-line row with a 44px Swap, and the two page-level acts (Save the plan, Cancel) leave the body entirely for a docked footer at border-top:1px solid #1B1D17 / background #EFEBDF / padding 12px 16px 16px, 48px each. RULED: that anatomy, built in a max-width block on top of the two existing collapse rules rather than beside them, with every row action on the DEC-393 floor. The plan wire contract, the criteria-freeze rules and every desktop pin are untouched -- this is geometry, and the desktop editor is frozen.

## Amendment (wave 102): the per-criterion TYPE control is a real select, and the editable criterion row grows to the frame's SIXTH track — the desktop frames demand it, so this is not a frozen-desktop violation

docs/design/audit/review-admin-v12.md §1 has said "needs a design call" for five waves and PlanEditor.tsx:1813-1826 carries the deferral in a source comment. The call is settled by the pack itself: the type control is drawn FOUR times — at Review.dc.html:500 (desktop frame `Plan editor · /review/plans/:id`, opens :454) and :747 (desktop frame `New plan · /review/plans/new`, opens :722) as `border-radius:4px; padding:0 12px`, and at :608 (phone `Plan editor · 390`) as `border-radius:6px; padding:0 13px`, all `min-height:46px` with a `▾` caret. review.css:985-994 already records the target shape verbatim — `docs/design/Chautauqua Review.dc.html:492 grid-template-columns:20px 1fr 232px 128px 122px 62px` — and says the five-track editor shape holds "until the Scale-or-Choice editor lands its type column". The mandate freezes desktop except where "a v12 desktop frame demands it"; two desktop frames demand it. RULED: the editable criterion row renders a real `<select class="chq-select">` over `CRITERION_KIND_LABELS` (`rating|dropdown|text`, the kinds `domain/evaluation/criteria.ts` already ships) writing through `updateCriterion(c, id, { kind })`, disabled when `activeRoundIsLocked` — a caret the frame draws is a control, never decoration. `.chq-review-criterion-row, .chq-review-criteria-head-row` go to the frame's six tracks, and PlanEditor.render.test.tsx's non-error-children pin moves 5 → 6 IN THE SAME COMMIT (that co-landing requirement is what blocked every prior attempt). The readonly/locked row already carries its type cell and is untouched.
