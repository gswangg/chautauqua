# DEC-874: A reviewer with one plan lands IN it; the queue row leads with its reference and ends with a full-width action

## Amendment (wave 91): exactly ONE .chq-phone-dock renders per phone surface, and the reviewer pin forbids MINTING a sign-out, not rendering one

ReviewerQueue.tsx mints .chq-phone-dock three times (:407 queue footer, :719 standing dock, :832 plans note). The dock is a pinned bottom band; two on one surface stack and the first wins every querySelector. That is why ReviewerQueue.phoneFrame.render.test.tsx:157 reads 'Your scores stay hidden…' (the :407 desktop footer copy, frame :401) where the frame's dock copy is 'Scores stay hidden…' (frame :442). Separately, review-phone-frames.test.ts:107 asserts ReviewerQueue never renders 'Sign out' while ReviewerQueue.phoneFrame.render.test.tsx:158 asserts it must — two green-authored lanes contradicting each other, both landed. The frame is geometry authority and draws the control, so the pin is reworded to what DEC-874 wave-86 actually meant: neither reviewer surface mints its own sign-out CONTRACT. The :407 footer keeps its desktop copy and loses the dock class.

## Amendment (wave 92): `.chq-link-button` gets a roomy phone variant — a shared token cannot serve a dense grid and a 390 row at once

app/src/styles.css:815 declares `.chq-link-button { padding: 0 }` with no phone treatment, yet it is reused verbatim as a real navigation/row target on phone (Contacts name button 246x26px; Overview's "All 9 · oldest 119 days", Review's "Remind laggards (2)" and "See all 30 results" all inherit the same 14-26px box) — the exact evasion mode DESIGN-RULINGS B8 names: a shared token used verbatim in a dense desktop grid and a 390 card cannot satisfy both. The dense form is legitimate and must not move (desktop is frozen), so the repair is an additive phone-scoped roomy variant reached by an opt-in class on the roomy call sites, never a blanket `.chq-link-button` phone override that would inflate every dense matrix cell. Same lane, same rule: `.chq-modal-head`'s `margin:-20px -26px 0; padding:0 26px 16px` is paired to the DESKTOP drawer inset, while styles.css:2338 cuts `.chq-drawer` to 16px at 390 — so the ModalFrame header band overhung the drawer on both sides at 390 and is re-paired to 16px in the same phone layer.
