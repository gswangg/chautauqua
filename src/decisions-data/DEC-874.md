# DEC-874: A reviewer with one plan lands IN it; the queue row leads with its reference and ends with a full-width action

## Amendment (wave 91): exactly ONE .chq-phone-dock renders per phone surface, and the reviewer pin forbids MINTING a sign-out, not rendering one

ReviewerQueue.tsx mints .chq-phone-dock three times (:407 queue footer, :719 standing dock, :832 plans note). The dock is a pinned bottom band; two on one surface stack and the first wins every querySelector. That is why ReviewerQueue.phoneFrame.render.test.tsx:157 reads 'Your scores stay hidden…' (the :407 desktop footer copy, frame :401) where the frame's dock copy is 'Scores stay hidden…' (frame :442). Separately, review-phone-frames.test.ts:107 asserts ReviewerQueue never renders 'Sign out' while ReviewerQueue.phoneFrame.render.test.tsx:158 asserts it must — two green-authored lanes contradicting each other, both landed. The frame is geometry authority and draws the control, so the pin is reworded to what DEC-874 wave-86 actually meant: neither reviewer surface mints its own sign-out CONTRACT. The :407 footer keeps its desktop copy and loses the dock class.

## Amendment (wave 92): `.chq-link-button` gets a roomy phone variant — a shared token cannot serve a dense grid and a 390 row at once

app/src/styles.css:716 declares `.chq-link-button` with padding: 0 6px and no phone treatment, yet it is reused verbatim as a real navigation/row target on phone (Contacts name button 246x26px; Overview's "All 9 · oldest 119 days", Review's "Remind laggards (2)" and "See all 30 results" all inherit the same 14-26px box) — the exact evasion mode DESIGN-RULINGS B8 names: a shared token used verbatim in a dense desktop grid and a 390 card cannot satisfy both. The dense form is legitimate and must not move (desktop is frozen), so the repair is an additive phone-scoped roomy variant reached by an opt-in class on the roomy call sites, never a blanket `.chq-link-button` phone override that would inflate every dense matrix cell. Same lane, same rule: `.chq-modal-head`'s `margin:-20px -26px 0; padding:0 26px 16px` is paired to the DESKTOP drawer inset, while styles.css:2591 cuts `.chq-drawer` to 16px at 390 — so the ModalFrame header band overhung the drawer on both sides at 390 and is re-paired to 16px in the same phone layer.

## Amendment (wave 96): One Sign out per reviewer phone surface: the dock's, declared to the shell by attribute — and a Sign out that fails says so

docs/design/Chautauqua Review.dc.html:414-421 draws the reviewer drill-in's phone header band as wordmark plus the reviewer's name and NOTHING else; :441-443 draws the single Sign out in the dock. app/src/styles.css:401 un-hides .chq-user-identity (and its Sign out, App.tsx:226) for any shell with no .chq-tabbar — which is exactly a reviewer, since App.tsx returns null below 2 primary tabs — while ReviewerQueue.tsx:705 mints the dock's own, so the drill-in renders two. The hub (:810) correctly mints none and keeps the shell's, which DEC-874's wave-86 amendment already ruled, so the carve-out cannot simply be deleted and cannot key off :has(.chq-phone-dock) (the hub's note carries that class too). RULED: a page that mints its own phone Sign out declares it on its page root as data-chq-phone-signout, and the shell's carve-out becomes .chq-shell:not(:has(.chq-tabbar)):not(:has([data-chq-phone-signout])) — the attribute-contract idiom app/src/styles.css:430 already uses for data-chq-phone-dock. Secondly, the control the frame draws must work: app/src/lib/api.ts:187 signOut() throws on a non-2xx /logout and both callers (App.tsx:145, ReviewerQueue.tsx:67) do .catch((err) => { throw err }), which produces only an unhandled rejection — no message, no state change, session still live, the exact outcome the adjacent comment claims is prevented. A failed sign-out surfaces in the page's existing error state (house fail-loudly: the message reaches the operator, DEC-023 wave-82).
