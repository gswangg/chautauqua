# DEC-383: Page and surface stylesheets carry no colour literal at all; one allowlisted exception; no drop shadow anywhere

## Amendment (wave 93): the palette word-ban is tree-wide and binds synthetic test fixtures too

test/palette-conformance.test.ts:169 scans wholeTreeFiles for `/\bred\b/i` and now flags three files landed by the w9 train: app/src/pages/overview/overview-phone-floor.test.ts:196 and app/src/pages/agenda/agenda-phone-floor.test.ts:177 (`.chq-appended-after { color: red; }`) and app/src/pages/content/content-phone-floor.test.ts:292 (`color: red;`), each a synthetic CSS string built as a cascade-scan control. RULING: the ban stands over test files; the fixture, not the scan, moves. A control's colour is arbitrary by construction, so a fixture uses a colour outside the banned vocabulary (e.g. `blue`) and the scan's population is NOT narrowed — narrowing it would open the exact hole (a banned literal reappearing in a file the scan stopped reading) the guard exists to close.

## Amendment (wave 98): the palette closure's banned-word rule covers only files that can reach a rendered surface; *.test.ts/*.test.tsx are out of scope

test/palette-conformance.test.ts rule 3 ('the removed pre-redesign error/conflict palette does not reappear') globs the WHOLE app/src and src trees for .ts/.tsx/.css and is RED on main today with three hits — agenda-phone-floor.test.ts:177, content-phone-floor.test.ts:292, overview-phone-floor.test.ts:196 — every one of them a `color: red` declaration inside a SYNTHETIC CSS fixture string that proves cascade order. Three lanes reached for the same sentinel independently, and three more unmerged branches add `.chq-test { color: red; }`, so this is a recurring class, not an accident. A fixture string is never served: it is constructed in a test, matched by a regex, and discarded. The scan already carves out src/decisions.ts and src/decisions-data/ for exactly this reason ('quote decision text describing the no-red rule itself, not a CSS colour value'); the same rule-shaped reason covers test files. Rules 1 and 2 are unaffected — their population was page sheets and SSR surface modules, which never included tests. The exclusion must ship with a positive control (the banned regexes still trip on a non-test fixture) and a vacuous-population guard, so the carve-out can never widen into a blindfold.
