# DEC-393: The 44px floor's third evasion is the row-action anchor: a bare <a>/<Link> is on the floor, and reaching it takes min-height AND horizontal padding

DESIGN-RULINGS.md:189 names the evasion verbatim ("padding alone does not reach the floor, and without padding the hit box is only as wide as the text"), and an independent reviewer measured it live: /admin/review's per-plan Progress/Results/Edit links render 21px tall at 390 because review.css:117 gives .chq-review-plan-actions only display/gap/font and its three react-router <Link>s declare no height and no padding. The instrument that should have caught it cannot see it: app/src/phone-tap-target.scan.test.ts's tapTargetTagTokens collects tokens from <input>/<select>/<button> and from <a> ONLY when the class list also carries chq-btn -- so the exact shape the ruling calls out is the one shape outside its population, and the scan has been green throughout. RULED: (a) an anchor or <Link> that is a row's affordance is an interactive element on the 44px floor regardless of whether it wears chq-btn; (b) reaching the floor means min-height >= 44px AND centred flex AND non-zero horizontal padding, all three, declared inside a max-width <= 700px block -- any one alone is non-conformance, not a partial pass; (c) the scan's population is EXTENDED BY ENUMERATION to bare anchors and to the *-actions row containers the codebase actually styles (derived from the CSS, never hand-listed), guarded by a vacuous-population tripwire; (d) offenders are held by a two-sided ratchet const that may only be LOWERED, never an allowlist read off the failure output (the house rule stated verbatim at phone-tap-target.scan.test.ts:30-32); (e) exemptions are STRUCTURAL inline comments beside the rule they exempt, never a file:line ledger (DEC-967 wave-62). Desktop is untouched: the floor is a phone rule and the fix lives in a max-width block.

## Amendment (wave 7): a flush row-action anchor reaches the 44px floor by growing its own box and absorbing the padding with an equal negative inline margin

DESIGN-RULINGS.md:189 requires min-height:44px PLUS centred flex PLUS horizontal padding, and names the two evasions (padding alone; a text-width hit box). But the row actions this app ships are <Link>/<a> elements that must stay optically flush with their row's left edge or the drawn grid moves — which is why 92 measured selectors still evade the floor and why every previous attempt either shifted the ink or added padding without height. RULING: the anchor itself takes `min-height:44px; display:flex; align-items:center; padding-inline:10px; margin-inline:-10px` inside a max-width:700px block. The hit box grows in both axes; the negative margin cancels the padding so nothing shifts — the same 'replaces padding rather than adding to it' technique B8 already blessed for the selected-row 3px olive edge. Two limits: it is applied to the ANCHOR, never to a container (a padded container with a text-width child still fails); and it is NEVER applied to a dense matrix-cell token — B8 pairs dense/roomy by density, and raising a dense speakers-grid cell to 44px breaks the 178px column the pair exists to serve.

## Amendment (wave 90): the 92-offender tap-floor backlog is closed SHEET BY SHEET in each sheet's true-terminal phone block, and a token declared in styles.css is never given a second phone rule by a cluster

w5-c enumerated 92 row-action-anchor offenders into docs/design/audit/tap-floor-v12.md and fixed only the four files it owned; the other 88 have had no owner for three waves because no ruling said WHO closes them or WHERE. Two failure modes are now live risks. First, five different cluster sheets each list `.chq-pill` / `.chq-modal-actions` / `.chq-bulkbar-actions` as offenders — tokens whose base rule lives in app/src/styles.css. If each cluster lane declares its own phone override, the tree gains five copies of one vocabulary (DEC-613: a second copy of a vocabulary is a trap with a delay fuse) and the shell lane's eventual fix silently loses or wins by source order. RULED: a cluster sheet fixes only the tokens IT declares; a token whose only top-level rule is in styles.css is recorded as a finding for the shell lane and left alone. Second, six of the eight sheets in scope have top-level rules declared AFTER their existing `@media (max-width:700px)` block (comms.css 1849/2054, contacts.css 846/919, settings.css 1368/1439, speakers.css 1274/1441, agenda.css 889/1276, overview.css 447/543), so a fix dropped into the EXISTING block can be shadowed on arrival by the same class of bug DEC-385 already ruled against. RULED: the fix is a NEW block appended at the true end of the file, after every top-level rule, never an edit inside the existing block and never a reorder — forty-plus worktrees hold hunks in these sheets and only an append survives them. The conforming shape stays exactly the one the wave-7 amendment set (`min-height:44px; display:flex; align-items:center; padding-inline:10px; margin-inline:-10px`), applied to the ANCHOR, never to its container; the only legal non-fix is a `/* tap-floor-exempt: <reason> */` comment sitting immediately above the top-level rule it exempts, and the only reasons that qualify are a dense matrix cell (B8's dense/roomy pair exists to serve the 178px column) or an element that is not a target at all.

## Amendment (wave 92): the docs pager's 44px pin is a FLOOR — the frame's own 46px satisfies it, and the assertion is reworked to an inequality

`test/docs-site-phone.test.ts` asserts /min-height:\s*44px/ on the docs Previous/Next pager pair, but docs/design/Chautauqua Docs.dc.html:201-202 draws both at `min-height:46px`, and docs-site.css.ts:280-290 correctly implements 46px with flex:1, centring and the frame's border/radius/background. The CSS is conformant and the pin is the divergence: DEC-393's floor was never an equality, and a frame that draws taller satisfies it. Reworked the assertion to accept the frame's literal — assert min-height is present and >= 44px, quoting :201 — rather than degrading conformant geometry to match a regex.

## Amendment (wave 93): the shared .chq-link-button gets a roomy phone variant; ink stays where the frame draws it

app/src/styles.css:713 declares `padding: 0 6px;` for .chq-link-button, with no phone variant anywhere in the file, and it is reused verbatim as a real row/nav target at 390 — contacts names, Overview's "All 9 - oldest 119 days" and "Open the grid", Review's "Remind laggards (2)" and "See all 30 results", the Delete view control — measuring 14-26px tall. DESIGN-RULINGS.md:189 names this exact evasion ("a shared token used verbatim in a dense desktop grid and a 390 card cannot satisfy both, so pair them") and :115 rules that anywhere a row can carry a real target gets the roomy variant, identical in colour and weight, differing only in geometry. RULING: the pairing is authored ONCE on the base token, in a terminal `@media (max-width: 700px)` block at true EOF of app/src/styles.css, using the geometry review.css:2653-2658 already proved — "min-height: 44px; display: flex; align-items: center; padding-inline: 10px; margin-inline: -10px" — so the hit box grows in both axes while the equal-and-opposite margin keeps the ink exactly where the frame draws it and no desktop pin moves. A genuinely dense phone context opts out with a named modifier class, never by re-declaring the base.

## Amendment (wave 94): The tap-floor scan's `(no CSS rule)` bucket is a token-resolution artifact, and an `*-actions` container is exempted by naming the anchor rule that carries the floor

Two shapes in the 61 live offenders are not defects and must stop being counted as them. First, `findAnchorFloorOffenders` decides a token is "owned" only if some TOP-LEVEL rule declares it, so a token whose only rule lives inside a conforming `@media (max-width: 700px)` block is reported as `(no CSS rule)` — five tokens sit in that bucket today and at least one (`.chq-review-plan-action-link`, already 44px/inline-flex/padded inside review.css's terminal block) is fully conformant. A phone-only token is the CORRECT authoring shape under DEC-385 single-direction; flagging it inverts the rule. RULED: resolution considers narrow-media rules too — a token whose narrow rule meets min-height + centred flex + horizontal padding is conformant wherever its rule lives; only a token with no rule anywhere at all is an offender. Second, roughly half the offenders are `*-actions` container rules (`.chq-comms-preview-actions`, `.chq-content-detail-actions`, `.chq-forms-header-actions`, `.chq-modal-actions`, …). DEC-393's wave-7 amendment already ruled the floor is reached by the ANCHOR and never by its container, so a container can never be fixed on its own terms — its only two honest outcomes are a descendant rule that reaches its anchors, or a `/* tap-floor-exempt: … */` comment. RULED: the exemption comment on a container must NAME the rule that carries the floor for its children, with file:line, so the exemption is a receipt rather than a shrug — and a container whose children are `.chq-btn`-family controls cites app/src/styles.css:1104-1116, which already carries the floor.

## Amendment (wave 95): the 44px sweep's population stopped at app/src, and its token reader only speaks React — the SSR half is built, and the wave-94 carrier citation is corrected

app/src/phone-tap-target.scan.test.ts:36 roots HERE at app/src and enumerates app/src/**/*.css plus app/src/**/*.tsx only, while the sibling Tier-2 probe DEC-989 built enumerates src/**/*.css.ts as well (phone-horizontal-overflow.scan.test.ts:67). Two probes of one mandate line disagree on their roots, so DESIGN-RULINGS B8's 44px floor has NEVER been machine-checked over src/routes/** or src/views/** — the public site, the CFP, the speaker portal, auth and the docs site, which carry 17 of the campaign's 53 phone frames (Public and Portal 14 / Account 2 / Docs 1). Worse, tapTargetTagTokens and bareAnchorTokens match `className\s*=` only (:83) and Hono JSX writes `class=` (src/routes/public/submit-views.tsx:658 `class="chq-btn chq-btn-primary"`), so the reader is structurally blind to SSR markup. Re-running the scan's own functions with the population repointed at src/ and `class=` accepted reads 49 offenders across 11 sheets (portal 9, docs-site 8, home 7, public agenda 5, rail 5, cards 4, auth 3, cfp 3, chrome 3, empty 1, error-states 1); with `className=` alone it reads 10, which is the measure of the blindness. RULED: (a) the sweep gets an SSR half — a NEW test/phone-tap-target.scan.test.ts mirroring the test/-vs-app/src split phone-cascade-order.scan.test.ts already uses, rooted at src/, enumerating src/**/*.css.ts and src/**/*.tsx with tests excluded (DEC-808, readdirSync, never a manifest); (b) a class attribute is `class=` OR `className=`, and its value may be a double-quoted string, a `{…}` expression or a template literal; (c) the SSR half carries its OWN two-sided ratchet seeded at the count it measures, plus vacuous-population tripwires on the file side and both token sides and a synthetic positive/negative control pair; (d) the conforming shape is unchanged — DEC-393 wave-7's geometry on the ANCHOR, never on its container, inside the sheet's terminal max-width block; (e) DEC-393's wave-94 amendment tells every container exemption to cite app/src/styles.css:1104-1116 as the rule carrying the .chq-btn floor, and that range is .chq-error plus a @keyframes header — the correct citation is app/src/styles.css:1134-1139, the range branch v12m-w12-a already uses. An exemption whose own citation is false is the defect DEC-976 exists to prevent, one layer out.

## Amendment (wave 105): the anchor-floor scan's `(no CSS rule)` row is a population artifact, and its sanctioned closure is a top-level restatement that is provably pixel-neutral

Measured on main: 5 of the 63 row-action-anchor offenders report `(no CSS rule)`. Four of them (`.chq-review-plan-action-link`, `.chq-speakers-card-actions`, `.chq-settings-public-pages-view-action`, `.chq-overview-link-btn-read`) are NOT unstyled -- each is fully declared inside its sheet's phone block. The scan reports them because `findAnchorFloorOffenders` only sets `ownedSomewhere` from a TOP-LEVEL rule, and `hasExemptionForToken` is consulted only after that flag is set, so no `tap-floor-exempt:` comment can ever reach them. During a desktop freeze the only closure that works with the scan as written is a top-level rule, which is exactly the change the freeze forbids inventing. RULED: such a row is closed by ONE top-level rule that RESTATES VERBATIM a declaration the element already inherits from its own parent rule in the same sheet, carrying a comment naming that parent rule by file:line (e.g. `.chq-review-plan-action-link { font-weight: 700; }` beside `.chq-review-plan-actions`, review.css:117, which already sets it). Never a new desktop value, never an empty rule, never a guess. Two riders: (a) the phone rule must satisfy the predicates LITERALLY -- `hasFlexCenter` tests /display\s*:\s*flex/ and therefore REJECTS `display: inline-flex`, which is why `.chq-review-plan-action-link` stays flagged today; a flex ITEM is blockified either way, so flipping inline-flex to flex at 390 is behaviour-identical and is the correct repair. (b) `.chq-pill` and `.chq-bulkbar-actions` are SHELL-lane tokens whose base rules live in app/src/styles.css (DEC-393 wave-90, restated by content-phone-floor.test.ts's own header): a page lane never floors them in its own sheet, it records them as a shell finding. The two evasion modes DESIGN-RULINGS.md:189 names still bind -- min-height plus centred flex plus non-zero horizontal padding, never padding alone -- and the DEVIATIONS cancelled-padding invariant rides with them: wherever two floored bare-text controls render as ADJACENT SIBLINGS, the rule owning their separation adds the cancelled padding back to its gap, or the rendered gap closes.


## Amendment (wave 106): the 44px floor governs CONTROLS; a URL inside a sentence is content, so `chq-overview-link-btn` splits rather than being floored or exempted

docs/design/audit/phone-tap-floor-links.md:102 ends with an explicit open design call that has outlived five waves: '`chq-overview-link-btn` needs a design decision, not just a CSS fix: one class currently serves both a row-action chip (ROOMY) and prose linking to a public URL (INLINE)'. Verified on main: Overview.tsx has five call sites. Four are row-action controls (Remind at :350, the muted variant at :355, the Read link at :424, the content link at :464). The fifth, :498, is an `<a href={`/e/${eventSlug}`}>` sitting INSIDE a `.chq-overview-row-meta` span of running text -- 'N sessions live, with speakers and schedule at /e/slug'. DESIGN-RULINGS.md:189 says every interactive element on a phone is min-height:44px plus centred flex plus horizontal padding; applying that to an anchor inside a sentence would break the sentence's line box and put a 44px block in the middle of prose, which is not what the ruling means and not what any frame draws. Exempting the whole token is equally wrong: it would silently exempt the four real chips. RULING: the class splits. `.chq-overview-link-btn` keeps its identity as the ROOMY row-action chip and carries the phone floor; the prose URL moves to a new `.chq-overview-link-inline` that shares the brand colour and hover treatment and declares NO min-height, with a written reason at the rule. The floor scan's population then judges each token on what it actually is, and the audit's open call is closed in code rather than being carried forward again.


## Amendment (wave 108): a 44px control box the USER files stands at every width, and the pin that forbade it is re-derived into an assertion of it — never satisfied by reverting the CSS

main is red on app/src/components/error-states-phone.test.ts:158 because the user's own commit 7f450624 gave .chq-detail-delete-link a real control box (display:inline-flex, align-items:center, min-height:44px, white-space:nowrap) at every width, and an earlier wave's pin asserts the top-level rule carries no min-height. A user override outranks the swarm's desktop freeze and outranks a pin an earlier wave wrote, so the CSS is right and the pin is stale. But a stale pin is not deleted: a DESKTOP-PRESERVED case exists to stop a later wave silently narrowing that rule, so it is rewritten to assert the user's contract positively, with the commit as its cited authority. The sibling half of the same commit (.chq-review-reviewer-form .chq-btn min-height:44px, review.css:1197) is asserted by nothing at all, which is how a user fix gets quietly reverted three waves later — it gets its own pin, scoped to that row, asserting the shared .chq-btn was not touched.

## Amendment (wave 109): The only route into a capability is a ROOMY target at phone width

`.chq-settings-section-action` (settings.css:210) opens a rule whose body sets settings.css:211 `font-size: 13px`, then `font-weight: 700`, `background: none`, `border: none` and `padding: 0` (:212-215) -- a bare-text control whose hit box is its ~15px line box, the tap floor's second evasion mode DEC-393 already names. It is the control that opens edit mode on SummarySection.tsx:80, PeopleRolesPanel.tsx:389 and TracksRoomsPanel.tsx:165/261, i.e. the ONE route into editing on all nine Settings panels, and it has no phone rule anywhere in the tree. DESIGN-RULINGS' density standard puts a control that is a real target in the ROOMY register, never dense. RULING: it takes the 44px phone floor with the cancelled-padding idiom (legal here -- it is borderless, per DEC-383 above), declared inside settings.css's single terminal `@media (max-width: 700px)` block. A control below the floor is not a small control; on the only route into a capability it is a removed capability.
