// Design-decision constants, range DEC_210-DEC_419 — part of the src/decisions.ts
// registry, split into modules to reduce merge contention. Regenerated by
// the swarm scribe from decisions/DEC-*.md each wave; do not edit by hand.
// Import via "../decisions" (the barrel), not this file directly.

export const DEC_210 = "Stage-1 exit reopened: DEC-208 ledger predicate was satisfied at 6807b67 but four verified product defects supersede it; wave 23 runs a fresh battery at the new DEC-114 sha";
export const DEC_211 = "Every evaluation write and read on /api/v1/review/* must verify the submission belongs to the plan's event, for organizers as well as reviewers";
export const DEC_212 = "Rating-less scorecards are legal; aggregateSubmission returns {count: evals.length, average: 0, perCriterion: {}} when the criteria list is empty; computeWeightedScore keeps its empty-list throw";
export const DEC_213 = "Per-round criteria freeze: PATCH /api/v1/plans/:id must reject any roundCriteria change that alters the RESOLVED criteria of a round that already has evaluations; future rounds without evaluations stay editable";
export const DEC_214 = "Speaker-side kind gates on PATCH /api/v1/task-assignments/:id: an owning speaker may only complete 'form' with a saved response and 'file_request' with an uploaded file; organizer overrides stay unrestricted";
export const DEC_215 = "Org-user temp-password re-issue: POST /api/v1/users/:id/reset-password (organizer-only, own org, csrfJson) returns a fresh one-time password in the JSON response and revokes the target's sessions; never emailed";
export const DEC_216 = "DEC-199 stage-1 disposition: no schema migration for case-insensitive email uniqueness; app-level normalization plus the existing raw unique index are sufficient";
export const DEC_217 = "app/src/routeManifest.ts gains authenticated /account/password entries for organizer, reviewer, and speaker so the render-sweep gate covers DEC-200's account routes";
export const DEC_218 = "task-w22-e and task-w22-f are dead stubs, rebound to wave-23 lanes; late commits from those branches are superseded";
export const DEC_219 = "The exit battery is planned only after the planner verifies every task-w* ref is merged or VOID, and each battery task carries the literal frozen sha in its task text";
export const DEC_220 = "POST /api/v1/users/:id/reset-password has no self-target guard: any own-org user id, including the caller's own, is a valid target";
export const DEC_221 = "task-w23-a/b/c are VOID-redundant: w22-e/f were never dead stubs — they late-drained during wave-23 planning and landed the entire w23 remit";
export const DEC_222 = "Wave-24 exit battery binds FROZEN sha 0a263d2e6e4dbf438f6ad9e98bffa6af527b965c (merge task-w22-e); six gates task-w24-a..f; one per-lane log file each; isolated worktrees and ports";
export const DEC_223 = "Sha-drift reconciliation: accept late w23-a/b merges into the baseline; wave-25 battery FROZEN literal is b2dc2c103309433732bc689b933610fc7cfb3b06 (merge task-w23-b)";
export const DEC_224 = "Wave-24 battery is VOID as gate evidence; task-w24-* refs closed with LATE-DRAIN-IMMUNE accounting — only docs/verification-log/task-w25-*.md counts toward the exit predicate";
export const DEC_225 = "Wave-25 exit battery binding: six gates task-w25-a..f at FROZEN b2dc2c1, one per-lane log file each, fresh ports 8963 (walkthrough) and 8964 (perf-smoke), allow-list-aware sha check";
export const DEC_226 = "Wave-25 battery disposition: task-w25-a PASS recorded; four new verified defects reopen fixes-first; the b2dc2c1 freeze is superseded and wave 27 reruns the FULL six-gate battery at a freshly derived sha";
export const DEC_227 = "A required checkbox means the box must be CHECKED: validateAnswers rejects required checkbox fields unless the value is exactly true";
export const DEC_228 = "CSRF and draft cookies get HttpOnly + conditional Secure via shared builders in src/auth/cookies.ts; the duplicated isSecureRequest helpers are consolidated there";
export const DEC_229 = "deleteTrack extends its 409-never-cascades guard to form.tracks_json, evaluation_plan filters, and plan_reviewer.track_id references";
export const DEC_230 = "zonedMinutesToUtc becomes DST-safe: two-pass offset correction with Temporal-'compatible' disambiguation (gap resolves forward, overlap resolves to the earlier instant)";
export const DEC_231 = "CRM-02 'no true multi-criteria filter / lossy segment-from-search' is CLOSED as implemented; the w24-f open item was a prose-grep false negative";
export const DEC_232 = "Wave-27 freeze: FROZEN LITERAL f01459a1d52b6867586dd0b5b7c81dfe09601cfd (merge task-w26-d); all four DEC-227..230 fixes verified on the tree at planning time";
export const DEC_233 = "Wave-27 exit battery: six gates task-w27-a..f at f01459a, ports 8965/8966, one log per lane; stage-1 completion declared at wave 28 only on 6/6 same-sha PASS with zero new verified defects";
export const DEC_234 = "deleteField dangling visible-if references: verified fail-safe, fix DECLINED for stage 1";
export const DEC_235 = "Wave-28 exit reading: DEC-233 predicate SATISFIED — stage 1 declared complete at frozen sha f01459a";
export const DEC_236 = "Wave-28 review-lens quartet dispositioned STALE — pre-fix citations of defects already closed by DEC-211..214; do not reopen absent new evidence";
export const DEC_237 = "Ratify the two production hotfixes as stage-1 code with regression tests; DEC-004 amended to 100,000 PBKDF2 iterations";
export const DEC_238 = "Mailer failure taxonomy: system sends are per-recipient best-effort; organizer-triggered batch sends return a structured {sent, failed} summary, never a 500";
export const DEC_239 = "API wire shapes are server-canonical and contract-tested: /users items {id,email,role}, review queue items {submissionId,ref,title,ratingsCount,alreadyRatedByMe}, duplicates items {contactIds,contacts[]}";
export const DEC_240 = "Task uploads join the content pipeline: task.deliverable_kind column, deterministic submission linkage, previous_file_id chaining on re-upload (supersedes DEC-029's submission_id-null rule)";
export const DEC_241 = "Results table: numeric Average stays rating-only; dropdown criteria get modal-plus-distribution columns; text criteria get no column; all result columns client-sortable";
export const DEC_242 = "Speaker deliverable self-service: completed file_request tasks show the current file, a replace-file form, and the file's comment thread in /portal/tasks";
export const DEC_243 = "Submissions table: Tracks column shows track names, a 'Format' dropdown form field surfaces as a default-visible column, and the Columns picker must be labeled and demonstrably functional";
export const DEC_244 = "Portal deliverable panel mechanics (implements DEC-242)";
export const DEC_245 = "SSR action-confirmation conventions (closes remaining Section D feedback findings)";
export const DEC_246 = "Contract-mismatch resolution rule for the DEC-239 sweep";
export const DEC_247 = "GET /api/v1/submissions/:id/files returns a flat {items: DeliverableFile[]} envelope; worklist deliverable counts = version-chain roots";
export const DEC_248 = "Task-file serve population drops the 'handout'-only predicate — any kind, submissionId-null, assignment-referenced (DEC-240 conformance; amends DEC-065)";
export const DEC_249 = "Format-column auto-show matches a fixed label allowlist {'format', 'session format'} (amends DEC-243)";
export const DEC_250 = "Wave-4 exit battery frozen at product sha c211d4c; allow-listed drift; six greppable sections under docs/verification-log/";
export const DEC_251 = "eval-findings Section D disposition: five items FIXED with cited evidence, three residuals WAIVED for stage-1 closure";
export const DEC_252 = "Local-first absolute-link origins: resolveBaseUrl precedence + relative same-origin hrefs";
export const DEC_253 = "Mobile bar for public, submit and portal surfaces (390x844), enforced by the render-sweep gate";
export const DEC_254 = "Wave-1 browser persona passes: lane-scoped fix authority, a regression test per fix, greppable log";
export const DEC_255 = "Stage-1 completion criterion for this campaign: a fresh six-section battery at a new frozen sha";
export const DEC_256 = "Wave-2 exit battery: freeze-by-rule at the newest code-bearing commit, eight greppable sections, unanimity predicate (supersedes DEC-255's six-section formulation)";
export const DEC_257 = "Fresh-clone evaluator bootstrap is a battery section: the stranger test the swarm's own gates structurally cannot run";
export const DEC_258 = "Frozen speaker attribution: participant.title_at_time / org_at_time, captured at participant creation, read by every public/export/.ics surface";
export const DEC_259 = "Wave-3 lanes are fix-authorized inside an owned file list, log as task-w3-<x>-c2-<scope>.md, and are evidence rather than battery sections";
export const DEC_260 = "Multiple CFP forms per event stays out of stage 1; one form with track options is the shipped model";
export const DEC_261 = "Wave 3 is campaign-2's last product-touching wave; wave 4 re-runs the DEC-256 eight-section battery and is the only place stage 1 is declared";
export const DEC_262 = "Exit-battery freeze needs quiescence + a product-quiet wave, not a one-shot ancestor check (amends DEC-256 step 1; defers the battery re-run from wave 4 to the first product-quiet wave)";
export const DEC_263 = "Hand-authored SQL migrations are canonical; `npm run db:generate` is retired and schema/migration parity is enforced by a test";
export const DEC_264 = "Wave-4 scope: battery-FAIL fixes plus the two never-executed sections run as EVIDENCE; logs carry the mandatory -c2- infix; two non-product wave-2 items are WAIVED";
export const DEC_265 = "Participant API responses carry the contact's name and email server-side: an optimistically appended row must render identically to a reloaded one";
export const DEC_266 = "Contact search semantics: AND-of-tokens x OR-of-columns, defined once as a pure predicate, with a superset SQL prefilter";
export const DEC_267 = "Every FK column is indexed, enforced by a schema tripwire test (closes all three spec-audit open items)";
export const DEC_268 = "The documented zero-setup Quickstart must produce a working /admin: predev builds the SPA bundle, render-sweep builds before boot, and a missing admin shell fails loudly instead of returning a bare 404";
export const DEC_269 = "task-w2-g Finding 2 (off-origin claim link) is a STALE false negative and is CLOSED with no code action; per-lane sha derivation is the root cause";
export const DEC_270 = "Wave 6 is the exit battery and nothing else; the PLANNER names the literal frozen sha and lanes never derive one (supersedes DEC-256 steps 1-2 and DEC-262's quiescence procedure)";
export const DEC_271 = "Reviewer conflict-of-interest / recusal (rubric ABS-12): a review_recusal row, a control in the scoring view, exclusion from the actionable queue, and honest organizer progress math";
export const DEC_272 = "ABS-14 (AI-assisted triage) is formally WAIVED for stage 1 and may not be listed as an open item again";
export const DEC_273 = "'unreviewed -> approve/maybe/deny' is a reviewer RECOMMENDATION expressed as a scorecard criterion, never a sixth submission-status literal; the seeded demo says the customer's words";
export const DEC_274 = "Public visibility is TWO predicates, not one: visibleSessionConditions (accepted + content-approved) gates sessions, visibleParticipantConditions (visible + invite-state) gates speakers, and session-rooted queries stop inner-joining participant";
export const DEC_275 = "cloneSubmission copies ACTIVE participants (invite_status none|accepted) as invite_status 'none'; a participant-less submission is a legitimate TBA session and acceptance planning is a documented no-op for it";
export const DEC_276 = "Bearer API tokens re-resolve the minting user on every request: existence + role='organizer' + org match, or the token does not authenticate";
export const DEC_277 = "A schedule slot's day must lie inside the event date range at write time; an out-of-range placement reads as UNSCHEDULED in the agenda payload, and the wire shape does not change";
export const DEC_278 = "Onboarding auto-creation fires at the LAST of (submission accepted, participant becomes active); one idempotent ensureOnboardingTasks with three call sites; 'invited' and 'declined' participants are excluded";
export const DEC_279 = "Wave 6 is a defect-fix wave; the eight-section exit battery moves to wave 7, the first wave whose planner emits zero product tasks (re-points DEC-270's wave number, keeps its declaration protocol verbatim)";
export const DEC_280 = "Exit-battery execution amended: post-S product commits are a recorded POST-S DELTA to re-check, never a STOP (supersedes DEC-270 step 3, keeps steps 1/2/4)";
export const DEC_281 = "Wave 8 is the exit-battery wave; only a P1 may displace it again";
export const DEC_282 = "Contact merge is total over every contact-referencing table: pipeline_entry/pipeline_activity carried, task_assignment deduped, twin login accounts refused, and a schema-derived tripwire keeps the list honest";
export const DEC_283 = "'Assign to all accepted' means all ACTIVE participants of accepted submissions -- DEC-278's invite gate applies to the task-creation expansion too";
export const DEC_284 = "A pre-battery mechanical gate lane runs in wave 7: it reports red, it never fixes red";
export const DEC_285 = "Wave-8 exit battery freezes at S=80b811d250285de0d37417ddc12f65445ce27f96 with a pre-registered KNOWN-IN-FLIGHT ledger and a second detached RECHECK worktree";
export const DEC_286 = "Wave-8 section->lane map: mandatory -c2- log infix, one new file per lane, and a disjoint port assignment with the fresh-clone lane owning default 8787";
export const DEC_287 = "The wave-9 exit predicate is a mechanical planner-only grep over the eight -c2- logs; stage 1 is declared at the greatest RECHECK SHA";
export const DEC_288 = "Wave 9 is a product wave, not the DEC-287 exit grep: the eight -c2- logs do not exist yet, so the exit predicate moves to the first planner invoked after they all land, at a NEW frozen sha that includes wave 9";
export const DEC_289 = "Public/embed URL contract: shared query params on both /e/ and /embed/, output format is a PATH SUFFIX, and every knob is a validated allowlist that degrades to today's behavior";
export const DEC_290 = "Event-roster add and roster CSV import ride the existing add-to-event push; no new roster table, no new route file — `eventId` becomes an optional field on the two contact endpoints that already exist; phone and bio become first-class import targets";
export const DEC_291 = "Form-task responses are organizer-readable through one read-only assignment-scoped endpoint, entered from the Speakers onboarding grid; travel/logistics data lives in those responses, never in a new schema column";
export const DEC_292 = "Contact custom fields are edited as key/value rows with a reserved `travel_logistics` key surfaced as a labeled 'Travel & logistics' textarea; duplicate keys are a loud validation error, never a silent overwrite";
export const DEC_293 = "The deferred DEC-287 exit grep, run at wave 10: PREDICATE FAILS (f/g/h are RESULT: FAIL); the two baseline-count doc-typo rows are WAIVED and must never be counted OPEN again";
export const DEC_294 = "Wave 11 is battery-only at a new frozen sha — all eight sections, zero product commits — and the wave-12 planner re-runs DEC-287 verbatim to declare stage 1";
export const DEC_295 = "A 304 from the ASSETS binding is a successful admin-shell fetch, not a missing bundle";
export const DEC_296 = "Stage-1 dev links: .dev.vars.example ships PUBLIC_BASE_URL=http://localhost:8787, and under DEV_MODE an observed loopback origin outranks a loopback PUBLIC_BASE_URL";
export const DEC_297 = "Public surfaces never emit a cacheable non-200: every 404/error response from src/routes/public/* carries Cache-Control: no-store";
export const DEC_298 = "Auto-schedule parameters are a bounded, integer-validated allowlist; the greedy loop asserts its own termination invariant";
export const DEC_299 = "A NULL participant attribution snapshot was never frozen: any write of contact.title/company backfills participant rows whose snapshot column IS NULL, and the CFP form gains no title/company fields";
export const DEC_300 = "Deleting a form field is a declared cascade: 409 with the dependent counts unless the caller passes cascade=1, then dependent rules are cleared and orphaned answers deleted in one operation";
export const DEC_301 = "A newly created event ships a submittable CFP: creation provisions a default 'General' track, and track choice is required only when the form actually offers tracks";
export const DEC_302 = "npm audit advisories: dev-dependency-only advisories are accepted for stage 1 and must be recorded, not waved off, by the wave-11 build-test section";
export const DEC_303 = "Wave-11's freeze is derived per-lane behind a seven-item WAVE-10 CONTENT GATE, not a planner-declared literal sha (main was provably mid-flight during planning)";
export const DEC_304 = "The stage-1 exit predicate for the -c3- battery: eight green sections, substance not sha-identity; the wave-12 planner declares stage 1 at refs/heads/main and returns goalComplete";
export const DEC_305 = "Wave-11 section->lane map, mandatory -c3- log infix, disjoint ports, and product-read-only lanes";
export const DEC_306 = "The contacts-merge task_assignment tie-break is CORRECT as written; the review-lens claim against it is refuted and must not be 'fixed'";
export const DEC_307 = "The battery verdict is read ONE WAVE LATE: wave 12 is the narrowing-fix wave, not the exit wave — the wave-11 predicate is already provably FAILED";
export const DEC_308 = "The drizzle-orm advisory is fixed by upgrading to ^0.45.2, not accepted; drizzle-kit and drizzle.config.ts are DELETED";
export const DEC_309 = "Perf grading gets SPEC §7 route classes measured against a /health overhead floor; schedule.ics is class `public`, not an admin read";
export const DEC_310 = "schedule.ics must query the requested ids, not hydrate the entire published agenda";
export const DEC_311 = "The 390x844 no-overflow bar extends to /docs/api and /dev/mailbox, and both join MOBILE_ROUTE_MANIFEST";
export const DEC_312 = "SQL WHERE clauses are normative; test doubles model the predicate, never the reverse";
export const DEC_313 = "Wave 13 is the CLOSING battery: five live sections re-run (a build-test, b walkthrough, c perf-smoke, d render-sweep, g fresh-clone) plus two closure lanes (i citation-delta, j eval-findings §D) — because wave 12 changed exactly what those sections measure";
export const DEC_314 = "Wave-13 lane letters keep their wave-11 section meaning, logs keep the '-c3-' infix, ports are planner-assigned, and the WAVE-12 CONTENT GATE polls BOUNDED then records PARTIAL rather than blocking";
export const DEC_315 = "The stage-1 exit predicate: seven wave-13 '-c3-' logs at OPEN ITEMS: 0 / RESULT: PASS; a planner that cannot see all seven returns ZERO tasks rather than padding";
export const DEC_316 = "docs/eval-findings.md §D (P3 polish) is CLOSED by live verification this wave, and graded functionally: a row is an OPEN ITEM only if the user action actually fails; latency/'occasionally' observations from campaign-1's deployed run are out of stage-1 local scope";
export const DEC_317 = "Participant invite state defines THREE scopes, not one: public display (visible AND active), notification (active only, ignoring `visible`), portal access (read = not declined; write/upload = active)";
export const DEC_318 = "A schedule_slot whose day falls outside the event's date range is never published: the public schedule reads carry the same [startDate,endDate] bound the admin agenda classifies on";
export const DEC_319 = "Reminder sends are capped and ordered like every other bulk send: MAX_REMINDER_BATCH=100 contacts per pass, deterministic contactId ordering, a 1-hour manual dedupe window, and a reported remainder — never a hard reject";
export const DEC_320 = "Wave-14 supersedes DEC-315's exit predicate: the wave-13 log battery is evidence at a PRE-FIX sha, so goalComplete additionally requires a wave-15 confirmation pass over the four surfaces wave-14 changes";
export const DEC_321 = "The default CFP form collects job title, company and bio as OPTIONAL locked speaker fields backed by contact columns; blank-only fill on an existing contact, and the frozen participant snapshot is taken from the resolved values";
export const DEC_322 = "Speaker social links are a public surface, and every externally-supplied URL passes a pure http/https allowlist before it can become an href";
export const DEC_323 = "`/e/:slug/schedule.ics` with no `ids` publishes the whole agenda through the shared agendaIcsEvents mapper; the ids filter never runs against an empty selection";
export const DEC_324 = "DEC-297's no-cacheable-non-200 rule moves to the public sub-app's own onError: setCacheHeaders has already run by the time anything throws, so every public ApiError and every unexpected 500 currently ships max-age=60";
export const DEC_325 = "File access under a pending ('invited') participation follows DEC-317's WRITE gate, not its read gate: files-authz.ts's ACTIVE_INVITE_STATUSES scope is ratified, not drift";
export const DEC_326 = "Walkthrough probes never hardcode a fact about the tree: dates are derived from the event under test, and static 'the SPA does X' assertions cite the module that actually owns the behaviour";
export const DEC_327 = "Wave-15 exit predicate: w13's six PASS logs stand, w13-b's FAIL closes through its three cited fixes carrying their own evidence, and three wave-15 confirmation logs complete DEC-320(ii)";
export const DEC_328 = "All four wave-16 review-lens findings are STALE and are closed by anchor; a finding whose cited line no longer holds its cited symbol must be re-verified at the SYMBOL before it can become a task";
export const DEC_329 = "A walkthrough probe whose premise contradicts a binding decision is a PROBE defect: rewrite it to assert BOTH directions (what must vanish AND what must remain); narrowing a FETCH is allowed, weakening an ASSERTION never is";
export const DEC_330 = "Stage-1 exit predicate, re-derived live: DEC-327's conditions are MET on main today; what remains is w15-b's single open item, plus build/test evidence at a sha containing the post-w15 decomposition";
export const DEC_331 = "Perf enforcement must cover all five public surfaces plus both whole-agenda .ics feeds; a new check over budget is a logged product finding, never a loosened budget or a dropped check";
export const DEC_332 = "The barrel decomposition of src/server/repo/public.ts is ratified; cite the submodule that owns the behaviour, and the DEC_258 compile-checked marker belongs in every module that reads the frozen attribution snapshot";
export const DEC_333 = "Scale-shaped admin read defects are STAGE-1 product defects (not stage-2 perf tuning); four injected lens findings are closed by anchor";
export const DEC_334 = "Card numbers are SQL aggregates, never materialized rows: the overview comms card is one COUNT/MAX query and aggregateCommsCounts is deleted";
export const DEC_335 = "listSubmissions is always ONE paginated SQL statement: q/trackId become correlated EXISTS predicates, the id-materialization and its JS fallback are deleted, and every sort carries a deterministic tiebreaker";
export const DEC_336 = "Contact directory search/sort/paging happens in SQL; DEC-266's AND-tokens x OR-columns semantics move from the pure predicate into the WHERE, and matchesContactQuery is deleted";
export const DEC_337 = "An index declared in src/db/schema.ts that no migration creates does not exist: migration parity is extended from columns to indexes, and the three composite indexes the new queries need land in migration 0018";
export const DEC_338 = "Perf enforcement extends to the three hot admin screens nobody measures, and the perf seed grows the rows that make them measurable; the overview check loses its `optional` flag";
export const DEC_339 = "Wave-18 product commits supersede task-w17-c's build/test evidence; wave 19 re-runs exactly the gates whose covered code moved, and nothing else";
export const DEC_340 = "The J6 onboarding grid becomes a server-paginated, server-filtered, searchable roster; DEC-023's whole-event grid envelope is superseded";
export const DEC_341 = "The J8 content worklist is server-driven: contentStatus filter and worklist ordering move into SQL, deliverable counts are hydrated page-bounded, and the per-row client fan-out is deleted";
export const DEC_342 = "The reviewer queue and the event files library were evaluated live and are DEFERRED with explicit triggers, not silently left";
export const DEC_343 = "Wave-19 gates freeze before the wave-19 product lanes; w17-e's FAILED closes only by a green six-module orchestrator; the wave-20 re-run set is fixed now";
export const DEC_344 = "The central files library becomes a server-paginated, server-filtered list; chain and lead-speaker resolution are page-bounded; the quadratic participant scan and the whole-event load in resolveLatestVersions are deleted";
export const DEC_345 = "The review results table is ranked, sorted and paged on the server; the SPA's client-side column sort is deleted, because paginating without moving the sort would recreate DEC-341's bug";
export const DEC_346 = "Plan-scoped submission loads shed `description`, and the reviewer queue's evaluation counts become SQL aggregates; queue PAGINATION stays deferred exactly as DEC-342 ordered it";
export const DEC_347 = "The wave-20 perf run is a BASELINE at a pre-fix tip: a new or newly-scaled check over budget is a logged finding owned by its fix lane, never an OPEN ITEM; a check over budget at UNCHANGED seed scale is a regression and is an OPEN ITEM";
export const DEC_348 = "DEC-201/DEC-202 REAFFIRMED against the wave-20 review lens: the global public-cache bump stays stage-1-accepted; and three lens findings are recorded as verified STALE at the symbol";
export const DEC_349 = "Wave 20 runs only the build/test gate; the full walkthrough and the authoritative perf run move to wave 21, whose three-item exit set is fixed now";
export const DEC_350 = "The J5 compose recipient picker becomes server-paged and searchable with an explicit total; its silent 200-row truncation is a stage-1 defect";
export const DEC_351 = "/plans/:id/progress and /plans/:id/remind stop loading whole-round evaluation rows; /results keeps the full load because ranking genuinely needs the scores";
export const DEC_352 = "Gate evidence is invalidated only by a change that can alter what that gate asserts; wave 22's exit set is exactly one build/test lane";
export const DEC_353 = "The bulk ZIP archive gets a pre-fetch TOTAL-byte budget read from D1 metadata, and buildZip assembles exactly once — DEC-160's \"cap keeps worst-case memory bounded\" premise is arithmetically false";
export const DEC_354 = "plan_reviewer scope ids are validated against the plan's event at write time, AND isSubmissionInReviewerScope's per-submission branch gets the event guard every other branch already has — both, not either";
export const DEC_355 = "Bulk acceptance planning becomes set-based: a bulk status change issues O(ids/90) + O(distinct task titles) statements, never O(ids) x O(tasks); DEC-079's plan-before-commit ordering is preserved";
export const DEC_356 = "CSV import never loads the org's whole contact table: the upsert lookup is chunked and keyed on the file's own emails, behind a 2,000-row per-import cap";
export const DEC_357 = "Bulk roster-add is set-based: one chunked contact load plus exactly ONE updateSubmissionStatuses call for every created submission; createSubmission stays per-row because seq is allocated by a SQL subquery";
export const DEC_358 = "Stage-1 exit accounting for wave 22: the global public-cache purge stays CLOSED, and wave 23's exit set is build/test/tripwires/fresh-migration + FULL walkthrough + perf:smoke at one tip";
export const DEC_359 = "Wave 23's stage-1 exit set is SIX gate lanes, not DEC-358's three: the browser render-sweep, the SPEC/rubric audit, and the zero-secret fresh-clone proof are all stale by many waves or have never run";
export const DEC_360 = "Wave-23 gate lanes are log-only: each owns exactly one new file (its own verification log) and never patches product code; a red gate is an OPEN ITEM with file:line for wave 24";
export const DEC_361 = "Every wave-23 gate verifies the ten wave-21/wave-22 lanes are ancestors of its FROZEN SHA before the expensive run; drift is a logged POST-S DELTA, never a STOP";
export const DEC_362 = "The stage-1 exit predicate, restated for the next planner: six wave-23 logs at OPEN ITEMS: 0 / RESULT: PASS, then goalComplete — supersedes DEC-358's three-log predicate";
export const DEC_363 = "Stage-1 exit predicate SATISFIED: all six wave-23 gate logs read OPEN ITEMS: 0 / RESULT: PASS at FROZEN SHA e3d558e, and every merge after that sha is a DEC-360 log-only lane, so main's product code is identical to the certified tree — goalComplete";
export const DEC_364 = "Gate e's two 'NOT STATICALLY VERIFIABLE' items are DISCHARGED, not carried: a deferral whose designated live lanes both ran green at the same FROZEN SHA is closed";
export const DEC_365 = "Wave-24 adjudication of the carried review-lens findings: all five are non-defects, already-fixed, or CLOSED-on-the-record — no product lane is warranted, and padding a completion wave would be a worse failure than declaring";
export const DEC_366 = "The docs/design/ handoff is this run's binding requirement; stage-1 function is frozen and complete";
export const DEC_367 = "The disabled token's two legal uses become executable at phone widths";
export const DEC_368 = "CSS file ownership: one shared stylesheet owned by the shell lane, co-located page stylesheets owned by page lanes";
export const DEC_369 = "Admin chrome: top header with horizontal nav replaces the sidebar; phone gets a five-item tab bar; nav carries destinations and exceptions only";
export const DEC_370 = "Overview worklist payload v2: exact contract, action-endpoint mapping, and the suggestions that are deliberately omitted";
export const DEC_371 = "SSR surfaces share one inlined THEME_CSS string in src/views/theme.ts; per-event branding recolours only .chq-brandable";
export const DEC_372 = "Wave-2 design-system lane owns BOTH shared stylesheets: the active pill is ink, SPA/SSR token names reach parity, nine shared classes are added";
export const DEC_373 = "SSR surface stylesheets: one co-located CSS-string module per surface family; tokens live only in THEME_CSS";
export const DEC_374 = "SSR CSS is injected via dangerouslySetInnerHTML and must be value-free; the per-event accent moves to a validated style attribute on <body>";
export const DEC_375 = "Settings stays one route; the section rail is client state and becomes a drill-in index at phone width";
export const DEC_376 = "Contacts splits across two lanes by view with a fixed file boundary; the merge screen is the one place an off-palette colour is allowed";
export const DEC_377 = "Mock figures are illustration: every number and caption on a redesigned screen must trace to an existing endpoint field";
export const DEC_378 = "One backdrop and one dismissal contract for every SPA dialog: .chq-scrim wraps .chq-modal, Escape closes, the two legacy backdrop classes are deleted";
export const DEC_379 = "app/src/styles.css closes the SPA's class vocabulary, and a test proves closure; the residual chrome names are adopted, not renamed";
export const DEC_380 = "Agenda phone flow: arm-then-tap over one room, 30-minute default placement, and no slot the API cannot describe";
export const DEC_381 = "The phone tab bar's five destinations are Overview, Submissions, Speakers, Content, More; the More sheet carries Sign out; the header identity block stops depending on :last-child";
export const DEC_382 = "The three operator surfaces (/, /docs/api, /dev/mailbox) share one TOOLS_CSS module and the shell pattern; they are chrome, not designed screens";
export const DEC_383 = "Page and surface stylesheets carry no colour literal at all; one allowlisted exception; no drop shadow anywhere";
export const DEC_384 = "The wave-3 render-sweep gate is log-only; its open items become wave-4 tasks, never in-lane patches";
export const DEC_385 = "Amendment (wave 107): the desktop-freeze pin's population is every stylesheet, not styles.css";
export const DEC_386 = "Phone cards come from CSS on the same markup, and a phone card shows exactly the desktop row's cells";
export const DEC_387 = "The render sweep gains an admin mobile pass, advisory in the wave that introduces it";
export const DEC_388 = "D1 bind values are primitives: a timestamp comparison inside a raw sql template interpolates epoch milliseconds, never a Date";
export const DEC_389 = "A gate script reports; it never throws";
export const DEC_390 = "The phone card transform adds no markup by default; ambiguous cells get a data-label mirrored from their own th";
export const DEC_391 = "The two mock affordances DEC-380 and DEC-386 dropped are settled, not deferred";
export const DEC_392 = "The admin phone chrome is the tab bar alone: at <=700px the desktop nav strip and the identity text are hidden, and the More control is unconditional so Sign out is always reachable";
export const DEC_393 = "Amendment (wave 108): a 44px control box the USER files stands at every width, and the pin that forbade it is re-derived into an assertion of it — never satisfied by reverting the CSS";
export const DEC_394 = "For this wave only, the shared-CSS lane splits by surface: styles.css + event-switcher.css to the SPA shell lane, theme.ts to the SSR floor lane";
export const DEC_395 = "A reviewer session never requests organizer-only aggregates: the nav-badge hook gates on role, not on catch()";
export const DEC_396 = "compose/preview and compose/send take a bounded id array: the one bulk endpoint DEC-182 missed";
export const DEC_397 = "A preview never mints credentials: in preview mode the portal link for a userless contact renders a fixed placeholder, and only the send path issues claim tokens";
export const DEC_398 = "A form task's form is picked from the event's own forms and validated server-side; findFormForEvent means the DEFAULT form";
export const DEC_399 = "The global public-cache bump stays CLOSED -- third and final reaffirmation of DEC-201/333/348";
export const DEC_400 = "The overview endpoint's wire keys are DEC-370's, not the repo's private ones: `triage` is the v2 rows section and the v1 aggregate ships as `triage-counts`; a repo-level contract test pins them to the SPA type";
export const DEC_401 = "The mobile pass measures the truth and names the offender: max element right edge as well as scrollWidth, plus the widest overflowing elements and the shortest control's selector";
export const DEC_402 = "Every `<table className=\"chq-table\">` in the admin SPA carries a page-prefixed class, and the import preview table gets the phone card it never had";
export const DEC_403 = "Whatever the mobile pass visits, the desktop pass visits: routeManifest is the union of SPA routes and every no-login surface";
export const DEC_404 = "At phone width no user-supplied string may widen the page: `overflow-wrap: anywhere` belongs to the shell's one 700px block, in the SPA and the SSR theme both";
export const DEC_405 = "No stylesheet hides horizontal overflow at the document level; a region that must be wider than the viewport gets its own scroller";
export const DEC_406 = "Every interactive control in the admin SPA carries a shell class; three surfaces never got the redesign's control tiers, and each gets its own lane before any repo-wide guard";
export const DEC_407 = "The walkthrough runs every area and prints a summary; one area's failure never hides another's, and J2 asserts the word the shipped copy keeps";
export const DEC_408 = "Public dates render in the event's own timezone, never UTC; one pure helper, and it throws rather than falling back";
export const DEC_409 = "The 2px olive focus ring is declared in both stylesheet roots, and no rule may remove an outline";
export const DEC_410 = "The interactive-control class guard lands repo-wide now, and the lane that adds it also closes any straggler; app/src only, because SSR is covered at the element level";
export const DEC_411 = "Gate scripts must survive tsx/esbuild keepNames: a raw-string __name shim goes into every Playwright page before any evaluate, and the phone manifest is the whole phone product, not one route of the portal";
export const DEC_412 = "Walkthrough repair policy: the design owns the copy, SPEC section 9 owns the behaviour, and each of the six areas has exactly one owning lane";
export const DEC_413 = "The speaker portal renders every date in the owning event's timezone, carried per row, because the portal spans events";
export const DEC_414 = "A 390px overflow is closed by giving the wide region its own scroller or letting it wrap -- never by hiding overflow, never by shrinking a control below 44px";
export const DEC_415 = "Speaker portal edits to job_title/company/bio persist to the contact record, exactly like first/last name";
export const DEC_416 = "An unknown track id is rejected even when the form offers zero tracks, and the public track join is scoped to the event";
export const DEC_417 = "One shared parseBoundedText caps every admin mutation's free-text field; oversized input is a 400, never a D1 SQLITE_TOOBIG 500";
export const DEC_418 = "Public list surfaces bound their result set in SQL and count separately -- never a JS slice, and never a LIMIT on fanned-out join rows";
