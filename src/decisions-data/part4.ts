// Design-decision constants, range DEC_630-DEC_840 — part of the src/decisions.ts
// registry, split into modules to reduce merge contention. Regenerated by
// the swarm scribe from decisions/DEC-*.md each wave; do not edit by hand.
// Import via "../decisions" (the barrel), not this file directly.

export const DEC_629 = "Merging duplicates is set-based: one call collapses a whole cluster into one survivor";
export const DEC_630 = "A phone-only block is display:none at desktop, enumerated across every stylesheet";
export const DEC_631 = "A confirmation is a dialog in our own contract; window.confirm/prompt/alert are banned by an enumerating scan";
export const DEC_632 = "A results row carries the decision the server holds";
export const DEC_633 = "The reviews are readable where the decision is made";
export const DEC_634 = "A public list filter is a query predicate, never a post-page sieve";
export const DEC_635 = "A request the router cannot answer still gets our answer: the API gets the envelope, a human gets a page";
export const DEC_636 = "Framing is a closed two-list: /embed/* is framable, every other surface denies";
export const DEC_637 = "The API reference is enumerated from the router, not maintained by hand";
export const DEC_638 = "Tracks are editable wherever they are read; a capability the API has and no screen exposes is a dead endpoint";
export const DEC_639 = "The Sessionboard importer gains a fourth entity, `participants`, whose identity is the resolved (submission, contact) PAIR — it mints no external_ref of its own";
export const DEC_640 = "An imported participant is a publishable speaker or it is a NAMED skip: it writes the DEC-258 attribution snapshot and an assigned order inside the insert, and an unresolvable reference never creates a stub";
export const DEC_641 = "Sessionboard layer 2 (the REST-API adapter) closes as a documented stage-1 non-goal, with its reason";
export const DEC_642 = "The audit's \"deliberately not built\" list is machine-falsifiable: every bullet carries an absence marker, and the test fails when the named artefact EXISTS";
export const DEC_643 = "The type scale is a token set, and Overview's key type roles are asserted by COMPUTED weight in the render sweep";
export const DEC_644 = "The perf harness measures a PROFILE — the same one the seeder built — and the one bar the scale mandate says to verify becomes a timed check";
export const DEC_645 = "The aie profile scales every dimension the scale mandate names, not just the row counts the first pass reached";
export const DEC_646 = "SEED_NOW's overdue window is set by the mock: three named speaker tasks at 4, 2 and 1 days late, across distinct speakers";
export const DEC_647 = "A closed two-list is closed in BOTH directions: every pubcache pattern matches a real route, and no path matches both lists";
export const DEC_648 = "Saved views are a VISIBLE tab row derived from the live filter state, never a disclosure — the floating panel is deleted, not restyled";
export const DEC_649 = "An export is the SAME where clause as the list it sits beside — the filter travels with the download or the file lies";
export const DEC_650 = "The CFP builder's primary content is the QUESTION LIST, and a condition renders the referenced field's LABEL — an id in the UI is an internal handle leaking";
export const DEC_651 = "Amendment (wave 7): ModalFrame's header is paired to its CONTAINER's inset at every width, and gains one optional back affordance";
export const DEC_652 = "Overview §04 carries the CONCRETE next action the server computed — \"Place at 11:30\" — and clicking it places; a suggestion the server cannot compute is absent, never invented";
export const DEC_653 = "The SPA has no second upload vocabulary: delete the mirror, import the pure core";
export const DEC_654 = "Scale is a GATE, not a paragraph: the scale-mandate functional bars are timed, named checks that exit non-zero";
export const DEC_655 = "A plan_reviewer row selects WITHIN the plan's filtered universe: the per-submission branch intersects plan.filters.trackIds like every other reader, and an out-of-universe assignment is refused at write time";
export const DEC_656 = "A speaker-added co-presenter is RECORDED, not PUBLISHED: portal-created participants land visible=false and reach the public site only through the organizer's existing visibility toggle (amends DEC-604)";
export const DEC_657 = "The speaker portal's file surface keeps no private upload vocabulary and ships no unlabelled byte stream; the DEC-551 nosniff guard enumerates the whole repo";
export const DEC_658 = "Cacheability is a closed rule with one default: a response that does not set its own Cache-Control gets no-store, app-wide (supersedes the /api/v1-only noStoreApi scope)";
export const DEC_659 = "A scope row carries its own LABEL from the server: GET /api/v1/plans/:id/reviewers emits trackName and submissionRef/submissionTitle beside the ids, and the plan editor never prints a ULID";
export const DEC_660 = "One merge-field vocabulary: the per-surface resolvable sets and the 100-recipient cap are declared once in pure core and reach the SPA through a single boundary module";
export const DEC_661 = "Every public surface has a guessable root: /e/:slug and /embed/:slug redirect to the sessions surface, and an unknown slug 404s rather than redirecting into one";
export const DEC_662 = "The onboarding grid's per-cell affordance is CONDITIONAL and QUIET; a progress row names the person, not their inbox; and the roster's create actions live on the page's one action row";
export const DEC_663 = "The phone import pager ends in the dry run, and the frame's existing-contact count renders only from the plan that produced it";
export const DEC_664 = "One reporter for every send: the SPA renders every reminder/send outcome through a single describeSendResult that must account for every intended recipient";
export const DEC_665 = "A surface never asserts a fact it has not yet measured, and a worklist opens on a view that can hold work";
export const DEC_666 = "An internal shorthand is not public prose: a session with no room renders \"To be announced\" from one exported label, while the JSON/ICS payloads keep room null";
export const DEC_667 = "A run reports what it declined to do: auto-schedule's result names the pre-existing clashes it left in place alongside what it placed";
export const DEC_668 = "The seed carries three evaluation plans in distinct lifecycle states, because a plans list of one cannot demonstrate the state vocabulary it renders";
export const DEC_669 = "Contact headshots reach the Files area as a SEPARATE, separately-paginated tab, never as rows unioned into the deliverable chain query";
export const DEC_670 = "A capped anonymous list reports the cap in the vocabulary of what it SHOWS, never a count of what it hides";
export const DEC_671 = "An export is the SAME where clause as the list it sits beside -- the contacts directory included";
export const DEC_672 = "/embed is a CLOSED surface: every link an embed page renders resolves inside /embed, and every path it renders is a registered route";
export const DEC_673 = "The embed builder speaks the SERVER's vocabulary and never asks the organizer to type an internal id";
export const DEC_674 = "The organiser Review landing is the three-region worklist reached without leaving the page";
export const DEC_675 = "An import speaks the product's OWN closed vocabularies, and an imported participant is RECORDED, not PUBLISHED";
export const DEC_676 = "A plan's criteria are a weighted, guided, freezable list; the editor states each criterion's share and names the lock";
export const DEC_677 = "The send reporter is the ONLY sender vocabulary: every send flow renders describeSendResult and no surface prints its own count";
export const DEC_678 = "Amendment (wave 91): the filtered empty-state voice has ONE home (app/src/pages/speakers/narrowing.ts); a component that inlines a second copy is the defect the module-reachability orphan is reporting";
export const DEC_679 = "The route manifest is DERIVED from the composed route table, and the /embed closure proof walks every embed page including the detail routes";
export const DEC_680 = "A list's `total` is a count(*), never the length of a materialized scan; and 'accepted speaker' is ONE predicate";
export const DEC_681 = "A conditional rule's value is TYPED by its trigger field's kind, and the browser toggler computes the same transitive fixed point as the server — one matcher, parity-locked";
export const DEC_682 = "{feedback} is a merge var only when feedback is actually attached, and attached feedback is scoped to the composing plan and round";
export const DEC_683 = "The public sessions page is list + rail, the rail never renders inside /embed, and \"Save\" is the ONE itinerary vocabulary";
export const DEC_684 = "An irreversible merge is a PAGE at its own URL, never a modal; and a list's search box lives in the toolbar row under the title";
export const DEC_685 = "A dialog's form is one shared FormRow inside ModalFrame — labels above, inputs at the modal's full measure";
export const DEC_686 = "A paged read touches only the page: per-page aggregates key to the page's rows, and a thread pages in SQL";
export const DEC_687 = "A function embedded in a page by .toString() carries no closure: every free identifier travels with it, and the emitted script is proved by EXECUTING it";
export const DEC_688 = "The speaker portal home IS the worklist: header, waiting-on-you, your session, done, identity footer — no pipe nav, no submissions table";
export const DEC_689 = "Three button tiers, and the tier class always wins: no element selector inside a tier rule, and an anchor styled as a button is a button";
export const DEC_690 = "The public hub speaks the event's own calendar grammar, gives a row ONE action, and spells small counts";
export const DEC_691 = "Settings is a read-only SUMMARY over the mock's seven sections; each section is a summary plus a drill-in, and Public pages is first-class with the embed builder reachable FROM it";
export const DEC_692 = "A content worklist row names the session, the speaker, the LATEST artefact and its status, and carries exactly two actions";
export const DEC_693 = "Where a test contradicts the design pack, THE TEST is wrong; and a not-found surface is a designed page on both sides of the wire";
export const DEC_694 = "A scope parameter goes on the send AND its preview, identically; a response viewer has one action over values that round-trip";
export const DEC_695 = "A thrown render is a DESIGNED error state, never a blank page: one route-level error boundary, asserted per role";
export const DEC_696 = "Organizer-authored prose renders through ONE escape-first Markdown renderer; a track fieldset uses the CFP option vocabulary and its own copy";
export const DEC_697 = "A response you did not create may have IMMUTABLE headers: every decorating middleware goes through one clone-on-failure helper, proved against a frozen-header response";
export const DEC_698 = "A grid row's cells are POSITIONAL: an absent fact renders an EMPTY cell, never a missing one";
export const DEC_699 = "Waitlisted is a HOLD, not a decision: a sixth submission status, never public, never 'decided', speaker-facing as \"Under review\"";
export const DEC_700 = "A badge is a live claim: exception counts refetch on navigation and after every mutating request";
export const DEC_701 = "Warn-never-block covers the KEYBOARD path: an occupied slot offers placement, and the offer names the clash it will create";
export const DEC_702 = "Seeded prose is real prose: the seed never ships text that says it is seed data";
export const DEC_703 = "A ranked result names the human and the track, and the export carries the table's columns in the table's order";
export const DEC_704 = "A bulk action's label states the scope it actually SENDS; the deadline strip's order is fixed and only the nearest is emphasised";
export const DEC_705 = "A merge preview is the merge function's own output: every field the merge will change, in the words of what it will do";
export const DEC_706 = "A section's action is a link on its rule; a filled primary lives on the title row or a form footer; a row is chosen by clicking the row, never by a radio";
export const DEC_707 = "Reviewer progress speaks three states, and a reminder's label names the scope it actually sends -- from ONE predicate both the route and the SPA import";
export const DEC_708 = "A person is named by their contact, resolved from the account by ONE batched rule; an unresolvable account shows its email, never an invented name";
export const DEC_709 = "Locked criteria are not a dead end: 'Start a new wave' carries the frozen criteria into a new round, and the plan editor is a summary plus inline rows";
export const DEC_710 = "A tab strip's selection is URL state (?tab=), never component state";
export const DEC_711 = "The contacts directory is a table plus a rail, and every figure the page states is endpoint-backed";
export const DEC_712 = "A contact's labels are DERIVED participation roles, batched one query per page -- no invented labels column";
export const DEC_713 = "A file version is deletable: the chain re-links across the gap, the thread is re-homed, the object goes with the row, and the removal is recorded";
export const DEC_714 = "A role control offers the app's OWN role vocabulary, imported -- never another system's role names";
export const DEC_715 = "Reordering has ONE affordance, operable by pointer and by keyboard: the drag handle IS a button";
export const DEC_716 = "The sign-in page names the event when there is exactly one, and offers the open call for papers as the only onward action -- suppressed when none is open";
export const DEC_717 = "Every writer of submission.status goes through the one status writer — an imported acceptance is an acceptance";
export const DEC_718 = "A validator rejects what the persistence layer cannot carry: number answers must be finite, and every accepted answer survives a JSON round trip";
export const DEC_719 = "A sandboxed embed child is identified by its source window and per-instance nonce; its origin is the literal string \"null\"";
export const DEC_720 = "Asking for changes is a message, not a column flip: one endpoint posts the note, optionally moves content status, and mails the speakers — the bare content-status route stays mailer-free";
export const DEC_721 = "The session content screen shows one deliverable at a time: a chip scopes both the version list and the single thread";
export const DEC_722 = "A detail page spans the same measure as its sibling list pages, and its decision panel decides only";
export const DEC_723 = "An evaluation read carries its plan's criteria and the plan's own weighted score; a review is never rendered from bare criterion ids";
export const DEC_724 = "The null-room agenda column is conditional, and it is called \"No room yet\"";
export const DEC_725 = "The Airtable push is incremental from a stored watermark, and a rate-limited response backs off instead of throwing";
export const DEC_726 = "A contact's Labels are its customFields, rendered in one keyed format everywhere (supersedes DEC-712)";
export const DEC_727 = "Test tiers are DERIVED by predicate, never hand-listed, and parallelism is capped in config (not the environment)";
export const DEC_728 = "Settings is a read-only summary with drill-in editing, and the drilled section is URL state";
export const DEC_729 = "The portal shows every submission the speaker owns, with its status — pending and declined included";
export const DEC_730 = "Amendment (wave 90): the v12 status inversion is ONE vocabulary in every state including write-failure — Speakers frame :549's filled Complete pill is a stale pre-inversion cut and does not bind";
export const DEC_731 = "A call for papers is published by its window, not a flag: the builder states the live state and writes open/close dates";
export const DEC_732 = "A dialog's form is one measure with labels above; a send names the constraint each recipient carries";
export const DEC_733 = "An action that cannot apply to a row is ABSENT, not disabled — and a library states only endpoint-backed figures";
export const DEC_734 = "A duplicate group carries the identity fields the merge screen shows, and 'Keep both' is a first-class outcome";
export const DEC_735 = "A class describes ONE layout behaviour; a suggestion names the room it would fill";
export const DEC_736 = "Anonymised review hides the SPEAKER from the REVIEWER; the organiser is always told who reviewed (supersedes DEC-622's null-iff-anonymized rule)";
export const DEC_737 = "A results row states ONE blended score; per-criterion detail lives behind its Reviews disclosure, and arrow, CSV link and row order are the same fact";
export const DEC_738 = "Labels ARE customFields minus the reserved travel key, formatted once server-side as `key value` chips in stable key order";
export const DEC_739 = "The seed must prove every read it enables: a complete task carries its answers, a batch carries its recipients, a contact carries its labels";
export const DEC_740 = "The sign-in page names the event and shows both public doors; the password minimum is 12, stated at the field and enforced by one constant";
export const DEC_741 = "The content note endpoint: POST /submissions/:id/content-note posts to the chain thread, optionally sets changes_requested, and mails the speakers";
export const DEC_742 = "A clash is shown at full size, and placing a session returns focus to what was placed";
export const DEC_743 = "The submission detail decides the SUBMISSION; content is decided on the content screen, and each section carries its own header";
export const DEC_744 = "One page measure: `--chq-measure` (820px) is the only desktop clamp a single-column subscreen may use; a page with an aside fills the frame";
export const DEC_745 = "The plan editor is the v4 shell: the plan's NAME is the page title, Duplicate + Save sit on the title row, rounds are waves not a field, and the anonymise switch moves into \"Who reviews what\"";
export const DEC_746 = "A task is created FOR the accepted roster — creation always expands, and task kinds are named for what the speaker does (Upload / Form / Acknowledge)";
export const DEC_747 = "Every Settings section is a read view with ONE action on its rule; the rail is SEVEN, and Import from Sessionboard is a row inside Your data";
export const DEC_748 = "The merge screen shows EVERY differing field, including value-versus-empty, and the duplicate rule is stated where the pairs are listed";
export const DEC_749 = "A person's name is entered as ONE field and split on the last whitespace";
export const DEC_750 = "A dialog may not promise what the store cannot carry, nor drop a control the data model needs — and dialog actions align left";
export const DEC_751 = "Recent sends live under Compose as well as in History — one component, two mounts, four rows and a link";
export const DEC_752 = "The bulk bar carries three moves and a Clear, and the primary is the move that applies to what you are looking at";
export const DEC_753 = "A list envelope's KEYS do not depend on its contents, and a seeded reviewer is a named person";
export const DEC_754 = "The speaker roster and the set a task expands over are ONE predicate: the onboarding grid's rows ARE the event's accepted speakers";
export const DEC_755 = "Every control in a creation dialog reaches the store: New submission persists Track and Format, and a contact is matched by (org, lower(email))";
export const DEC_756 = "The content session detail shows ONE deliverable at a time: a chip scopes the version list AND the note thread, and Approve is absent once approved";
export const DEC_757 = "An author is a PERSON: attribution resolves the stored contact, then the account's contact, then the address — and never prints \"Unknown\"";
export const DEC_758 = "A contact is deleted only when nothing depends on it; the refusal names the counts, and merge is the answer for a contact with history";
export const DEC_759 = "A placed agenda card sizes to its content and overlapping sessions split the slot into lanes — no card is an inner scroll region";
export const DEC_760 = "The Review landing states counts as facts, and the remind link is absent at zero";
export const DEC_761 = "A submission detail knows its position in the list you came from, and re-derives it from the URL rather than router state";
export const DEC_762 = "The field-kind vocabulary the organiser reads is the mock's, defined once; and a public link is disclosed exactly once per screen";
export const DEC_763 = "Evaluations-for-submission is plan-scoped, and the results export carries the sort the table is showing";
export const DEC_764 = "Add-to-event creates a real, named session -- the model has no contact-to-event roster to create instead";
export const DEC_765 = "A contact pushed to an event is linked by its own id, and the dialog's role reaches participant.role";
export const DEC_766 = "Every send ATTEMPT writes an email_log row; failure is a status, not an absence";
export const DEC_767 = "A public surface's published count is the number that surface's own query returns";
export const DEC_768 = "Public agenda rows grow to their content; a block never clips";
export const DEC_769 = "Occupancy excludes the session being placed, and while armed a cell owns its own click";
export const DEC_770 = "\"Not a duplicate\" is a persisted fact about a pair, not a session mood";
export const DEC_771 = "The seed is the grader package: nothing collides on the identity a human matches by, and every screen shows its own shape";
export const DEC_772 = "A session's length is its format's, not the grid's default";
export const DEC_773 = "The files library is ONE list; a headshot is a file kind, not a tab";
export const DEC_774 = "The sessions surface filters on the facets it displays";
export const DEC_775 = "XML is an output format of the surface feeds, not a gap in the builder";
export const DEC_776 = "Overdue is ONE predicate, over the roster the grid can actually show";
export const DEC_777 = "The speaker's own session page: one fact per row, event-timezone dates, no organiser-only sections";
export const DEC_778 = "A role you can see is a role you can change";
export const DEC_779 = "A separator earns its place only when both sides exist";
export const DEC_780 = "The organiser's submission detail carries the format and the placement";
export const DEC_781 = "Call for papers is a read-only summary with one drill-in";
export const DEC_782 = "A public detail page owes the reader every action its card offered, and the same date grammar";
export const DEC_783 = "The itinerary surfaces read the same query vocabulary as the sessions list, and a control names its state";
export const DEC_784 = "A participant's role is chosen where the participant is added, and always rendered as a label";
export const DEC_785 = "A saved embed is a named record whose disabled state is a 404, not a grey pill";
export const DEC_786 = "Reviewer workload is distributed by a pure, deterministic round-robin behind a preview";
export const DEC_787 = "A directory filter is a SET of rules; the rail composes with it, never replaces it";
export const DEC_788 = "A duplicate is warned about at creation, by the same predicate the Duplicates tab uses";
export const DEC_789 = "A speaker's workflow status is organizer-settable and roster-filterable; the overdue mark says OVERDUE";
export const DEC_790 = "The public call-for-papers page offers an account before it needs one";
export const DEC_791 = "The agenda's chrome speaks the reader's language, and the builder links to where rooms are made";
export const DEC_792 = "Compose speaks the whole merge vocabulary; a seeded template is a promise the send path must keep";
export const DEC_793 = "A field the composer will reject is never advertised, and a rejected recipient is named";
export const DEC_794 = "Arming a placement never moves the grid, and the target you must hit is never buried";
export const DEC_795 = "You add someone to the event you are looking at, and a repeat roster line announces itself";
export const DEC_796 = "Seeded history shows what was sent, not the template that made it";
export const DEC_797 = "Test isolation is a harness guarantee, not a per-file habit";
export const DEC_798 = "A disclosure replaces its preview, and every link says where it goes";
export const DEC_799 = "A ratchet counts only the promises actually made";
export const DEC_800 = "Two records with one name are a candidate even when the companies differ — with the reason named";
export const DEC_801 = "A task cannot be late before it was assigned: an assignment's effective due date";
export const DEC_802 = "A merge preview reads BOTH records, names the record it discards, and strikes only what is really thrown away";
export const DEC_803 = "A pipeline card states how long it has sat there, and a decline states why";
export const DEC_804 = "A public surface renders a control for every filter it applies, and offers none it ignores";
export const DEC_805 = "Inviting a speaker to the portal is a send, not a pill";
export const DEC_806 = "/admin resolves to /admin/overview";
export const DEC_807 = "The event switcher is one quiet control";
export const DEC_808 = "One page measure, finally adopted by the admin SPA";
export const DEC_809 = "A saved view re-saved under its own name is one view, and the directory headline states every figure it computes";
export const DEC_810 = "Adding a contact to an event never invents a talk title";
export const DEC_811 = "A malformed cookie is external input, not an invariant — parseCookies never throws";
export const DEC_812 = "The cron tick is configured in the repo, and its two jobs cannot take each other down";
export const DEC_813 = "Every write that makes a participant ACTIVE creates their onboarding tasks";
export const DEC_814 = "An unverified stranger may name a contact, never edit one";
export const DEC_815 = "A settings section is a read-only summary until you ask to change it";
export const DEC_816 = "Each public surface's count is the set that surface actually renders";
export const DEC_817 = "A field accepts exactly the value its own placeholder shows";
export const DEC_818 = "A version number is an identity, not a position among the survivors";
export const DEC_819 = "The Review surface names the number it is showing";
export const DEC_820 = "Every portal page says who is signed in";
export const DEC_821 = "Pipeline fit is two nullable columns, set when someone is enrolled and editable after, ranking only within a stage";
export const DEC_822 = "A saved embed saves the recipe, not just the name — and turning one off is an intentional blank, not a broken page";
export const DEC_823 = "The seed ships duplicate PAIRS on purpose; DEC-771's no-collision rule is scoped to the contacts a grader matches by name";
export const DEC_824 = "Distribution states its cap and names what it could not do";
export const DEC_825 = "The content worklist's chips are the three questions an organiser asks, each counted by its own predicate";
export const DEC_826 = "The effective due date is the date every surface SHOWS, not merely the one the server selects on";
export const DEC_827 = "Import lives in Contacts; Speakers links to it with the event already chosen";
export const DEC_828 = "A placed session says when and where it is, on the organiser's submission detail too";
export const DEC_829 = "The Speakers roster lists every participant of an accepted submission; ACTIVE is the predicate for expansion and access, never for LISTING";
export const DEC_830 = "Participation status is a MENU of named states, each stating its consequence; the portal invite is an ACTION in that menu, not a state it writes";
export const DEC_831 = "A scoped queue is headed by the plan it is scoped to and counts what is LEFT; a nav link's href is the route it must highlight";
export const DEC_832 = "The composer sends the text it shows — a template is a starting point, not a mode";
export const DEC_833 = "A send is auditable to its words: the organiser can read exactly what a recipient received";
export const DEC_834 = "At an irreversible choice, a column is headed by the RECORD, not just by the person";
export const DEC_835 = "A public day pill is a navigation, not a scroll — every public day view names its day in the URL";
export const DEC_836 = "The seed demonstrates the reads a grader performs: two plans open at once, and a worklist with work in it";
export const DEC_837 = "A link is the route it lands on: an in-app target is basename-relative, and a link into a URL-state page carries that page's state";
export const DEC_838 = "A knob bound to nothing is a lie; the accent binds only where the default already renders that colour";
export const DEC_839 = "The saved-embed wire contract: options are a parsed recipe validated by the live route's own parsers, and OFF is an empty 200";
export const DEC_840 = "The distribute wire contract: the cap is a parameter of this run, echoed back, and the shortfall is part of the answer";
export const DEC_841 = "The error surface follows the REQUEST's path, not the middleware that happened to run — one predicate shared with the 404 handler";
export const DEC_842 = "A submitted blank is an instruction to CLEAR; an absent key is silence";
export const DEC_843 = "A list and the export beside it read status through ONE reader, and an unknown token is loud on both";
export const DEC_844 = "Narrowing an event's window never blocks — but it names every session it unschedules, at the moment of the write";
export const DEC_845 = "A queue headed by a plan carries the plan's facts on its own wire, including the reviewer's own score";
export const DEC_846 = "A composer sends the text on screen; a template is a starting point, not a hidden source of truth — and history owes the WORDS";
export const DEC_847 = "A subject is one line: a block merge field is body vocabulary, and an inline stated-absence value carries no terminal period";
export const DEC_848 = "Two plans open at once, sharing one reviewer, is seed data — a scoped queue nobody can see scoped proves nothing";
export const DEC_849 = "A committed conflict marker is a build break, not a formatting nit — the guard is a scan, and the saved-embed resolution checks DISABLED the moment the row is found";
export const DEC_850 = "A saved embed's single URL serves its SAVED format; a recipe that stores a format and always answers HTML is lying about the recipe";
export const DEC_851 = "One filter contract for a public surface: the page, its .json twin, its .xml twin and the embed builder's knob table all read the same enumerated set";
export const DEC_852 = "A deadline the product silently moved is visible before it bites, and a date outside this year names its year";
export const DEC_853 = "A write that succeeds says what it did, in the vocabulary its automated twin already uses — including the clash it just created";
export const DEC_854 = "A seeded worklist that is empty for 28 of 30 rows fails its read no matter how correct the code is";
export const DEC_855 = "A submission's tracks live ONLY in submission_track: the frozen scalar is neither written nor read, and a scan enumerates it";
export const DEC_856 = "A preflight names EVERY merge field each recipient is missing, in one message shape, and the banner announces itself";
export const DEC_857 = "A reviewer's queue row names the action it offers and the session's shape; nothing organizer-scoped mounts before the shell knows who is signed in";
export const DEC_858 = "Two names differing only by case are the same name at an irreversible merge; and a skip control belongs on every row it can skip";
export const DEC_859 = "The add-to-the-pipeline dialog is the frame's dialog: ModalFrame, chips, and the consequence stated before the write";
export const DEC_860 = "One saved embed is created one way: the builder is the only save path, and the row that lists an embed can delete it";
export const DEC_861 = "A single-choice filter is a select, not four pressable pills that silently clear each other; and two filter axes on one screen each name their axis";
export const DEC_862 = "The CFP confirmation names the reference and the address it emailed, and each of DEC-098's three states carries the design's own primary label";
export const DEC_863 = "A speaker name typed into the New submission dialog is never silently dropped, and no placeholder in the product advertises a test-harness identity";
export const DEC_864 = "A rail that prints N segment counts scans the directory ONCE: one bounded pass, the same matchesSegment predicate, N counts";
export const DEC_865 = "The org user directory is organizer/reviewer only: a speaker portal login is not an org user and cannot be listed, re-passworded, or promoted through /api/v1/users";
export const DEC_866 = "A portal co-presenter is shown under the name the speaker typed; a CRM identity matched from a supplied email is never echoed back";
export const DEC_867 = "Blank is ABSENT for every field kind, and `ne` never fires on an absent trigger";
export const DEC_868 = "The Contacts filter row states its conjunction, edits in place, and counts N of M; a half-typed rule is never a query";
export const DEC_869 = "A menu of states names each state's consequence, marks the one in force, and emphasizes the only item that sends mail";
export const DEC_870 = "The armed no-room affordance lives inside the grid column, never as a sibling grid item — the Unscheduled tray is never displaced";
export const DEC_871 = "A <td> is a table cell, never a flex container";
export const DEC_872 = "A role is a fact about a USER: a note with no user has no role, and the viewer's own note says \"You\"";
export const DEC_873 = "A rating is chosen from the scale, the blend is computed by the server's own function, and an evaluation is submitted or absent — the frame's \"Save draft\" ships as \"Save\"";
export const DEC_874 = "Amendment (wave 91): exactly ONE .chq-phone-dock renders per phone surface, and the reviewer pin forbids MINTING a sign-out, not rendering one";
export const DEC_875 = "The open review plan carries a real per-talk cap: null is not a neutral default when three surfaces read it";
export const DEC_876 = "A send's body is a detail read, not a widened list: comms history opens one message and shows what was actually sent";
export const DEC_877 = "The frame's reading column is 820px CENTERED: a single-column page clamps BOTH its header row and its content column to one token; a page with an aside clamps nothing";
export const DEC_878 = "The per-submission decision rail is a triage ACTION, not a status picker: Accept is the primary, Decline|Waitlist the secondary pair, and the caption says deciding sends nothing";
export const DEC_879 = "A recording is a deliverable: the upload allowlist grows a video tier and `recording` is a file kind, both derived from one enumeration";
export const DEC_880 = "A reset scopes to the properties it means: a chip trigger never strips its family's outline; and Add speaker is a dialog, not a page-displacing panel";
export const DEC_881 = "\"Re-uploaded\" counts re-uploads: the worklist's status cell and the header's number read ONE predicate";
export const DEC_882 = "A frozen round reads as TEXT, the criteria table names its columns, and the lock card explains below what the rows already show";
export const DEC_883 = "A toggle that needs a companion choice reveals the choice and WAITS; the merged reviewer feedback is a quoted block in the PREVIEW, never in the sent body";
export const DEC_884 = "The portal header prints the EVENT's name as the event writes it: \"chautauqua\" is the only lowercase wordmark, and the tagline leaves the header";
export const DEC_885 = "The public speaker directory is three-up at the desktop frame, an absent headshot is a DRAWN placeholder, and every day pill says whether it is the day being shown";
export const DEC_886 = "A session can be deleted, from a confirmation PAGE that names what goes with it, and a submitted evaluation makes it undeletable";
export const DEC_887 = "Six Settings frame elements are unrepresentable because the seed never writes them — so the seed writes them";
export const DEC_888 = "A Settings edit view speaks the same form vocabulary as every other form: no browser bullets, no native fieldset, and no off-palette colour widget";
export const DEC_889 = "The scorecard is a rating surface: a clamped abstract with one disclosure, not a second copy of the submission detail";
export const DEC_890 = "Templates is a page with a header and rows that say what each template is for and when it was last actually sent";
export const DEC_891 = "A deliverable belongs to the session the speaker NAMES; a tie-break is not a choice";
export const DEC_892 = "Submission history is DERIVED from rows that already exist -- one union, no audit table, no new column";
export const DEC_893 = "A number answer is a finite decimal number or it is an error -- never a silent reinterpretation, never a silent blank";
export const DEC_894 = "Every headshot path runs the same gate; WEBP is not exempt, and the organizer's path thumbnails too";
export const DEC_895 = "A suggested placement is sized by the session's own format, and no two suggestions in one payload claim the same room-minute";
export const DEC_896 = "A settings row is label / value / hint, and the rail names the section actually in view";
export const DEC_897 = "Required is an attribute, not an asterisk; a start and an end are one row";
export const DEC_898 = "A pipeline column accepts a dropped card, and a stage picker opens on the stage you are in";
export const DEC_899 = "A same-room pile-up is ONE card at any size — the pair case was never the rule, only the case we implemented";
export const DEC_900 = "The agenda's page actions live on its title row; a control offered when its action is not needed is clutter";
export const DEC_901 = "A deliverable detail is headed like every other detail page, and a version row always names its number";
export const DEC_902 = "The library counts its kinds in ONE grouped query, and a chip for a kind nobody has is not offered";
export const DEC_903 = "A drag handle drags — the field list joins the app's ONE drag contract, and a disabled row looks disabled";
export const DEC_904 = "A saved view is private until its author shares it";
export const DEC_905 = "A list the reader just changed refetches through the mutation signal the app already owns; a printed count is its own query's count";
export const DEC_906 = "Results are ranked: RANK leads the table, REF and the evaluation tally leave it, and a decision is one line";
export const DEC_907 = "One date grammar for the admin app, in one formatter — and 'who wrote this' is a name and a relative time";
export const DEC_908 = "Submission detail is the frame's three-and-three: ABSTRACT/FORM ANSWERS/REVIEWS reading left, DECISION/SPEAKER/HISTORY in the rail — editing sinks below the reading order and Meta dies";
export const DEC_909 = "A form field states its own optionality and its own budget — one renderer, one grammar, no asterisks";
export const DEC_910 = "A role count counts the role — 'everyone who is not an organiser' is not a reviewer";
export const DEC_911 = "There is one public 404 and it is the designed card — a bare c.text is an undesigned second 404";
export const DEC_912 = "Whether a recipient has a slot is a fact about the recipient, not about the calendar checkbox — and a recipient row names the talk";
export const DEC_913 = "A tab strip's counts are ONE grouped query on the list envelope, not one request per tab";
export const DEC_914 = "A back link names the route it LANDS on, and the portal has one back-link grammar";
export const DEC_915 = "A settings edit row is a DRAFT with an explicit Save, everything listed is editable, and a section you can enter is a section you can leave";
export const DEC_916 = "A per-row count rides its own list's envelope, computed by one grouped query -- never one request per row";
export const DEC_917 = "One optionality grammar for every form row in the product: no asterisks, '· optional' on the skippable, and a prop that renders nothing is deleted";
export const DEC_918 = "The server has ONE calendar-day grammar module, and a scan test says so -- a range printed twice is printed two ways";
export const DEC_919 = "Amendment (wave 7): a 390 head is reached by RE-LINING, never by removing a control";
export const DEC_920 = "A link to a file names the file";
export const DEC_921 = "Deleting a session takes its agenda placement with it and REOPENS (never destroys) the task responses its files completed; the confirmation page reads ONE plan that names both";
export const DEC_922 = "A file version chain never crosses a submission or a kind -- including the portal task upload path";
export const DEC_923 = "The mailer is the ONLY author of an email_log row, and a failed attempt has exactly one name";
export const DEC_924 = "Assigning one reviewer to a chosen SET of talks is one request, all-or-nothing";
export const DEC_925 = "One count-phrase helper: a printed number and its noun always agree";
export const DEC_926 = "Deleting a file version never leaves a task_assignment pointing at a hole: repoint to the surviving neighbour, else REOPEN the task";
export const DEC_927 = "The speaker portal's round-trip count is independent of its row count: a chain read carries its own version numbers, a home read is set-based";
export const DEC_928 = "The deliverable kind an organizer picks IS the file-kind enumeration -- imported, ordered, exhaustive";
export const DEC_929 = "Deleting an evaluation plan states its blast radius from ONE read, and takes the recusals with it";
export const DEC_930 = "A speaker has ONE event-scoped detail page at /admin/speakers/:contactId, served by ONE bounded GET";
export const DEC_931 = "A refused delete names the rows that refuse it, and finds every one of them in ONE query";
export const DEC_932 = "Every active accepted speaker holds an assignment for EVERY task the event owns: activation BACK-FILLS, it never snapshots";
export const DEC_933 = "A task column is editable and removable from the grid it defines, and removing one states what it destroys";
export const DEC_934 = "A roster row the product will never chase says so IN the cells it leaves blank";
export const DEC_935 = "One session's content lives at its own URL: /admin/content/:submissionId, not a query parameter on the worklist";
export const DEC_936 = "A roster row that covers two of the event's sessions carries BOTH participations; a lowest-id pick is not an answer to \"which of your talks\"";
export const DEC_937 = "A stylesheet may only reference tokens that EXIST, and a button base rule always names its own face -- both scan-locked";
export const DEC_938 = "The armed agenda grid quiets a clash card with INK, never with opacity; the armed lattice is a 1px inset ring";
export const DEC_939 = "A rating scale is a radio group of EQUAL segments filled in brand, read at the product measure, and the conflict declaration comes AFTER the work";
export const DEC_940 = "The participation trigger resets ONLY what a chip cannot inherit, and the test asserts the chip, not the reset";
export const DEC_941 = "Amendment (wave 91): when a panel splits into a hook and a view, the delete guard resolves through the PAIR — the hook owning apiDelete is guarded by the sibling view that renders its ConfirmDialog";
export const DEC_942 = "The seed exercises the states the product can reach: a recusal, a spread of scores, and a rated pipeline";
export const DEC_943 = "A hub event row's meta names its SHAPE while it is live and its SIZE once it is past";
export const DEC_944 = "The auth family renders at the frames' own card metrics, and an expired claim link is a card, not a sentence";
export const DEC_945 = "One 404 card, three renderers — and it never prints the path it failed to match";
export const DEC_946 = "A cron job that catches per-item failures must still fail the tick";
export const DEC_947 = "A calendar invite's ORGANIZER is the deployment's configured sender; noreply@chautauqua.local is dev-only";
export const DEC_948 = "A rate limit is an atomic counter in D1, never a read-modify-write against KV";
export const DEC_949 = "One live claim grant per contact and event, and the organizer's audit view never renders it";
export const DEC_950 = "Every dialog field is a FormRow, and the rule is a scan, not a habit";
export const DEC_951 = "The public CFP keeps the multi-track model and speaks the product's one optionality grammar; the frame's radio group is refused with reason";
export const DEC_952 = "Every rendered page names itself exactly once, and the innermost mounted view owns the <h1>";
export const DEC_953 = "The portal speaks the frames' task vocabulary: To do / Done, while the wire keeps pending | complete";
export const DEC_954 = "One slot predicate on the compose preview, and a blocked send is disabled, not narrated";
export const DEC_955 = "A merge chip whose value depends on an attachment toggle reveals that toggle where the chip is";
export const DEC_956 = "A refusal names the rows it refuses over, and states both ways forward";
export const DEC_957 = "One count grammar, on both sides of the app/ boundary — '(s)' is not a plural";
export const DEC_958 = "A dialog field is a FormRow, and the scan bans the retired class by name — not merely a missing one";
export const DEC_959 = "The event hub renders the frame it was drawn from: one action per row, and a published row says what is PUBLISHED";
export const DEC_960 = "The scrim IS the dialog, and exactly one module in the SPA is allowed to draw it";
export const DEC_961 = "A terminal CFP page carries the way forward, and the confirmation says how long editing stays open";
export const DEC_962 = "A batched read carries its scope in the WHERE, not in its caller's discipline";
export const DEC_963 = "One SPA calendar-date grammar, and it does not depend on who is reading";
export const DEC_964 = "A row inside a group prints what DIFFERS, and the column that identifies it gets the width";
export const DEC_965 = "A version number is the row's stored identity, everywhere — a chain length is not a version";
export const DEC_966 = "The seeded call for papers is the frame's question table — eight questions, three of them optional, each with the hint the frame gives it";
export const DEC_967 = "A send is irreversible, so it asks once and names what it is about to do";
export const DEC_968 = "The public session row is the frame's row: caps meta, no abstract — the abstract belongs to the detail page and to an embed that asks for it";
export const DEC_969 = "One menu primitive: a popup menu dismisses on an outside press, takes focus, roves with arrows, and hands focus back";
export const DEC_970 = "The server-rendered stylesheets join the CSS contract: no undefined token, no class the markup uses and no rule defines";
export const DEC_971 = "Two chains of one kind are two deliverables, and the chip you pick is the chain you see";
export const DEC_972 = "A record prefix is per-event data, never a string literal — the public CFP confirmation quotes the event's own prefix, and a scan bans the literal form";
export const DEC_973 = "A hidden trigger hides its dependents structurally — the transitive fixed point marks, it does not delete the answer";
export const DEC_974 = "The admin agenda's speakers are the ACTIVE participants — a declined co-presenter neither clashes nor blocks auto-schedule";
export const DEC_975 = "DEC-904's read gate gets its write counterpart: you cannot delete a saved view you could not list";
export const DEC_976 = "Amendment (wave 91): a frame citation in PRODUCTION source carries the same receipt as one in a test file — the backtick-quoted literal, verbatim at the cited line";
export const DEC_977 = "A contact's Labels export the way they render — the empty tags column is retired";
export const DEC_978 = "The event switcher is an organiser control: it fetches nothing and renders nothing for a reviewer";
export const DEC_979 = "A task assignment and a pipeline entry are JOIN rows, not documents: contact delete cascades them (and the dialog names the cascade); only rows that would lose a document keep refusing";
export const DEC_980 = "A stage move and a fit edit are different writes: stage is optional on PATCH /pipeline/:id, a same-stage PATCH is never a move, and the card can edit fit after enrolment";
export const DEC_981 = "The one active-participant predicate reaches the data that LEAVES the product, and every participant reader is enumerated by a scan rather than spot-checked";
export const DEC_982 = "A shared row-grid class is a contract on CELL COUNT: the saved-embed row owns its own grid";
export const DEC_983 = "A visibility predicate is applied WHOLE or not at all, and an ALLOWED reason may never park a known defect";
export const DEC_984 = "A recusal is server state, not session state: the reviewer submission detail carries the caller's own recusal and the scorecard hydrates from it";
export const DEC_985 = "HTML_ROUTE_EXCLUDED holds only routes that render no HTML or cannot be visited idempotently — every other GET route is in ROUTE_MANIFEST";
export const DEC_986 = "The public CFP picks ONE track; the many-to-many model, the wire shape and the multi-select edit surfaces stay, and the seed keeps exactly two multi-track sessions";
export const DEC_987 = "One pluralization vocabulary per package, and the scan that enforces it carries no legacy allow-list";
export const DEC_988 = "Portal branding is edited where it is summarised: the Speaker-portal section's Change drill carries the WHOLE portal_settings record, and there is exactly one save path for it";
export const DEC_989 = "Amendment (wave 90): a phone overflow escape is PHONE-SCOPED — the 65 measured offenders are closed in the terminal max-width block, never by adding overflow:hidden to a base rule";
export const DEC_990 = "Amendment (wave 91): the docs article's 390 H1 is the frame's 28px literal — the SPA's --chq-type-page-title-phone-drill (25px) is a different register and never substitutes for it";
export const DEC_991 = "A clip exception names the ELEMENT's nature; a branch-ownership note or a deferred visual pass is not a reason, and the map holds exactly one entry";
export const DEC_992 = "The merge compare table names BOTH records and their vintages, and the primary names its target with the swap beside it";
export const DEC_993 = "One 'Insert a field' control per composer, and the merge-field sample values live beside the vocabulary they describe";
export const DEC_994 = "Session minting is ONE helper that revokes first: login, claim and password-change all rotate, and no path inserts an auth_session by hand";
export const DEC_995 = "A served file's Content-Type is the validated DB column; the object store returns bytes and never a content type";
export const DEC_996 = "The production mailer is the Cloudflare Email Service send_email binding; Resend is deleted, not mis-typed";
export const DEC_997 = "DEC-417's caps are a property of the COLUMN, not of the admin API: every public and portal HTML-form field passes the same four named caps";
export const DEC_998 = "The Content detail links straight into the submission's edit form, its history and its speakers — which means those two panels become URL state";
export const DEC_999 = "A public agenda block is a content-sized box in its own lane; the day grid neither clips a block nor lets one lane overrun another";
