// Design-decision constants, range DEC_420-DEC_629 — part of the src/decisions.ts
// registry, split into modules to reduce merge contention. Regenerated by
// the swarm scribe from decisions/DEC-*.md each wave; do not edit by hand.
// Import via "../decisions" (the barrel), not this file directly.

export const DEC_419 = "Wave 10's three gate lanes re-measure build/test, perf, and the phone sweep against the redesigned tree, log-only";
export const DEC_420 = "Reminder emails render each outstanding task's OWN due date, in the owning event's timezone";
export const DEC_421 = "The 10px type-floor becomes a measured, offender-naming render-sweep pass — advisory on the wave it lands";
export const DEC_422 = "Free-text caps and the public rate limiter extend to the two write paths DEC-417's scope does not reach";
export const DEC_423 = "Wave 11's three verification lanes are log-only, must use non-colliding filenames, and the spec-audit lane emits the stage-1 completion ledger";
export const DEC_424 = "The 390px overflow pass stops counting elements a horizontal scroller legitimately holds, and names the content-spill when the document overflows but no box escapes";
export const DEC_425 = "DEC-417's caps reach the last three uncapped free-text writes: reviewer scorecard text + comment, API-token name, upload filename";
export const DEC_426 = "WCAG AA contrast becomes the third measured design invariant -- desktop-only, advisory, offender-naming, in its own module";
export const DEC_427 = "A failed public-cache version bump must not turn a committed mutation into a 500 -- DEC-399's purge scope stays closed and untouched";
export const DEC_428 = "SPEC.md's PBKDF2 clause is amended to the binding DEC-004/DEC-237 value, and the evaluator-facing docs are diffed against the fixtures";
export const DEC_429 = "Stage-1 dependency-advisory policy: devDependency advisories are recorded, not chased; a runtime-dependency advisory is a lane";
export const DEC_430 = "WCAG AA remedies change the pixels, never the instrument's scope: the drag glyph moves to --chq-muted and the public track chip stops using data-supplied colour as a text background";
export const DEC_431 = "DEC-387's flip fires for the two passes that have now read all-PASS; contrast stays advisory one more wave";
export const DEC_432 = "Event scope belongs in the WHERE: the two surviving JS post-filters over unbounded row sets move into SQL";
export const DEC_433 = "The public ?page= parameter gets two bounds, because one number endangers two different things";
export const DEC_434 = "One dev-mode predicate: DEV_MODE === \"1\" and nothing else, spelled once";
export const DEC_435 = "formatRef is the only place a record ref is built; the Airtable sync's hardcoded SES- prefix is a stage-1 SPEC §5 repair, not stage-2 wiring";
export const DEC_436 = "CONTRAST_BLOCKING's flip is wave 14's, and only from wave 14's own reading";
export const DEC_437 = "SPEC §10 items 2, 3 and 4 are deferred by decision, not missing";
export const DEC_438 = "The completion ledger names the sha it audited and splits open items into FAIL-unowned versus PENDING-OWNED";
export const DEC_439 = "The three measured SPEC §7 budget misses are stage-1 product defects, and each is fixed by shrinking what one request loads";
export const DEC_440 = "buildResults keeps its JS aggregation: aggregateSubmission's fail-loudly throws must not be laundered into SQL";
export const DEC_441 = "SPEC §10 #3 (assisted chasing) ships now; #2 (decision-meeting view) waits exactly one more wave, because its natural implementation reuses the function this wave is rewriting";
export const DEC_442 = "A public GET joins the edge cache exactly when its URL carries no per-user parameter: bare schedule.ics is cacheable, ?ids= is not";
export const DEC_443 = "CONTRAST_BLOCKING's flip passes to wave 15's build/test lane under DEC-436's unchanged rule";
export const DEC_444 = "The last WCAG AA offender is remedied by re-pointing the rule to --chq-muted; --chq-disabled's value and the contrast checker are both untouchable";
export const DEC_445 = "The CONTRAST_BLOCKING flip passes to wave 16's build/test lane under DEC-436's rule, unchanged and unweakened";
export const DEC_446 = "SPEC §10 #2 (decision-meeting view) and #4 (resubmit-with-guidance) close as out-of-stage-1-scope; DEC-441's 'waits exactly one more wave' resolves to 'not in stage 1'";
export const DEC_447 = "The completion ledger is scheduled behind its wave's only source-changing lane, so it audits a tree that contains its own wave's fix";
export const DEC_448 = "`npm run dev` and its predev ensure-dev-vars hook are the one supported zero-setup entrypoint; verification lanes may not hand-copy .dev.vars";
export const DEC_449 = "The reviewer queue's remaining budget miss is fixed by deleting per-90-id chunked round-trips, not by paging the queue or reshaping assignment; a plan+round COUNT GROUP BY is not the SQL laundering DEC-440 forbids";
export const DEC_450 = "The Airtable sync serves exactly one org, named by AIRTABLE_ORG_ID, and refuses loudly when configured without it; its code correctness is stage-1 even though its wiring is stage-2";
export const DEC_451 = "\"Every Drizzle read carries a WHERE\" becomes a scanning test with an empty, deliberate allowlist";
export const DEC_452 = "Wave 17 is a fix wave, not a closing wave: it dispatches two source fixes and ships no completion ledger";
export const DEC_453 = "A completion ledger may not grade a measured budget from code presence, and must re-read every evidence log that landed after the previous ledger's sha";
export const DEC_454 = "One canonical email rule (src/domain/email.ts), applied at every contact.email write and every lookup by email";
export const DEC_455 = "\"Required\" means non-blank: a string answer whose trim is empty is ABSENT to validateAnswers";
export const DEC_456 = "\"Does this person already have an account?\" is answered by contact_id (OR email), never by email alone; contact.email writes cascade to the linked user row or fail with 409";
export const DEC_457 = "KV keys never carry raw external input: scopedRateLimitKey bounds its id at construction";
export const DEC_458 = "Wave 18 is a three-fix wave and cuts NO ledger; DEC-452's schedule is superseded";
export const DEC_459 = "A ledger row phrased with a universal quantifier is graded from an enumeration of the whole population, never from a sample";
export const DEC_460 = "\"Server pagination on all admin lists\" becomes an enforced bound on every /api/v1 list envelope, with no \"naturally small\" exemptions";
export const DEC_461 = "One uniform mechanism for DEC-460: optional repo page param + sibling count fn + deterministic ORDER BY; internal non-HTTP callers stay unbounded";
export const DEC_462 = "Manual session creation is the last unvalidated contact.email write, and DEC-454 covers it: reject at the route with 400, normalize with the one canonical function";
export const DEC_463 = "The react-router v6 advisories are closed as NOT-APPLICABLE to stage 1, with citations, rather than left drifting unowned";
export const DEC_464 = "Wave 19 is a five-source-lane fix wave and cuts NO ledger; wave 20 is the closing wave, and its ledger grades pagination from DEC-460's endpoint list and authz/input from wave 18's two enumerations";
export const DEC_465 = "One `listPerPage` in src/lib/pagination.ts replaces five per-route copies of DEC-461(a)'s rule; three call sites never implemented that rule at all";
export const DEC_466 = "The DEC-460 population was under-enumerated by three; the enumeration criterion is now mechanical, not a hand-built list";
export const DEC_467 = "user.email is subject to the DEC-454 rule too, and exactly one normalizeEmail survives in the product";
export const DEC_468 = "A server cap the UI cannot see is a new \"falls through the cracks\" defect: a truncated admin list must report its true total and offer the rest";
export const DEC_469 = "The CI perf smoke must be able to measure the two lists DEC-460 named as unbounded-over-an-org's-lifetime";
export const DEC_470 = "Wave 20 is a fix wave and cuts NO ledger; wave 21 closes, and its SPEC.md:353 row is graded from a re-derived mechanical enumeration";
export const DEC_471 = "Re-running DEC-466's own criterion found two more list reads it never named; `/api/v1/submissions/:id/files` and `/api/v1/files/:fileId/comments` get the DEC-013 envelope";
export const DEC_472 = "A branch is not a landing: every graded row cites `git merge-base --is-ancestor`, and a decision doc is evidence of a decision, never of a fix";
export const DEC_473 = "The list-envelope enumeration is a re-runnable artifact with one row per site in four fixed classes, not prose";
export const DEC_474 = "Wave 21 = one source lane + four independent evidence lanes + the ledger last; the ledger's PASS requires DEC-465..469 and DEC-471 to be ancestors of its own sha";
export const DEC_475 = "A conditional rule's fieldId is re-keyed exactly like the field's own id, or the rule is silently dead";
export const DEC_476 = "Schedule-slot minutes are day-bounded at isValidSlotInput, the single slot write, using the same MINUTES_PER_DAY as AUTO_SCHEDULE_BOUNDS";
export const DEC_477 = "The public row ceiling is MAX_PUBLIC_PAGE x PER_PAGE and must clear SPEC's top-of-range speaker count -- and the new deepest page is MEASURED, not assumed";
export const DEC_478 = "ONE import row cap; two constants named MAX_IMPORT_ROWS with different values make the 400 message a lie";
export const DEC_479 = "mergeContacts owes DEC-456's cascade too: after a merge, user.email equals the surviving contact.email";
export const DEC_480 = "ONE perPage clamp, and 'every list endpoint' is graded by an executable enumeration test, never by prose";
export const DEC_481 = "Re-grade every open decision from the tree at wave start; a doc comment inside src/ is narration too";
export const DEC_482 = "The three surviving perPage clamp copies collapse onto clampPerPage (default 50), NOT listPerPage (default 200)";
export const DEC_483 = "The clamp enumeration scans the clamp EXPRESSION, not the existence of a page-size constant";
export const DEC_484 = "The public JSON feed envelope carries the same paging truth as the HTML surface, and honors ?limit= identically";
export const DEC_485 = "The import row cap is the write-burst bound; the import write path is O(rows) serial by design and no audit may grade it O(rows/90)";
export const DEC_486 = "projectFieldForAnswers in src/forms/types.ts is the single projection from a stored form_field row to the render/validate FormFieldDef";
export const DEC_487 = "The three public paging constants live together in src/server/repo/public/bounds.ts, and hasMore is a shared predicate beside them";
export const DEC_488 = "A form's field set is actually bounded, and the reorder echo obeys the list-envelope contract";
export const DEC_489 = "Every embed knob is honored identically by a surface's HTML and its .json twin, from one declared surface->knob table";
export const DEC_490 = "The embed builder offers only the knobs the selected surface honors";
export const DEC_491 = "The CSV import's per-row write cost is measured, and the row cap is stated in terms of it";
export const DEC_492 = "One atomic, set-based ics_sequence bump; auto-schedule's write burst is bounded and declared";
export const DEC_493 = "The walkthrough harness is product evidence: resolve the seeded event by slug, and fill a locked field by the product's own rule";
export const DEC_494 = "An event-clock string carries its event's timezone with it; the browser's zone is never a default";
export const DEC_495 = "A ceiling that is never filled is not a measurement: the perf seed reaches SPEC's 800-speaker top end";
export const DEC_496 = "The closing ledger grades at file:line at its own sha, classifies a missing fix by ancestor-check on the branch that owns it, and strikes a carried open item whose own source contradicts it";
export const DEC_497 = "Phone and render evidence predating the embed rework is not evidence about it";
export const DEC_498 = "Concurrent evidence lanes each own a port and never kill by process-name pattern";
export const DEC_499 = "iCalendar parameter values are sanitized at the serializer: CN strips DQUOTE and every control character";
export const DEC_500 = "A field's options are validated against its EFFECTIVE kind (patch value ?? stored value), and options exist only on dropdowns";
export const DEC_501 = "An answer never outlives the rule that made it askable: the writer deletes what the validator hid";
export const DEC_502 = "A paged JSON feed returns ONE page window; HTML's cumulative show-more is not the feed's contract";
export const DEC_503 = "The phone manifest covers every surface the desktop manifest covers, proven by an executable test";
export const DEC_504 = "The README's alternate-port procedure runs the same predev the default path runs";
export const DEC_505 = "A field's kind and section are patchable, and a kind change is refused while answers exist";
export const DEC_506 = "ONE likeContains, escape-only and never case-folding, paired with ESCAPE '\\' at every LIKE call site including the unauthenticated public search boxes";
export const DEC_507 = "Wave 27 is the stage-1 exit wave: every evidence lane re-measures at a sha containing the wave-26 fixes, and verification-log filenames carry the -stage1 suffix";
export const DEC_508 = "Dropdown options are required when a PATCH CHANGES a field's kind to 'dropdown', not merely when options are sent";
export const DEC_509 = "An evaluation plan's numeric fields are validated at the route on BOTH POST and PATCH; a bad value is a 400, never a silent coercion and never an uncast bind";
export const DEC_510 = "Event start/end dates are strict ISO YYYY-MM-DD, validated at the API boundary, with ONE isIsoDate home";
export const DEC_511 = "The LIKE-escaping invariant is locked by a globbed enumeration over the repo layer, not by the spot checks that let it rot three times";
export const DEC_512 = "Every participant READ declares its invite-status audience; the list of reads that legitimately span all four statuses IS the enumeration, and it is a test";
export const DEC_513 = "README evaluator credentials are contract-tested against the vendored fixture, in both directions";
export const DEC_514 = "The stage-1 exit wave carries ZERO product-behavior tasks, because evidence cut at a wave's base sha cannot describe that wave's own fixes";
export const DEC_515 = "Exports carry the substance, not just the identifiers: submissions gain `description` + one column per custom form field; speakers gain bio/headshot/social links";
export const DEC_516 = "The public JSON feed asks D1 for ONE window (LIMIT+OFFSET); the route never slices a cumulative prefix";
export const DEC_517 = "ONE `isEpochMs` predicate home, and a CFP form / evaluation plan may never be saved with its close date before its open date";
export const DEC_518 = "Amendment (wave 93): a shared render-test helper is a legitimate ENTRY_OR_ALLOWED member, not dead code";
export const DEC_519 = "ics_sequence is bumped by every write that changes a field the VEVENT serializes — not only by slot writes";
export const DEC_520 = "Auto-created onboarding tasks carry a due date derived from the event start date, because J6's dashboard column and its cron are both due-date-driven";
export const DEC_521 = "The acceptance write path inserts task_assignment rows set-based and carries an explicit per-request write-burst bound, refused before the first write";
export const DEC_522 = "Amendment (wave 7): a day label is EXPANDED before it is formatted, on every surface without exception, and its unit test is fed the inputs production actually mints";
export const DEC_523 = "portal-config's `/events/*` middleware is the exact wildcard events.ts documents as forbidden, and role gates are locked by an order-INDEPENDENT composition test";
export const DEC_524 = "`app/src/lib/dates.ts` is the ONE home for date-input <-> epoch conversion in the SPA";
export const DEC_525 = "The global pubcache version bump stays fail-safe and global for stage 1; KV's same-key write ceiling is a stage-2 platform concern, dispositioned here so it is not re-raised";
export const DEC_526 = "The reviewer bulk reminder counts outstanding work exactly the way the progress panel does — recusals excluded — because a nudge and a dashboard that disagree are two implementations of one invariant";
export const DEC_527 = "Every ms-epoch value accepted at the API boundary passes DEC-517's isEpochMs, and the rule is locked by a globbed enumeration over the route layer rather than three hand-picked call sites";
export const DEC_528 = "A multi-row INSERT is chunked by BOUND-PARAMETER budget, not by row count, through one canonical helper that derives columns-per-row from the rows themselves; the organizer task-assign path joins the set-based writers under its own named bound";
export const DEC_529 = "The evaluations export carries one labelled column per scored criterion plus the weighted score, derived from the score keys actually present — a raw scoresJson blob keyed by opaque criterion ids is the lock-in DEC-515 forbade";
export const DEC_530 = "Per-recipient account and feedback lookups inside a batch send are resolved by ONE batched query each; findAccountUserId's contact-id-OR-email rule stays single-sourced and is contract-tested against its batch twin in both directions";
export const DEC_531 = "The overview's speaker counters are SQL conditional aggregates — the same form the J6 grid already uses — and aggregateSpeakerCounts is deleted rather than left standing as a second implementation";
export const DEC_532 = "Conditional-field visibility resolves to a FIXED POINT: a field gated on a hidden field is itself hidden, because a hidden field has no answer";
export const DEC_533 = "Conflict detection is bucketed by (day, room) and (day, speaker) and computed ONCE per payload -- autoSchedule already proved the index; findConflicts is the same invariant done the slow way";
export const DEC_534 = "Every LIMIT+OFFSET list ends its ORDER BY in a UNIQUE column, and the rule is enforced by a globbed scan rather than by remembering";
export const DEC_535 = "The J4 bulk reviewer nudge carries a named per-request bound, the same way its J6 sibling does; the reviewer-side dedupe window is deliberately deferred, not forgotten";
export const DEC_536 = "DEC-318's out-of-range-slot rule is not a public-surface rule, it is THE schedule-read rule -- the speaker portal joins it";
export const DEC_537 = "The cron's event sweep is a DISTINCT, not a whole-table scan reduced in JS";
export const DEC_538 = "The saved-view validator IMPORTS its vocabularies; a hand-copied enum is a second implementation and this one has already drifted";
export const DEC_539 = "No source file may contain a raw control byte, because every invariant in this repo is guarded by a globbed source scan and a file ripgrep calls binary is invisible to all of them";
export const DEC_540 = "iCalendar TEXT property values are control-byte sanitized at the SERIALIZER, exactly as DEC-499 did for PARAM values -- the output's only control bytes are its CRLF terminators";
export const DEC_541 = "A submission's answers are written by ONE set-based upsert keyed on the (submission_id, form_field_id) unique index -- never a per-field read-then-write loop";
export const DEC_542 = "Cloning a session is a set-based copy: one chunked multi-row INSERT per child table, never one statement per copied row";
export const DEC_543 = "A list endpoint never selects a column none of its renderers display: the email-log list and the email-log detail read have separate projections";
export const DEC_544 = "The CSRF double-submit rule has exactly one implementation, and it is the one the middleware calls -- a test-only twin tests nothing";
export const DEC_545 = "The admin SPA has ONE date-time formatter, in app/src/lib/dates.ts, and the ban on page-level toLocale* is enforced by a globbed scan rather than by a comment";
export const DEC_546 = "/dev/mailbox is organizer-only and scoped to the viewer's org — the wave-18 \"public by design\" disposition rested on a false premise";
export const DEC_547 = "makeMailer selects the dev sink ONLY in dev mode; every other configuration must present an EMAIL binding or return an UnconfiguredMailer that logs then throws from send(), never from construction";
export const DEC_548 = "The public agenda is a bounded query like every other public list: ?day= filters in SQL, the scan carries LIMIT + a truthful COUNT, and its ORDER BY totally orders the row";
export const DEC_549 = "A task upload becomes a session deliverable only when its task declares deliverable_kind; a task with no declared kind produces a file private to its assignee";
export const DEC_550 = "Every GET route is authz-gated or on a declared public-by-design list, proven by an anonymous probe over the composed app's own route table — not by a markdown inventory";
export const DEC_551 = "Every route that streams stored user content sets X-Content-Type-Options: nosniff";
export const DEC_552 = "An upsert over a UNIQUE index is ONE atomic statement — the five surviving SELECT-then-INSERT-or-UPDATE pairs become INSERT ... ON CONFLICT";
export const DEC_553 = "Public read surfaces send Access-Control-Allow-Origin: * ; the cookie-authenticated /api/v1 never does";
export const DEC_554 = "A whole-directory contact scan selects only the columns its own pure predicate and sort read, refuses above a declared cap, and re-hydrates only the page it returns";
export const DEC_555 = "The itinerary picker MERGES localStorage with the checkboxes actually rendered; a day-filtered or row-capped render can never delete a pick";
export const DEC_556 = "The two join tables whose uniqueness three modules assume get real UNIQUE indexes; their inserts become atomic, and merge keeps only the dedupe a constraint cannot make";
export const DEC_557 = "A schedule conflict is rendered by its kind, in the names a producer knows — one describeConflict, never raw ids";
export const DEC_558 = "DEC-534's total-order rule extends to every LIMITed admin worklist and to the JS 'best row' reducers beside them";
export const DEC_559 = "The J6 reminder carries the recipient's portal link, resolved by the ONE shared portal-link rule — a notification with nothing to click is half-built";
export const DEC_560 = "Every export is deterministically ordered — rows totally ordered, and every list-valued cell in a declared order";
export const DEC_561 = "The reviewer's submission view shows the submission — named speakers, the CFP's custom answers in form order, and their own stored evaluation; the queue keeps completed items instead of erasing them";
export const DEC_562 = "A submission's people and its answers have ONE order, expressed in SQL, at every surface that renders them";
export const DEC_563 = "Room and track order is the producer's — assigned on create, totally ordered on every read, and honored by the public day grid and the auto-scheduler that both invent their own today";
export const DEC_564 = "The notify pipeline is reproducible: recipients, reviewer-feedback numbering and reminder task lists each carry a declared order";
export const DEC_565 = "A nullable column may never sit inside a negated set predicate used as a safety pre-check: read the row, decide in JS, and export the decision as a pure function";
export const DEC_566 = "Reviewer scope is ONE predicate, and completion is only ever counted inside the assigned set";
export const DEC_567 = "The reviewer feedback a speaker reads is the round that decided THEM — latest evaluated round per plan, event-scoped, totally ordered — and the unbatched reader is deleted";
export const DEC_568 = "Content approval is a bulk action: one event-scoped endpoint plus multi-select in the J8 worklist, so accepting N talks is N clicks away from publishing, not 2N";
export const DEC_569 = "participant.order is the producer's to set: a full-set reorder endpoint, or the column that now governs every rendered speaker list is dead";
export const DEC_570 = "Desktop agenda placement is keyboard-first: arm-then-place, mirroring the phone; drag stays as an extra pointer affordance";
export const DEC_571 = "Agenda cards clip to their slot; track identity is carried by type, not by a coloured border";
export const DEC_572 = "A reviewer track assignment is previewed and confirmed, and the preview count comes from the same predicate the reviewer queue uses";
export const DEC_573 = "A file comment thread belongs to the version CHAIN, not to one file row; the comment list carries an envelope";
export const DEC_574 = "An upload control never discards the unsaved fields around it";
export const DEC_575 = "On CSV import UPDATE, a blank cell means \"no value supplied\" and never clears an existing value; custom fields merge key-by-key";
export const DEC_576 = "Amendment (wave 7): the shell's .chq-phone-head / -head-drill / -back is the ONE drill-in head, and a page that already draws a breadcrumb band re-classes onto it rather than declaring a second";
export const DEC_577 = "Native controls are styled once in the shell lane; converting a select into a segmented control is per-page markup and stays with that page's redesign";
export const DEC_578 = "The seed's wipe list is derived from the schema, with a source-scanning tripwire that fails when a table is missing";
export const DEC_579 = "CFP track selection stays multi-select; the defect is the label, not the control";
export const DEC_580 = "A drill-in resolves its own subject instead of trusting whichever list happens to be loaded";
export const DEC_581 = "The hub's visibility predicate and grouping live in ONE pure module; the repo returns candidates, the pure function decides what a stranger may see";
export const DEC_582 = "GET / is an anonymous surface: signed-in visitors are redirected by role, and it is styled from the public CSS family — revising DEC-382 for / only";
export const DEC_583 = "Demo credentials are one importable module conformance-tested against the fixture, and the login prefill renders only when those exact accounts exist in this database";
export const DEC_584 = "The public agenda at phone width is a SECOND markup, not a reflowed grid — and duplicated itinerary checkboxes must mirror each other or unchecking silently fails";
export const DEC_585 = "The SSR half of the native-control fix lives once in THEME_CSS; surface CSS modules never restyle a bare control selector";
export const DEC_586 = "A reviewer's landing is their work, never a picker: /review renders queues across every assigned open plan, headed by plan only when there is more than one";
export const DEC_587 = "The organiser Review landing is a worklist carrying inline progress and a decision affordance on results; the standalone progress route survives only as a deep link";
export const DEC_588 = "Settings gains 'Call for papers' and 'People and roles', built strictly on endpoints that already exist";
export const DEC_589 = "A completion figure names its own denominator, and conflict wording is imported from the vocabulary that owns it";
export const DEC_590 = "The speaker portal leads with a worklist whose headline count equals the rows it renders, and sign-out becomes page furniture rather than chrome";
export const DEC_591 = "The seed has ONE clock: every seeded instant is an offset from SEED_NOW, and the demo shows a day's work rather than a backlog";
export const DEC_592 = "A public session's Format is the answer to one named form field, and an event without that field renders no Format row";
export const DEC_593 = "The gallery is the speakers directory's photo-led twin: same person fields, one card, differing only in layout";
export const DEC_594 = "An embed is a closed surface, and a query parameter is either honored by both renderings or deleted";
export const DEC_595 = "The publish result counts through the public visibility gate, never through placement";
export const DEC_596 = "The organiser reads the same evaluation the reviewer wrote, and anonymity is decided by the plan, not by the surface";
export const DEC_597 = "Contacts are creatable and exportable from the directory itself; contacts becomes a first-class export kind";
export const DEC_598 = "A submission's tracks and session format are organiser-editable through the same join writer the portal edit uses";
export const DEC_599 = "A completed task response can be reopened, and the control says what it does instead of implying an email";
export const DEC_600 = "The speaker-facing status vocabulary is deliberately not the organiser's, and no surface may reconcile them";
export const DEC_601 = "Attribution is resolved by the route that owns the row; a client-side 'Unknown' fallback is the symptom, not the fix";
export const DEC_602 = "/agenda is the time grid and /schedule is the chronological itinerary list; a grid slot has a fixed height and holds nothing interactive";
export const DEC_603 = "A send is a batch even when it has one recipient: history groups by COALESCE(batch_id, id), minted once per fan-out";
export const DEC_604 = "A speaker may add a co-presenter to their own submission, and adding one never writes to somebody else's contact record";
export const DEC_605 = "The version chain is a speaker-visible fact, and a per-version download is authorised by chain membership, not by the id in the URL";
export const DEC_606 = "An export the browser performs silently still reports itself, and no screen prints a total it did not count";
export const DEC_607 = "One clipboard helper: a copy either confirms in a live region or admits it failed and reveals the text";
export const DEC_608 = "A route that belongs to another role is neither a 404 nor a blank page: every role subtree ends in a catch-all that names the boundary";
export const DEC_609 = "The content worklist is four columns: a row is a session, its deliverables, its status and its actions";
export const DEC_610 = "Saving a view is a dialog like every other dialog, and on a phone the decision lives on the card";
export const DEC_611 = "Overview names the public surfaces it produces, and links to them by the event's own slug";
export const DEC_612 = "External record identity is one namespaced external_ref column per imported table, unique inside its own owner scope";
export const DEC_613 = "Amendment (wave 94): One token cannot serve two densities: `.chq-overview-link-btn` splits into a roomy row-action token and an inline prose token";
export const DEC_614 = "Layer 2 (their REST API) is named in prose, never shown as a dead control";
export const DEC_615 = "Auto-schedule reports a reason per unplaced session, from a closed vocabulary with one renderer";
export const DEC_616 = "The contact drawer is a record, not a form: labelled facts, one history section, one action bar";
export const DEC_617 = "The hardened embed element is one file its own test executes, and it fails visible";
export const DEC_618 = "AUDIT.md's claims are enumerated by a test, not maintained by hand";
export const DEC_619 = "Scale is a named profile threaded through the seeder, not a second seeder";
export const DEC_620 = "Vertical clipping is a measured gate: content taller than its own box, with nothing able to scroll, is a failure";
export const DEC_621 = "The Comms phone landing's draft card renders from a real persisted draft: a frame element wired to a literal null is dead code, not an implementation";
export const DEC_622 = "Reviewer identity resolves in ONE place, and every surface that can name a reviewer ends in it";
export const DEC_623 = "A ref the product prints is a ref the API accepts";
export const DEC_624 = "Anonymity is a ratchet: it can be granted, never revoked once evaluations exist under it";
export const DEC_625 = "A locked built-in field is unconditionally visible; a visibility rule can never be attached to one";
export const DEC_626 = "An error raised on a plain-form surface renders as a page, never as a JSON blob — and the CFP submit keeps what the submitter typed";
export const DEC_627 = "The public edge cache is purged by publish-affecting writes only, classified by a closed two-list enumeration";
export const DEC_628 = "Security middleware coverage is enumerated from the source, not spot-checked";
