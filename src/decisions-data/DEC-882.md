# DEC-882: A frozen round reads as TEXT, the criteria table names its columns, and the lock card explains below what the rows already show

## Amendment (wave 110): the criteria table's header names all SIX of the frame's columns, in the frame's order (Type at 128px, Weight at 122px)

docs/design/Chautauqua Review.dc.html:492-493 spells the header cells literally -- blank / Criterion / `Guidance for reviewers · optional` / Type / Weight / blank -- over `grid-template-columns:20px 1fr 232px 128px 122px 62px`. Both body rows in PlanEditor.tsx (locked at :1729, editable at :1786) already render six cells in that order. The header renders FIVE: the Type head is missing outright and `Weight` therefore lands over the Type track. review.css's own comment compounds it by naming track 4 `Weight 128px` and track 5 `Type 122px` -- the reverse of the frame it cites. RULED: the header gets its Type cell and its trailing blank; no track width changes (the declaration was always right); and the comment's prose is corrected, because a comment that misreads the frame it cites is how the next lane gets the order wrong again.
