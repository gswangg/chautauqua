// DEC-613 (wave 7): single-sourced criterion-type vocabulary for the SPA,
// same crossing idiom as batch-caps.ts -- the only module that crosses the
// app/ -> src/ boundary for CRITERION_KIND_LABELS.
export { CRITERION_KIND_LABELS } from '../../../src/domain/evaluation';
