# Eval findings — round 1 (post-convergence final review, 2026-08-10)

Source: the campaign's fresh-eyes final review (Fable, review-final lens),
independently spot-checked. Per DEC-069, fixing any of these is code-bearing
and reopens the exit predicate: re-run gates before re-declaring stage-1.
Prune entries as they land (stale entries cost planner cycles).

**2. Acceptance side effects are at-most-once, not "exactly once, idempotently" (J6, §5 invariant).**
`/Users/wednesdayniemeyer/Documents/gniemeyer/Projects/chautauqua/src/server/repo/submissions/status.ts:123-142` commits the status/acceptedAt UPDATE first, then runs `runAcceptancePlanning` in separate non-transactional statements; `fireAcceptance` is keyed on `acceptedAt === null` (`/Users/wednesdayniemeyer/Documents/gniemeyer/Projects/chautauqua/src/domain/status.ts:53-58`). If planning throws mid-loop (D1 hiccup, or the >100-id crash from issue 1 aborting the batch partway), the submission is permanently marked accepted with onboarding tasks silently never created and no code path that ever retries — the planner's idempotency is unreachable because the trigger never re-fires.

**3. The public surfaces (J10) use the same unchunked `inArray` pattern on unbounded id lists — the always-current program pages fail at scale.**
`/Users/wednesdayniemeyer/Documents/gniemeyer/Projects/chautauqua/src/server/repo/public.ts` never imports chunking: `hydrateSessions` (`:149-158`) takes one `inArray` over all page ids — `getPublicAgenda` (`:337-365`) feeds it every scheduled visible session (a real AIE-scale agenda is well past 100), sessions show-more feeds it `page*perPage` cumulative ids (`:238`), the track filter feeds `inArray` with every submission on a track (`:122-129`), and `getPublicSessionsByIds` (`:245-261`) takes an unbounded, user-controlled `?ids=` list from `/e/:slug/schedule.ics` (`parseItineraryIds` has no cap — `/Users/wednesdayniemeyer/Documents/gniemeyer/Projects/chautauqua/src/lib/itinerary.ts:13-24`). Same gap in `loadIcsScheduleData` (`/Users/wednesdayniemeyer/Documents/gniemeyer/Projects/chautauqua/src/server/repo/comms.ts:243-249`) even though the very same file chunked `loadComposeSubmissions` two functions earlier.

**4. Review surface (J4) does full-event scans per reviewer — violates §7 budgets ("one round trip per view", p95 < 50 ms) at the 2k-submission / 10–50-reviewer targets.**
`listPlanFilteredSubmissions` loads every submission row in the event plus the full track join (`/Users/wednesdayniemeyer/Documents/gniemeyer/Projects/chautauqua/src/server/repo/review.ts:254-278`), and the progress endpoint calls it once per reviewer via `resolveReviewerSubmissions` (`/Users/wednesdayniemeyer/Documents/gniemeyer/Projects/chautauqua/src/routes/review.ts:239-248`) — ~50 full scans per dashboard view. Every rating PUT also re-runs a full scan plus `listEvaluationsForPlan` (all plan evaluations) just to check the cap (`review.ts:448-467`), so "keyboard-fast review" pays O(event) per keystroke.

**5. Evaluation `rounds` is a dead knob: accepted, stored, PATCHable — and ignored.**
Plan create/update accept `rounds` (`/Users/wednesdayniemeyer/Documents/gniemeyer/Projects/chautauqua/src/routes/review.ts:152`, `:181`) and the repo persists it, but the evaluation PUT hardcodes `const round = 1` (`/Users/wednesdayniemeyer/Documents/gniemeyer/Projects/chautauqua/src/routes/review.ts:459`), so J4's "optional multiple rounds" can never happen and a producer who configures 2 rounds gets silently different behavior — against the repo's own fail-loudly principle. No decision doc records this deferral (DEC-018 fixes uniqueness "per round" but never closes multi-round; DEC-061's closure list doesn't mention it). Either implement round advancement or reject `rounds != 1` loudly and record the deferral.

## Decision docs that narrow requirements rather than implement them

- **DEC-054** (`/Users/wednesdayniemeyer/Documents/gniemeyer/Projects/chautauqua/decisions/DEC-054.md`) — defers SPEC §5's "(+ custom)" statuses. Honestly recorded, defensible against swyx's stated minimum, but it is a narrowing of the written spec, and it silently drags §10 item 4 ("resubmit with guidance") out of scope with it.
- **DEC-022** (`/Users/wednesdayniemeyer/Documents/gniemeyer/Projects/chautauqua/decisions/DEC-022.md`) — narrows J10's "edge-cached, **purged on publish-affecting writes**" and §7's purge mechanic to "no purge machinery in stage 1; bounded 60 s staleness". J10's purge language is stage-1 product text, not stage-2 wiring; the decision reframes it as acceptable staleness rather than implementing invalidation.
- **DEC-059** (`/Users/wednesdayniemeyer/Documents/gniemeyer/Projects/chautauqua/decisions/DEC-059.md`) — narrows §7 "headshots thumbnailed at upload" to a client-side canvas downscale on the portal profile page only; headshots arriving via seed, CSV import, or direct API upload are stored and served full-size under an `immutable` cache header.
- **DEC-061** (`/Users/wednesdayniemeyer/Documents/gniemeyer/Projects/chautauqua/decisions/DEC-061.md`) — closes §10 items 2–3; this one is sanctioned by SPEC §10's own "only after J1–J12 are green" gate, listed for completeness.

Minor notes not in the top 5: `submittedAt` is just `createdAt` in the list contract (`/Users/wednesdayniemeyer/Documents/gniemeyer/Projects/chautauqua/src/server/repo/submissions/list.ts:275`), so draft-vs-submitted timing is not distinguishable; branding `accentColor` is interpolated into `<style>` blocks (organizer-controlled, hono/jsx-escaped, low risk).
