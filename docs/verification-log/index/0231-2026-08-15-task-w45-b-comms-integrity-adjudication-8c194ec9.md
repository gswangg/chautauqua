## 2026-08-15 task-w45-b — comms-integrity adjudication @ 8c194ec9

QUALIFYING (advisory to the DEC-069 predicate — this scope classifies to none of the five slots)

INVALIDATED BY: src/** app/src/** migrations/** package.json

DEC-644 ref-state receipt (verbatim, `npm run ref-state`): HEAD
`8c194ec91ede63942022550bbced9bf3ba00f1b5`; newest first-parent
product-code-bearing sha `14da2921a5be66408057712be877bc44c19de6c4`; every
live ref (`main`, `manual-qa`, `task-custodian-w68-4`, `task-w44-a`,
`task-w44-b`, `task-w44-c`, `task-w44-d`, `task-w44-f`, `task-w44-g`,
`task-w45-a`, `task-w45-b`, `task-w45-c`, `task-w45-d`, `task-w68-d`,
`task-w71-c`, `task-w71-d`, `task-w71-e`) confirmed an ancestor of HEAD via
`git merge-base --is-ancestor`. NON-ancestor refs (not confirmed):
`mail-rich-shape-fallback`, `task-w17-i`, `task-w44-e`, `task-w44-i`,
`task-w68-b`, `task-w68-c`, `task-w68-e`, `task-w71-a`, `task-w72-a..j`.
`git merge --no-edit main` from a freshly cut worktree reported "Already up
to date" (worktree cut at `main`'s tip).

Four-claim adjudication against SPEC.md:123-127 (J5) and SPEC.md:199-201
(§2 principle 4 — status changes never auto-email):

| # | Claim | Verdict | file:line | Check |
|---|---|---|---|---|
| 1 | send-door 100-recipient cap | NOT-CONFIRMED | `src/routes/comms/compose-core.ts:107-109` | `MAX_COMPOSE_RECIPIENTS` is enforced inside `resolveComposeInput` via `parseBoundedIdArray(b.submissionIds, "submissionIds", {maxCount: MAX_COMPOSE_RECIPIENTS})` (`compose-core.ts:107`). `src/routes/comms/send.ts:45` calls `resolveComposeInput` directly — it is the SAME function `preview.ts:32` calls, not a preview-only path. An API caller POSTing straight to `/compose/send` cannot bypass the cap: `resolveComposeInput` runs before any D1 read (`compose-core.ts` doc-comment at line 104-106, DEC-396). No independent second enforcement exists at the send door, but none is needed — the cap lives on the one function both doors share, so there is no bypass route. |
| 2 | per-recipient log + partial-failure contract | DELIBERATE-BY-DESIGN | `src/routes/comms/send.ts:210-239`; `src/mail/email-binding.ts:227-256`; `src/mail/unconfigured.ts:16-38`; `src/mail/dev-sink.ts:12-30` | `send.ts`'s per-recipient loop (`for (const rendered of toSend)`) wraps `mailer.send(attempt)` in a per-iteration try/catch (line 235) that pushes to `failed[]` on throw and continues the loop — recipient k's failure does not abort k+1..n. Every concrete `Mailer` (`EmailBindingMailer`, `UnconfiguredMailer`, `DevSinkMailer`) writes exactly one `email_log` row PER ATTEMPT before returning/throwing (DEC-923 "single-writer discipline", `email-binding.ts:249-254` writes then rethrows at line 256; `unconfigured.ts` writes then throws). So for recipients 1..k-1: logged status `sent`. Recipient k (mailer rejects): logged status `failed` via the SAME write-then-throw ordering, not silently dropped. Recipients k+1..n: still attempted (loop continues) and each logged per its own outcome. No row can name a recipient that was never attempted (`mailer.send` is the only writer and is called once per `toSend` entry), and no attempted send goes unlogged. Neither overstates nor understates what was sent. |
| 3 | status changes never auto-email (DEC-009, DEC-720 sole exception) | NOT-CONFIRMED | enumerated below | Every `mailer.send`/`makeMailer` call site reachable from `src/routes/**` (`grep -rn "mailer.send\|makeMailer" src/routes/`): `content-notes.ts:157` — already-ruled DEC-720 exception (content status move + mail is ONE deliberate action; not re-filed per task instructions). `auth-reset.tsx:170` — deliberate-send (password-reset request, no status write). `tasks.ts:617` (`/onboarding/remind`) — deliberate-send (organizer-triggered reminder); `tasks.ts:686` (`runDueReminders`, called from `src/index.ts`'s `scheduled()`) — schedule-driven (cron), explicitly commented "Never reachable from a status-change path (DEC-009)". `comms/portal-invites.ts:102` — deliberate-send (organizer-triggered portal invite). `comms/send.ts:235` — deliberate-send (compose send, the organizer's explicit action). `public/submit-post.tsx:485` — deliberate-send, fires on CREATE (new submission), not a status transition. `review/plans-progress.ts:333` — deliberate-send (reviewer progress-reminder, organizer-triggered, DEC-238 class 2). `api/users.ts:111` — deliberate-send (welcome email on account CREATE, best-effort per DEC-238). `api/contacts/bulk-email.ts:281` — deliberate-send (organizer bulk email). Cross-checked the two content-status writers named in DEC-720's wave-32/wave-53 amendments: `src/routes/files.ts:6-7` and `src/routes/api/submissions.ts:5-6` both carry the invariant comment "this module MUST NEVER import a mailer" and neither file imports `makeMailer`/a `Mailer` type (confirmed via grep — zero mailer-related hits besides the invariant comments and unrelated `email` string/contact fields). No status-driven mailer call exists outside the DEC-720 exception. |
| 4 | unknown merge token ({speaker_name}/{talk_title}) render-time behavior | DELIBERATE-BY-DESIGN | `src/mail/render.ts:159-166` (throw); `src/domain/compose.ts:195-225` (preflight gate) | `renderTemplate` (`render.ts:159`) throws `MergeFieldError` for any placeholder not present in `vars` (fail-loudly, matching the file's header comment at `render.ts:1-3`: "Fail loudly on any placeholder absent from vars — no silent blanks in emails to real speakers"). In the actual send path this throw is a dead-code safety net, not the live gate: `preflightRender` (`compose.ts:195`) calls `missingMergeFields` (render.ts:143, a non-throwing scan) for BOTH subject and body BEFORE calling `renderTemplate` (`compose.ts:204-205,223-224`), and `send.ts:76-83` (first pass) and `send.ts:98-100` (second pass, post-mint) both reject the WHOLE batch with a 400 (`ApiError("invalid", ...)`) if any recipient is missing a field — this runs before the mailer loop even starts, so no partially-rendered or token-leaking mail is ever sent. An unknown token can never reach a recipient's mailbox: it either aborts the whole compose call at preflight (real send path) or throws `MergeFieldError` (single-value renderers, e.g. reminders, per the `missingMergeFields` doc-comment at `render.ts:138-140`). Matches SPEC's fail-loudly house style; no raw-token leak, no silent empty-string render. |

Full detail: `docs/verification-log/task-w45-b-comms-integrity-adjudication-8c194ec9.md`.

RESULT: PASS — no CONFIRMED-DEFECT found across the four adjudicated J5 claims; the 100-recipient cap, per-recipient log integrity, DEC-009/DEC-720 status-email boundary, and unknown-merge-token fail-loud behavior all check out against SPEC.md:123-127/199-201 at this HEAD.
OPEN ITEMS: 0
