## 2026-08-15 task-w48-g — triage-closure @ 243b3094

QUALIFYING

INVALIDATED BY: src/** app/src/** migrations/** package.json

STEP 0 -- sync-then-measure (identical protocol to the sibling gate lanes).
Bounded poll (max 10) over every live `task-w47-*` ref, individually via
`git merge-base --is-ancestor <ref> HEAD`, never a glob of
`.git/refs/heads/*`: after 10 iterations, 3 of 8 `task-w47-*` refs
(`task-w47-a`, `task-w47-g`, `task-w47-h`) remained non-ancestor -- these
are unmergeable-as-is at this measurement (real, unmerged commits; see
CENSUS below), not a sync failure, so per DEC-069 wave-37 ("a ref not
merged after bounded poll stays live, not blocking") measurement proceeded.

`npx tsx scripts/ref-state.ts` receipt (VERBATIM):

> DEC-644 three-sha boundary: HEAD `243b3094dcc4dd9125dc01f9cec01dc396251e89`;
> newest first-parent product-code-bearing sha
> `243b3094dcc4dd9125dc01f9cec01dc396251e89`; every live ref (`main`,
> `manual-qa`, `task-custodian-w68-4`, `task-w46-g`, `task-w47-b`,
> `task-w47-c`, `task-w47-d`, `task-w47-e`, `task-w47-f`, `task-w48-b`,
> `task-w48-d`, `task-w48-e`, `task-w48-g`, `task-w68-d`, `task-w71-c`,
> `task-w71-d`, `task-w71-e`) confirmed an ancestor of HEAD via
> `git merge-base --is-ancestor`. NON-ancestor refs (NOT confirmed via
> `git merge-base --is-ancestor`): `mail-rich-shape-fallback`, `task-w17-i`,
> `task-w47-a`, `task-w47-g`, `task-w47-h`, `task-w48-a`, `task-w48-c`,
> `task-w48-f`, `task-w68-b`, `task-w68-c`, `task-w68-e`, `task-w71-a`,
> `task-w72-a`, `task-w72-b`, `task-w72-c`, `task-w72-d`, `task-w72-e`,
> `task-w72-f`, `task-w72-g`, `task-w72-h`, `task-w72-i`, `task-w72-j`.

MEASURED_SHA = `243b3094dcc4dd9125dc01f9cec01dc396251e89` (HEAD, also the
newest first-parent product-bearing sha per the receipt above).

## STEP 1 -- adjudicate the whole population (FILE, NEVER FIX; no product
code touched by this lane; all reads at MEASURED_SHA above unless a
specific fix's sha is named)

| # | Row | Verdict | Landed file:line | Falsifying check |
|---|---|---|---|---|
| 1 | `0232` defect 1: cron reminder send-then-stamp dedupe race (`sendReminderEmails`) | STILL-OPEN | n/a at MEASURED_SHA -- `src/server/repo/tasks/reminders.ts:416-452` still sends in the loop then issues the dedupe-stamp UPDATE afterward in a separate chunked pass (`:449-452`); a wave-48 amendment in this same tree batches that UPDATE but does not change its post-send timing, so the race shape is unchanged | The actual fix (conditional claim-before-send UPDATE with `.returning()`, DEC-023) is real and already written at `task-w47-a` (`030c91706a679d31dbf21b45d016114989d4eff8`, `src/server/repo/tasks/reminders.ts:390-436` on that branch) but that branch is NON-ancestor of MEASURED_SHA (bounded poll exhausted, see STEP 0) -- the fix has not landed in this tree. owner: wave-49 lane, branch to be named (or land `task-w47-a` as-is). |
| 2 | `0232` defect 2: portal read doors materialize content before authz (`getPortalSubmissionDetail`, `getResourceDownloadScope`) | CLOSED | `src/server/repo/portal/data.ts:208-273` (`submissionOwnedByContact(contactId)` ANDed into the WHERE, :262) and `src/server/repo/portal/resources.ts:117-152` (`contactParticipatesInResourceEvent(contactId, orgId)` ANDed into the WHERE, :151) | `test/portal-detail-download-scope-sql.test.ts:109` ("getPortalSubmissionDetail's SELECT WHERE binds the requesting contactId, not just submissionId/orgId") and `:133` (twin for `getResourceDownloadScope`) -- both pass at MEASURED_SHA (`npx vitest run test/portal-detail-download-scope-sql.test.ts`: 4/4 passed). |
| 3 | `0233`: auto-schedule accounting invariant unreachable/race-fragile (`runAutoSchedule` summary.unplaced length-comparison threw a hard 500 under a benign concurrent write) | CLOSED | `src/server/repo/agenda/auto-schedule.ts:195,201` (`snapshotIds` set, reconciliation scoped to this run's own read, set-based not length-based) | `test/auto-schedule-unplaced-reconciliation.test.ts` -- 2/2 passed at MEASURED_SHA. |
| 4 | `0234` claim 1: CSV import duplicate-destination column mapping silently last-write-wins | CLOSED | `src/domain/contacts-parts/import.ts:63-72` (`validateImportMapping` throws naming both offending columns and the shared target) | `test/contacts-import-mapping-injective.test.ts` and `test/contacts-import.test.ts` -- both pass at MEASURED_SHA (`npx vitest run test/contacts-import.test.ts`: 6/6 passed). |
| 5 | `0234` claim 2: merge preview/execute preflight parity (preview never ran `planMergeFold`/`detectMergeConflicts`) | CLOSED | `src/server/repo/contacts/merge-preflight.ts` + `src/server/repo/contacts/merge.ts:700-750` (`checkMergeConflicts` extracted and shared by both the preview route and `mergeContacts`) | `test/contacts-merge-preview-parity.test.ts` ("merge preview / write preflight parity (DEC-026 wave-47 amendment)") -- 3/3 passed at MEASURED_SHA. |
| 6 | `0234` claim 4: per-contact history `events` unbounded `selectDistinct` | CLOSED | `src/server/repo/contacts/history.ts:126-149` (`MAX_CONTACT_HISTORY_EVENTS` LIMIT + separate `eventsTotal` count query, "w47-f" comment) | `test/contacts-history-event-id.test.ts:303` ("caps events at MAX_CONTACT_HISTORY_EVENTS, reports the true eventsTotal...") -- 6/6 passed at MEASURED_SHA. |
| 7 | `0235`: file version-number mint race (`insertFile` read-then-write, no unique-index backstop) | STILL-OPEN | n/a at MEASURED_SHA -- `src/server/repo/files-versions.ts:477-497` is still a plain `SELECT` then separate `db.insert`, and `src/db/schema/content.ts:75-78` still has no `uniqueIndex` on `file` covering `(previous_file_id)`/`(submission_id, kind, version_no)` | The fix (DEC-818 unique-index contract) is written at `task-w47-g` (`9a5417...`, "File version mint gets its unique-index contract") but that branch is NON-ancestor of MEASURED_SHA (bounded poll exhausted, see STEP 0) -- not landed in this tree. owner: wave-49 lane, branch to be named (or land `task-w47-g` as-is). |
| 8 | `0236` OPEN ITEM: 24-vs-7 non-conforming index headers, incl. 15 sha-less legacy entries (0140-0157) | CLOSED | `scripts/assemble-verification-log.ts:79-97` (`deriveSyntheticHeader`, deliberately NOT hex-sha-validated per its own doc comment) + `:129-140` (`assembleEntry`, prepends the synthetic header, keeps the original first line as body) -- verified in-tree: `docs/verification-log.md:2686-2687` now carries a synthetic header (`## 2026-08-12 task-w12-a — render sweep mobile overflow instrument @ correction`) immediately preceding `0140`'s original first body line, i.e. `0140` is now its own parsed section instead of silently donating its body to a preceding section. Ruling on the sha-less legacy entries: RESOLVED by design, not by a new DEC -- `deriveSyntheticHeader` takes the filename's trailing hyphen-segment verbatim as a notional (non-hex-validated) sha token rather than fabricating a real one; this is the DEC-068 wave-46 amendment's own documented choice, already in this tree. `reportNonConformingHeaders`/`nonConformingHeaders` (still finding the same 24 files) is retained as an advisory report only -- it does not mean those 24 sections are unparsed; `assembleEntry` already closes that. | `test/verification-log-assemble.test.ts:206` ("assembleEntry prepends a synthetic header and preserves the original first line as body") and `:216` (bracket-suffix variant) -- 21/21 passed at MEASURED_SHA; `npx tsx scripts/assemble-verification-log.ts --check` exits 0 at MEASURED_SHA (reports the 24 non-conforming filenames as an advisory list, same run, exit 0 -- verified live, see STEP 1 evidence below). |
| 9 | `0237` OPEN ITEM: `isAncestor` uncaught-crashes on git exit 128 (unresolvable object) | CLOSED | `scripts/exit-predicate.ts:336-356` (`gitAncestorResult`: status 128 degrades to `false` with one `console.warn` naming both shas, survivable; only a genuinely unexpected status rethrows) | `test/exit-predicate-ancestry.test.ts:41` ("git exit 128 (unresolvable object) degrades to false with exactly one warning naming both shas") and `:96-110` (corpus-level regression against a real unresolvable header sha) -- passed at MEASURED_SHA. EVIDENCE ONLY (not a five-slot verdict, that is task-w48-h's section): `npm run exit:predicate -- --product-sha 243b3094dcc4dd9125dc01f9cec01dc396251e89` at MEASURED_SHA ran to completion and printed a five-row VOID table (no verdict-bearing section exists yet at this exact sha) with no crash/stack-trace, where the pre-fix behavior (per `0236`/`0237`'s own before/after tables) was an uncaught `fatal: Not a valid object name` crash producing no table at all. |
| 10 | `0220`: `mergeContacts` fake-db call-order desync (3/12081 test failures, `test/contacts-repo.test.ts`) | CLOSED (claimed by merge-train `85452d06`, re-verified here) | `test/contacts-repo.test.ts:241-345` -- the three `fakeDb([...])` queues in the `mergeContacts (DEC-101 participant dedupe + six-table FK repoint)` describe block each now carry the 4 additional preflight-select entries (`// preflight: findContactById(keepId)` etc., :249-252, :293-296, :343-346) ahead of the per-pair sequence, matching `mergeContacts`' actual call order (whole-operation preflight before `mergeOnePair`) | `npx vitest run test/contacts-repo.test.ts` at MEASURED_SHA: 23/23 passed (0 failures; the 3 tests named in `0220` -- "repoints file and file_comment...", the dedupe test, and the third fixture -- are among the 23 and all green). |
| 11 | `0216` item 1: mechanically-graded triage-closure FAIL from a stale higher-sequence section outranking a fresher one by append order | CLOSED | `scripts/exit-predicate.ts:243-279` (`gradePredicate`'s `survivors` filter: a candidate whose sha is a strict ancestor of another candidate's sha is discarded before the last-appended tiebreak runs -- "newest measured tree", not append order, decides) | `test/exit-predicate.test.ts:244` ("[negative control] discards a lower-sequence-but-newer-tree candidate's older sibling: the newer tree decides, not append order") and `:306` (stale-PASS mirror) -- 43/43 passed at MEASURED_SHA. |
| 12 | `0216` item 2: `mergeContacts` multi-id merge non-atomicity (partial writes possible before a mid-fold conflict) | CLOSED | `src/server/repo/contacts/merge.ts:762-781` (`mergeContacts`: whole-operation `checkMergeConflicts` preflight runs and can throw BEFORE the fold loop calling `mergeOnePair` ever starts, docstring: "a refusal always means ZERO writes, never a write of contacts 1..k-1 followed by a 409 on contact k") | `test/contacts-merge-integrity.test.ts:288` ("both have user rows -> conflict thrown, and no delete/update was issued": `expect(updates).toEqual([]); expect(deletes).toEqual([])`) -- 24/24 passed at MEASURED_SHA. |
| 13 | `0216` item 3: `autoSchedule()` window-blind `existing` filter (an out-of-range persisted slot could be silently dropped or double-counted) | CLOSED | `src/server/repo/agenda/auto-schedule.ts:64-68` (`inRangeSlotted`/`outOfRangeSlotted` split via `isDayWithinEventRange`, shared with the payload reader, DEC-615 wave-43 amendment) | `test/agenda-unplaced-accounting.test.ts` ("DEC-615 wave-43: out-of-range slots are named unplaced, never silently dropped") -- 5/5 passed at MEASURED_SHA. |
| 14 | `0216` item 4: `scripts/perf-seed.ts` perf-speaker wiring gap (never called `PERF_SPEAKER_*` exports, so `perf-smoke.ts`'s speaker login 401'd, blocking the whole gate) | STILL-OPEN (UNFALSIFIABLE per DEC-358 wave-37 amendment) | `scripts/perf-seed.ts` (commit `af7ef380f381e4170ae91522c8a67d1ae10553f1`, ANCESTOR of MEASURED_SHA: mints the speaker `user`/`contact`, `PERF_SPEAKER_SUBMISSION_COUNT` participant rows, and `task_assignment` rows) IS a real, landed fix, verified by code reading -- but no existing repo test would fail if this commit were reverted: `test/perf-seed-lib.test.ts`'s 25 tests exercise only the PURE `perfSpeaker*` functions (which existed and passed before `af7ef380` too, since the functions themselves were not new), and `test/perf-seed.test.ts` has zero assertions on `INSERT INTO user`/`contact`/`participant` lines for the `seed_perf_speaker_` id namespace (only `test/perf-seed.test.ts:713-715` filters OUT the speaker's `task_assignment` lines from an unrelated count, which would still pass whether or not those lines exist). The commit's own verification was a manual `wrangler dev` + `perf:smoke` run (39/39 checks), not a repo-committed test. Per DEC-358 wave-37 ("a code-shape citation alone is labelled UNFALSIFIABLE and stays open"), this row stays open pending a test asserting `scripts/perf-seed.ts`'s generated SQL contains the speaker user/contact/participant rows. owner: wave-49 lane, branch to be named. |

Excluded from this population per this task's brief: the admin Speakers
`[List | Grid]` toolbar row, already ruled VOID four times
(`docs/design/README.md:343` is headed "**Public** filter bar"; the control
ships at `src/routes/public/speakers.tsx:20,235,314`) -- not re-adjudicated
here.

## STEP 1 evidence (targeted test runs, all at MEASURED_SHA, this worktree)

```
npx vitest run test/contacts-repo.test.ts                        -> 23 passed (23)
npx vitest run test/contacts-merge-integrity.test.ts              -> 24 passed (24)
npx vitest run test/portal-detail-download-scope-sql.test.ts \
  test/portal-idor-probe.test.ts                                  -> 6 passed (6)
npx vitest run test/auto-schedule-unplaced-reconciliation.test.ts -> 2 passed (2)
npx vitest run test/contacts-import.test.ts \
  test/contacts-merge-preview-parity.test.ts                      -> 9 passed (9)
npx vitest run test/contacts-history-event-id.test.ts             -> 6 passed (6)
npx vitest run test/verification-log-assemble.test.ts             -> 21 passed (21)
npx vitest run test/verification-log-verdict-contract.test.ts     -> 11 passed (11)
npx vitest run test/exit-predicate.test.ts                        -> 43 passed (43)
npx vitest run test/agenda-unplaced-accounting.test.ts            -> 5 passed (5)
npx tsx scripts/assemble-verification-log.ts --check              -> exit 0 (24 files listed as advisory, non-blocking)
npm run exit:predicate -- --product-sha 243b3094dcc4dd9125dc01f9cec01dc396251e89
  -> five-row VOID table, no crash (evidence for item 9 only, no 5-slot verdict published by this lane)
```

## STEP 2 -- CENSUS

Every live `task-w4*` ref that is a NON-ancestor of MEASURED_SHA
(`243b3094`), branch / sha / one-line scope. A ref with a real commit is
OWNED (never re-filed); a ref at or past base and created this wave is
RUNNING, not dead (DEC-069 wave-37).

| Branch | SHA | Status | Scope |
|---|---|---|---|
| `task-w46-g` | `74d682d3` | **now ANCESTOR** (landed via merge `185fa8d7`, no longer live-non-ancestor) | DEC-020 upload accepted-type/size-cap scan-lock (J8 ENUMERATION) -- named here only to correct the record: this wave's field-guide note calling it unmerged is now stale, it landed. |
| `task-w47-a` | `030c9170` | OWNED, unmerged | DEC-023 claim-before-send fix for `0232` defect 1 (cron reminder dedupe). Real commit, bounded-poll-exhausted non-ancestor. See row 1 above. |
| `task-w47-g` | `9a541796` | OWNED, unmerged | DEC-818 unique-index contract fix for `0235` (file version mint race). Real commit, bounded-poll-exhausted non-ancestor. See row 7 above. |
| `task-w47-h` | `416d2eb5` | OWNED, unmerged | Scribe wave-47: rebase `docs/eval-findings.md`, files wave-45's 7-item defect ledger. Docs-only. |
| `task-w48-a` | `ea772bc8` | RUNNING (my own wave-48 sibling gate lane) | `task-w48-a`: build+test+bundle gate @ `0ecff8aa` (2 test failures filed, owner wave-49). Not graded by this lane (DEC-069 wave-44 clause c: slots graded independently). |
| `task-w48-b` | `2c3c4210` | RUNNING (past base at measurement time -- active work) | `task-w48-b`: DEC-069 gate slot 2/5 J1-J12 walkthrough @ `243b3094` (FAIL producer). Not graded by this lane. |
| `task-w48-c` | `2330f7cd` | RUNNING | `task-w48-c` perf-smoke gate @ `0ecff8aa` (index `0242`). Not graded by this lane. |
| `task-w48-e` | `6b03b916` | RUNNING (past base at measurement time -- active work) | Rebase `docs/eval-findings.md` onto wave-48 boundary (`243b3094`). Not graded by this lane. |
| `task-w48-f` | `6b343bff` | RUNNING | `task-w48-f`: advisory render-sweep gate @ `243b3094`. Not graded by this lane. |

`task-w48-d` (`243b3094`, "merge task-w47-e") and `task-w48-g` (this lane,
`243b3094`) are both ANCESTOR/self and excluded from the table above.

## STEP 1 arithmetic (OPEN ITEMS)

14 rows adjudicated: 11 CLOSED, 3 STILL-OPEN (row 1: `0232` defect 1 cron
reminder dedupe, owner: wave-49 lane, branch to be named; row 7: `0235`
file version mint race, owner: wave-49 lane, branch to be named; row 14:
`0216` item 4 perf-seed speaker wiring, unfalsifiable, owner: wave-49 lane,
branch to be named).

RESULT: FAIL -- 3 STILL-OPEN rows remain (cron reminder dedupe race, file
version mint race, and the unfalsifiable perf-seed speaker-wiring fix), all
with real fix commits already written on unmerged wave-47 branches
(`task-w47-a`, `task-w47-g`) or landed-but-untested (`af7ef380`); 11 of the
14 population rows are CLOSED with a falsifying test at MEASURED_SHA;
`0236` and `0237`'s OPEN ITEMs are both CLOSED at MEASURED_SHA.
OPEN ITEMS: 3
