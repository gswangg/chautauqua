## 2026-08-15 task-w29-e — review-side TIER-0 perf (reviewer queue + plan results) @ b7060152
[QUALIFYING]

INVALIDATED BY: src/server/repo/review/**, src/routes/review/**

Owned `src/server/repo/review/**` plus `src/routes/review/reviewer.ts` and
`src/routes/review/plans-progress.ts`. Confirmed DEC-338's original
description against the current tree (its evaluation-count aggregate was
already SQL `GROUP BY`, not a whole-row load) and found the live DEC-829-
shaped violation instead: `countEvaluationsBySubmission`'s driving relation
was the plan's WHOLE round population, never narrowed to the reviewer's own
`resolveReviewerSubmissions`-scoped set. Fixed via an OPTIONAL 4th param
(chunkIds-batched `inArray`) — every existing 2-arg whole-plan caller
(including the locked contract in test/review-repo-aggregates.test.ts) is
byte-identical; the queue route's only production call now passes its own
scoped ids. DEC-440 (buildResults' JS aggregation, kept for its fail-loud
data-integrity throws over SQL's silent NULL coercion) is unamended and
binding, so no SQL-aggregate rewrite of the results scoring math was
attempted — the results-endpoint fix instead parallelizes the two chunked
`listSpeakerNamesForSubmissions`/`listTrackNamesForSubmissions` batch reads
(disjoint id sets per batch, Promise.all replacing sequential awaits); the
queue's format/audienceLevel batch reads got the same treatment, and its
seven independent post-scope reads now run as one Promise.all instead of
seven sequential awaits. No JSON envelope change either endpoint; DEC-018
plan-window enforcement, MAX_REVIEWER_SCOPE_ROWS/MAX_PLAN_SUBMISSION_SCAN
refusal caps, and DEC-345 ranking untouched. Full detail + before/after
tables: docs/verification-log/task-w29-e-review-perf-b7060152.md.

TESTS: `npx vitest related` over the four touched files (246 files/1873
tests) + `npx vitest run test/*review*.test.ts test/*plan*.test.ts
test/*evaluation*.test.ts` (74 files/605 tests) — all PASS. `npm run build`
green.

MEASURED (default profile, `npm run seed` -> `npm run perf:seed` ->
`wrangler dev --port 8894` -> `PERF_URL=... npm run perf:smoke`, server
killed after use), against the wave-27 baseline (ceda66f2: reviewer queue
85.4ms adj, plan results 69.1ms adj, both vs 50ms budget): three re-seeded
runs at b7060152 measured reviewer queue at 69.7/66.6/76.1ms adj and plan
results at 60.6/74.8/61.6ms adj — improved but still FAIL against the 50ms
read budget, with high run-to-run variance on this machine noted honestly.
RESULT: FAIL (both checks, improved) — LOGGED PRODUCT FINDING: further
reduction on plan results would require revisiting DEC-440's foreclosure of
SQL-aggregate scoring math, not a call this task makes unilaterally.

