## 2026-08-15 task-w36-b — walkthrough @ f5783479

QUALIFYING

INVALIDATED BY: src/** app/src/** migrations/** package.json

DEC-644 three-sha boundary: HEAD `f5783479` (docs-only scribe commit);
newest first-parent product-code-bearing sha `3a041507` ("merge
task-w35-c"); every live `task-w3*` ref (`task-w35-a`, `task-w35-e`,
`task-w35-f`, `task-w36-a`, `task-w36-c`) confirmed an ancestor of HEAD
via `git merge-base --is-ancestor`.

Full detail: docs/verification-log/task-w36-b-walkthrough-f5783479.md

Ran the DEC-069 required section-2 persona walkthrough (`ensure-dev-vars`
-> `vite build` -> `db:migrate` -> `seed` -> `wrangler dev --port 8971`
-> `scripts/walkthrough.ts`). First run FAILed at producer/J2 on an
off-origin scraped reset link (`http://localhost:8787` vs `--url`'s
`:8971`) — a gitignored `.dev.vars` `PUBLIC_BASE_URL=http://localhost:8787`
default (DEC-296), precedented and fixed the same way at
docs/verification-log/index/0163-2026-08-15-task-w26-f-walkthrough-73f380f2.md
("unrelated to any product code... corrected the gitignored `.dev.vars`").
Corrected `.dev.vars` to the actual port, wiped `.wrangler/state`,
re-migrated/re-seeded, restarted the server, and re-ran clean. Because
producer runs J1/J2/J3/J5/J9 sequentially in one process, the first run's
J2 abort meant J3/J5/J9(break lifecycle) never ran in that attempt —
confirmed absent from that transcript rather than assumed, per this
task's brief.

Second (measured) run — Summary quoted verbatim:

```
Summary:
  PASS producer
  PASS review
  PASS speaker
  PASS public
  PASS data
  PASS scale

walkthrough OK
```

Confirmed the DEC-063 wave-66/68 amendments' sections actually ran:
producer printed `Running J9 (break lifecycle: create -> list -> public
agenda -> delete) against devflow-conf-2027... ok`, and public printed
`ok   J10 /e/devflow-conf-2027/programme renders the whole programme` and
`ok   J10 GET / is the anonymous event hub and redirects a signed-in
user`. Zero `FAIL` lines anywhere in the second run's transcript across
all six areas.

RESULT: PASS — all six walkthrough areas pass at product sha `3a041507`,
including the break-lifecycle, printable-programme, and anonymous-hub
sections, after correcting a gitignored env-config mismatch (not a
product defect).
OPEN ITEMS: 0
