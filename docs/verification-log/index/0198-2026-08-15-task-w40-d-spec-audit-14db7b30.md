## 2026-08-15 task-w40-d — spec-audit @ 14db7b30

QUALIFYING

INVALIDATED BY: src/** app/src/** migrations/** package.json

DEC-644 wave-40 three-sha boundary: HEAD `14db7b30` (`main`'s tip at
worktree cut, `git merge --no-edit main` reported "Already up to date");
newest product-bearing first-parent sha `ed5c679e` (`merge task-w39-e`).
`npm run ref-state` confirmed every live ref an ancestor of HEAD; no
`task-w39-*` ref still exists (all merged/deleted per field guide), so the
STEP 0 ancestry-check is vacuously satisfied — no re-sync needed. §8/§9
(DEC-063 wave-27): one-line citations, nine-consecutive-PASS retirement;
SPEC §9's four named cheap invariants (DEC-063 wave-35) cited as the closed
population `test/spec9-invariants.test.ts`, all four `describe(` blocks
re-confirmed present at this HEAD. Five §6/§7 static checks (DEC-063
wave-27): (1) FK-index cross-check — `git diff --stat f5783479..HEAD --
src/db/schema migrations` is EMPTY (zero schema/migrations churn across
waves 38-39); the `f5783479`-boundary 65+2-row table carries forward
unchanged, zero gaps. (2) `app/src/App.tsx` code-split by route — diff
against `f5783479` also empty; every page still `lazy(pageLoaders.X)`.
(3) `< 300 KB gz` — run FIRST-HAND this wave inside one
`with-test-lock.sh` acquisition (`npm run build && npm run bundle:check`):
`Entry bundle: index-DRSpxsXW.js + index-DpG2gFFa.css = 69.20 kB gzip
(budget 300.00 kB)` / `bundle:check PASSED`. This own-measured figure
discharges exit-ledger OPEN ITEM 5's `PENDING-OWNED(task-w36-a)` label
(DEC-069 wave-39 amendment (c)) — cited as this lane's own reading, not a
sibling's. (4) parameterized queries only — zero raw-SQL
string-concatenation hits outside the Drizzle `sql` tag. (5) no HTML
content type served — `assertServedContentTypeHeader`
(`src/domain/files.ts:546-548`) unchanged, still called before every
served response (`src/routes/files.ts:695`). `docs/eval-rubric/*.yaml`
coverage: 116 IDs total, identical per-file breakdown to every prior audit
since `task-w25-e` (no rubric file added/removed/ID-modified). Full
detail: `docs/verification-log/task-w40-d-spec-audit-14db7b30.md`.

RESULT: PASS
OPEN ITEMS: 0
