## 2026-08-15 task-w45-c — onboarding-lifecycle adjudication @ 8c194ec9

QUALIFYING (advisory to the DEC-069 predicate — this scope classifies to none of the five slots)

INVALIDATED BY: src/** app/src/** migrations/** package.json

### Step 0 receipt (verbatim, `npm run ref-state`)

```
DEC-644 three-sha boundary: HEAD `8c194ec91ede63942022550bbced9bf3ba00f1b5`; newest first-parent product-code-bearing sha `14da2921a5be66408057712be877bc44c19de6c4`; every live ref (`main`, `manual-qa`, `task-custodian-w68-4`, `task-w44-a`, `task-w44-b`, `task-w44-c`, `task-w44-d`, `task-w44-f`, `task-w44-g`, `task-w45-a`, `task-w45-b`, `task-w45-c`, `task-w45-d`, `task-w68-d`, `task-w71-c`, `task-w71-d`, `task-w71-e`) confirmed an ancestor of HEAD via `git merge-base --is-ancestor`. NON-ancestor refs (NOT confirmed via `git merge-base --is-ancestor`): `mail-rich-shape-fallback`, `task-w17-i`, `task-w44-e`, `task-w44-i`, `task-w68-b`, `task-w68-c`, `task-w68-e`, `task-w71-a`, `task-w72-a`, `task-w72-b`, `task-w72-c`, `task-w72-d`, `task-w72-e`, `task-w72-f`, `task-w72-g`, `task-w72-h`, `task-w72-i`, `task-w72-j`.
```

`git merge --no-edit main` on branch `task-w45-c` (forked from `main` at the same commit): "Already up to date." Short HEAD: `8c194ec9`.

### Verdict table (J6/J7, SPEC.md:129-144)

| # | Claim | Verdict | Evidence |
|---|-------|---------|----------|
| 1 | Cron reminder idempotence — same-day double tick cannot double-send | **CONFIRMED-DEFECT** | `src/server/repo/tasks/reminders.ts:416-459` (`sendReminderEmails`): the mailer send loop (`:416-452`) runs to completion, and the `lastRemindedAt` dedupe stamp is only written AFTERWARD in a separate chunked `UPDATE` (`:454-459`). There is no claim-before-send step and no unique/ledger row per (assignment, day) — `lastRemindedAt` is a plain mutable timestamp, not a constraint. Two overlapping cron invocations (a platform retry firing while the first is still in flight, or literally two triggers) both run `listDueReminderContactIds` (`reminders.ts:645-688`) before either has written its stamp, so both see the same contacts as due and both send. Per the task prompt's specific hint: `src/server/scheduled.ts:23-28` wraps `runDueReminders` in a try/catch that pushes into `failures[]` and rethrows one aggregate error at `:38-40`; inside `runDueReminders` (`src/routes/tasks.ts:665-698`) the same per-event try/catch pattern repeats (`:677-691`). If the chunked stamp-update at `reminders.ts:454-459` throws partway (e.g. a D1 blip on the 2nd of N chunks) AFTER the mail loop has already sent every recipient in that event, the exception propagates out of `sendReminderEmails`→`sendDueRemindersForEvent`, is caught at `tasks.ts:688`, and the event is added to `failedEventIds` and retried (by the platform, or the next cron tick). On retry, the contacts whose stamp update did NOT commit are still selected by `listDueReminderContactIds` (their `lastRemindedAt` is unchanged) and receive a second reminder email for tasks they were already reminded about in the failed pass. No dedupe key or unique index guards this window — confirmed by reading `migrations/0019_join_table_uniqueness.sql`, which adds a `(task_id, contact_id)` unique index for `task_assignment` rows but nothing constraining reminder-send count per day. |
| 2 | Due-day boundary computed in the event's own timezone, not UTC | **NOT A DEFECT (already fixed)** | `src/domain/reminders.ts:99-112` (`isReminderDue`): `const dueEnd = dayLabelEndInstant(a.dueDate, timeZone);` — every comparison (`REMINDER_OVERDUE_TAIL_MS` tail at `:109`, `DUE_WINDOW_MS` window at `:111`) is against `dueEnd`, not the raw UTC-midnight `dueDate` day label, and `timeZone` is threaded in from the event row all the way from `sendDueRemindersForEvent` (`src/server/repo/tasks/reminders.ts:710,731`, `eventTimezone` sourced from `outstanding[0].timezone`, throws at `:725` if unresolved). `src/domain/task-due.ts:1-60` documents the same DEC-801 (wave-58) fix for the sibling grid/portal surfaces via `effectiveAssignmentDueDayLabel`/`dayLabelOfInstant`. Both the reminder gate and the due-date-display path are event-timezone-aware, not UTC. |
| 3 | Acceptance auto-creates onboarding task set idempotently (accept→decline→accept; bulk write including an already-accepted submission) | **NOT A DEFECT** | Guarded at TWO levels. (a) JS pre-check: `src/server/repo/submissions/status.ts:222-234` selects existing `(contactId, task.title)` pairs before planning, and `planAcceptance` (pure, `src/domain/*`) is fed `existingTaskTitlesByContact` so it never re-plans a pair already present. (b) DB-level real guard: `src/server/repo/submissions/status.ts:290-294` inserts with `.onConflictDoNothing({ target: [schema.taskAssignment.taskId, schema.taskAssignment.contactId] })`, backed by the real `UNIQUE INDEX task_assignment_task_id_contact_id_idx (task_id, contact_id)` added in `migrations/0019_join_table_uniqueness.sql:32` (comment at `:1-5` explicitly documents this replacing a JS-only pre-check race). So even a genuine concurrent double-accept cannot duplicate task_assignment rows — the DB constraint is authoritative, the JS pre-check is only an optimization to avoid a doomed insert. DEC-932 (status.ts:297-309) additionally back-fills any custom task the organizer added after first acceptance, using the identical select-then-`onConflictDoNothing`-insert shape (`crud.ts`/`status.ts` set-based insert), so a decline→accept cycle converges on the full current task set without duplicating existing assignments. Per the task's own instruction, the DEC-932 fan-out itself is NOT re-filed as a defect. |
| 4 | Portal scoping is absolute — reads scoped in the query's WHERE, not JS-filtered after an unscoped read | **CONFIRMED-DEFECT** | Two occurrences of the flagged anti-pattern in the named file set, both returning actual content to the caller (not merely an authz-gate object consumed by an explicit `assert*` call before a write, which is a distinct, deliberate idiom used elsewhere — e.g. `assertOwnAssignment` at `src/server/repo/portal/tasks.ts:306-310`, `getParticipantScope` at `src/server/repo/portal/invitations.ts:73-88` — and is out of scope here). (a) `src/server/repo/portal/data.ts:208-273` `getPortalSubmissionDetail`: the initial `SELECT` (`:214-246`) is scoped only by `submissionId` + `orgId` (`:245,248`) — no `contactId` anywhere in that query's WHERE/JOIN. Ownership is decided afterward in JS at `:273` (`if (!isOwnedByContact(participantContactIds, contactId)) return null;`) against a SEPARATE participant-rows query (`:263-271`) that also omits `contactId` from its WHERE. The function's own comment (`:202-207`) argues this shape is needed so "doesn't exist" and "not mine" both 404 uniformly — but that same uniform-404 property is achievable with `contactId` inside the WHERE/JOIN (as every other reader in this file set does, e.g. `data.ts:99-101,134`), so the unscoped-read shape is not required by the stated goal. (b) `src/server/repo/portal/resources.ts:117-152` `getResourceDownloadScope`: the resource row is fetched by `resourceId` only (`:123-133`, no `contactId`), then ownership is checked afterward in JS (`:137-138`, `getMyEventIds` + `isParticipantInEvent`) before the file row is read and returned (`:140-151`). Per the task's own standard this counts as CONFIRMED-DEFECT even though today's observable behavior is correct in both cases (both do check ownership before returning data) — it is one refactor (e.g. dropping the `if (!row \|\| ...)` guard, or a future caller invoking the query helper directly) away from a leak, because the query itself does not enforce the boundary. `src/server/repo/portal/tasks.ts:227-232` (`listDeliverableCandidates`'s sibling) is the counter-example already audited under DEC-962 and correctly carries `contactId` inside the same query's WHERE — cited as the shape (a) and (b) should converge on. |

### Targeted tests (DEC-644, `npm run test:targeted`)

15 files run: `test/task-due.test.ts`, `test/onboarding-grid-query.test.ts`, `test/portal-preview.test.ts`, `test/reminders.test.ts`, `test/reminders-contact-scope.test.ts`, `test/reminders-timezone.test.ts`, `test/reminder-window-timezone.test.ts`, `test/reaccept-onboarding.test.ts`, `test/tasks-due-reminders.test.ts`, `test/tasks-run-due-reminders-aggregate-failure.test.ts`, `test/portal-idor-probe.test.ts`, `test/portal-resources-scope.test.ts`, `test/portal-invite-scope.test.ts`, `test/portal-batched-scope.test.ts`, `test/portal-submission-detail-round-trip-depth.test.ts`.

```
Test Files  15 passed (15)
     Tests  84 passed (84)
```

No test in this targeted run exercises the two confirmed defects above (both are adjudicated by direct code reading, not by a failing/new test — this task is FILE, NEVER FIX; no test files were added or modified).

### Fix directions (wave-46 owners)

- Defect 1 (cron reminder idempotence race): make the dedupe stamp atomic with the send decision — either claim-then-send (an `UPDATE ... SET lastRemindedAt = now WHERE lastRemindedAt IS NULL OR lastRemindedAt <= cutoff` issued per-recipient BEFORE the mailer call, checking rows-affected before sending) or a per-(assignment, day) unique ledger row inserted with `onConflictDoNothing` before the send, mirroring the `task_assignment_task_id_contact_id_idx` pattern already used for onboarding-task idempotence (`migrations/0019_join_table_uniqueness.sql`). Owner: wave-46, J6 lane.
- Defect 2 (portal read-then-filter): rewrite `getPortalSubmissionDetail` (`src/server/repo/portal/data.ts:208-273`) and `getResourceDownloadScope` (`src/server/repo/portal/resources.ts:117-152`) to carry `contactId` inside the query's WHERE/JOIN (participant-join scoped by `contactId`, mirroring `listDeliverableCandidates` at `src/server/repo/portal/tasks.ts:227-232`), preserving the existing uniform-404 behavior. Owner: wave-46, J7 lane.

RESULT: PASS — adjudication complete, targeted tests green (84/84), 2 CONFIRMED-DEFECTs filed with fix directions, no product code touched.
OPEN ITEMS: 2
