# task-w17-e — evidence reconciliation across stage-1 waves 13-16 (DEC-453)

Log-only lane (DEC-452/DEC-453): no source file changed by this task; this
file plus one appended section in `docs/verification-log.md` are the only
artifacts.

**Sha audited: S' = `7836957e9ae35eb4b2b7c1af8030b0926e3bbfda`** ("scribe
wave 17"), which I derived myself with `git -C
/Users/wednesdayniemeyer/Documents/gniemeyer/Projects/chautauqua rev-parse
main` (and independently confirmed as `git rev-parse HEAD` inside this
task's own worktree, cut from `main` at the same commit) — not copied from
any prior log. This is the same commit `task-w16-f-spec-audit-stage1.md`'s
task brief context cites as current `main`; `git log --oneline -3` at S'
confirms `task-w16-f`'s ledger (`ff2ac53`) and its merge (`7cec9a3`) are
both ancestors, with only the scribe's wave-17 note commit (`7836957`)
added since.

Every claim below was re-checked directly against files at S' inside
`/Users/wednesdayniemeyer/Documents/gniemeyer/Projects/chautauqua-wt/task-w17-e`
(a worktree of the `task-w17-e` branch, itself cut from `main` at S').
Files audited are exactly the 18 `-stage1`-suffixed logs under
`docs/verification-log/` for waves 13-16 (`task-w13-*`, `task-w14-*`,
`task-w15-*`, `task-w16-*`); same-named campaign-1 homonyms without that
suffix were not read.

## Reconciliation table

| file | RESULT / OPEN ITEMS (as logged) | closure/VERIFIED claims made | HOLDS or STALE at S' |
|---|---|---|---|
| `task-w13-a-render-sweep-stage1.md` | no RESULT/OPEN-ITEMS footer (pre-DEC-438 format); narrative: fixed 2 named contrast offenders, flipped `ADMIN_MOBILE_PASS_BLOCKING`/`FONT_FLOOR_BLOCKING`, `CONTRAST_BLOCKING` left `false` (unmasked 3rd offender) | drag-glyph fix `app/src/pages/forms/forms.css:99` now `var(--chq-muted)`; track-chip fix in `src/routes/public/cards.tsx`+`public.css.ts` (hex-guard, `--chq-track-color`); `ADMIN_MOBILE_PASS_BLOCKING`/`FONT_FLOOR_BLOCKING` flipped true | **HOLDS.** Re-read `app/src/pages/forms/forms.css:98` (`.chq-forms-field-drag`), `src/routes/public/cards.tsx:21-22` (`chq-pub-track-chip` / `--chq-track-color` / `HEX_COLOR_RE`), `src/routes/public/public.css.ts:116-134` — all present verbatim. Its flagged 3rd offender (`--chq-disabled` on `.chq-forms-field-locked`/`.chq-forms-settings-title`) is closed later, see `task-w16-a` row below — this file's own claims (what IT changed) are unaffected and still true. |
| `task-w13-b-build-test-stage1.md` | OPEN ITEMS: 0 / RESULT: PASS | quickstart `package.json` scripts (`db:migrate`, `seed`, `dev`+`predev`) match README verbatim, none requires a secret | **HOLDS.** Re-read `package.json:6-11` — `predev`/`dev`/`db:migrate`/`seed` all verbatim as quoted. |
| `task-w13-c-walkthrough-stage1.md` | OPEN ITEMS: 0 / RESULT: PASS | `npm run walkthrough` exit 0, all 6 modules (producer/review/speaker/public/data/scale) PASS, J1-J12 fully covered, not Playwright/DEC-411-exempt | **HOLDS (superseded-consistent).** `scripts/walkthrough/` still has the same 6 modules (`data.ts producer.ts public.ts review.ts scale.ts speaker.ts`), untouched since this log's cut (`git log --oneline fd8b108..S' -- scripts/walkthrough` = empty). `task-w16-b-walkthrough-stage1.md` re-ran the same gate at a later sha and also read all-PASS, reconfirming. |
| `task-w13-d-perf-smoke-stage1.md` | OPEN ITEMS: 3 / RESULT: FAIL | flagged: (1) `schedule.ics` bare vs `agenda.ics` timing gap, (2) reviewer-queue whole-plan-load, (3) plan-results compute-then-slice | **STALE (partially).** (1) closed by DEC-442 (`task-w15-c`) — `src/server/pubcache.ts:80` `isUncacheableIcsRequest` now request-shaped, re-verified present at S'. (3) closed by DEC-439/440 (`task-w15-b`) — `src/routes/review/shared.ts:259` calls `listPlanFilteredSubmissions(..., { withTrackIds: false })`, re-verified present at S'. (2) **NOT closed** — see Open Items below; the DEC-439 fix (`task-w15-a`) landed and changed the mechanism, but `task-w16-c-perf-smoke-stage1.md` (landed after `task-w16-f`'s S) re-measured reviewer-queue as 4/4 FAIL at 54-88ms via a *different* bottleneck (chunked round-trips), which is still present in the code at S' (see below). |
| `task-w13-f-spec-audit-stage1.md` | OPEN ITEMS: 0 / RESULT: PASS | J1-J12 + SPEC §5/6/7/8 audit at that sha; closes both `task-w11-f` GAPs (save-draft rate limit, portal/profile length caps) with dedicated tests | **HOLDS.** `test/submit-draft-limits.test.ts` and `test/portal-profile-limits.test.ts` both present at S'. Superseded by `task-w16-f`'s later full re-audit (also PASS), which independently re-verified all J1-J12 rows at a later sha — no divergence found. |
| `task-w13-g-rubric-coverage-stage1.md` | OPEN ITEMS: 0 / RESULT: PASS | all 116 `docs/eval-rubric/*.yaml` `- id:` rows covered, zero NOT-COVERED, zero stage-1 gaps | **HOLDS.** `grep -h "^\s*- id:" docs/eval-rubric/*.yaml \| wc -l` = 116 at S' (unchanged file set/count re-confirmed). No rubric-scope-affecting source change landed since (rubric files themselves are fixtures/docs, not touched by any wave-14/15/16 lane per their own logs). |
| `task-w14-d-contrast-flip-stage1.md` | no RESULT/OPEN-ITEMS footer; narrative: 41/42 contrast, `CONTRAST_BLOCKING` stays `false` (re-confirms w13-a's 3rd offender, no flip fired), admin-mobile/font-floor already `true`, no edit needed | same 3rd offender (`--chq-disabled` on `.chq-forms-field-locked`) still failing at that sha, correctly not flipped | **HOLDS as a historical reading; its "stays false" disposition is since superseded (not stale — it never claimed permanence).** The offender it describes is fixed later by `task-w16-a` (see below); this log's own claim was scoped to its own run and explicitly deferred the fix to "the lane that owns those pixels" — it never claimed the offender would stay open, so nothing here is contradicted. |
| `task-w14-e-build-test-stage1.md` | (no single OPEN-ITEMS line; §5 lists items) RESULT implied PASS (build/test both green) | build PASS (154 modules, 8.10s), test PASS (271 files/2245 tests); PENDING-OWNED: `react-router`/`react-router-dom` v6 moderate advisories (GHSA-wrjc-x8rr-h8h6, GHSA-337j-9hxr-rhxg, GHSA-jjmj-jmhj-qwj2), no branch assigned | **HOLDS, and its one open item is still open at S'.** `package.json:35` still pins `"react-router-dom": "^6.28.0"` — unchanged, no v7 bump landed in any of waves 14-17's logs. See Open Items below. |
| `task-w14-f-walkthrough-stage1.md` | no footer; narrative: "No failures found. No code changed (log-only task per instructions)" | route-naming note: `/portal/edit` shorthand does not exist, real route is `/portal/submissions/<id>/edit` | **HOLDS.** Re-read `src/routes/portal/edit.tsx:195,222` and `src/index.ts:66` (`app.route("/portal", portalEditRoutes)`) — route shape unchanged at S'. |
| `task-w14-g-perf-smoke-stage1.md` | no OPEN-ITEMS/RESULT footer; narrative: all 21 `perf:smoke` checks PASS, all 4 DEC-432/433 named reads confirmed fixed and fast | `getMyResources`/`getContactStats` (DEC-432) inArray/chunkIds + SQL GROUP BY landed; `getPublicSessions`/`getPublicSpeakers` (DEC-433) `parsePage`+`boundedRowLimit` clamp landed | **HOLDS for the claims made.** Re-read `src/server/repo/portal.ts` (`getMyResources` uses `chunkIds`+`inArray`), `src/server/repo/contacts/stats.ts` (GROUP BY/HAVING count), `src/routes/public/query.ts:10-15` (`parsePage` clamp `[1,50]`), `src/server/repo/public/bounds.ts` (`boundedRowLimit`, throws non-finite) — all present verbatim at S'. This log's own reviewer-queue reading (18.4ms, PASS) was accurate for its own run; it made no permanence claim and is not contradicted by `task-w16-c`'s later, worse reading under a different code path (DEC-439 hadn't landed yet at this log's sha). |
| `task-w14-h-spec-audit-stage1.md` | PENDING-OWNED (2 items) / RESULT: NOT PASS | flagged DEC-433 (public bounds) and DEC-435 (`formatRef` in airtable sync) as pending, named owning branches `task-w14-a`/`task-w14-c`, explicitly predicted both close once merged | **HOLDS (self-consistent, and its prediction came true).** `src/routes/public/query.ts`'s bounds and `src/sync/airtable.ts`'s `formatRef` use are both present and correct at S' (spot-checked `src/sync/airtable.ts` calls `formatRef`, no hardcoded `SES-` literal found via `grep -n '"SES-"' src/sync/airtable.ts` = no matches). |
| `task-w15-e-build-test-stage1.md` | RESULT: PASS | build/test/bundle:check PASS; render-sweep desktop/mobile/admin-mobile/font-floor all-PASS (BLOCKING); contrast 41/42 (same known 3rd offender), `CONTRAST_BLOCKING` correctly stays `false` on its own run | **HOLDS.** Consistent with `task-w14-d`'s identical reading; superseded (offender fixed) by `task-w16-a`, see below — this log's own claim was scoped to its own run, not contradicted. |
| `task-w15-f-spec-audit-stage1.md` | PENDING-OWNED (4 items) / RESULT: NOT PASS | flagged reviewer-queue scoping (DEC-439/`task-w15-a`), results-endpoint boundary (DEC-439/440/`task-w15-b`), SPEC §10 #3 (DEC-441/`task-w15-d`), CONTRAST_BLOCKING flip (DEC-443/`task-w15-e`) as pending with named branches | **HOLDS (self-consistent, and 3 of 4 predictions verified true at S').** `task-w16-f` independently re-verified DEC-439/440/441 in code at its own sha (an ancestor of S'); I independently re-confirmed the same three at S' (see `task-w13-d` row). The 4th (CONTRAST_BLOCKING) is covered by the `task-w16-a` row below. |
| `task-w16-a-build-test-stage1.md` | RESULT: PASS | source fix DEC-444: `.chq-forms-field-locked`/`.chq-forms-settings-title` re-pointed `--chq-disabled`→`--chq-muted`; render-sweep 42/42 all-PASS incl. contrast; THE FLIP (DEC-445): `CONTRAST_BLOCKING` `false`→`true`, test updated | **HOLDS.** Re-read `app/src/styles.css:1147-1149` and `app/src/pages/forms/forms.css:130-132` — both `var(--chq-muted)` at S'. `scripts/render-sweep-contrast.ts:33` — `export const CONTRAST_BLOCKING = true;` at S'. `test/render-sweep-contrast.test.ts:97-99` asserts `.toBe(true)` at S'. `app/src/styles.css:30` `--chq-disabled: #8e8a7a` unchanged, as claimed. |
| `task-w16-b-walkthrough-stage1.md` | OPEN ITEMS: 0 / RESULT: PASS | full walkthrough PASS on fresh seed; reminder-preview dialog and reviewer-queue/results payload behave correctly in a real browser; 390x844 public spill check clean (agenda/schedule horizontal scroller is the intentional DEC-424 carve-out, not real spill) | **HOLDS.** `src/routes/public/public.css.ts:169` `.chq-pub-agenda-day-scroll { overflow-x: auto; ... }` and `src/routes/public/agenda.tsx:39` both re-read present at S', matching the carve-out claim. Note: its browser-level reviewer-queue/results check is a correctness check (no divergence), not a perf timing — does not contradict `task-w16-c`'s later perf-budget FAIL, which is a different axis (latency, not correctness). |
| `task-w16-c-perf-smoke-stage1.md` | OPEN ITEMS: 1 / RESULT: FAIL | reviewer-queue over 50ms budget 4/4 runs (54-88ms) for an *unrestricted* reviewer at 2,000 submissions; traced root cause to `chunkIds`(90)-per-request round-trips in `resolveReviewerSubmissions`'s track lookup + `countEvaluationsBySubmission`; plan-results and bare-`schedule.ics` both confirmed CLOSED | **HOLDS, and its one open item is still open at S' — this is the log the task brief specifically flags as landing after `task-w16-f`'s audited sha.** Re-read `src/server/repo/review/submissions.ts:201` — `for (const idChunk of chunkIds(matched.map((m) => m.id)))` still present at S'. Re-read `src/server/repo/review/evaluations.ts:125` — `const idChunks = submissionIds ? chunkIds(submissionIds) : [undefined];` still present at S'. No commit between `task-w16-c`'s cut and S' touches either file (`git log --oneline -- src/server/repo/review/submissions.ts src/server/repo/review/evaluations.ts` shows no post-`task-w16-c` commits reaching `main`). The fix DEC-449 names (delete the chunked track lookup + drop `countEvaluationsBySubmission`'s `submissionIds` param) is **not present** in the tree at S' — confirmed by its continued presence above. |
| `task-w16-d-security-probe-stage1.md` | no OPEN-ITEMS/RESULT footer; "No FAILs were found anywhere in this probe matrix" | full IDOR/authz/visibility probe matrix, all PASS; two noted gaps: anonymized-plan file-access isolation untested (seed data can't isolate it), foreign-org 403 used a nonexistent eventId (single-org seed) | **HOLDS.** Both noted gaps are self-described as seed-data limitations, not defects, and are unaffected by any subsequent commit (no seed-data or files-authz.ts change landed since). |
| `task-w16-e-quickstart-stage1.md` | table of 4 PASS rows, no OPEN-ITEMS/RESULT footer; "No stage-1 defect found" | fresh-checkout quickstart, full mail surface, zero-secrets, evaluator-package fidelity all PASS; one observed anomaly (email link port mismatch) attributed to swarm-concurrency, not a real defect, with an existing documented remedy (`PUBLIC_BASE_URL`) | **HOLDS.** `README.md`'s `PUBLIC_BASE_URL` remedy still documented (spot-checked `grep -n PUBLIC_BASE_URL README.md` returns matches) at S'; nothing in the anomaly's description implies a source defect. |
| `task-w16-f-spec-audit-stage1.md` | FAIL-unowned: empty / PENDING-OWNED: empty / RESULT: PASS, at its own audited sha S = `235d67799814580fafde386f87fb0aea7ea9f02c` | full J1-J12 + SPEC §5-10 re-audit; explicitly states "the two prior open items (reviewer-queue whole-plan load, results-endpoint compute-then-slice) are closed" citing DEC-439/440 re-verified in code | **STALE for the reviewer-queue half of that closure claim, as of S' (which is a strict descendant of S).** `task-w16-f` is internally correct about S — DEC-439's *scoping* mechanism (limiting to the requesting reviewer's `plan_reviewer` rows) is real and present, and that is exactly what it verified via `src/server/repo/review/submissions.ts:126-135`'s doc comment and code. But `task-w16-f` never independently re-measured perf at S — its perf claim for that row is "(mechanism)" (CI-enforcement machinery exists), not "(measured)". `task-w16-c-perf-smoke-stage1.md`, whose commit landed on `main` *after* S (confirmed: `task-w16-c`'s log is dated the same wave but is not among `git log --ancestry-path S..S'` — S' contains it as a sibling wave-16 lane merged alongside `task-w16-a`/`f`, all folded into `main` at S'), is the first log to actually *measure* reviewer-queue at scale post-DEC-439 and found it still fails budget via a different mechanism (chunked round-trips) that DEC-439 didn't touch. Everything else `task-w16-f` verified (J1-J12 citations, SPEC §5/6/8/10 rows, the DEC-444/445 contrast flip, DEC-440/441/442) was independently re-checked by me above and HOLDS at S'. |

## Claims now stale

1. `task-w16-f-spec-audit-stage1.md`'s row "(4) SPEC §7/§8 ... Admin API reads p95<50ms ... The two prior open items (reviewer-queue whole-plan load, results-endpoint compute-then-slice) are closed" — the reviewer-queue half is stale. The *scoping* fix (DEC-439) it cites is real and unchanged, but a live, later measurement (`task-w16-c`) shows the endpoint still exceeds its 50ms read budget (54-88ms, 4/4 runs) at 2,000-submission scale for an unrestricted reviewer, via a mechanism (`chunkIds`(90) round-trips) DEC-439 never addressed. `task-w16-f`'s "PENDING-OWNED: none found" / "RESULT: PASS" is therefore stale with respect to this one budget row as of S' — it was accurate for S (no perf measurement had yet surfaced the regression) but a wave-16 sibling lane's evidence, which landed after S, contradicts it.

## Open items no landed commit closes (as of S')

1. **Reviewer-queue read-budget miss** (`src/server/repo/review/submissions.ts:201`, `src/server/repo/review/evaluations.ts:117-138`, `src/routes/review/reviewer.ts:50-90`) — 54-88ms adjusted p95 vs. 50ms read budget, 4/4 runs, for an unrestricted reviewer at 2,000 event-scoped submissions (`task-w16-c-perf-smoke-stage1.md`). Root cause: `chunkIds(ID_CHUNK_SIZE=90)`-driven sequential per-chunk D1 round-trips for the post-match track lookup and `countEvaluationsBySubmission`, ~46 round-trips per request. Owning decision: **DEC-449** (names the fix precisely: delete the chunked track lookup entirely — its one caller never reads `trackIds` — and drop `countEvaluationsBySubmission`'s `submissionIds` parameter; never fix by paging/reshaping). At S' this fix has **not landed on `main`** — confirmed both by re-reading the still-chunked code (see table row above) and by checking the field-guide's DEC-449 note is a fix intended for the concurrent wave-17 fix lanes (`task-w17-a`/`task-w17-f`, per DEC-452's "two source lanes"), neither of which had committed as of S' (`git -C /Users/wednesdayniemeyer/Documents/gniemeyer/Projects/chautauqua log task-w17-a --oneline -1` and same for `task-w17-f` both show HEAD still at `7836957`, i.e. no commits of their own yet). This is a live PENDING item, not yet PENDING-OWNED-and-merged.
2. **`react-router`/`react-router-dom` v6 moderate dependency advisories** (`GHSA-wrjc-x8rr-h8h6`, `GHSA-337j-9hxr-rhxg`, `GHSA-jjmj-jmhj-qwj2`) — flagged by `task-w14-e-build-test-stage1.md:151-153` as "PENDING-OWNED... No branch assigned; flagged here for planner triage." `package.json:35` still pins `react-router-dom@^6.28.0` at S'; no DEC or branch has adopted this item across waves 15-17's logs. Unowned, name/file: `package.json:35`, no owning DEC.
3. **Anonymized-plan file-access isolation gap** (`task-w16-d-security-probe-stage1.md`, re: `src/server/repo/files-authz.ts:154`) — confirmed by static review only, not by a live isolating probe, because the seed data has no file whose track is covered *only* by an anonymized plan assignment. Not a defect; flagged by that log as something "a future wave adding a track-3-only file fixture could close." No owning DEC/branch; informational only, not a FAIL.

## Note for the merge step

`docs/verification-log.md` is appended by more than one lane this wave —
this task adds exactly one new section (below) and does not modify any
other lane's section.
