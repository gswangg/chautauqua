import { describe, expect, it } from "vitest";
import { DECISIONS_INDEX } from "../src/decisions";

describe("skeleton", () => {
  it("builds and the test runner works", () => {
    expect(DECISIONS_INDEX).toBe(true);
  });
});
