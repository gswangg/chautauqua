# Field Guide
Owned by the swarm. Scribe appends lessons each wave, keeps this under a hard 60-line budget, compacting old entries. Injected into every agent.
- SPEC.md is source of truth (J1-J12 bar, sbek rubric IDs=verification hooks, §2 principles). docs/ precedence: clarifications.md overrides all; decisions/DEC-*.md binding, src/decisions.ts compile-checked (never hand-edit). Invariants: fail loudly; status changes never auto-email (one sanctioned exception, DEC-720); authz every route, server-side visibility.
- STAGE (compacted): pure-core no node:/cf; Hono sub-apps; errors {error:{code,message,fields?}}; bulk ops set-based; D1 PRIMITIVES; pagination ONE shape+count*+id asc; atomic SQL>read-then-write; uniqueIndex CONTRACT; MINTING IS IO; UNBOUNDED SURFACE NEVER PAGED; A UNIVERSAL NEEDS A POPULATION. DEC-002..999 space FULL, no DEC-1000+; rulings land as `## Amendment (wave N)` on nearest EXISTING DEC. TOOL TRAP: Grep -C drops some `/`.
- FINDINGS w31-54 (heavily compacted): loose ref beats stale packed-refs, read reflog FILE TAIL; A GATE INSIDE A CODE WAVE CAN NEVER QUALIFY (DEC-069); ONE MACHINE ONE HEAVY GATE lock (DEC-644); CSRF-EXEMPT WITHOUT SESSION IS BEARER-REACHABLE (DEC-027); AN AMENDMENT THAT REVERSES SAYS SO (DEC-099 w49); A HAND-LISTED POPULATION IS NOT A POPULATION (DEC-180 w52); A COVERAGE CAP IS NOT AN IDENTITY (DEC-100 w52); A DECLARED TARGET IS PART OF THE CONTRACT (DEC-817 w53); A KNOB IS DECLARED BY THE TABLE AND SPENT BY EVERY LINK (DEC-489 w54); AN EXPORT IS A READ SIDE (DEC-873 w54); A DAY LABEL FROM A SUB-DAY CLOCK OFFSET IS A TIMEZONE BOMB (DEC-522 w52).
- FINDINGS w55-58 (compacted): A DECLARED KNOB WITH NO READER IS A LIE (DEC-851 w55, embed trackId); A LITERAL MATCH IS NOT A POPULATION, TWICE (DEC-941/DEC-989 w56, delete-confirm scan + page-measure scan); A SETTING WITH NO READER IS A LIE (DEC-988 w56, showResources); A DRAWING WIDTH IS NOT A MEASURE, adjudicated NOT-A-DEFECT (DEC-989 w56); A DIRECTORY IS NOT A POPULATION (DEC-367/DEC-808 w57, theme.ts phone tap floor invisible to path-prefix scans); A CITATION IS NOT AN ASSERTION (DEC-967 w57, line numbers in comments are not tripwires); 820+300+60=1180 arithmetic not a ruler (DEC-939 w57); THE DESIGN AUTHORITY MOVED AND THE SPA HAS NO STATE LAYER (DEC-383 w58, V11 hover/active/motion net-new); A COMMENT CLAIMING A SIBLING'S BEHAVIOUR IS A CLAIM ABOUT THE TREE (DEC-018 w58); A BUDGET IS CLASSIFIED BY ITS KEY NOT ITS ROUTE (DEC-072 w58, IDENTITY budgets refund, IP flood guards don't); A DEFAULT IS AN INVITATION TO GUESS (DEC-678 w58).
- FINDINGS w59-61 (compacted): A ROSTER IS NOT A PROJECTION OF ITS TASKS (DEC-829 w59); A HISTORY OF POST-EDIT SNAPSHOTS CANNOT RESTORE ANYTHING (DEC-158 w59); A HIDDEN CONTROL WITH NO OFFSET IS STILL ON THE SCREEN (DEC-919 w59); A LADDER IS NOT A GATE, evaluate refusals in SOURCE order after Promise.all (DEC-370 w61); A DENOMINATOR MADE OF ROWS THAT EXIST CANNOT COUNT WHAT IS LATE (DEC-596 w61); ONE ROW ELECTED TO SPEAK FOR THE REST IS A LIE WITH A QUORUM (DEC-936 w61); A REFUSAL CAN BE DISCREET WITHOUT BEING USELESS, scope existence-hiding to what caller can't already infer (DEC-211 w61); A SECOND COPY OF A VOCABULARY IS A TRAP WITH A DELAY FUSE (DEC-613 w60); THE PROMISE AND THE RECEIPT ARE COMPUTED BY DIFFERENT DOORS, preview vs send dedupe (DEC-238 w60); A CONFIRM WITH NO ARMING IS THE MIRROR OF A DELETE WITH NO CONFIRM (DEC-941 w60).
- FINDINGS w62-64 (compacted): A LADDER IS A PAGE NOT A QUERY, one Promise.all per level (DEC-370 w62); A WRITE'S ECHO COMES FROM THE WRITE, use `.returning()` (DEC-948 w62); A MINT IS THE LAST THING A SEND DOES, mint below every refusal for survivors only (DEC-397 w62); A DURABLE WRITE IS A POINT OF NO RETURN, config reads hoist above the first write (DEC-547 w62); `db.batch` used NOWHERE in src — needs its own lane, never a drive-by; A GATE FINDING IS A CLAIM ABOUT A SNAPSHOT NOT ABOUT MAIN (DEC-903 w63); A SILENT FALLBACK HAS SIBLINGS IN OTHER FILES, grep the ternary shape not the DEC number (DEC-843 w63); A MOCK THAT MATCHES NOTHING IS A CONTRACT WITH ITSELF, diff emitter type vs reader type (DEC-663 w64); A DECLARED DURATION WITH NO READER IS DEC-851 IN CSS (DEC-851 w64, motion tokens).
- WAVE 65 IS A CODE WAVE (DEC-069 w65): no gate lane, no index section, no seq allocated. Main `63788505`. A MIRROR THAT SERIALIZES THE REAL REQUEST IS NOT A MIRROR (DEC-011/DEC-478 w65, app csv.ts duplicated src/lib/csv.ts despite tsconfig cross-package access already used elsewhere). TWO FUNCTIONS THAT DIFFER ONLY IN A SAFETY RULE MUST DIFFER IN THEIR NAMES (DEC-179 w65, formula-injection neutralization is an export property — `toCsvVerbatim` for round-tripped payloads). A SWALLOWED SIDE EFFECT IS STILL AN OUTCOME (DEC-238 w65, welcome-mail receipt never reported; last-organizer demotion done as a cap not an identity). DEFERRED not closed (w65): contacts import custom.<key> mapping, blocked because `ImportWizard.tsx` was owned by w64.
- WAVE 66 IS A CODE WAVE (DEC-069 w66): no gate lane, no index section, no seq allocated. Main is `4acfd6ef` ("merge train w64: relocate the duplicate cap to domain, close three CSS gaps") -- the reflog FILE TAIL (`.git/logs/refs/heads/main`, lines 2591-2606) ran SIXTEEN entries past wave 65's read point `63788505`, landing w62 `-i/-j`, all six of w63, w64 `-a`..`-d`, a manual-qa fast-forward (`9b18f7ff`, orchestrator hotfix8) and **the GATE-11 SWEEP VERDICTS** (`7718ab4f`) -- a whole new mandate section wave 65 could not have seen. Refs that exist UNMERGED: `task-w64-e` (`f51768a8`, NewContactModal+TracksRoomsPanel copy) and `task-w64-f` (`5b52ee76`, motion tokens + status-cell ring, owns `src/views/theme.ts` AND `app/src/styles.css`). Wave 65 `-a/-b/-c/-e` sit at main's tip with zero commits; `-d` was planned and never cut -- OWNED. OFF-LIMITS, all five w65 scopes: `users.ts`'s welcome-mail receipt + `PeopleRolesPanel`, the last-organizer atomic demotion in `repo/users.ts`, every in-route refusal under `src/routes/portal/**`, and the app CSV module + `ImportWizard.tsx`.
- A COMMENT THAT DESCRIBES ITS OWN FILE CAN STILL BE FALSE (DEC-846 w66): `ComposeWizard.tsx:919` ("then clear the selection") and `:275-288` ("when the composer still holds one, it's posted too") describe opposite behaviours forty lines apart, and the first one is the code -- `setTemplateId('')` on every dropdown pick, so every template-based send writes `template_id = NULL` and History's TEMPLATE cell, its template chips and Templates' "Last used" are all permanently blank. The `else` branch below it (`id !== '' && !found`) is unreachable dead code. When two comments in ONE file disagree, the newer ruling is not necessarily the one that shipped.
- A DIFFERENTIAL RULE THAT RESTATES ITS BASE IS A NO-OP WITH A NAME (DEC-021 w66): `.chq-agenda-break-band-flagged` sets `#efebdf` + `#565a4b`, which is exactly what `--chq-surface-sunk` and `--chq-muted` already compute on the base band. Class applied, suffix rendered, zero pixels changed. A scan that asks "is the modifier class present?" passes forever; the assertion owed is "does the modifier DIFFER from what it overrides?" -- same family as DEC-941's dead confirm and DEC-851's unread token.
- A CARVE-OUT IS A HAND-LIST (DEC-383 w66): `a:not(.chq-btn):hover` names ONE class and therefore repaints the label of every OTHER filled anchor -- `.chq-home-action-primary` (the hub's "Submit a talk") and `.chq-pub-day-pill-active` both ship dark-on-dark hover today. The derivable population is "a rule that sets `color: var(--chq-on-brand)`". Specificity is load-bearing in the fix: `.x:hover` (0,2,0) LOSES to `a:not(.chq-btn):hover` (0,2,1), so the repair must be anchor-qualified or carry the tier class.
- THE VOCABULARY WAS ALREADY IN THE NEXT FILE, TWICE (DEC-650/DEC-238 w66): `FieldModal` makes an organiser pick "eq" from a dropdown while `condition.ts:16-25`'s unexported `verbFor` already says "is"/"is not"/"is one of" for the row beneath it; and `/plans/:id/remind` has no dedupe at all although `loadRecentlySent`/`dedupeCutoff`/`dedupeKey` are shared, pure and one import away -- so clicking "Remind laggards" twice mails every reviewer twice and the panel prints the literal "Skipped: unknown." Before designing a receipt or a label map, grep for the one that exists.
- THREE FINDINGS ADJUDICATED, do not re-file (w66): a SKIP IS NOT AN EMAIL -- `email_log` records transport attempts (provider/status/sent_at) and is the per-contact audit; the gate-11 "history row omits the skipped count" is closed by ruling, not by a third `EMAIL_LOG_STATUSES` value. BreaksPanel's Location row is a documented app-only column (`:468-471`), a frame gap and not an extra control. And all four wave-65 review-lens findings were RE-VERIFIED closed on main (send.ts mints at `:162-177`, below both dedupe stages and the templateId 400; `content-notes.ts:105`; both `?sort=` readers throw).
