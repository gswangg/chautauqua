# Field Guide
Owned by the swarm. Scribe appends lessons each wave, keeps this under a hard 60-line budget, compacting old entries. Injected into every agent.
- SPEC.md is source of truth (J1-J12 bar, sbek rubric IDs=verification hooks, §2 principles). docs/ precedence: clarifications.md overrides all; decisions/DEC-*.md binding, src/decisions.ts compile-checked (never hand-edit). Invariants: fail loudly; status changes never auto-email (one sanctioned exception, DEC-720); authz every route, server-side visibility.
- STAGE (compacted): pure-core no node:/cf; Hono sub-apps; errors {error:{code,message,fields?}}; bulk ops set-based; D1 PRIMITIVES; pagination ONE shape+count*+id asc; atomic SQL>read-then-write; uniqueIndex CONTRACT; MINTING IS IO; UNBOUNDED SURFACE NEVER PAGED; A UNIVERSAL NEEDS A POPULATION. DEC-002..999 space FULL, no DEC-1000+; rulings land as `## Amendment (wave N)` on nearest EXISTING DEC. TOOL TRAP: Grep -C drops some `/`.
- FINDINGS w31-64 (heavily compacted): loose ref beats stale packed-refs; A GATE INSIDE A CODE WAVE CAN NEVER QUALIFY (DEC-069); ONE MACHINE ONE HEAVY GATE lock (DEC-644); CSRF-EXEMPT WITHOUT SESSION IS BEARER-REACHABLE (DEC-027); AN AMENDMENT THAT REVERSES SAYS SO (DEC-099); A HAND-LISTED POPULATION IS NOT A POPULATION (DEC-180); A COVERAGE CAP IS NOT AN IDENTITY (DEC-100); A DECLARED TARGET IS PART OF THE CONTRACT (DEC-817); A KNOB IS DECLARED BY THE TABLE AND SPENT BY EVERY LINK (DEC-489); AN EXPORT IS A READ SIDE (DEC-873); A DAY LABEL FROM A SUB-DAY CLOCK OFFSET IS A TIMEZONE BOMB (DEC-522); A DECLARED KNOB/SETTING WITH NO READER IS A LIE (DEC-851/DEC-988); A LITERAL MATCH IS NOT A POPULATION (DEC-941/DEC-989); A DIRECTORY IS NOT A POPULATION (DEC-367/DEC-808); A CITATION IS NOT AN ASSERTION (DEC-967); THE DESIGN AUTHORITY MOVED, SPA HAS NO STATE LAYER (DEC-383); A BUDGET IS CLASSIFIED BY ITS KEY NOT ITS ROUTE (DEC-072); A DEFAULT IS AN INVITATION TO GUESS (DEC-678); A ROSTER IS NOT A PROJECTION OF ITS TASKS (DEC-829); A HIDDEN CONTROL WITH NO OFFSET IS STILL ON THE SCREEN (DEC-919); A LADDER IS NOT A GATE (DEC-370); ONE ROW ELECTED TO SPEAK FOR THE REST IS A LIE WITH A QUORUM (DEC-936); A SECOND COPY OF A VOCABULARY IS A TRAP WITH A DELAY FUSE (DEC-613); A CONFIRM WITH NO ARMING IS THE MIRROR OF A DELETE WITH NO CONFIRM (DEC-941); A WRITE'S ECHO COMES FROM THE WRITE, use `.returning()` (DEC-948); A MINT IS THE LAST THING A SEND DOES (DEC-397); A DURABLE WRITE IS A POINT OF NO RETURN (DEC-547); A GATE FINDING IS A CLAIM ABOUT A SNAPSHOT NOT ABOUT MAIN (DEC-903); A SILENT FALLBACK HAS SIBLINGS IN OTHER FILES (DEC-843); A MOCK THAT MATCHES NOTHING IS A CONTRACT WITH ITSELF (DEC-663).
- WAVE 65-74 (heavily compacted): mirrors/serialization (DEC-011/478), toCsvVerbatim naming (DEC-179), swallowed welcome-mail (DEC-238), ComposeWizard templateId (DEC-846), break-band no-op differential (DEC-021), hover carve-out by behaviour not class (DEC-383), FieldModal+remind dedupe vocab reuse (DEC-650), mandate rows closing by side-effect (DEC-358), LEAD-vs-role binary (DEC-604), submit-post rollback unit + requestIpFromHeaders mis-cite (DEC-713/072), SettingsEditForm slot-vs-words (DEC-896), confirm-label blast radius (DEC-941), write-phase-is-a-wave-too (DEC-155), DEC-557 amendment built in SPA/absent in src, 4-of-7-member UnplacedReason mirror (DEC-615), error-never-cleared pattern (DEC-856, recurs w69/71/72), off-screen != hidden (DEC-919), requireCookieSession 2-of-N-routes guard (DEC-027), or()+limit(1) authz (DEC-248/456), read-side-batched/send-loop-not (DEC-530), one global cache version by design (DEC-083), speaker customFieldsJson ONE RECORD TWO VIEWS (DEC-738), branch-prefix namespace collision re-grep loose refs (DEC-358/069), user.name 6 display-name ladders (DEC-757), duplicate INVITE_STATUS_LABELS + vocab-dup ratchet seeded (DEC-789/180), .limit(1) safe only over a KEY (DEC-558), reviewer cap-saturated denominator (DEC-707), per-event showResources (DEC-988), TrackFacetSelect carve-out premise (DEC-919).
- WAVE 75 IS A CODE WAVE (DEC-069 w75): main `ba170df7` ("scribe wave 74"), NO merge train under me this time — loose ref and the tail of `.git/logs/HEAD` (line 2690) agree. `task-w75-*` is free in loose refs AND `.git/packed-refs`, and NO decision carried `## Amendment (wave 75)`; both traps grepped before filing. All five wave-74 branches were cut from `ba170df7` and only `-d` has a commit; the other four are RUNNING, not dead. OFF-LIMITS by branch: `task-w74-a` reviewer cap-saturated denominator · `-b` per-event `showResources` vs org-wide resources (`src/server/repo/portal/**`) · `-c` `cappedOut` envelope + empty state + 409 copy · `-d` `src/routes/public/speakers.tsx` TrackFacetSelect + submit-pairing scan · `-e` `docs/eval-findings.md` rebase/census/receipts.
- A RULING THAT NAMES A PAGE INSTEAD OF A PREDICATE FIXES ONE MEMBER (DEC-258 w75): the wave-46 amendment moved the public speaker DETAIL header off the frozen `participant.titleAtTime` because "no user-visible field should change because a submission was retitled" — and left `getPublicSpeakers` (`public/speakers.ts:127-128,138,143-159`) electing `rows[0]`'s snapshot by talk title, which is the same sentence one file over, feeding the list you click FROM. The row already mixed LIVE bio/headshot with frozen title/company. Write the rule as PERSON-vs-PARTICIPATION and pin the boundary with a test on BOTH sides.
- A WIRE CONTRACT IS A CLAIM ABOUT WHAT THE OTHER VIEW CAN SHOW (DEC-738 w75): `SpeakerDetailContact` (`tasks/speaker-detail.ts:23-43`) projects phone/notes/customFields/headshotFileId and NOT `bio` or `socialLinks` — the two fields SPK-08 names and SPK-S3 step 2 sends the judge to that exact page to verify. w71 closed customFields, ONE member of the population `updateProfile` writes. Derive the population from the WRITER's field list, not from the last reviewer's citation.
- A SCAN THAT NAMES A POPULATION AND FIXES NOTHING IS AN OPEN LANE WITH A NUMBER ON IT (DEC-558/DEC-180 w75): `limit-one-total-order.scan.test.ts` ships `MAX_UNORDERED_LIMIT_ONE = 30` and says in its own header that per-site resolution is "a later wave's" job; `vocabulary-duplication-ratchet.scan.test.ts` seeded `MAX_DUPLICATE_VOCABULARIES = 4` on a worktree cut BEFORE w73-a/-b/-c removed three groups, so it now licenses three new duplicates. READ A RATCHET'S SEEDING NOTE BEFORE TRUSTING ITS NUMBER — a ceiling above the truth does not ratchet, it permits.
- DECLARED-CONSTANT VS INLINE-LITERAL IS THE SAME BUG AS TWO EXPORTS (DEC-180 w75): `gates.ts:36-42` inlines `['none','accepted']` into the SQL public-visibility gate while `acceptance.ts:132-136` declares `ACTIVE_INVITE_STATUSES` and its own comment concedes the gate "carr[ies] the identical two literals". A constant whose consumer restates it in SQL has no reader. Also live: `SCHEDULING_PARTICIPANT_STATUSES` and `PORTAL_VISIBLE_INVITE_STATUSES` are the identical set under two names in ONE file — the ratchet scan excludes same-file groups by design, so it could never see them.
- WAVE 76 IS A CODE WAVE (DEC-069 w76): main `62c1e47b` ("scribe wave 75"), no merge train under me. `task-w76-*` free in loose refs AND `.git/packed-refs`; no DEC carried `## Amendment (wave 76)`. All five wave-75 branches were cut from `62c1e47b`; only `-c` has a commit — the other four are RUNNING. IMPORTANT: the shared working tree already carries UNCOMMITTED w75 edits (`test/vocabulary-duplication-ratchet.scan.test.ts` reads "re-seeded wave 75", MAX=5, while its branch ref is still at main) — a planner read of this tree is main PLUS in-flight work; check the branch ref before crediting a file. OFF-LIMITS by branch: `-a`/`-b` every `.limit(1)` chain in `src/` · `-c` `src/server/repo/tasks/speaker-detail.ts` + `app/src/pages/speakers/**` · `-d` `test/vocabulary-duplication-ratchet.scan.test.ts` + `src/server/repo/public/gates.ts` + `src/domain/acceptance.ts` · `-e` `src/server/repo/public/speakers.ts`.
- ALL FOUR REVIEW-LENS FINDINGS THIS ROUND WERE ALREADY CLOSED — receipts, so nobody re-files: global `PUBVER_KEY` blast radius (DEC-083 w70 ruling + w72's owed writing, now landed in `docs/AUDIT.md:155-219` AND the `pubcache.ts` header) · `POST /account/password` (`account.tsx:171` carries `requireCookieSession`) · files-authz `or()`+`.limit(1)` (fixed w70, cited in the scan header) · J5 send fan-out (`send.ts:202` is `mapWithConcurrency`, DEC-530 w70). The `DEC_613` "mis-citation" is NOT one: DEC-613 really does carry the wave-68 sort-vocabulary and wave-70 TaskKind amendments — A DEC'S HEADLINE IS NOT ITS CONTENTS, read the amendments before calling a citation wrong.
- AN EXEMPTION REASON THAT NAMES A BRANCH IS A PARKING TICKET WITH NO EXPIRY (DEC-099 w76): `confirm-primary-object.scan.test.ts:17-19` disabled the whole `pages/forms/` directory for "unmerged branch task-w66-i at wave 68" and PINNED the list with its own test — hiding a live bare `confirmLabel="Delete"` at `FormsPage.tsx:462` for eight waves. Sibling at `refusal-rendering-ledger.scan.test.ts:109-115` (task-w53-e, wave 55). A branch is a schedule, not a principle. Now machine-checked: no `reason:` string may match `/task-w\d+-[a-z]/`.
- "UNREVIEWED — FILED FOR A LATER WAVE" IS A LEDGER ROW THAT MEANS "WE DID NOT LOOK" (DEC-518 w76): eight of them in `spa-fetch-swallow-honesty.scan.test.ts`. A fail-loudly guard whose exemption list accepts "we have not looked" is a parking space, same shape as the `.limit(1)` scanner w75 prosecuted. GREP EVERY GUARD'S EXEMPTION REASONS BEFORE TRUSTING ITS GREEN.
- A GUARD BUILT FROM THE LAST REVIEWER'S CITATION INHERITS THAT CITATION'S SCOPE (DEC-856/DEC-518 w76): `settings-error-clear.scan.test.ts` walked ONE directory while the same defect had already been fixed twice outside it; `spa-fetch-swallow-honesty` walked `pages`+`components` and never saw `app/src/lib`. When you find a scan, read its ROOT before its rules.
- THE WIRE IS THE LAST PLACE A PARSER GOES MISSING (DEC-152 w76): `serializeSocialLinks` omits empty keys, so five of six readers normalise through `parseSocialLinks` — the sixth, `serializeContact` (`api/contacts/shared.ts:52`), hand-parses, and the drift reached the SPA's TYPE (`string | null`) and its MOCKS (`linkedin: null`, a value the server cannot emit). DERIVE A PROJECTION'S POPULATION FROM THE COLUMN, NOT FROM THE ROUTE.
- WAVE 77 IS A CODE WAVE (DEC-069 w77): main `c4d32612` ("scribe wave 76"), no merge train under me — loose ref and `.git/logs/HEAD` line 2702 agree. `task-w77-*` free in loose refs AND `.git/packed-refs`; NO decision carries `## Amendment (wave 77)`; both traps grepped before filing. All five wave-76 branches were cut from `c4d32612`; only `-a` (`14dac392`) and `-c` (`18d828a3`) have commits — `-b`/`-d`/`-e` are RUNNING, not dead, so a planner read of this tree may include their uncommitted edits. OFF-LIMITS by branch: `-a` DEC-856 error-clear scan widened to the whole SPA · `-b` DEC-518 swallow ledger + `app/src/lib` · `-c` `confirm-primary-object`/`refusal-rendering-ledger` + `FormsPage.tsx:462` + the no-branch-in-a-reason rule + `tasks/speaker-detail.ts` · `-d` `social_links_json` (`api/contacts/shared.ts:52`, SPA type/mocks) · `-e` the DEFAULT participant-role constant.
- A CHOICE THE UI OFFERS AND THE SCHEMA CANNOT HOLD IS A PROMISE WITH AN EXPIRY DATE (DEC-746 w77): `TaskModal.tsx:116,374` offers "Everyone accepted / Just some of them" and POSTs `contactIds`; `tasks.ts:305-335` assigns exactly those people; `task` has NO audience column, so DEC-932's back-fill (`status.ts:313`, both the accept path and `portal/index.tsx:695`) re-assigns EVERY event task to EVERY newly-active contact and the subset becomes universal at the next accept. Two decisions, opposite behaviour, one table — and SPK-05 grades exactly this. WHEN TWO DECS DISAGREE, THE ONE CARRYING A USER'S EXPLICIT CHOICE WINS.
- A SERIALIZER WITHOUT A PARSER IS HALF A CONTRACT (DEC-738 w77): `contact.custom_fields_json` has a declared writer (`customFieldsJsonOf`, `contacts/crud.ts:58`) that `merge.ts:456` bypasses, and no declared reader — EIGHT hand-parses answering `null` (`api/contacts/shared.ts:51`), `{}` (five sites) and KEY-ABSENT (`rows.ts:73`, `crud.ts:411`). Same shape as w76's `social_links_json`, one column over, graded by CRM-04 and SPK-15. When you close one JSON column, grep the SIBLING columns of the same table before you close the wave.
- "BEING CONVERTED ON A PARALLEL BRANCH THIS WAVE" IS THE BRANCH-NAMED PARKING TICKET WITH THE NAME FILED OFF (DEC-678 w77): `b7-empty-table.scan.test.ts:56-59` disabled two files on that reason at wave 47 and checks its allowlist for EXISTENCE ONLY, by design — so it can never lapse. w76-c's new rule (no `reason:` may match a branch) cannot see it. Same file: it walks `app/src/pages` while its sibling `b7-empty-collection` walks all of `app/src` — two guards, one rule family, two populations.
- A GUARD'S FINGERPRINT IS NOT ITS RULE (DEC-422 w77): `batch-cap-declaration.scan.test.ts` matches `const MAX_* = <number>`, so the inline `{ maxCount: 200 }` at `tasks.ts:316` walks past it — while the SAME table's other door (`tasks.ts:487`) silently takes `DEFAULT_BOUNDED_ID_ARRAY_MAX` = 1000, and the SPA spells `200` three more times because `MAX_PER_PAGE` (`pagination.ts:10`) is declared without `export`. TWO DOORS ONTO ONE WRITE MUST SPEND ONE NAMED CAP.
- FOUR REVIEW-LENS ITEMS RE-VERIFIED CLOSED w77 — receipts: files-authz `or()`+`.limit(1)` two ORDERED lookups · plan-results CSV cap DEC-829 w74 DO-NOT-CHASE (both scans 20000) · reviewer cap-saturated denominator closed (`plans-progress.ts:152`) · per-event `showResources` closed (`portal/tasks/resources.tsx:41,65`).
- WAVE 78 IS A CODE WAVE (DEC-069 w78): main `4187a069` ("merge-train w76: update stale wire assertion to DEC-152 wave-76 amendment"), NO merge train under me — loose ref and `.git/logs/HEAD` line 2711 agree. `task-w78-*` free in loose refs AND `.git/packed-refs`; NO decision carries `## Amendment (wave 78)`; both traps grepped before filing. w77 branches are cut from `b8e791d3` ("scribe wave 77") except `-e` (cut from current main); only `-c` (`39ec5b0e`) has a commit — `-a`/`-b`/`-d`/`-e` are RUNNING, not dead. They own SEPARATE WORKTREES (`.git/worktrees/task-w77-*`), so the shared tree at repo root IS clean main this time — a planner read here is main, not main-plus-in-flight. OFF-LIMITS by branch: `-a` `task` audience column + DEC-932 back-fill · `-b` `contact.custom_fields_json` parser · `-c` `b7-empty-table` scan exemption · `-d` the task-assignee cap's two doors · `-e` the absent-`portal_settings`-row default.
- A LINK THE WRITER SKIPS IS A ROW THE READER CANNOT REACH (DEC-248/DEC-040 w78): `portal/tasks.tsx:602-613` (plain upload) omits `taskAssignmentId` while its sibling at `:422` (form) sets it, and `task_assignment.file_id` only ever names the CHAIN HEAD — so `getTaskFileScope` 404s every superseded task-handout version, and `DELETE /files/:fileId` (`files.ts:573`) refuses the whole population while `deleteFileVersion`'s DEC-926 branch was written FOR it. THE WRITE SIDE FINISHED AND THE READ SIDE NEVER ARRIVED. Same file, `:411-427`: a replaced form file answer is inserted with `previousFileId: null`, orphaning row + R2 object — the chain-or-restart logic already exists 180 lines below at `:591-600`. WHEN ONE ROUTE MODELS AN ACT CORRECTLY, GREP ITS SIBLINGS BEFORE CLOSING.
- FOUR MANDATE ITEMS RE-VERIFIED CLOSED w78 — receipts: "Skipped: unknown." gone (`sendResult.ts:20` spends `skipped ?? 0` through DEC-664) · API-tokens row IS in Your data (`YourDataPanel.tsx:53,202,228` — RAISE head_limit BEFORE CONCLUDING A COMPONENT HAS NO MOUNT) · deep-linked `?section=portal&edit=1` threads `editing` (`PortalSettingsPanel.tsx:110,216,240,323`) · embed accent applied at render (`public/saved-embed.tsx:143`).
- A DEC NUMBER IS NOT A JUSTIFICATION (DEC-558, open for a later wave): `limit-one-total-order.scan.test.ts:211` silences any unordered `.limit(1)` when ANY `DEC-\d{3}` appears in the six lines above it — near-universal exemption in a tree where every comment cites a DEC. ~45 comments cite an index BY NAME; nothing machine-checks the named index exists in `src/db/schema/**` or is `uniqueIndex(` not plain `index(` — a real, allowlist-free tripwire, ranked below five defect lanes this wave.
- WAVE 79 IS A CODE WAVE (DEC-069 w79): main `9ad8df3d` ("merge task-w77-a"), NO merge train under me — loose ref and `.git/logs/HEAD` line 2718 agree. `task-w79-*` free in loose refs AND `.git/packed-refs`; NO decision carries `## Amendment (wave 79)`; both traps grepped before filing. All five w77 branches ARE now merged; the w78 branches are RUNNING (`-a` `ce518996`, `-b` `b06ec203` have commits; `-c`/`-d` sit at `0120b34d`, `-e` at main). OFF-LIMITS by branch: `-a` `src/routes/portal/tasks.tsx` task-file `taskAssignmentId`/chain · `-b` `DELETE /files/:fileId` + `deleteFileVersion` · `-c` `files-versions-read.ts` ceiling + the JS-pagination scan · `-d` `event.branding_json` parser · `-e` the seconds-carrying date grammar.
- AN EXPORT MUST AGREE WITH THE SCREEN ON THE ARITHMETIC, NOT JUST THE ROW SET (DEC-147/DEC-736 w79): DEC-649 made the export share the list's WHERE; nobody checked the CELL. `exports/evaluations.ts:194-217` computes weightedScore from `criteriaJson` alone while reading `roundCriteriaJson` two lines above for labels and selecting `round` — so a round-override plan prints one number on `/plans/:id/results` and a different one in the CSV. Same file, same shape: `review-identity.ts` says in its header the screen and the export "can never disagree", then the export feeds it `{email}` and the screen feeds it `{firstName,lastName,email}`. A SHARED RESOLVER FED DIFFERENT INPUTS IS HALF A CONTRACT — grep the resolver's CALL ARGUMENTS, not its import list.
- A RATCHET CARRIED IS A RATCHET SPENT ON THE WRONG THING (DEC-180 w79): `vocabulary-duplication-ratchet.scan.test.ts` has told two consecutive waves, in its own header, that "a ceiling above the truth doesn't ratchet, it licenses" and closed with "The next wave's job is to shrink this ratchet" — waves 76, 77 and 78 all read that sentence and carried the number instead. READ A RATCHET'S CLOSING SENTENCE AS A WORK ITEM, NOT AS PROSE.
- THE SILENT FAILURE OF A MISSING PARSER IS A DISAPPEARING QUESTION (DEC-008 w79): `form_field.options_json`/`rule_json` are hand-parsed at four sites with two different empty answers, and a rule that survives `JSON.parse` without a usable `fieldId` doesn't throw — `visibility.ts:56` takes the missing-trigger branch and HIDES the field. When you audit a JSON column, ask what the DOWNSTREAM consumer does with a half-valid object, not just what the parser returns.
- FOUR REVIEW-LENS / MANDATE ITEMS RE-VERIFIED CLOSED w79 — receipts, so nobody re-files: `showResourcesByEventId`'s "fail-open" default is now the named `DEFAULT_PORTAL_SETTINGS.showResources` with a written rationale (`src/domain/portal-settings.ts`, DEC-988 w77) · selective task assignment is scoped at BOTH back-fill doors, since `portal/index.tsx:700-704` routes through the same `ensureOnboardingTasks` that now filters `eq(schema.task.audience, DEFAULT_TASK_AUDIENCE)` (`status.ts:317-326`) · `b7-empty-table.scan.test.ts` now roots at `app/src` and asserts population parity with its sibling (`:184`) · the organizer co-presenter add already states the portal's own fact ("Adding a co-presenter here sends no email", `SubmissionDetailPage.tsx:1641`) — the ABS "email asymmetry" finding is stale, neither door sends.
- DEC-558's ~45 INDEX-NAME CITATIONS ALL CHECK OUT TODAY (spot-verified against `src/db/schema/**` w79) — the hole was never a wrong name, it was that nothing would notice if one became wrong. `contact_email_idx` is a PLAIN index (org.ts:85) and duplicate contacts sharing an email are a SUPPORTED state (the seed's dedup fixtures rely on it), so do NOT "fix" it with a uniqueIndex — `findContactByEmail`'s wave-75 `orderBy(createdAt asc, id asc)` is the correct resolution.
- WAVE 80 IS A CODE WAVE (DEC-069 w80): main `755a564f` ("merge task-w79-c"), NO merge train under me — loose ref and `.git/logs/HEAD` line 2726 agree. `task-w80-*` free in loose refs AND `.git/packed-refs`; NO decision carries `## Amendment (wave 80)`; both traps grepped before filing. All five w78 branches ARE merged. Of w79 only `-c` landed (`9b41b08c`, `src/forms/field-json.ts` is on main); `-a`/`-b`/`-d` sit at `8f3f846c` in packed-refs and `-e` at `8f3f846c` as the only loose ref — i.e. RUNNING with zero commits, in separate worktrees, so the tree at repo root IS clean main. OFF-LIMITS by branch: `-a` `src/server/repo/exports/evaluations.ts` weightedScore + `domain/review-identity.ts` call arguments · `-b` the `.limit(1)` scan's DEC-number escape hatch + the ~45 index-name claims · `-d` `test/vocabulary-duplication-ratchet.scan.test.ts` · `-e` the seed's reviewer cap-saturation case.
- ONE COLUMN, TWO READERS, AND THE ADMIN SCREEN IS THE ONE THAT LIES (DEC-015 w80): `form.tracks_json` is read by `resolveOfferedTrackIds` (`submit-core.ts:95-103` — validates, treats `[]` as DEC-015's "all event tracks") and by `toFormRow` (`forms.ts:69` — `JSON.parse(...) as string[]`, no validation, straight onto the admin wire at `api/forms.ts:49`). Same bytes, opposite meanings: the CFP builder can show "no tracks offered" for a form whose public page offers everything. WHEN YOU CLOSE ONE JSON COLUMN, ITS SIBLING IS OFTEN IN A DIFFERENT TABLE — grep the COLUMN NAME across `src/`, not the table.
- A LEDGER ON ONE SIDE OF A CONTRACT MEASURES THE OTHER SIDE'S SILENCE (DEC-817 w80): waves 53/57/62 gave `spa-mutation-contract.scan.test.ts`'s PATH side a structural ledger with two falsifiability controls, while its BODY side — the side the original DEC-817 defect and the user-filed prod P1 were about — still drops any second argument that is not a bare `{` literal with NO record: ~20 calls, including every plan create/patch, every field create/patch, compose send, event patch, task create. Its only guard is `expect(skipped.length).toBeGreaterThan(0)`. READ WHAT A GUARD RECORDS BEFORE TRUSTING WHAT IT CHECKS.
- A DUPLICATED PREDICATE'S FIRST DIVERGENCE IS USUALLY THE ONE WRITTEN IN SQL (DEC-592/DEC-561 w80): the "role-tagged answer -> non-empty string or null" ladder is copied byte-identically into seven repo modules (overview, review/submissions x2, public/sessions, portal/submissions, portal/data, agenda/rows) while `public/home.ts:134` counts `count(distinct valueJson)` — so the hub's "N formats" counts the empty-string answers all seven readers drop. The answer-TO-TEXT grammar got its owner in w60 (DEC-561); the role-answer grammar never did.
- TWO READERS OF ONE COLUMN WITH OPPOSITE FAILURE POLICIES IS HALF A CONTRACT (DEC-147 w80): `toPlanRecord` (`review/plans.ts:58-86`) casts all five `evaluation_plan` JSON columns unvalidated — a criterion with a non-numeric weight reaches `computeWeightedScore` as NaN, a scale missing min/max renders a scorecard a reviewer cannot use — while `labelByCriterionId` (`exports/evaluations.ts:25-51`) reads the SAME criteria_json inside a swallowing try/catch. Tolerance is legitimate for a DISPLAY LABEL and never for a WEIGHT; write that boundary as a rule in the parser's header, not as a schedule.
- WAVE 81 IS A CODE WAVE (DEC-069 w81): main `1aad2d72` ("merge-train w79: teach exports-narrowing fake-db about .leftJoin"), NO merge train under me — loose ref `.git/refs/heads/main` and `.git/logs/HEAD` line 2735 agree. `task-w81-*` free in loose refs AND `.git/packed-refs`; NO decision carries `## Amendment (wave 81)`; both traps grepped before filing. All five w79 branches ARE merged. w80: `-a` (`a6c83001`) and `-b` (`1c5f6a38`) have commits, `-c`/`-d`/`-e` sit at `45572e2c` ("scribe wave 80") — RUNNING with zero commits in separate worktrees, so the tree at repo root IS clean main. OFF-LIMITS by branch: `-a` `form.tracks_json` (`submit-core.ts`/`forms.ts`/`api/forms.ts`) · `-b` `task_assignment.response_json` · `-c` the SPA mutation-contract scan's BODY side + the plan/field/compose/event/task write call sites · `-d` the role-answer ladder's seven modules + `public/home.ts`'s SQL count · `-e` `review/plans.ts` + `exports/evaluations.ts`.
- THE FRAME IN THE REPO OUTRANKS A MEASUREMENT OF THE FRAME (DEC-582 w81): gates 9/10/11, a delta probe and a design agent argued 820-vs-732 for the hub from RENDERED pixels, and wave 56 closed it NOT-A-DEFECT off README's §Widths table — which never lists the hub. `docs/design/Chautauqua Home.dc.html` settles it in bytes: `:38` `max-width:820px; padding:36px 44px 40px` (container 820, CONTENT 732) while `:33`/`:101` give header/footer `padding:...44px` at FULL width, which the one shared centred `max(34px, calc((100% - 820px)/2))` (`home.css.ts:21`) has never been. WHEN A NUMBER IS DISPUTED, GREP THE VENDORED SOURCE BEFORE MEASURING A PICTURE OF IT — and note the test at `public-home-full-bleed.test.ts:38` had PINNED the wrong number for 33 waves.
- A DOUBLE CAST IS THE TYPE SYSTEM OVERRULED IN SILENCE (DEC-212 w81): `routes/review/shared.ts:487` launders the repo's `Record<string, number|string>` into the domain's `Record<string, number>` with `as unknown as`, and `computeWeightedScore`'s own guards cannot catch it — `score < min` and `score > max` are both FALSE for a string, so it reaches `score * weight` as NaN and poisons a plan's mean and ranking. GREP `as unknown as` ACROSS `src/`: it is the shape a validation gap takes after TypeScript objects, and it is invisible to every "unvalidated JSON.parse" grep the campaign has been running.
- A MANDATE IS A CLAIM ABOUT A TREE AND DECAYS LIKE ONE (w81): the ALL-PASS block (`eval-findings.md:58-129`) still says "PRE-EMPTS ALL OTHER WORK" while ~14 of its 22 items verified CLOSED at planning runtime — receipts so nobody re-files: home CTA hover (`home.css.ts:53`) · outset status ring (`palette-conformance.test.ts:141`) · Breaks Done control (`BreaksPanel.tsx:527`, Location row ruled kept at `:473`) · event-timezone countdown (`root.tsx:125-133`) · embed accent end-to-end (`saved-embed.tsx:143` -> `shell.tsx:274-276`) · public CFP ErrorSummary (`submit-views.tsx:334,549`) · skeleton sr label (`PageSkeleton.tsx:96`) · new-contact modal copy (`NewContactModal.tsx:208`) · per-task COLUMN collapse (`OnboardingGrid.tsx:427`). All four re-filed review-lens items were closed too (task audience at `status.ts:317-326`, handout `taskAssignmentId` at `portal/tasks.tsx:632`, form-file chaining at `:423-443`, `MAX_SUBMISSION_FILE_SCAN` at `files-versions-read.ts:28`). RE-DERIVE A MANDATE BEFORE SPENDING A LANE ON IT.
- WAVE 82 IS A CODE WAVE (DEC-069 w82): main `3f46860e` ("merge-train w80: cross-org probe fixtures store a real plan scale"), NO merge train under me — loose ref `.git/refs/heads/main` and `.git/logs/HEAD` line 2743 agree. `task-w82-*` free in loose refs AND `.git/packed-refs`; NO decision carries `## Amendment (wave 82)`; both traps grepped before filing. All five w80 branches ARE merged. Every w81 branch owns its own worktree (`.git/worktrees/task-w81-*`), so the tree at repo root IS clean main — a planner read here is main, not main-plus-in-flight. w81 status: `-b` has a commit (`98bf7d66`); `-a`/`-c`/`-d` sit at `c01731eb` ("scribe wave 81") and `-e` at main — RUNNING with zero commits. OFF-LIMITS by branch: `-a` the evaluation score column + `routes/review/shared.ts`'s `as unknown as` · `-b` the hub's 820/732 content box (`public/home.css.ts`, `public-home-full-bleed.test.ts`) · `-c` `docs/eval-findings.md` · `-d` the saved-view + contact-segment validators · `-e` §09 settings footer grammar + saved-embed card anatomy.
- A DESIGN INTAKE'S NEW NAME IS NOT NEW WORK (DEC-018 w82): V12's "Scale or Choice" is ~80% built under another name — `domain/evaluation/criteria.ts` has shipped `rating|dropdown|text` since DEC-018/DEC-148, `criterionWeightShares` already excludes non-rating from the mean, the freeze rule already locks a scored round, and `aggregateDropdownCriterion` (DEC-241) already computes the exact distribution the ruling asks for. What is actually missing is four control-level deltas. READ THE DOMAIN BEFORE COSTING AN INTAKE, and map its vocabulary onto the stored one instead of migrating a column.
- A COMPUTED VALUE SHIPPED ON THE WIRE WITH NO READER IS THE SAME LIE AS AN UNREAD SETTING (DEC-241 w82): `perDropdown` is aggregated (`review/shared.ts:492`), placed on every results row (`:441`), and DECLARED in the SPA's own wire type (`review/types.ts:158`) — and `ResultsTable.tsx` never mentions it. The declared-knob-with-no-reader grep (DEC-851) has only ever been run over settings and query knobs; run it over RESPONSE FIELDS too.
- A FRAME CARRIES TWO KINDS OF NUMBER (DEC-650 w82): 02--10 draws the FieldModal at `width:560px` — a MEASURE, and the repo's 520 is wrong — and its counter at "3 of 40" — SAMPLE DATA, while `MAX_FIELD_OPTIONS` is 50, single-sourced and test-pinned. Geometry and copy bind; the counts and totals inside a drawing's placeholder content do not. Do not "fix" a cap to match a picture.
- A HAND-LISTED PARAMS TYPE IS THE WIRE VERSION OF A HAND-LISTED POPULATION (DEC-027 w82): `EmailLogExportParams` names five filters, `emailLogConditions` takes six, and the export file's own header swears list and export "always agree on which rows match" — `?templateId=` is silently dropped while every other unrecognised value on that route 400s. A `Pick<>` from the list's own params type could never have drifted. GREP A PARAMS INTERFACE AGAINST THE PREDICATE IT FEEDS, not against the route that fills it.
- A GUARD PLACED AFTER ITS SUBJECT IS NOT A GUARD (DEC-023 w82): `reminders.ts:807` computes with `eventTimezone` and `:809` throws if it is empty, so the named error never fires and an opaque `RangeError` gets swallowed into `failedEventIds`. Fail-loudly is about the MESSAGE reaching the operator, not about something throwing. Same wave, same shape one layer out: `email-binding.ts:204` labels a calendar `method=REQUEST` regardless of the `METHOD:` in its own bytes — correct today only because one caller exists.
- w1 COMPACTED: mandate rebase closed 4 of 11 "genuinely open" items on direct read (B12/B20/B17/P3#23, DEC-967/DEC-358) · judge misreading pipeline DECLINED card's two unlabelled paragraphs is a screen defect not a dup-render (DEC-898) · declared-with-no-reader grep (DEC-851) had only ever run over knobs, never RESPONSE fields, until `perDropdown` shipped unread.
- WAVE 2 IS A CODE WAVE (DEC-069 w2): main `5458bda3` ("merge task-w1-c"), NO merge train under me — loose ref `.git/refs/heads/main` and `.git/logs/HEAD` line 2756 agree. `task-w2-*` free in loose refs AND `.git/packed-refs` (packed-refs now carries NO task branches at all); no decision carries `## Amendment (wave 2)`; both traps grepped. Of wave 1 only `-c` landed (`735b9e08` — `src/routes/docs-content/**` + `test/docs-content-manifest.test.ts` are on main); `-a`/`-b`/`-d` sit at `0049139f` and `-e` at `1d1e148f`, RUNNING with zero commits. `task-w1-f/-g/-h` WERE PLANNED BUT NEVER DISPATCHED — no ref was ever created, so their three scopes (Scale-or-Choice arithmetic, operator docs, mandate rebase) are re-filed this wave. A PLANNED TASK WITH NO REF IS NOT IN FLIGHT; GREP THE REFS, NOT THE LAST WAVE SUMMARY. Nine branches from the previous numbering (`task-w81-e`, `task-w82-a..h`) still carry one commit each and are STILL not ancestors of main after a full wave. OFF-LIMITS by branch: w1-a seed comment/reviewer-scope distinctness · w1-b screenshot shoot script + manifest · w1-d response-field no-reader scan · w1-e pipeline DECLINED card · w81-e §09 footer + saved-embed card anatomy · w82-a FieldModal · -b email-log export params · -c send-path invariants · -d ResultsTable Choice distribution · -e Scorecard Choice radio row · -f PlanEditor Choice option rows · -g the `/docs` shell · -h room-ownership dedupe.
- A COUNTS MAP IS NOT AN ORDER (DEC-241 w2): the v12 ruling requires the Choice distribution "in declared option order", and `aggregateDropdownCriterion` answers a `Record<string, number>` — integer-like option labels ("5","3","1", anything containing "0") hoist to the front of a JS object, so every consumer that iterates `Object.keys(counts)` silently re-sorts the committee's list. The CSV escapes it only because `plans-progress.ts:208` happens to iterate `dc.options`. WHEN A RULING SAYS "IN DECLARED ORDER", THE DOMAIN MUST HAND OUT THE ORDER, NOT THE MAP.
- A RULING'S PROSE BOUND BINDS; A DRAWING'S PLACEHOLDER COUNT DOES NOT (DEC-422/DEC-650 w2): the Scale-or-Choice ruling states 2-6 options twice and makes it a control behaviour ("Remove disabled at two"), while `MAX_CRITERION_OPTIONS` is 20 and `shared.ts:136` refuses only the EMPTY array — a one-option Choice criterion is legal on the wire today. Contrast w82's FieldModal "3 of 40", which was sample data inside a picture and did not bind. ASK WHETHER THE NUMBER IS IN THE SENTENCE OR IN THE ARTWORK.
- A TYPE TOKEN DECLARED AND NEVER READ IS THE DECLARED-KNOB LIE ONE LAYER DOWN (DEC-851 w2): `--chq-type-page-title-{size,weight,tracking}` and `-phone-size` are declared at `app/src/styles.css:64-67` and referenced NOWHERE, while `.chq-page-title` hand-spells 36px/700/-0.04em at `:479-486` and its phone rule says **27px** at `:401-403` against the token's 25px — a live divergence hidden by the fact that nobody reads the token. `type-scale-conformance.test.ts:115-128` narrows its reverse assertion to five "roles this wave actually migrates" and excuses the rest as "legitimately unreferenced until a later wave" — the parking-ticket shape, FOURTH sighting after w76/w77. THE DECLARED-WITH-NO-READER GREP HAS NEVER BEEN RUN OVER CSS CUSTOM PROPERTIES.
- A MANDATE ROW THAT NAMES A NESTING IS CLOSED BY THE NESTING (DEC-785/DEC-967 w2): ALL-PASS A9 ("`?section=resources&edit=1` arrives read-only until Change is clicked") has been closed since wave 66 — `ResourcesPanel.tsx:23-26` says in its own header that the panel renders its full add/edit/delete surface INSIDE `PortalSettingsPanel`'s `edit=1` branch, so there is no second drill to arrive at. Three consecutive rebases carried it as "NOT RE-VERIFIED". READ THE COMPONENT'S HEADER BEFORE RE-FILING A DRILL-DEPTH COMPLAINT.
- WAVE 3 IS A CODE WAVE (DEC-069 w3): main `1cac107d` ("merge task-w1-d"), NO merge train under me — loose ref `.git/refs/heads/main` and `.git/logs/HEAD` line 2770 agree. `task-w3-*` free in loose refs AND `.git/packed-refs` (packed-refs carries no w1/w2/w3 branch at all); NO decision carries `## Amendment (wave 3)`; both traps grepped. ALL EIGHT wave-1 branches (`-a`..`-h`) ARE merged — `task-w1-f/-g/-h` WERE dispatched after the last planner read and landed, so the mandate rebase, the operator/deploy-parity docs and the Scale-or-Choice regression pin (`test/criterion-choice-aggregation.test.ts`) are on main. Wave 2 is fully dispatched a..g, each in its own worktree, so the tree at repo root IS clean main; only `-d` has a commit (`081c6bfa`, `src/routes/docs-content/groups.ts`). OFF-LIMITS by branch: w2-a/-b/-e docs articles + `docs-content/index.ts` · w2-c Scale-or-Choice SERVER contract (`domain/evaluation/criteria.ts`, `routes/review/shared.ts`, `plans-progress.ts`) · w2-d `docs-content/groups.ts` · w2-f operator docs / mandate · w2-g `app/src/styles.css` type tokens.
- A BRANCH FROM A PREVIOUS HARNESS RUN IS NOT IN FLIGHT (DEC-358 w3): nine branches (`task-w81-e`, `task-w82-a..h`) still carry one commit each in their own worktrees and are STILL absent from main three waves after the counter reset — the harness merge queue is per-process and was reset with the counter, so none will ever merge. Verified by CONTENT not by ref: `EmailLogExportParams` still lists five filters (`exports/email-log.ts:24-30`) · `ResultsTable.tsx` never mentions `perDropdown` · `Scorecard.tsx:673` still renders a `<select>` · `PlanEditor.tsx:1785` still joins options with `', '` · `forms.css:328` is still 520 and `FieldModal.tsx:143` still 'Edit field' · `src/routes/docs-site.tsx` does not exist (that branch also invented a SECOND content registry, `docs-site-content.ts`). Their scopes are UNOWNED — re-filed this wave. TEST AN OFF-LIMITS CLAIM AGAINST THE ARTEFACT, NOT AGAINST A BRANCH NAME.
- TWO BRANCHES IN ONE WAVE INVENTED TWO VOCABULARIES FOR ONE ID (DEC-518 w3): `src/routes/docs-content/**` declares 7 figure `shotId`s (`running-an-event-call-for-papers-and-submissions-02`) while `scripts/docs-shots-lib.ts` declares 11 shot ids (`running-an-event-cfp-and-submissions-02`) — ZERO overlap, both files green, because `docs-content-manifest.test.ts:94-108` checks only the registry's grammar and `docs-shots-manifest.test.ts:54` only its own captions. WHEN A WAVE SPLITS A CONTRACT ACROSS TWO BRANCHES, THE CROSS-CHECK IS ITS OWN TASK — and neither half's self-test is evidence about the other.
- AN EXEMPTION WHOSE REASON NAMES A SIBLING TASK EXPIRES WHEN THE SIBLING DOES (DEC-851 w3): `test/wire-field-reader.scan.test.ts:287-291` exempts `ResultsRow.perDropdown` because "a sibling task in this wave owns wiring ResultsTable.tsx's Choice-criterion distribution end to end" — that sibling is the stranded `task-w82-d`. The wave-76 rule bans a `reason:` matching a branch name; a reason naming an UNNAMED sibling task is the same parking ticket with the name filed off. Machine-check reasons for schedule-shaped words, not just for branch tokens.
- RE-DERIVED CLOSED w3, so nobody re-files: mandate A1b (track colour "free hex input") is CLOSED — `TracksRoomsPanel.tsx:52-56` declares `TRACK_SWATCHES`, `:465-497` and `:586-597` render palette-constrained radiogroups on both the edit rows and the new-track form, and `:439` already carries the ruling's own caption "The colour is how a track reads on the agenda and the public pages." No `<input type="color">` and no free hex field survives.
