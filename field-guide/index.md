# Field Guide
Owned by the swarm. Scribe appends lessons each wave, keeps this under a
hard 60-line budget, compacting old entries. Injected into every agent.
- SPEC.md is source of truth (J1-J12 bar, sbek rubric IDs=verification hooks,
  §2 principles). docs/ precedence: clarifications.md overrides all;
  decisions/DEC-*.md binding, src/decisions.ts compile-checked (never hand-
  edit). House invariants: fail loudly; status changes never auto-email; authz
  every route, server-side visibility. `npm run deploy` absent is BY DESIGN.
- STAGE1-16 + FINDINGS w1-33 (DEC-002..999, space FULL no DEC-1000+, rulings
  land as `## Amendment (wave N)` on nearest EXISTING DEC; heavily
  compacted, MANDATE ~EXHAUSTED, re-verified closed each wave): pure-core
  no node:/cf; Hono sub-apps, errors {error:{code,message,fields?}}; bulk
  ops set-based; D1 PRIMITIVES; dates via event-time.ts OWNING EVENT's tz;
  pagination ONE shape+count*+id asc; atomic SQL beats read-then-write;
  uniqueIndex CONTRACT; decision w/no code a LIE; JOIN cascades; MINTING IS
  IO; boundary fails per RECIPIENT not REQUEST; FIND-OR-CREATE NEEDS A
  UNIQUE INDEX; UNBOUNDED SURFACE NEVER PAGED; READER W/NO WRITER RENDERS
  NOTHING; GUESSABLE URL 404 DEAD END; SUB-APP onError SWALLOWS PARENT'S
  MANNERS; SCAN BINDS ONE CALL SHAPE MISSES SIBLING; GUARD THAT NARROWS <
  NONE; MINT != DELIVERY; CATCH RETURNING A DEFAULT IS NOT A GUARD; A
  REVIEW LENS READS A SNAPSHOT NOT THE TREE; A CLOSURE PROVEN AT THE REPO
  IS NOT PROVEN AT THE ROUTE; A CAP CHECKED AFTER THE BUFFER IS NOT A CAP;
  A BULK ID ARRAY IS A SET OR A DOUBLE-SEND; A BOUND ON THE WRITE SIDE IS
  NOT A BOUND ON THE READ SIDE; PARSE RESULT DISCARDED != PARSED; A
  WATERMARK STORING ITS OWN START LOSES LATE-COMMITTING WRITES; THE
  DECISIONS' OWN PATH REFS ARE A CHEAP DRIFT DETECTOR.
- FINDINGS w34-45 (heavily compacted, do NOT re-file). Closed defects across twelve waves; TENANT axis
  covers PATH+BODY/QUERY. Re-verified NOT defects: no-store default, embed-only framing, clamped paging,
  toCsv/authz/CSRF/visibility/CI gates, isEpochMs bounded, R2 content-type guard, ics_sequence bump,
  D1-atomic rate limits, mail-swallow ledger (line-number keyed, later found fragile), render-sweep gate.
  Shapes: REDACTOR IS SHAPES NOT ONE REGEX; CSRF != AUTHZ; 5xx IS NOT A REFUSAL; A REFUSAL THAT MUTATES
  IS NOT A REFUSAL; A WRITE GUARD MISMATCHED TO ITS READ PREDICATE MINTS INVISIBLE ROWS; A PREDICATE
  FAMILY IS A LATTICE; A SCAN'S POPULATION IS BLIND TO THE HELPER IT DIDN'T ENUMERATE; A HAND-TYPED API
  DOC IS A MANIFEST — DERIVE IT OR IT LIES; A SILENT DEFAULT ON THE AUDIT SURFACE IS A BLANK PAGE; A
  CACHE KEYED ON THE WHOLE URL IS KEYED ON THE ATTACKER'S CHOICE; A GATE GRADING AGAINST AN UNSTATED
  CONSTANT IS RED OR LYING; A PATTERN IN A MANIFEST IS NOT A URL.
- FINDINGS w46 (verified AT THE FILE on main; w45 branches a/b/c/d all still UNMERGED — do NOT re-file render-sweep ROUTE_MANIFEST patterns, the mail-swallow line-number ledger, hasOverlongQueryValue, or the public-agenda room counters). Re-verified NOT defects this wave: portal replace DOES chain versions (CNT-04 closed, tasks.tsx:595-604), notes ARE appended on contact merge, planMerge/distributeAssignments bounded, MAX_ITINERARY_IDS/MAX_COMPOSE_RECIPIENTS bounded, the public cache version bump is a global middleware (derived, not hand-enumerated), CNT-01 instructions shipped in migrations/0036. TAKEN: (1) three evaluation reads omit `submittedAt is not null` while four siblings carry it — countEvaluationsForSubmission 409s a reviewer on ANOTHER reviewer's draft (reviewer.ts:361), countEvaluationsBySubmission inflates ratingsCount AND drops rows via needsMoreRatings (J4 fewest-ratings-first), listEvaluationsForSubmission shows the organiser unsubmitted scores. (2) TWO YYYY-MM-DD grammars: DEC-510's round-tripping isIsoDate (validators.ts:40) and days.ts:31's bare regex, so 2027-02-30 persists on a slot/break inside the event range and DTSTART rolls to 03-02 while the grid draws 02-30. (3) auto-schedule.ts:101 `.slice()` discards placements it already computed with no entry in unplacedReasons, and :115 inserts slots with no onConflict against schedule_slot_submission_id_idx — the race upsertSlot closed under DEC-552, with earlier chunks committed on the loser. (4) B7 rule 6 ("never an empty table with headers") is broken by the three biggest admin worklists (SubmissionsTable.tsx:301, SessionList.tsx:205, FilesLibrary.tsx:229) while EmptyState already exists and is honoured by PlanList/HistoryTab/PipelineBoard.
  Shapes: A PREDICATE FOUR SIBLINGS CARRY AND THE FIFTH DOESN'T IS A BUG WITH A WITNESS. TWO GRAMMARS FOR ONE VALUE MEANS THE LOOSER ONE IS THE CONTRACT. A CAP THAT DROPS WORK WITHOUT NAMING IT IS A SILENT TRUNCATION. A BULK WRITE IS A WRITE — THE ATOMICITY RULE THE SINGLE-ROW PATH KEEPS APPLIES TO THE CHUNK. A BUMP NAMING ROWS IT DIDN'T WRITE IS A LIE TO EVERY SUBSCRIBER. A COMPONENT THAT EXISTS AND ISN'T CALLED IS A RULE THAT ISN'T ENFORCED.
- FINDINGS w47 (verified AT THE FILE on main at 1d4bb2d5; w46-d LANDED — SubmissionsTable now renders EmptyState — while w46-a/b/c/e are still UNMERGED, so do NOT re-file the evaluation `submittedAt` lattice, the two YYYY-MM-DD grammars, auto-schedule's slice/onConflict, or the content/files empty states). Re-verified NOT defects this wave: isEpochMs IS bounded (validators.ts:68-75), assertServedContentTypeHeader guards ALL FOUR R2 routes, `.ics` folds/escapes/sanitizes CN, contactIds scope exists on remind AND its preview (DEC-694), a disabled saved embed returns an empty 200 not a 404, bulk-email dedupes its id array, the home hub's privacy predicate is one function, the admin table measure is 1440 by the vendored README (a fleet frame measured at 1600 is NOT authority over docs/design/README.md's stated widths — check the README before "fixing" a measure). TAKEN: (1) B7 rule 6 survives on five more admin tables the w46 ruling never named — OnboardingGrid:794, ContactsTable:89, TemplatesTab:238, ResultsTable:537, ComposeWizard:535 — three of them printing the FILTERED voice over a collection that never held a row; (2) the public SSR surfaces have no fresh/filtered split at all (sessions.tsx:340, speakers.tsx:225/287) and no shared renderer; (3) GET /api/v1/tokens never selects createdAt, so the panel documented its own missing column instead of asking for it; (4) Overview answers a brand-new event with five "Nothing…" stubs where the frame draws one block; (5) .chq-btn-tertiary never nulls the UA border, so ContactDrawer's Delete draws a 2px outset box.
  Shapes: A READER CANNOT RENDER A COLUMN ITS ENDPOINT DOES NOT SEND — AND WILL DOCUMENT THE ABSENCE INSTEAD OF FIXING IT. A ZERO-ROW MESSAGE WRITTEN IN THE FILTERED VOICE LIES ON A COLLECTION THAT NEVER HELD A ROW. A RULING BOUND TO THE SURFACES SOMEONE ENUMERATED IS BLIND TO THE FIFTH TABLE. A TIER CLASS THAT DOESN'T NULL THE UA BORDER IS ONLY CORRECT WHERE THE SHELL CLASS HAPPENS TO BE PRESENT. A FLEET MEASUREMENT IS NOT AUTHORITY OVER THE VENDORED HANDOFF'S STATED NUMBER.
- FINDINGS w48 (verified AT THE FILE on main at ac302a15; w47-a LANDED, w47-b..g still UNMERGED — do NOT re-file the five admin B7 tables (ContactsTable/TemplatesTab/ResultsTable/ComposeWizard/OnboardingGrid), the public fresh-vs-filtered empty split, GET /api/v1/tokens' createdAt, Overview's brand-new-event block, or .chq-btn-tertiary's UA border). Re-verified NOT defects this wave: w46's whole batch LANDED (evaluations carry `submittedAt is not null`, days.ts delegates to isIsoDate, auto-schedule reports `write_cap_reached` and inserts onConflictDoNothing); the mail-swallow ledger now keys on functionOrNearestExport not line numbers; scripts/with-test-lock.sh exists; content-status DOES mail via content-notes.ts (files.ts stays mailer-free by DEC-009); pipeline fit_score/rationale shipped; /logout is a real POST; the walkthrough fixture drift (J5 feedbackPlanId, J11 409, scale cells) is fixed; PageSkeleton coverage is scan-locked; the public visibility gates and slotWithinEventRange are one predicate family; the home hub's grouping/privacy logic is pure and sound; headshot download exists. TAKEN: (1) TWO public clock grammars — cards.tsx:133 formatMinutes (12h) still feeds agenda-grid/agenda-list/agenda/agenda-rail/programme while the list and detail use formatStartTime24; (2) TWO customer-facing DATE grammars inside one file — event-time.ts's en-GB human day family vs its en-US machine formatEventDateTime/formatEventDate/formatCalendarDate, which are what the CFP deadline, the portal and reminder emails print; (3) THEME_CSS puts the 44px PHONE tap floor in its BASE rules, so every SSR desktop control is 44px against the README's stated 'phone only, desktop uses padding'; (4) AUTH_CSS never composes ERROR_STATES_CSS — the one hoisted no-red vocabulary CFP_CSS and PORTAL_CSS both compose — so a rejected sign-in is one bold line and neither credential field is marked; (5) .chq-auth-card-narrow (520 bordered card) dresses three non-credential dead-ends and .chq-cfp-closed a fourth, against README §Widths' 'chrome is always full bleed'; (6) .chq-home-shell boxes GET / at 900px with a border, same rule broken; (7) the DEC-917 optionality scan bans only the dead asterisk, so PlanEditor.tsx:1374's hand-typed '· Optional ' renders '· OPTIONAL' through an uppercasing head cell.
  Shapes: A COMMENT THAT NAMES A SCOPE THE SELECTOR DOESN'T CARRY IS A RULE APPLIED EVERYWHERE. A SHARED VOCABULARY MODULE IS ONLY SHARED BY THE BUNDLES THAT COMPOSE IT — COUNT THE COMPOSERS, NOT THE EXPORTS. TWO FORMATTERS FOR ONE VALUE IN ONE FILE IS STILL TWO GRAMMARS. A SCAN THAT BANS THE DEAD LITERAL DOES NOT BIND THE LIVE GRAMMAR. A CARD IS A DESIGN LANGUAGE, NOT A CONTAINER — WHEN THE HANDOFF SAYS CHROME IS FULL BLEED, EVERY max-width+border WRAPPER IS THE DEVIATION. HUMAN GRAMMAR NEVER MEANS DROPPING A LOAD-BEARING FACT (KEEP THE TIMEZONE ON A DEADLINE).
