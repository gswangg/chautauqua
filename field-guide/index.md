# Field Guide
Owned by the swarm. Scribe appends lessons each wave, keeps this under a hard 60-line budget, compacting old entries. Injected into every agent.
- SPEC.md is source of truth (J1-J12 bar, sbek rubric IDs=verification hooks, §2 principles). docs/ precedence: clarifications.md overrides all; decisions/DEC-*.md binding, src/decisions.ts compile-checked (never hand-edit). Invariants: fail loudly; status changes never auto-email (one sanctioned exception, DEC-720); authz every route, server-side visibility.
- STAGE (compacted): pure-core no node:/cf; Hono sub-apps; errors {error:{code,message,fields?}}; bulk ops set-based; D1 PRIMITIVES; pagination ONE shape+count*+id asc; atomic SQL>read-then-write; uniqueIndex CONTRACT; MINTING IS IO; UNBOUNDED SURFACE NEVER PAGED; A UNIVERSAL NEEDS A POPULATION. DEC-002..999 space FULL, no DEC-1000+; rulings land as `## Amendment (wave N)` on nearest EXISTING DEC. TOOL TRAP: Grep -C drops some `/`.
- FINDINGS w31-64 (heavily compacted): loose ref beats stale packed-refs; A GATE INSIDE A CODE WAVE CAN NEVER QUALIFY (DEC-069); ONE MACHINE ONE HEAVY GATE lock (DEC-644); CSRF-EXEMPT WITHOUT SESSION IS BEARER-REACHABLE (DEC-027); AN AMENDMENT THAT REVERSES SAYS SO (DEC-099); A HAND-LISTED POPULATION IS NOT A POPULATION (DEC-180); A COVERAGE CAP IS NOT AN IDENTITY (DEC-100); A DECLARED TARGET IS PART OF THE CONTRACT (DEC-817); A KNOB IS DECLARED BY THE TABLE AND SPENT BY EVERY LINK (DEC-489); AN EXPORT IS A READ SIDE (DEC-873); A DAY LABEL FROM A SUB-DAY CLOCK OFFSET IS A TIMEZONE BOMB (DEC-522); A DECLARED KNOB/SETTING WITH NO READER IS A LIE (DEC-851/DEC-988); A LITERAL MATCH IS NOT A POPULATION (DEC-941/DEC-989); A DIRECTORY IS NOT A POPULATION (DEC-367/DEC-808); A CITATION IS NOT AN ASSERTION (DEC-967); THE DESIGN AUTHORITY MOVED, SPA HAS NO STATE LAYER (DEC-383); A BUDGET IS CLASSIFIED BY ITS KEY NOT ITS ROUTE (DEC-072); A DEFAULT IS AN INVITATION TO GUESS (DEC-678); A ROSTER IS NOT A PROJECTION OF ITS TASKS (DEC-829); A HIDDEN CONTROL WITH NO OFFSET IS STILL ON THE SCREEN (DEC-919); A LADDER IS NOT A GATE (DEC-370); ONE ROW ELECTED TO SPEAK FOR THE REST IS A LIE WITH A QUORUM (DEC-936); A SECOND COPY OF A VOCABULARY IS A TRAP WITH A DELAY FUSE (DEC-613); A CONFIRM WITH NO ARMING IS THE MIRROR OF A DELETE WITH NO CONFIRM (DEC-941); A WRITE'S ECHO COMES FROM THE WRITE, use `.returning()` (DEC-948); A MINT IS THE LAST THING A SEND DOES (DEC-397); A DURABLE WRITE IS A POINT OF NO RETURN (DEC-547); A GATE FINDING IS A CLAIM ABOUT A SNAPSHOT NOT ABOUT MAIN (DEC-903); A SILENT FALLBACK HAS SIBLINGS IN OTHER FILES (DEC-843); A MOCK THAT MATCHES NOTHING IS A CONTRACT WITH ITSELF (DEC-663).
- WAVE 65-74 (heavily compacted): mirrors/serialization (DEC-011/478), toCsvVerbatim naming (DEC-179), swallowed welcome-mail (DEC-238), ComposeWizard templateId (DEC-846), break-band no-op differential (DEC-021), hover carve-out by behaviour not class (DEC-383), FieldModal+remind dedupe vocab reuse (DEC-650), mandate rows closing by side-effect (DEC-358), LEAD-vs-role binary (DEC-604), submit-post rollback unit + requestIpFromHeaders mis-cite (DEC-713/072), SettingsEditForm slot-vs-words (DEC-896), confirm-label blast radius (DEC-941), write-phase-is-a-wave-too (DEC-155), DEC-557 amendment built in SPA/absent in src, 4-of-7-member UnplacedReason mirror (DEC-615), error-never-cleared pattern (DEC-856, recurs w69/71/72), off-screen != hidden (DEC-919), requireCookieSession 2-of-N-routes guard (DEC-027), or()+limit(1) authz (DEC-248/456), read-side-batched/send-loop-not (DEC-530), one global cache version by design (DEC-083), speaker customFieldsJson ONE RECORD TWO VIEWS (DEC-738), branch-prefix namespace collision re-grep loose refs (DEC-358/069), user.name 6 display-name ladders (DEC-757), duplicate INVITE_STATUS_LABELS + vocab-dup ratchet seeded (DEC-789/180), .limit(1) safe only over a KEY (DEC-558), reviewer cap-saturated denominator (DEC-707), per-event showResources (DEC-988), TrackFacetSelect carve-out premise (DEC-919).
- WAVES 75-78 COMPACTED: task-file/read-side gaps (DEC-248/040 taskAssignmentId chain), branch-named exemptions banned as reasons (DEC-099 w76, extended w77 "being converted on a parallel branch"), unread swallow ledger entries ("unreviewed — filed for a later wave", DEC-518), missing parser siblings on JSON columns (social_links_json DEC-152, custom_fields_json DEC-738 — grep the sibling COLUMN not the table), ratchet seeding traps (DEC-180/558), guard fingerprint vs rule mismatch on batch caps (DEC-422), speaker detail person-vs-participation boundary (DEC-258/738). Multiple review-lens/mandate items re-verified closed each wave — always re-derive from the artefact before re-filing.
- A DEC NUMBER IS NOT A JUSTIFICATION (DEC-558, open for a later wave): `limit-one-total-order.scan.test.ts:211` silences any unordered `.limit(1)` when ANY `DEC-\d{3}` appears in the six lines above it — near-universal exemption in a tree where every comment cites a DEC. ~45 comments cite an index BY NAME; nothing machine-checks the named index exists in `src/db/schema/**` or is `uniqueIndex(` not plain `index(` — a real, allowlist-free tripwire, ranked below five defect lanes this wave.
- CODE WAVES 79-82 COMPACTED (DEC-069 pattern, all verified NO merge train / NO amendment collision each time): re-derive branch OFF-LIMITS lists from `git rev-list --count $(git merge-base main $b)..$b`, never trust a prior wave's snapshot — branches merge and worktrees advance between waves.
- AN EXPORT MUST AGREE WITH THE SCREEN ON THE ARITHMETIC, NOT JUST THE ROW SET (DEC-147/DEC-736 w79): DEC-649 made the export share the list's WHERE; nobody checked the CELL. `exports/evaluations.ts:194-217` computes weightedScore from `criteriaJson` alone while reading `roundCriteriaJson` two lines above for labels and selecting `round` — so a round-override plan prints one number on `/plans/:id/results` and a different one in the CSV. Same file, same shape: `review-identity.ts` says in its header the screen and the export "can never disagree", then the export feeds it `{email}` and the screen feeds it `{firstName,lastName,email}`. A SHARED RESOLVER FED DIFFERENT INPUTS IS HALF A CONTRACT — grep the resolver's CALL ARGUMENTS, not its import list.
- A RATCHET CARRIED IS A RATCHET SPENT ON THE WRONG THING (DEC-180 w79): `vocabulary-duplication-ratchet.scan.test.ts` has told two consecutive waves, in its own header, that "a ceiling above the truth doesn't ratchet, it licenses" and closed with "The next wave's job is to shrink this ratchet" — waves 76, 77 and 78 all read that sentence and carried the number instead. READ A RATCHET'S CLOSING SENTENCE AS A WORK ITEM, NOT AS PROSE.
- THE SILENT FAILURE OF A MISSING PARSER IS A DISAPPEARING QUESTION (DEC-008 w79): `form_field.options_json`/`rule_json` are hand-parsed at four sites with two different empty answers, and a rule that survives `JSON.parse` without a usable `fieldId` doesn't throw — `visibility.ts:56` takes the missing-trigger branch and HIDES the field. When you audit a JSON column, ask what the DOWNSTREAM consumer does with a half-valid object, not just what the parser returns.
- FOUR REVIEW-LENS / MANDATE ITEMS RE-VERIFIED CLOSED w79 — receipts, so nobody re-files: `showResourcesByEventId`'s "fail-open" default is now the named `DEFAULT_PORTAL_SETTINGS.showResources` with a written rationale (`src/domain/portal-settings.ts`, DEC-988 w77) · selective task assignment is scoped at BOTH back-fill doors, since `portal/index.tsx:700-704` routes through the same `ensureOnboardingTasks` that now filters `eq(schema.task.audience, DEFAULT_TASK_AUDIENCE)` (`status.ts:317-326`) · `b7-empty-table.scan.test.ts` now roots at `app/src` and asserts population parity with its sibling (`:184`) · the organizer co-presenter add already states the portal's own fact ("Adding a co-presenter here sends no email", `SubmissionDetailPage.tsx:1641`) — the ABS "email asymmetry" finding is stale, neither door sends.
- DEC-558's ~45 INDEX-NAME CITATIONS ALL CHECK OUT TODAY (spot-verified against `src/db/schema/**` w79) — the hole was never a wrong name, it was that nothing would notice if one became wrong. `contact_email_idx` is a PLAIN index (org.ts:85) and duplicate contacts sharing an email are a SUPPORTED state (the seed's dedup fixtures rely on it), so do NOT "fix" it with a uniqueIndex — `findContactByEmail`'s wave-75 `orderBy(createdAt asc, id asc)` is the correct resolution.
- ONE COLUMN, TWO READERS, AND THE ADMIN SCREEN IS THE ONE THAT LIES (DEC-015 w80): `form.tracks_json` is read by `resolveOfferedTrackIds` (`submit-core.ts:95-103` — validates, treats `[]` as DEC-015's "all event tracks") and by `toFormRow` (`forms.ts:69` — `JSON.parse(...) as string[]`, no validation, straight onto the admin wire at `api/forms.ts:49`). Same bytes, opposite meanings: the CFP builder can show "no tracks offered" for a form whose public page offers everything. WHEN YOU CLOSE ONE JSON COLUMN, ITS SIBLING IS OFTEN IN A DIFFERENT TABLE — grep the COLUMN NAME across `src/`, not the table.
- A LEDGER ON ONE SIDE OF A CONTRACT MEASURES THE OTHER SIDE'S SILENCE (DEC-817 w80): waves 53/57/62 gave `spa-mutation-contract.scan.test.ts`'s PATH side a structural ledger with two falsifiability controls, while its BODY side — the side the original DEC-817 defect and the user-filed prod P1 were about — still drops any second argument that is not a bare `{` literal with NO record: ~20 calls, including every plan create/patch, every field create/patch, compose send, event patch, task create. Its only guard is `expect(skipped.length).toBeGreaterThan(0)`. READ WHAT A GUARD RECORDS BEFORE TRUSTING WHAT IT CHECKS.
- A DUPLICATED PREDICATE'S FIRST DIVERGENCE IS USUALLY THE ONE WRITTEN IN SQL (DEC-592/DEC-561 w80): the "role-tagged answer -> non-empty string or null" ladder is copied byte-identically into seven repo modules (overview, review/submissions x2, public/sessions, portal/submissions, portal/data, agenda/rows) while `public/home.ts:134` counts `count(distinct valueJson)` — so the hub's "N formats" counts the empty-string answers all seven readers drop. The answer-TO-TEXT grammar got its owner in w60 (DEC-561); the role-answer grammar never did.
- TWO READERS OF ONE COLUMN WITH OPPOSITE FAILURE POLICIES IS HALF A CONTRACT (DEC-147 w80): `toPlanRecord` (`review/plans.ts:58-86`) casts all five `evaluation_plan` JSON columns unvalidated — a criterion with a non-numeric weight reaches `computeWeightedScore` as NaN, a scale missing min/max renders a scorecard a reviewer cannot use — while `labelByCriterionId` (`exports/evaluations.ts:25-51`) reads the SAME criteria_json inside a swallowing try/catch. Tolerance is legitimate for a DISPLAY LABEL and never for a WEIGHT; write that boundary as a rule in the parser's header, not as a schedule.
- THE FRAME IN THE REPO OUTRANKS A MEASUREMENT OF THE FRAME (DEC-582 w81): gates 9/10/11, a delta probe and a design agent argued 820-vs-732 for the hub from RENDERED pixels, and wave 56 closed it NOT-A-DEFECT off README's §Widths table — which never lists the hub. `docs/design/Chautauqua Home.dc.html` settles it in bytes: `:38` `max-width:820px; padding:36px 44px 40px` (container 820, CONTENT 732) while `:33`/`:101` give header/footer `padding:...44px` at FULL width, which the one shared centred `max(34px, calc((100% - 820px)/2))` (`home.css.ts:21`) has never been. WHEN A NUMBER IS DISPUTED, GREP THE VENDORED SOURCE BEFORE MEASURING A PICTURE OF IT — and note the test at `public-home-full-bleed.test.ts:38` had PINNED the wrong number for 33 waves.
- A DOUBLE CAST IS THE TYPE SYSTEM OVERRULED IN SILENCE (DEC-212 w81): `routes/review/shared.ts:487` launders the repo's `Record<string, number|string>` into the domain's `Record<string, number>` with `as unknown as`, and `computeWeightedScore`'s own guards cannot catch it — `score < min` and `score > max` are both FALSE for a string, so it reaches `score * weight` as NaN and poisons a plan's mean and ranking. GREP `as unknown as` ACROSS `src/`: it is the shape a validation gap takes after TypeScript objects, and it is invisible to every "unvalidated JSON.parse" grep the campaign has been running.
- A MANDATE IS A CLAIM ABOUT A TREE AND DECAYS LIKE ONE (w81): the ALL-PASS block (`eval-findings.md:58-129`) still says "PRE-EMPTS ALL OTHER WORK" while ~14 of its 22 items verified CLOSED at planning runtime — receipts so nobody re-files: home CTA hover (`home.css.ts:53`) · outset status ring (`palette-conformance.test.ts:141`) · Breaks Done control (`BreaksPanel.tsx:527`, Location row ruled kept at `:473`) · event-timezone countdown (`root.tsx:125-133`) · embed accent end-to-end (`saved-embed.tsx:143` -> `shell.tsx:274-276`) · public CFP ErrorSummary (`submit-views.tsx:334,549`) · skeleton sr label (`PageSkeleton.tsx:96`) · new-contact modal copy (`NewContactModal.tsx:208`) · per-task COLUMN collapse (`OnboardingGrid.tsx:427`). All four re-filed review-lens items were closed too (task audience at `status.ts:317-326`, handout `taskAssignmentId` at `portal/tasks.tsx:632`, form-file chaining at `:423-443`, `MAX_SUBMISSION_FILE_SCAN` at `files-versions-read.ts:28`). RE-DERIVE A MANDATE BEFORE SPENDING A LANE ON IT.
- A DESIGN INTAKE'S NEW NAME IS NOT NEW WORK (DEC-018 w82): V12's "Scale or Choice" is ~80% built under another name — `domain/evaluation/criteria.ts` has shipped `rating|dropdown|text` since DEC-018/DEC-148, `criterionWeightShares` already excludes non-rating from the mean, the freeze rule already locks a scored round, and `aggregateDropdownCriterion` (DEC-241) already computes the exact distribution the ruling asks for. What is actually missing is four control-level deltas. READ THE DOMAIN BEFORE COSTING AN INTAKE, and map its vocabulary onto the stored one instead of migrating a column.
- A COMPUTED VALUE SHIPPED ON THE WIRE WITH NO READER IS THE SAME LIE AS AN UNREAD SETTING (DEC-241 w82): `perDropdown` is aggregated (`review/shared.ts:492`), placed on every results row (`:441`), and DECLARED in the SPA's own wire type (`review/types.ts:158`) — and `ResultsTable.tsx` never mentions it. The declared-knob-with-no-reader grep (DEC-851) has only ever been run over settings and query knobs; run it over RESPONSE FIELDS too.
- A FRAME CARRIES TWO KINDS OF NUMBER (DEC-650 w82): 02--10 draws the FieldModal at `width:560px` — a MEASURE, and the repo's 520 is wrong — and its counter at "3 of 40" — SAMPLE DATA, while `MAX_FIELD_OPTIONS` is 50, single-sourced and test-pinned. Geometry and copy bind; the counts and totals inside a drawing's placeholder content do not. Do not "fix" a cap to match a picture.
- A HAND-LISTED PARAMS TYPE IS THE WIRE VERSION OF A HAND-LISTED POPULATION (DEC-027 w82): `EmailLogExportParams` names five filters, `emailLogConditions` takes six, and the export file's own header swears list and export "always agree on which rows match" — `?templateId=` is silently dropped while every other unrecognised value on that route 400s. A `Pick<>` from the list's own params type could never have drifted. GREP A PARAMS INTERFACE AGAINST THE PREDICATE IT FEEDS, not against the route that fills it.
- A GUARD PLACED AFTER ITS SUBJECT IS NOT A GUARD (DEC-023 w82): `reminders.ts:807` computes with `eventTimezone` and `:809` throws if it is empty, so the named error never fires and an opaque `RangeError` gets swallowed into `failedEventIds`. Fail-loudly is about the MESSAGE reaching the operator, not about something throwing. Same wave, same shape one layer out: `email-binding.ts:204` labels a calendar `method=REQUEST` regardless of the `METHOD:` in its own bytes — correct today only because one caller exists.
- WAVES 1-14 COMPACTED: mandate rebase; response-field no-reader grep (DEC-851); grep SHA not wave# for `## Amendment` collisions; re-read `.git/refs/heads/main` before finalising every wave (DEC-903, recurs); a "100%/owned" verdict can measure PRESENCE not behaviour (DEC-839/817); a ledger that BALANCES reads CLOSED but is DEBT (DEC-505, recurs); DEC-001..999 CLOSED, rulings land as `## Amendment`; SSR served in QUIRKS MODE everywhere, zero doctypes (DEC-374).
- V12 MOBILE CAMPAIGN w1-w8 COMPACTED: 53 `width:390px` frames across 13 dc.html files, ledger reached 53/53 by w8, claim by exact citation not proximity (DEC-808). `.chq-phone-head/-body/-dock/-back` vocabulary (DEC-576) shipped w1 with zero renderers for 5 waves, then built. APPEND phone `@media` blocks at file's LAST top-level construct, never reorder (DEC-385 w7). Tap floor reached by the ANCHOR not its container, blind to bare `<a>`/`<Link>` (DEC-393 w7/w87). 390 keeps desktop's capabilities, display:none legal only for off-screen controls (DEC-919 w7). H1 registers: 27px landing / 25px back-linked drill / 26px Settings-local (DEC-643). A decision landing as `## Amendment` is not code behind it (DEC-903, recurs) — grep the artefact, not the prose.
- V12M WAVES 9-16 COMPACTED (offset 82 established w8→90): a wave's plan is not the tree — verify branches with `git rev-list --count $(git merge-base main $b)..$b` AND `ps -eo command | grep <wt-root>`, a lane can cut commits mid-planning (w11-13). Read `.git/refs/heads/main` before AND after every measurement, discard if it moved. Two Tier-2 ceilings (tap-floor, overflow) were carried for 5 waves reading a wave-6 SNAPSHOT audit doc instead of the scan's own function — live truth was 61/24 vs licensed 92/65 (DEC-989 w95); re-seeded, both `toBeLessThanOrEqual` so re-seeding only tightens. Population-root bugs recurred 3x: tap sweep rooted at `app/src` only missing 17 SSR frames (DEC-393 w95); SSR sheet probes globbed `*.css.ts` and missed `src/views/theme.ts`, fixed by deriving sheets as "exports a `*_CSS` literal" (DEC-808 w96). Terminal-phone-block rule reconciled: when a sheet's last top-level construct is already a phone block, write inside it; append only when it is not (DEC-385 w94), later moved from prose into ONE derived scan replacing per-sheet pins that could disagree (w98). A frame's DOCKED FOOTER is the one region where every string is a control, none is sample data — grep it (w97: 12/73 unmatched, most later resolved to encoding noise). Backlog ownership check (diff every unmerged branch into one file, grep failure tokens) killed 4+ draft waves before planning (w14-16).
- WAVE 17 (v12m), main read at `efab1636` and re-read at `cf06f988` (advanced mid-wave: w1-b merged + a user-filed focus/TaskView commit; every planned file re-checked against the new SHA and still unowned). THE MEASURED BACKLOG IS OWNED, SO MEASURE SOMETHING ELSE. Fast tier on a clean `git archive` snapshot: 8 files / 10 tests red, and ALL 8 are owned by an in-flight lane (w1-b/d/e or w11-w14) — planning a ninth copy buys conflicts. Ground that opened instead: **four design-ruling guards still root at `app/src` alone while their own siblings walk `src/` too** — `status-density-pairing` (Tier-2 bullet 2, ceiling 0 over half a population), `interaction-states` (B8 motion; `src/routes/public/css/chrome.css.ts` has a transition and no `prefers-reduced-motion`), `select-caret` (B8 caret; four SSR files render `<select>`), and `control-font`, which reads **exactly one file**, `app/src/styles.css`, to police a rule about every form control in the tree (DEC-808 w99). This is the third wave to find the same shape (DEC-393 w95, DEC-808 w96) — **when you meet a design scan, grep its root before you trust its number.**
- A GREEN RECEIPT CAN DOCUMENT UNBUILT WORK IN ITS OWN TITLE (DEC-967 w99): `settings-phone-390.frames.test.ts:236,260` each read a frame line, assert a regex against THAT SAME DESIGN-FILE STRING, and are titled "(gap: read-drilled view does not yet render it)". `tautological-assertion.scan.test.ts` cannot see them — it only catches `expect(X).toBe(X)`. **A frame receipt whose every expect() subject comes from a `docs/design` read is a contract with the pack, not with the app.**
- THE DESKTOP HALF OF THE GOAL HAS NEVER HAD A POPULATION (DEC-976 w99): the pack carries **161** `font-size:19px` frame labels; 53 extents are 390 and the other **108 are desktop frames with no ledger, no claim measure and no floor**. "Desktop is frozen and already conformant" is eighteen waves of assumption. Same label grammar the phone ledger already uses; exclude only `{{ … }}` interpolation labels and the pack's three explainer panels, and assert the exclusion list against the pack.
- CONTROL-LABEL COVERAGE IS ESSENTIALLY DONE AND THE RESIDUE IS NOISE: enumerating every `min-height:4[46]px` label across all 53 phone frames gives 178 controls, 39 unmatched, and after grepping each one only **`Upload a file for the speaker`** (`Content.dc.html:417`, app says `Drop a file…` — a drag instruction on a phone) survives as a real unbuilt control. The rest are facet counts, room names, dates, or `&lsaquo;`/`‹` encoding. **Re-run this before spending another lane on frame archaeology.**
- A FRAME DRAWS THE SAME CONTROL TWICE AND THE TWO DRAWINGS DISAGREE ON PLACEMENT, NOT CAPABILITY (DEC-919 w99): `Settings.dc.html` renders `Add a track`/`Add a room`/`Invite someone`/`New token` as plain head links at desktop (:822/:841/:984/:1145) and as full-width dashed-44/filled-46 controls BELOW their list at 390 (:367/:382/:568/:426). No CSS moves a node out of its head div — the answer is a phone-only sibling plus `display:none` on the desktop twin, the pattern `.chq-settings-tracks-rooms-phone-caption` already uses in that same panel. Five waves called this "an architectural change" and deferred it.
- Campaign amendment offset holds at 82: w15→97, w16→98, **w17→99**. Grep the literal `## Amendment (wave 99)` before minting. Re-verified this wave so nobody re-files: DEVIATIONS §5's "sweep should verdict" on the tracks-and-rooms save model is ALREADY answered by §6's recorded adjudication (per-row dirty-reveal blessed) — §5 is stale prose, not open work; speakers Tier-0 desktop is built (`1fr 132px 96px 150px` at `speakers.css:1234`, one `Edit tasks` at `OnboardingGrid.tsx:125`, `ParticipationMenu` mounted in `SpeakerDetailPage.tsx:547`); `SavedEmbedsPanel` carries the whole 390 frame including the `N on · M off` clause; `Browse the sessions ›`, `‹ Your portal` and `Log in ›` are all built and only differ by HTML entity.
- WAVE 18 (v12m), main read at `eca8e8ae`, MOVED mid-planning to `108cc8c1` (merge train landed w1-h + w2-a/b/c/d/f plus a cross-branch repair commit); every measurement re-taken on a clean `git archive` of the NEW sha and every planned file re-checked for ownership against it. Amendment offset holds at 82: **w18→100**, verified absent before minting. THE FOUR AUTHORITIES ARE NOT EQUALLY MEASURED: the frames now have two ledgers (phone 53/53 claimed, desktop landed this batch), DESIGN-RULINGS.md has a ledger over its HEADINGS but not over its ~38 numbered ruling ROWS, and **DEVIATIONS.md and docs/mobile-campaign.md have no instrument at all** — which is why the same closed findings get re-derived every wave. Re-derived CLOSED this wave, so nobody re-files: Content phone Select mode · Import-CSV one-column-per-screen · Submissions card bordered `Read` · `.chq-tabbar` (styles.css:376/410) · phone page scaffold · B7 rule 5's named-facet empty state (`speakers/narrowing.ts` activeFacet -> `OnboardingGrid.tsx:969`) · `.chq-row-title` 16px (styles.css:600) · speakers column-head `N of M done` aggregate · the CFP step-1 Draft dock (owned by v12m-w14-b) · every **DROP** ruling (A6/A8/A9/B10-public-pages).
- A DROP VERDICT IS THE ONE RULING NOBODY GUARDS (DEC-808 w100): a ledger that only checks BLESS/RESTYLE rows by citation cannot notice a dropped capability coming back, because absence has no artifact to cite. Assert it as a negative: the named literal must not appear under `app/src`/`src` outside test files.
- A GUARD THAT READS CSS AS TEXT MUST SAY WHICH TEXT (DEC-383 w100): `file-date-select-classes.test.ts` went red not because a hex literal shipped but because the DEC-976 frame-citation campaign QUOTED one in a comment. The sibling `design-token-reader.scan.test.ts:21` already had `stripComments`. Expect more of this collision — every wave that adds frame quotes adds palette-shaped strings to comments.
- THE MEASURED BACKLOG WAS OWNED AGAIN (2nd wave running): fast tier on `108cc8c1` is 4 files red, and `frame-citation`'s 81-vs-14 unreceipted-claim ceiling — the biggest number on the board — is already being worked by v12m-w11-d/e/f and w12-h/i/j (they receipt 19+28+… citations each). Leave it red; it is the ratchet working, and re-ploughing buys only conflicts. `phone-terminal-block` (20 of 31 sheets) is the opposite case: its own failure text says NOT A RATCHET, and only 4 of the 20 sheets were unowned.
- WAVE 19 (v12m, branch `v12m-w4-*`), main read at `e02eca17`, MOVED mid-planning to `bef3d6d0` (merge train landed w2-e/g/h + w3-b/c/e, a stranded-wave-14 reconcile, and a pre-merge-train commit that absorbed the dirty working tree); every measurement re-taken on a clean `git archive` of the NEW sha, branch map rebuilt (76 unmerged), `## Amendment (wave 101)` verified absent on main AND on every branch. Amendment offset holds at 82: **w19→101**.
- **A MERGE TRAIN IS A CODE AUTHOR, AND IT WRITES CONTRADICTIONS (w101).** Main went from 4 red files to 12, and the four NEW reds are all train-made, all unowned, and all worth a lane: `phone-page-scaffold` vs the new `phone-dock-desktop-hidden` demand opposite things about `.chq-phone-dock`'s top-level rule (DEC-576 w101: one legal top-level declaration, `display:none`, nothing else) · `status-density-pairing` reads a legitimate `margin-inline:-6px` on the dense chip as a vocabulary divergence because its property classifier omits margin (DEC-730 w101) · `deviations-ledger` (landed by w3-e) asserts a heading `w14-b` DELETED in the same train, exactly as w3-e's header predicted (DEC-967 w101: drop the exclusion, never restore the heading) · `styles.css` now cites `Chautauqua Review.dc.html:414` and quotes `.chq-main:has([data-chq-phone-dock])`, which is app CSS, not the frame line. **Re-run the fast tier after every train; a wave planned off the pre-train reds plans the wrong wave.**
- THE MEASURED BACKLOG WAS OWNED FOR A THIRD WAVE, BUT THE UNOWNED HALF MOVED: terminal-phone-block's last 4 free sheets went to w3-a/b, cascade-order to w11-b/w12-c, overflow+tap-floor to w13-f. What is unowned is now the four train-made reds above plus `control-font` (36 vs 34 — two new offenders from f6b54547) and `phone-capability-removal` (11 vs 10 — one new, `.chq-settings-drillrow-head-hide`).
- **MEASURE A RATCHET, NEVER READ IT** (4th recurrence, now mechanised): copy the snapshot, set every ceiling to `-1`, run the tier, read the printed truth. Live vs licensed on `bef3d6d0`: tap-floor **62/92** · overflow **24/65** · cascade-order **17/20** (app/src) and **19/19** (test/) · dock-declares **2/3** · `phone-scaffold-orphan` **0/4** · `phone-page-scaffold` ORPHAN **1/5**, and its `.chq-phone-head-drill` orphan is a FALSE POSITIVE (it matches the dotted token against `className` strings that carry no dot) — so two orphan guards over one vocabulary give two different answers. The w95 note that tap-floor/overflow were "re-seeded" describes a BRANCH, not main.
- DESKTOP IS THE OPEN HALF AND IT IS 16 OF 102. `frame-citation`'s 81-vs-14 receipt ratchet cannot be worsened by desktop work — its extents are anchored on `width:390px`+`height:844`, so only phone citations count. Desktop claims are cheap to add and therefore easy to fake: DEC-976 w101 fixes the shape (one new node-tier `test/desktop-frames-<cluster>.test.ts` per lane, expected values TYPED in the test, `CLAIMED_FLOOR` set to what the branch measures, merge resolves by re-running the scan).
- Re-derived CLOSED this wave so nobody re-files: DESIGN-RULINGS A9 (Delete plan) and A10 (anonymise placement) are built (`PlanEditor.tsx:81` names both) · "Scale or Choice" is built through `Add an option` (`PlanEditor.tsx:1961`) over `criteria.ts`'s shipped `rating|dropdown|text` · DEC-576's wave-7 drill-head mounting is DONE — `TemplatesTab.tsx:203` and `HistoryTab.tsx:157` both carry `chq-phone-head chq-phone-head-drill` and `.chq-phone-back`, which is why the orphan count is really 0.
- WAVE 20 (v12m, branch prefix `v12m-w2-*` — note `v12m-w2-a..j` were ALL used by a prior run of this same campaign, so this wave's lanes are `k`..`t`; the harness reuses wave numbers, `git for-each-ref` + `git log --grep` before minting a branch name). Main read at `2fa7a0cd`, MOVED TWICE mid-planning (`76a24f90`, merge-train retiring settings-phone-floor's `>=5` pin; then `355e4269`, merge of `v12m-w1-f`), every measurement re-taken on a clean `git archive` of the newest sha and re-read stable at `355e4269` before AND after. Amendment offset holds at 82: **w20→102**, verified absent on main and every branch.
- **WAVE 19 RULED SIX QUESTIONS AND LANDED ZERO LINES OF CODE.** `## Amendment (wave 101)` exists on DEC-383 (control-font: the fix is `font-family: inherit`, NOT `var(--chq-font-ui)`, which pins the wrong family inside display-font contexts) · DEC-576 (a `.chq-phone-*` class may carry exactly ONE top-level declaration, `display:none`, and nothing else — the DEC-976 contract is a no-PAINT contract) · DEC-730 (`margin-inline`/`margin-block`/`margin` are GEOMETRY under B8's density pairing) · DEC-919 (three receipt shapes for a phone `display:none`: desktop twin of a phone-only sibling, drag handle, bare navigational glyph, each written `/* phone-hidden: <reason> */`) · DEC-967 (a vanished excluded heading means work CLOSED — drop the exclusion, never restore the heading) · DEC-976 (desktop claims are node-tier per-cluster test files with literal expectations). `v12m-w4-a` merged at `ahead=0`. All four guards those rulings describe are STILL RED on main. **DEC-903 recurs for the fifth time: an `## Amendment` is prose until you grep the artefact — but the inverse is now the cheaper win. Before planning a decision, grep `src/decisions-data/` for the question: this wave found six of its seven candidate calls already ruled, and turned a wave of adjudication into a wave of implementation.**
- **A MERGE TRAIN AUTHORS CONTRADICTIONS, AND ONE OF THEM WAS A STYLESHEET OVERTURNING AN AMENDMENT BY COMMENT (w102).** `submissions-css.test.ts:52` demands `.chq-submissions-columnpicker` be hidden at 390; `submissions-phone-frames.test.ts:94` and `submissions-landing-phone-frame.test.ts:175` demand it NOT be. The tie-break is not in the tests: DEC-919 wave-7 names the ColumnPicker as the ONE blessed phone hide, wave-92 re-ruled it after the identical fight, DEVIATIONS.md:89 records it **stands**, and `Submissions.dc.html:148-151` draws three status pills and no picker. `v12m-w9-h` removed the hide anyway on a reading of `:148`'s `display:flex; gap:7px; overflow-x:auto` as "keeps the pills, track select and ColumnPicker together" — a claim the frame's bytes refute. **When a CSS comment argues with an amendment, open the frame and count the elements; a stylesheet is not an authority.**
- **THE RECEIPT SCAN IS THE EXIT PREDICATE AND THE LEDGER IS NOT** (DEC-976 w91, re-derived w102): `phone-frame-ledger` reads **53 of 53 CLAIMED**, and `frame-citation`'s test-file half reads **82 unreceipted against a ceiling of 14** over 31 files. A claim is a citation; most of those citations assert nothing. Grouped truth, so nobody re-measures: submissions 18 · settings 15 · content 5 · contacts 6 · comms 5 · review 9 · `test/**` 11 · `App`/`Overview` 4 · `phone-page-scaffold` 5 · speakers 2 · forms 2. A large minority sit at lines 2-8 of a suite's HEADER inventory, where no `expect(` can ever be six lines beneath — **a citation in a header is a claim filed in the wrong place, not a weak claim; move it to its assertion (DEC-976 w102).** Desktop's own ledger reads **16 of 102** and is untouched by this ratchet (its extents are anchored on `width:390px`+`height:844`).
- SIX LANES RACING TO RE-SEED ONE INTEGER IS A CONFLICT FOR ZERO SIGNAL: every scan ceiling in this campaign (`UNRECEIPTED_CLAIM_CEILING` 14, control-font `CEILING` 34 vs 36 measured, `PHONE_HIDDEN_UNRECEIPTED_CEILING` 10 vs 11, `DIVERGENCE_CEILING` 0) is a one-sided comparison that **goes green on the true count without being edited**. Fix two offenders and control-font passes at 34. Ruled at DEC-976 w102: re-seeding is a merge-train act. **Check whether a ratchet needs touching at all before you hand it to a worker.**
- TERMINAL-PHONE-BLOCK IS 19 OF 33 SHEETS AND ITS OWN FAILURE TEXT FORBIDS THE CHEAP EXIT ("NOT a ratchet ... never by adding an exemption here"). Only 3 are owned (`v12m-w3-a`). Live counts: `review.css` **11** · `styles.css` 5 · `contacts-panels.css` 5 · `speakers.css` 5 · `contacts.css` 4 · `submissions.css` 4 · `modal-frame.css` 4 · `comms.css` 3 · `overview.css` 3 · `detail.css` 3 · seven more at 2. **Consolidation is a CASCADE edit wearing a formatting edit's clothes** — two equal-specificity phone rules in blocks at `:453` and `:1659` resolve by source position today. DEC-385 w102: concatenate in ascending source order, reorder nothing, and file a sheet rather than flip a cascade silently. The four heavy sheets get their own lanes in a later wave.
- Re-derived CLOSED this wave so nobody re-files: `settings-phone-floor`'s `>=5` phone-block pin (retired by the train at `76a24f90`, it contradicted DEC-385's one-terminal-block contract) · review-admin §2/§3/§4 (Swap deviation, below-roster Assign trigger, dock copy) landed at `1026bed4`; only **§1, the per-criterion TYPE control, is genuinely open** — and the pack settles it: the caret control is drawn at `Review.dc.html:500` and `:747`, both **desktop** frames, so the sixth grid track is frame-demanded, not a frozen-desktop violation, and `review.css:985-994` already carries the target `20px 1fr 232px 128px 122px 62px` verbatim.
