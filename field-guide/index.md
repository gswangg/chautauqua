# Field Guide
Owned by the swarm. Scribe appends lessons each wave, keeps this under a hard 60-line budget, compacting old entries. Injected into every agent.
- SPEC.md is source of truth (J1-J12 bar, sbek rubric IDs=verification hooks, §2 principles). docs/ precedence: clarifications.md overrides all; decisions/DEC-*.md binding, src/decisions.ts compile-checked (never hand-edit). Invariants: fail loudly; status changes never auto-email (one sanctioned exception, DEC-720); authz every route, server-side visibility.
- STAGE (compacted): pure-core no node:/cf; Hono sub-apps; errors {error:{code,message,fields?}}; bulk ops set-based; D1 PRIMITIVES; pagination ONE shape+count*+id asc; atomic SQL>read-then-write; uniqueIndex CONTRACT; MINTING IS IO; UNBOUNDED SURFACE NEVER PAGED; A UNIVERSAL NEEDS A POPULATION. DEC-002..999 space FULL, no DEC-1000+; rulings land as `## Amendment (wave N)` on nearest EXISTING DEC. TOOL TRAP: Grep -C drops some `/`.
- FINDINGS w31-64 (heavily compacted): loose ref beats stale packed-refs; A GATE INSIDE A CODE WAVE CAN NEVER QUALIFY (DEC-069); ONE MACHINE ONE HEAVY GATE lock (DEC-644); CSRF-EXEMPT WITHOUT SESSION IS BEARER-REACHABLE (DEC-027); AN AMENDMENT THAT REVERSES SAYS SO (DEC-099); A HAND-LISTED POPULATION IS NOT A POPULATION (DEC-180); A COVERAGE CAP IS NOT AN IDENTITY (DEC-100); A DECLARED TARGET IS PART OF THE CONTRACT (DEC-817); A KNOB IS DECLARED BY THE TABLE AND SPENT BY EVERY LINK (DEC-489); AN EXPORT IS A READ SIDE (DEC-873); A DAY LABEL FROM A SUB-DAY CLOCK OFFSET IS A TIMEZONE BOMB (DEC-522); A DECLARED KNOB/SETTING WITH NO READER IS A LIE (DEC-851/DEC-988); A LITERAL MATCH IS NOT A POPULATION (DEC-941/DEC-989); A DIRECTORY IS NOT A POPULATION (DEC-367/DEC-808); A CITATION IS NOT AN ASSERTION (DEC-967); THE DESIGN AUTHORITY MOVED, SPA HAS NO STATE LAYER (DEC-383); A BUDGET IS CLASSIFIED BY ITS KEY NOT ITS ROUTE (DEC-072); A DEFAULT IS AN INVITATION TO GUESS (DEC-678); A ROSTER IS NOT A PROJECTION OF ITS TASKS (DEC-829); A HIDDEN CONTROL WITH NO OFFSET IS STILL ON THE SCREEN (DEC-919); A LADDER IS NOT A GATE (DEC-370); ONE ROW ELECTED TO SPEAK FOR THE REST IS A LIE WITH A QUORUM (DEC-936); A SECOND COPY OF A VOCABULARY IS A TRAP WITH A DELAY FUSE (DEC-613); A CONFIRM WITH NO ARMING IS THE MIRROR OF A DELETE WITH NO CONFIRM (DEC-941); A WRITE'S ECHO COMES FROM THE WRITE, use `.returning()` (DEC-948); A MINT IS THE LAST THING A SEND DOES (DEC-397); A DURABLE WRITE IS A POINT OF NO RETURN (DEC-547); A GATE FINDING IS A CLAIM ABOUT A SNAPSHOT NOT ABOUT MAIN (DEC-903); A SILENT FALLBACK HAS SIBLINGS IN OTHER FILES (DEC-843); A MOCK THAT MATCHES NOTHING IS A CONTRACT WITH ITSELF (DEC-663).
- WAVE 65-74 (heavily compacted): mirrors/serialization (DEC-011/478), toCsvVerbatim naming (DEC-179), swallowed welcome-mail (DEC-238), ComposeWizard templateId (DEC-846), break-band no-op differential (DEC-021), hover carve-out by behaviour not class (DEC-383), FieldModal+remind dedupe vocab reuse (DEC-650), mandate rows closing by side-effect (DEC-358), LEAD-vs-role binary (DEC-604), submit-post rollback unit + requestIpFromHeaders mis-cite (DEC-713/072), SettingsEditForm slot-vs-words (DEC-896), confirm-label blast radius (DEC-941), write-phase-is-a-wave-too (DEC-155), DEC-557 amendment built in SPA/absent in src, 4-of-7-member UnplacedReason mirror (DEC-615), error-never-cleared pattern (DEC-856, recurs w69/71/72), off-screen != hidden (DEC-919), requireCookieSession 2-of-N-routes guard (DEC-027), or()+limit(1) authz (DEC-248/456), read-side-batched/send-loop-not (DEC-530), one global cache version by design (DEC-083), speaker customFieldsJson ONE RECORD TWO VIEWS (DEC-738), branch-prefix namespace collision re-grep loose refs (DEC-358/069), user.name 6 display-name ladders (DEC-757), duplicate INVITE_STATUS_LABELS + vocab-dup ratchet seeded (DEC-789/180), .limit(1) safe only over a KEY (DEC-558), reviewer cap-saturated denominator (DEC-707), per-event showResources (DEC-988), TrackFacetSelect carve-out premise (DEC-919).
- WAVES 75-78 COMPACTED: task-file/read-side gaps (DEC-248/040 taskAssignmentId chain), branch-named exemptions banned as reasons (DEC-099 w76, extended w77 "being converted on a parallel branch"), unread swallow ledger entries ("unreviewed — filed for a later wave", DEC-518), missing parser siblings on JSON columns (social_links_json DEC-152, custom_fields_json DEC-738 — grep the sibling COLUMN not the table), ratchet seeding traps (DEC-180/558), guard fingerprint vs rule mismatch on batch caps (DEC-422), speaker detail person-vs-participation boundary (DEC-258/738). Multiple review-lens/mandate items re-verified closed each wave — always re-derive from the artefact before re-filing.
- WAVES 79-81 COMPACTED: DEC-069 branch off-limits lists re-derived via `git rev-list --count`, never trusted from a prior snapshot; export-vs-screen arithmetic drift (DEC-147/736 eval CSV, review-identity.ts resolver-fed-different-inputs); ratchet-carried-not-shrunk (DEC-180 vocab-dup ceiling); silent JSON-parser failures hiding a field (DEC-008 visibility.ts); DEC-558's ~45 index-name citations verified real but unguarded; two-readers-one-column lies (DEC-015 tracks_json, DEC-147 evaluation_plan JSON casts); ledger measuring only one side of a contract (DEC-817 body-side ungated); duplicated predicate diverging in SQL (DEC-592/561); frame-in-repo outranks a pixel measurement (DEC-582 hub width); silent `as unknown as` cast (DEC-212); mandate documents decay, re-derive before spending a lane (14/22 eval-findings items closed at read time).
- WAVES 82-104 COMPACTED (v12m mobile campaign, offset 82 W8→90..104): 53/53 phone-frame ledger reached w8, claim by exact citation not proximity (DEC-808); `.chq-phone-*` vocab (DEC-576); tap floor by ANCHOR not container (DEC-393); `display:none` legal only off-screen (DEC-919); terminal-phone-block appends at LAST top-level construct in ascending source order, never reorders (DEC-385); ratchet ceilings MEASURED not read from a snapshot doc (DEC-989, recurs w95/103/104 — copy test, force ceiling `-1`, read offender list); a frame receipt whose every `expect()` is a `docs/design` read is a pack-vs-pack tautology invisible to `tautological-assertion.scan` unless its population widens past TITLES-only (DEC-967/976 w99/w104); desktop ledger had NO population for 18 waves, then climbed 16→64/102 (DEC-976 w99-w105), claim assertions anchor file CONTENT not app-file line numbers, `CLAIMED_FLOOR` raises are a merge-train act only; a merge train is a code author and writes contradictions — re-run the fast tier after every train (w101-104, a CSS comment once overturned an amendment, DEC-919 w102); zsh's `git show $b:path` applies the `:a` history modifier and silently rewrites the path, always quote `"${b}:path"`; a scan sentinel anchored on a real offender makes fixing that offender illegal, delete it rather than re-point (DEC-967 w103); terminal-block consolidation is a CASCADE edit — concatenate in ascending source order, never reorder, and tests that count `@media` blocks by index break silently and must be found by grepping `indexOf('@media` first.
- WAVE 105 (v12m-w5) measured clean on main `9e7a54b4`: terminal-phone-block 8 offending sheets; DEC-989 overflow 24 live vs stale ceiling 65; DEC-393 anchors 63 live vs stale ceiling 92 (both one-sided ratchets with slack, never able to fail); desktop ledger 64/102, phone 53/53, tautology 0. Scan predicates are literal (`hasFlexCenter` misses `inline-flex`; `(no CSS rule)` can mean the scan can't reach a fully-floored phone-only class) — read the predicate before calling a row a defect. Check whether a scan names YOUR rule as its live-offender canary before fixing it (DEC-967 w105).

## Wave 106 (planner, prefix v12m-w2-*) — what main actually is

MEASURED on a clean `git archive` of main `8e5aec96` (SHA re-read after; main advanced to `114b4d2c`, docs-only). The live worktree is dirty with an IN-FLIGHT uncommitted repair, so nothing was measured in it.

- FULL suite on main: 13 files / 23 tests RED (fast tier 5/15): `tap-target-floor.test.ts`(10), `error-states-phone.test.ts`(2), `phone-terminal-block.scan`, `phone-horizontal-overflow.scan`, `frame-citation.scan`, `submissions-landing-phone-frame`, `SessionboardImportPanel/ExportsPanel/ApiTokensPanel.render`, `DeliverableDetail.render`, `ImportWizard.phoneColumns.render`, `api-mutation-bump.scan`, `App.signout.render`.
- The live worktree's uncommitted repair (18 files + new `app/src/test-utils/phoneBlock.ts`) turns **8 of the 13** green when applied to a clean snapshot (tap-target-floor, error-states-phone, App.signout, api-mutation-bump, DeliverableDetail, ApiTokensPanel, agenda-phone-floor, frame-citation) — do not re-plough those. It does NOT fix SessionboardImportPanel, ExportsPanel, ImportWizard.phoneColumns, submissions-landing-phone-frame.
- ONE root cause under most of that red: DEC-385's one-terminal-block forward-merge moved provenance comments INSIDE rule bodies, and those comments quote CSS (`.chq-main{padding}` ×10 in styles.css, commas, the literal `@media`) — any naive brace-walker or comma-splitter desynchronises. `tap-target-floor`'s `phoneMediaBodies` returns ZERO matches against styles.css; the floors are actually declared at styles.css:2355-2390. A pin over a stylesheet must strip comments before it walks braces (DEC-613 w106).
- Ledgers: phone-frame-ledger 53/53 CLAIMED (mandate half met). desktop-frame-ledger 64/102, all 38 unclaimed frames OWNED (`v12m-w4-k` Review×6, `-w5-a` Settings×8, `-w5-b` Settings dialogs×8, `-w5-c` Portal×6, `-w5-d` Overview/Home×6, `-w5-e` Agenda/Content×4) — scheduling any buys a conflict, not a claim. All 8 `phone-terminal-block` violators owned; watch `v12m-w13-c` (chrome.css.ts 1→2 blocks) vs `v12m-w4-h` (fixes non-terminality) for a merge-train reconciliation.
- Stale one-sided ratchets found and given two-sided companions (DEC-808 w106): `ORPHAN_CEILING=4` in `phone-scaffold-orphan.scan` (truth 0); `PHONE_HIDDEN_UNRECEIPTED_CEILING=10` in `phone-capability-removal.scan` (truth 7, file's own comment already said so).
- Method: ownership map via `for b in $(git branch --no-merged main --list 'v12m-*'); do git diff --name-only main...$b; done` (61 unmerged branches) — check before spending a lane, six of eight red files on main were already owned. Audit docs go stale fast — verified already-built but still filed open: speakers-v12.md §2/§3, comms-drill-v12.md's three held items, review-admin-v12.md §3. Re-derive, never inherit (DEC-976 w106).
- DEC-976 w106 extends the citation contract to `src/decisions-data/DEC-*.md` itself: every path:line a decision doc cites must resolve and every backtick literal must appear verbatim, enumerated and ratcheted — the registry was the one artefact nothing checked (caught DEC-986 documenting phone-CFP fixes that don't exist in the tree).

## Wave 107 (planner, prefix v12m-w3-*) — measured on main `5f649926`

- THE DESKTOP EXIT IS UNREACHABLE BY CONSTRUCTION, not by remaining work. Copying the six in-flight claim files (w4-k, w5-a..e) onto a clean archive takes the desktop ledger to **99 of 102**; two of the three survivors (`Chautauqua Settings.dc.html:1111`, `:1507`) are already deferred by DEVIATIONS §6 under a user ruling, and the lanes that owned them wrote divergence notes rather than force-fit a claim. A ledger with exclusion classes for template interpolations and explainer panels but none for its own authority's deferrals can never reach 100% (DEC-976 w107). The third, `Public and Portal.dc.html:1167`, is in NO deviation — and the data model already answers it (`event.orgId` + `startDate`), so deferring it on "no data model" would have been a phantom justification. Built, not deferred (DEC-745 w107).
- TWO SCANS OF ONE POPULATION THAT DISAGREE: `phone-scaffold-orphan.scan` reads 0, `phone-page-scaffold`'s copy reads 1 against ceiling 5. The copy asks `src.includes('.chq-phone-head-drill')` of TSX markup, which never spells the dot — four classes pass by coincidence, one fails, and the ceiling hides both. READ THE PREDICATE before believing either number; the answer is deletion, not reconciliation (DEC-613 w107).
- STALE ONE-SIDED RATCHETS, re-measured by forcing the const to `-1`: control-font `CEILING`=34 vs **28**; frame-citation `UNRECEIPTED_CLAIM_CEILING`=14 vs **1**; `phone-page-scaffold ORPHAN_CEILING`=5 vs a broken 1. `phone-capability-removal`=6 vs 6 is honest. And the receipt contract is PHONE-ONLY: 99 desktop claims are about to land checked for line resolution and nothing else (DEC-808 w107).
- OWNERSHIP FIRST, ALWAYS. 61 unmerged branches. Every page stylesheet in the Tier-2 backlog (review/scorecard/submissions/detail/styles/content/overview/settings/speakers/forms/comms) is held by 2-6 lanes and all 8 terminal-block violators are owned, so this wave scheduled ZERO contended CSS. Re-derive with `for b in $(git branch --no-merged main --list 'v12m-*'); do git diff --name-only main...$b; done`, and re-read the SHA after: main moved four times inside one planning pass and a merge train fixed two of the reds mid-plan (`settings-lists.css` citation, `decision-doc-citation` tautology) — plan against a `git archive`, never the live worktree.
- THE MANDATE DECAYS UPWARD TOO: `docs/mobile-campaign.md` gained two new sections DURING this pass. Read it LAST. Verify its claims before spending a lane: its DEC-919 bulk-bar miscitation was real (2 sites), its DEC-302 one was not (every reference is a correct npm-audit citation).

## Wave 108 (planner, prefix v12m-w4-*) — measured on `git archive` of main `08b23a88`, re-verified at `28f00481`

- MAIN IS RED ON EXACTLY THREE FILES, and two of them are contradictions a merge train or the USER wrote, not unfinished work: `error-states-phone` (the user's own 7f450624 gave `.chq-detail-delete-link` a 44px control box at every width; an earlier pin asserts the opposite) and `ImportWizard.phoneColumns` (three artefacts disagree — CSS renders the dedupe note, the test pins `display:none`, the component's comment claims desktop-only). The third, `phone-terminal-block` (8 sheets), is fully owned. When a pin and the tree disagree, ADJUDICATE against the frame and rewrite the pin positively — deleting the assertion loses the guard, reverting the CSS loses the user's fix.
- A RATCHET'S TEST NAME IS NOT ITS BEHAVIOUR. `phone-horizontal-overflow`'s ceiling `it()` is titled "two-sided ratchet: may only fall" over a single `toBeLessThanOrEqual` (65 vs a measured 24); `phone-tap-target`'s is titled "never raises it silently" with nothing detecting staleness (92 vs 64). Read the body, then re-measure by forcing the const to `-1` (DEC-808 w108).
- TWO REGISTRIES, ONE NUMBER SPACE: all 31 `src/decisions-data/DEC-*.md` differ from their `decisions/DEC-*.md` namesakes, and several are DIFFERENT RULINGS under one id (DEC-967, DEC-989, DEC-991 contradict their registry twins; 18 of 999 constants in `part*.ts` now carry an amendment headline in place of the decision's own statement). A "DEC-NNN" citation in campaign prose means the `src/decisions-data/` copy. Do not spend a lane reconciling it — the scribe rewrites these every wave.
- MANDATE ITEMS DECAY BOTH WAYS: verified already-built at read time — task-view per-kind actions, Content Select mode, Comms `PhoneDraftCard` wiring, the 5-item tab bar, Merge·390 one-line block, and all three `speakers-v12.md` findings. Verified still open — DEC-986's two phone-CFP defects (unchanged since wave 92). And `docs/mobile-campaign.md` grew two NEW user/swarm items (D19 cross-event switch, D21 SWR cache) mid-pass. Read it last, then read it again after.
