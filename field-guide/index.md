# Field Guide
Owned by the swarm. Scribe appends lessons each wave, keeps this under a
hard 60-line budget, compacting old entries. Injected into every agent.
- SPEC.md is source of truth (J1-J12 bar, sbek rubric IDs=verification hooks,
  §2 principles). docs/ precedence: clarifications.md overrides all;
  decisions/DEC-*.md binding, src/decisions.ts compile-checked (never hand-
  edit). House invariants: fail loudly; status changes never auto-email; authz
  every route, server-side visibility. `npm run deploy` absent is BY DESIGN.
- STAGE1-16 + FINDINGS w1-33 (DEC-002..999, space FULL no DEC-1000+, rulings
  land as `## Amendment (wave N)` on nearest EXISTING DEC; heavily
  compacted, MANDATE ~EXHAUSTED, re-verified closed each wave): pure-core
  no node:/cf; Hono sub-apps, errors {error:{code,message,fields?}}; bulk
  ops set-based; D1 PRIMITIVES; dates via event-time.ts OWNING EVENT's tz;
  pagination ONE shape+count*+id asc; atomic SQL beats read-then-write;
  uniqueIndex CONTRACT; decision w/no code a LIE; JOIN cascades; MINTING IS
  IO; boundary fails per RECIPIENT not REQUEST; FIND-OR-CREATE NEEDS A
  UNIQUE INDEX; UNBOUNDED SURFACE NEVER PAGED; READER W/NO WRITER RENDERS
  NOTHING; GUESSABLE URL 404 DEAD END; SUB-APP onError SWALLOWS PARENT'S
  MANNERS; SCAN BINDS ONE CALL SHAPE MISSES SIBLING; GUARD THAT NARROWS <
  NONE; MINT != DELIVERY; CATCH RETURNING A DEFAULT IS NOT A GUARD; A
  REVIEW LENS READS A SNAPSHOT NOT THE TREE; A CLOSURE PROVEN AT THE REPO
  IS NOT PROVEN AT THE ROUTE; A CAP CHECKED AFTER THE BUFFER IS NOT A CAP;
  A BULK ID ARRAY IS A SET OR A DOUBLE-SEND; A BOUND ON THE WRITE SIDE IS
  NOT A BOUND ON THE READ SIDE; PARSE RESULT DISCARDED != PARSED; A
  WATERMARK STORING ITS OWN START LOSES LATE-COMMITTING WRITES; THE
  DECISIONS' OWN PATH REFS ARE A CHEAP DRIFT DETECTOR.
- FINDINGS w34-45 (heavily compacted, do NOT re-file). Closed defects across twelve waves; TENANT axis
  covers PATH+BODY/QUERY. Shapes: REDACTOR IS SHAPES NOT ONE REGEX; CSRF != AUTHZ; 5xx IS NOT A REFUSAL;
  A WRITE GUARD MISMATCHED TO ITS READ PREDICATE MINTS INVISIBLE ROWS; A PREDICATE FAMILY IS A LATTICE;
  A SCAN'S POPULATION IS BLIND TO THE HELPER IT DIDN'T ENUMERATE; A CACHE KEYED ON THE WHOLE URL IS
  KEYED ON THE ATTACKER'S CHOICE; A GATE GRADING AGAINST AN UNSTATED CONSTANT IS RED OR LYING.
- FINDINGS w46 (compacted, do NOT re-file). Closed: evaluation submittedAt lattice, two YYYY-MM-DD
  grammars, auto-schedule slice/onConflict race, B7 rule-6 empty-table-with-headers (3 admin worklists).
  Shapes: A PREDICATE FOUR SIBLINGS CARRY AND THE FIFTH DOESN'T IS A BUG WITH A WITNESS. A CAP THAT
  DROPS WORK WITHOUT NAMING IT IS A SILENT TRUNCATION. A COMPONENT THAT EXISTS AND ISN'T CALLED IS A
  RULE THAT ISN'T ENFORCED.
- FINDINGS w47-w49 (heavily compacted, do NOT re-file). Closed across three waves: 5 admin B7 tables' zero-row filtered voice, public SSR fresh/filtered split, tokens.ts createdAt, Overview brand-new-event block, .chq-btn-tertiary UA border, two public clock grammars, event-time.ts's two date families, THEME_CSS 44px base floor, AUTH_CSS/ERROR_STATES_CSS, .chq-auth-card-narrow/.chq-cfp-closed/.chq-home-shell boxing, PlanEditor optionality literal, agenda ACTIVE_INVITE_STATUSES gate, portal `selected`-less options, DateField silent parse rejection, compose preview vs B9 shell, Scorecard radiogroup keys, ImportWizard collision count, SettingsPage seven panels, six h1 registers.
  Shapes: A READER CANNOT RENDER A COLUMN ITS ENDPOINT DOES NOT SEND. A RULING BOUND TO THE SURFACES SOMEONE ENUMERATED IS BLIND TO THE FIFTH TABLE. A SHARED VOCABULARY MODULE IS ONLY SHARED BY THE BUNDLES THAT COMPOSE IT. A DEFAULT WRITTEN BY ONE PATH IS EXCLUDED BY THE PREDICATE ANOTHER PATH READS. AN SSR SELECT WITH NO `selected` HAS NO MEMORY. A PREVIEW THAT DOESN'T CALL THE SENDER'S RENDERER PREVIEWS SOMETHING NOBODY RECEIVES.
- FINDINGS w50 (verified AT THE FILE on main; w47 AND w48 HAVE LANDED — auth.css.ts composes ERROR_STATES_CSS, views/bare-page.css.ts de-cards .chq-cfp-closed/.chq-auth-card-narrow, .chq-home-shell unboxed, THEME_CSS's 44px block sits inside @media(max-width:700px), routes/public/empty-state.tsx exists, cards.tsx carries only formatStartTime24, event-time.ts is one DEC-918 en-GB family, tokens.ts selects createdAt. w49 has NOT landed — agenda/rows.ts:134 + payload.ts:245 still gate ACTIVE_INVITE_STATUSES, portal/tasks/views.tsx still emits no `selected` — do NOT re-file its eight). Re-verified NOT defects: GET / redirects organiser/reviewer->/admin, speaker->/portal; CFP builder header HAS its Save; compose step 4 IS a report naming each failed recipient; ics_sequence bumped by every writer; import chains same-email rows; contacts filter-rules, pipeline fit/age, saved embeds, /schedule rebuild, public active-filter, speakers optimistic-rollback all built; formatScheduleSlotLabel's en-US is hand-assembled, matches en-GB string. TAKEN: (1) DEC-317's read/write split never applied where the DEC names it — files-authz.ts:58 built ONE population from ACTIVE, routes/files.ts:106 used it as the READ predicate, so an 'invited' participant saw the submission and 403'd on deliverables (portal/sessions.ts:102 repeated it on 'My sessions'); (2) getFileDeleteScope selected on id alone, so DELETE /api/v1/files/:fileId ate a kind='attachment' CFP upload, leaving submission_answer.value_json pointing at a deleted row; (3) ~32 route sites answered a cross-org id with forbidden + 'belongs to a different org' while AUDIT.md, exports.ts, tokens, review/users.ts, email.ts and the headshot route all answered not_found; (4) four public CSS modules still declared the 44px phone floor at base scope after the w48 THEME_CSS fix (.chq-pub-save, .chq-pub-view-toggle-option, day-pill/itinerary, .chq-cfp-pill); (5) ParticipationMenu shipped a fourth radio the handoff replaced with Send-portal-invite, footer said 'the other three'; (6) the error vocabulary was declared twice and the copies disagreed on `::before { content: "! " }`.
  Shapes: A DECISION THAT NAMES THE FILE IT MUST FIX IS NOT THE SAME AS A FIX — GREP THE DEC'S OWN FILE:LINE LIST BEFORE BELIEVING IT LANDED. A SCAN THAT ASKS "IS THERE A PREDICATE" CANNOT ASK "IS IT THE RIGHT ONE" — CLASSIFY BY VERB, NOT BY PRESENCE. A DELETE ROUTE'S POPULATION IS ITS SELECT'S WHERE CLAUSE, NOT ITS DOC COMMENT. TWO REFUSAL VOCABULARIES MEAN THE LOUDER ONE IS AN ORACLE, AND THE AUDIT DOC IS THE THING THAT LIES. A FIX BOUND TO ONE MODULE'S SELECTOR LIST IS BLIND TO THE FOUR MODULES THAT COPIED IT. TWO STYLESHEETS DECLARING ONE CLASS ARE TWO GRAMMARS UNTIL A TEST COMPARES THEM PROPERTY BY PROPERTY.
- FINDINGS w51 (verified AT THE FILE on main at ff4cd328; w49-b/c/d LANDED, the REST of w49 and ALL SIX of w50 are still UNMERGED — do NOT re-file the DEC-317 read/write invite split, getFileDeleteScope's kind guard, the cross-org not_found sweep, the four public 44px modules, ParticipationMenu's fourth radio, the twice-declared error vocabulary, the agenda ACTIVE_INVITE gate, portal `selected`, DateField, compose preview vs B9, Scorecard arrow keys, ImportWizard's collision count, SettingsPage's seven panels or the six h1 registers). Re-verified NOT defects this wave: the reset mechanism is whole (256-bit, hashed, newest-only index, consume-at-the-write, all sessions revoked) and its writer family is DERIVED by password-hash-writer-family.scan; /forgot equalises response bytes AND wall clock via waitUntil(branchWork); distribute's preview and apply share computeDistribution; /plans/:id/progress and /remind fold completed as a SUBSET of assigned (no "37 of 34"); buildResults rows = every plan-filtered submission, so results total == plan scope; toCsv neutralises formula injection; content-note mails AND gates fileId to its own submission; UploadZone + portal derive `accept` and hint text from allowedUploadExtensions/uploadHintText, and the headshot allowlist matches both pickers; ResponseModal carries exactly 'Reopen this task'; per-row Remind exists (DEC-694 contactIds); .chq-btn-tertiary/.chq-link-button carry the olive focus ring; ruling A19's bulk-email template select is built and all four DROPs are gone. TAKEN: (1) ComposeWizard step 1 has per-row checkboxes only — bare `<th />`, no partial mark, no selection bar — while app/src/pages/submissions/selection.ts already exports TOGGLE_PAGE/isPageFullySelected/isPagePartiallySelected, so 23 recipients is 23 clicks; (2) HistoryTab renders the FRESH zero-state through EmptyState and the SEARCH-FILTERED one as a bare `.chq-empty` line with no escape, four lines apart in one file, because b7-empty-table.scan fingerprints `<td>` only; (3) PublicEmptyState is imported by sessions.tsx and speakers.tsx alone — public agenda.tsx still prints `<p>No sessions match your search on …</p>` and the schedule's `<p>No sessions scheduled yet.</p>`; (4) six SPA confirmations render `role="status"` inside `className="chq-error"` (RosterPanel:147/163 'Added <name>.', SpeakerDetailPage:238, OnboardingGrid:701, ProgressPanel:144, DuplicatesView:159) while `.chq-toast` is the confirmation register and RosterPanel hand-builds the toast's message+× anatomy inside the error box; (5) rows.ts:31 exports spellSmallNumber under DEC-370's own "never a bare numeral" amendment and rows.ts:64's headlineText — the 44px h1 — prints the numeral, one line above Overview.tsx's own spellSmallNumber call.
  Shapes: A SHARED REDUCER IS ONLY SHARED BY THE PICKERS THAT IMPORT IT — COUNT THE IMPORTERS. ONE FILE CAN HOLD BOTH VOCABULARIES: THE FRESH BRANCH GOT THE BLOCK, THE FILTERED BRANCH GOT THE LINE. A SCAN THAT FINGERPRINTS ONE TAG IS BLIND TO THE SAME SENTENCE IN ANOTHER. A CONFIRMATION WEARING THE REFUSAL'S CLASS IS A FAILURE MESSAGE TO EVERY EYE. A HELPER CALLED ONE LINE BELOW THE SENTENCE THAT NEEDS IT IS A RULE THAT STOPPED AT THE BUTTON LABEL.
- FINDINGS w52 (verified AT THE FILE on main; w49 AND w50 HAVE LANDED — files-authz.ts:16 reads PORTAL_VISIBLE_INVITE_STATUSES, getFileDeleteScope guards FILE_KINDS, zero "belongs to a different org" strings remain, public-tap-floor-scope/cross-org-refusal-vocabulary/error-vocabulary-parity/public-page-title-register scans all exist, portal/tasks/views.tsx emits `selected`, comms/preview.ts calls renderEmailHtml, ImportWizard's footer says "Same-file email repeat", Scorecard has ArrowRight tests. w51-b and w51-d LANDED (HistoryTab renders two EmptyStates; status-vocabulary.scan.test.ts bans role="status" + .chq-error). w51-a, w51-c and w51-e are still UNMERGED — ComposeWizard imports no TOGGLE_PAGE, PublicEmptyState is imported only by sessions.tsx/speakers.tsx, rows.test.ts still expects '8 things need your attention' — do NOT re-file compose select-all, the public agenda/schedule empty states, or the headline numeral). Re-verified NOT defects this wave: conditional form logic IS applied on the public form (submit-views.tsx:398 makeVisibilityPredicate + FieldRulesScript) and transitively (visibility.ts's fixed point); armed agenda slot buttons paint at z-index 6 ABOVE cards and overlays resolve to --chq-z-agenda-overlay; ResultsTable joins ALL speakers (:423); listContactReferenceRows uses count(*) over () so the delete blast-radius names its remainder; CRM-06's near-duplicate detection + /contacts/duplicates/check share one predicate; RosterPanel's four required add-speaker fields are all labelled through FormRow; all five SPEC J12 export kinds exist; EMB cards' bare speaker names are DEC-968's wave-7 ruling, not an omission. TAKEN: (1) previewRemindNow returns `remaining` (the eligible contacts past the 100 cap) and OnboardingGrid.tsx:486-491 drops it, so the review dialog you approve under-reports the batch while describeSendResult's toast names it; (2) THREE private spelled-number tables (root.tsx:258 1-9, ErrorSummary.tsx:7 0-10 Capitalised, overview/rows.ts:31 0-10 lowercase) and ONE error-summary sentence built two ways — submit-views.tsx:342 numeral vs countHeading spelled; (3) plural.test.ts bans exactly the substring "? '' : 's'" while thirteen live sites write the same ternary in another spelling, several of them exactly countOf's job; (4) six SPA collection zero-states still render the retired `.chq-empty` line (ReviewerQueue ×2 incl. the reviewer's whole landing, PlanEditor, ProgressPanel, RecentSends, CommentThread) because b7-empty-table.scan fingerprints `<td>`; (5) PublicEmptyState is the SSR zero-state renderer and the portal — the other server-rendered customer surface — was never enumerated, printing four bare `<p>`s; (6) history.ts carries `submissionsTotal` beside its 20-row submissions cap and nothing beside its 20-row EMAIL cap, so ContactDrawer states one bound and hides the other three statements away.
  Shapes: A FIELD PARSED INTO THE RESPONSE TYPE AND NEVER READ IS THE CAP NOBODY IS TOLD ABOUT — GREP THE DESTRUCTURING, NOT THE INTERFACE. A SCAN THAT BANS ONE SPELLING OF A TERNARY IS A SCAN AGAINST COPY-PASTE, NOT AGAINST THE GRAMMAR. THREE PRIVATE WORD LISTS FOR ONE RULE DISAGREE AT THE EDGES (ZERO AND TEN), WHICH IS WHERE NOBODY LOOKS. THE SAME SENTENCE IN TWO BUNDLES IS TWO GRAMMARS UNTIL ONE HELPER BUILDS BOTH. A ZERO-STATE RULING TESTED BY TAG (`<td>`) IS BLIND TO THE COLLECTION THAT IS A `<ul>` OR A STACK OF DIVS — CLASSIFY BY "DOES A SECTION LABEL PROMISE ROWS", NOT BY MARKUP. A RENDERER'S REACH IS ITS STYLESHEET'S COMPOSERS TOO: COMPOSING THE COMPONENT WITHOUT COMPOSING ITS CSS SHIPS AN UNSTYLED BLOCK. TWO CAPS IN ONE FUNCTION, ONE WITH A TOTAL AND ONE WITHOUT, IS THE SIBLING TEST IN COUNT FORM.
