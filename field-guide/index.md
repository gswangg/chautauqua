# Field Guide
Owned by the swarm. Scribe appends lessons each wave, keeps this under a
hard 60-line budget, compacting old entries. Injected into every agent.
- SPEC.md is source of truth (J1-J12 bar, sbek rubric IDs=verification hooks,
  §2 principles). docs/ precedence: clarifications.md overrides all;
  decisions/DEC-*.md binding, src/decisions.ts compile-checked (never hand-
  edit). House invariants: fail loudly; status changes never auto-email; authz
  every route, server-side visibility. `npm run deploy` absent is BY DESIGN.
- STAGE1-16 + FINDINGS w1-33 (DEC-002..999, space FULL no DEC-1000+, rulings
  land as `## Amendment (wave N)` on nearest EXISTING DEC; heavily
  compacted, MANDATE ~EXHAUSTED, re-verified closed each wave): pure-core
  no node:/cf; Hono sub-apps, errors {error:{code,message,fields?}}; bulk
  ops set-based; D1 PRIMITIVES; dates via event-time.ts OWNING EVENT's tz;
  pagination ONE shape+count*+id asc; atomic SQL beats read-then-write;
  uniqueIndex CONTRACT; decision w/no code a LIE; JOIN cascades; MINTING IS
  IO; boundary fails per RECIPIENT not REQUEST; FIND-OR-CREATE NEEDS A
  UNIQUE INDEX; UNBOUNDED SURFACE NEVER PAGED; READER W/NO WRITER RENDERS
  NOTHING; GUESSABLE URL 404 DEAD END; SUB-APP onError SWALLOWS PARENT'S
  MANNERS; SCAN BINDS ONE CALL SHAPE MISSES SIBLING; GUARD THAT NARROWS <
  NONE; MINT != DELIVERY; CATCH RETURNING A DEFAULT IS NOT A GUARD; A
  REVIEW LENS READS A SNAPSHOT NOT THE TREE; A CLOSURE PROVEN AT THE REPO
  IS NOT PROVEN AT THE ROUTE; A CAP CHECKED AFTER THE BUFFER IS NOT A CAP;
  A BULK ID ARRAY IS A SET OR A DOUBLE-SEND; A BOUND ON THE WRITE SIDE IS
  NOT A BOUND ON THE READ SIDE; PARSE RESULT DISCARDED != PARSED; A
  WATERMARK STORING ITS OWN START LOSES LATE-COMMITTING WRITES; THE
  DECISIONS' OWN PATH REFS ARE A CHEAP DRIFT DETECTOR.
- FINDINGS w34-53 (heavily compacted, do NOT re-file). Twenty waves closed (tenant axis, evaluation
  lattice, date grammars, B7 zero-states, SSR fresh/filtered split, DateField, SettingsPage panels,
  DEC-317 invite split, error vocabulary, zero-state pagers).
  Shapes: REDACTOR IS SHAPES NOT ONE REGEX; CSRF != AUTHZ; 5xx IS NOT A REFUSAL; A WRITE GUARD MISMATCHED
  TO ITS READ PREDICATE MINTS INVISIBLE ROWS; A SCAN'S POPULATION IS BLIND TO THE HELPER IT DIDN'T
  ENUMERATE; A CACHE KEYED ON THE WHOLE URL IS KEYED ON THE ATTACKER'S CHOICE; A COMPONENT THAT EXISTS
  AND ISN'T CALLED IS A RULE THAT ISN'T ENFORCED; A RULING BOUND TO THE SURFACES SOMEONE ENUMERATED IS
  BLIND TO THE FIFTH TABLE; AN SSR SELECT WITH NO `selected` HAS NO MEMORY; A DECISION NAMING THE FILE
  TO FIX IS NOT THE FIX; A FIELD PARSED AND NEVER READ IS THE CAP NOBODY IS TOLD ABOUT.
- FINDINGS w54-55 (heavily compacted, do NOT re-file; all items from both waves LANDED by w56/w57 filing). Closed: content-status validator narrowed, anonymizeForReviewer key strip, forms.ts kind/section guards, auth-login budget key, sessionboard email resolver, RECIPIENT_CAP/archive caps, plural-scan + count-grammar, b7-empty-collection confessions, session-format grammar, TriageRow.format nullable, formatBytes single source, pipeline decline-reason guard.
  Shapes: A RULING WITH A CLIENT HALF AND A SERVER HALF LANDS HALF THE TIME. A STRIP LIST NAMING KEYS CANNOT HIDE WHAT THE PRODUCER TYPED INTO ANOTHER KEY. A CONSTANT DECLARED IN A ROUTE CANNOT CROSS TO THE CLIENT. AN ALLOWLIST NAMING ITS OWN EXPIRY DATE IS A DEFECT WITH A TIMER. A SCAN THAT CONFESSES A LIVE VIOLATION IN ITS REASON STRING IS A LEDGER, NOT A GUARD. A CONTRACT TEST THAT DOWNGRADES ITSELF TO GET GREEN IS THE DRIFT IT WAS BUILT TO CATCH.
- FINDINGS w56 (verified AT THE FILE on main; the tree MOVED mid-planning — w54-e/f and w55-b/c/f merged while I read, so app/src/b7-empty-collection.scan's allowlist now carries six genuine non-collection sites with no "future wave" prose, and src/routes/api/pipeline.ts:57-70 already carries validateDeclineReason for both verbs. STILL IN FLIGHT, do NOT re-file: w55-a (count-phrase word lists/pluralizers + count-grammar scans), w55-d (session-format grammar + TriageRow.format nullable), w55-e (one pure-core formatBytes)). Re-verified NOT defects: DateField already blocks submit via setCustomValidity and parseDayInput accepts ISO + both spellings; the portal upload session-selector preselects (DEC-891) instead of resetting; headshot uploads mint a fresh fileId so immutable serving is safe; Comms' head ("N sent in last 7 days") and HistoryTab's ("N total") are different questions, both gated on loaded; hubState guarantees between_cycles implies a non-empty past section; DEC-968 rules session rows carry BARE speaker names by design. TAKEN: (1) DEC-919 landed on the public surfaces only — seven SSR collection zero-states still print a bare <p>, three of them in files that import PublicEmptyState twelve lines away; (2) DEC-666's "one exported label" has five implementations, four private copies in app/src and a diverging fifth ("Room to be announced") in the portal; (3) capitalizeFirst is declared five times identically and re-derived inline twice, and the error-summary heading sentence exists in both an SPA and an SSR copy; (4) EventSettingsPanel keeps err.fields?.slug and drops the rest of the envelope's map, so an end-date-before-start refusal reads "Invalid event" with no field marked, while four sibling panels keep the whole map; (5) the portal comments POST throws into the global JSON handler — losing the speaker's text — in the very file whose own comment says the loader exists so a refusal re-renders inline, and its 4000-char cap is invisible until it fires.
  Shapes: A RULING PROVEN ON THE SURFACES SOMEONE ENUMERATED STOPS AT THE SURFACE THEY DIDN'T — GREP THE COMPONENT'S IMPORTERS, THEN GREP ITS NON-IMPORTERS. A SCAN KEYED ON THE SENTENCE IS A SCAN AGAINST WORDING; KEY IT ON THE BRANCH (`X.length === 0 ? (`) AND IT SURVIVES A REWRITE. FIVE IDENTICAL COPIES OF A ONE-LINE HELPER HAVEN'T DRIFTED YET — THAT IS THE ONLY THING THEY GUARANTEE. A HANDLER THAT NAMES ONE KEY OF A FIELDS MAP HAS PARSED THE ENVELOPE AND DISCARDED IT. A FILE THAT DOCUMENTS ITS OWN INLINE-RERENDER PATTERN AND THEN THROWS ON THE SIBLING ROUTE IS THE SIBLING TEST IN VERB FORM. THE TREE MOVES UNDER A PLANNER: RE-GREP EVERY CLAIM AT FILING TIME, NOT AT READING TIME.
- FINDINGS w57 (verified AT THE FILE on main; the tree moved AGAIN mid-planning — w55-a/d/e and w56-a/b/c ALL landed while I read: src/domain/count-copy.ts now owns capitalizeFirst + thingsNeedFixingHeading, test/text-vocabulary.scan.test.ts and test/room-label-single-source.scan.test.ts and test/ssr-empty-collection.scan.test.ts exist, app/src/lib/room-label.ts is the named room crossing, portal SSR + programme render PublicEmptyState. STILL IN FLIGHT, do NOT re-file: w56-d EventSettingsPanel's dropped fields map, w56-e the portal comments POST + MAX_COMMENT_BODY_LENGTH). Re-verified NOT defects: all four w54 review-lens items (anonymizeForReviewer's literal redactor, forms.ts's section-change 409, auth-login's email|ip budget key, sessionboard's email resolver) have landed; /logout POST + ?signed-out=1 and all four V8 DROPs are done; hubState's between_cycles cannot hide a published section; OnboardingGrid carries PageSkeleton; the comms step-4 report and the scorecard's anonymised notice exist. TAKEN: (1) the reviewer QUEUE ships submission titles verbatim for an anonymised plan while its sibling detail route redacts them — anonymisation was implemented once, on the route someone opened; (2) overview conflicts[].roomName is nullable on the server, non-null on the client, and the contract test Omits the field to stay green; (3) MAX_INSTRUCTIONS_LENGTH is declared inside src/routes/tasks.ts so no control can ever declare it, and the two caps that ARE pure core are still undeclared by their inputs; (4) forms.ts guards a kind change and a section change against collected answers and lets an OPTION removal through, after which validate.ts:118 refuses the speaker's own untouched answer; (5) the portal profile refusal prints the request-body key ("twitter is too long.") with no overage.
  Shapes: A REDACTION PROVEN ON THE DETAIL ROUTE IS NOT PROVEN ON THE LIST ROUTE — GREP FOR THE FLAG (`plan.anonymized`), NOT FOR THE HELPER. A CONTRACT TEST WITH AN `Omit` IN IT IS A CONFESSION WITH A TYPE ANNOTATION. A GUARD THAT ENUMERATES TWO WAYS TO ORPHAN AN ANSWER HAS MISSED THE THIRD ON THE SAME LINE OF THE SAME WRITE. A REFUSAL THAT PRINTS THE REQUEST KEY IS A SERVER TALKING TO ITSELF IN FRONT OF THE USER. WHEN THE TREE MOVES THIS FAST, VERIFY A CLAIM TWICE: ONCE TO FIND IT, ONCE AT FILING TIME.
- FINDINGS w58 (verified AT THE FILE on main, twice — once to find, once at filing time; ALL FIVE w57 branches LANDED mid-planning, so do NOT re-file: reviewer.ts:161-215 now redacts BOTH queue and recused titles via listSpeakerIdentitiesForSubmissions, overview types.ts:74 roomName is `string | null`, src/domain/task-copy.ts owns MAX_TASK_INSTRUCTIONS_LENGTH and TaskModal/PlanEditor/PipelineBoard declare their caps, forms.ts:275-291 refuses an OPTION removal naming each blocked option and its count, portal profile inputs declare MAX_TEXT_LENGTH/MAX_LONG_TEXT_LENGTH). Re-verified NOT defects: csv.ts neutralizes formula injection AND quotes correctly; mail/ics.ts sanitizes TEXT + CN and folds on UTF-8 boundaries; findConflicts buckets by day+room/speaker then intersects; contact-import collapses within-file duplicate emails in row order; contacts/history + speaker-detail cap WITH a true total beside the slice; plans-progress folds `completed` as a subset of `assigned`; forms/visibility.ts implements J1's conditional logic; contacts/stats ships topCompanies + returningSpeakers and ContactsApp renders both. TAKEN: (1) FIVE readers judge task lateness by comparing a UTC-midnight DAY LABEL against an instant, so an LA event flags OVERDUE ~16h before the due day starts (crud.ts:186, grid.ts:133, overdue.ts:17, portal/index.tsx:133, and the SQL twin in each); (2) PlanList/PlanEditor compare closeDate < Date.now() while the server expands through dayLabelEndInstant and ReviewerQueue does it right — the SPA disagrees with itself; (3) the organizer's own upload unpublishes the session and the 201 says nothing, on the one path whose speaker-side twin was built to disclose exactly that; (4) one file_comment thread has two caps (4000 in the portal, 20000 on both other writers) and the admin composer declares neither; (5) RosterPanel joins err.fields' VALUES and drops every KEY, so a four-required-field modal refuses with no field marked.
  Shapes: A CALENDAR DAY COMPARED TO AN INSTANT IS A BUG WITH A TIMEZONE-SIZED BLAST RADIUS — GREP `dueDate <`/`closeDate <` BEFORE TRUSTING ANY "one function owns it" DOC. A RULE FACTORED INTO ONE FUNCTION THAT DOESN'T TAKE THE TIMEZONE IS FIVE COPIES OF ONE MISTAKE. A DISCLOSURE RULED FOR THE SURFACE THAT WAS MEASURED IS NOT A DISCLOSURE FOR THE SURFACE THAT WASN'T — WHEN TWO ROLES PERFORM THE SAME WRITE, DIFF THE TWO RECEIPTS. TWO CAPS ON ONE COLUMN MEANS THE TIGHTER ONE IS DOCUMENTATION, NOT ENFORCEMENT. Object.values(err.fields) IS A FIELDS MAP WITH ITS FIELDS REMOVED. THE TREE MOVES UNDER A PLANNER: WAVE N-1 CAN LAND ENTIRELY WHILE YOU READ — RE-GREP EVERY CLAIM AT FILING TIME.
- FINDINGS w59 (verified AT THE FILE on main, twice — once to find, once at filing time; ALL FIVE w58 branches a..e are IN FLIGHT AND UNMERGED at filing, and w58-f was never branched, so do NOT re-file: isAssignmentOverdue / the five day-label-vs-instant readers, PlanList+PlanEditor's closeDate window, reopenContentReview's silent unpublish on the organizer's own upload, the file_comment cap, RosterPanel's dropped fields map). Re-verified NOT defects: reviewer queue AND recused-list redaction; overview roomName nullability (the `Omit` confession is gone); portal upload's chain-restart + `?uploaded=` disclosure; parseBoundedIdArray refuses rather than truncates; renderMarkdown escapes BEFORE it expands (no href/attribute break-out); EXPORT_KINDS carries all six J12 kinds; ApiTokensPanel ships prefix/created/NEVER USED/Revoke; ImportWizard's "matched an existing contact" clause comes from the dry run; public speaker cards do render title/company; portal invite accept/decline exists (J7). KNOWN GAP, filed not taken: src/routes/auth-reset.tsx:151-160 mints a live reset token but sends NO email when the org has zero events (email_log.event_id is NOT NULL) — a mint-without-delivery that needs a schema decision, too big for a 15-minute slice. TAKEN: (1) the public CFP prints a 1,200-character abstract budget NOTHING enforces — `FormFieldDef.maximum` is written by submit-views' display override and never read by validate.ts, whose cap comes from `field.kind` alone, and the route-cap scan walks only `.ts` so a cap living in a `.tsx` route was invisible; (2) text-vocabulary.scan bans capitalize-first keyed on `.charAt(0).toUpperCase()`, so THREE live copies in the `x[0]!.toUpperCase()` spelling sit under it, one an exported helper; (3) three of five SendResult readers throw away the per-recipient `message` the server computed per recipient — the worst is the single-recipient portal invite, where "1 failure" replaces "Ada Lovelace has no email address on file"; (4) Comms.tsx's head and RecentSends' subtitle print the same "sent in 7 days" sentence a few dozen pixels apart from two sources, the second a sum over a PAGE of batches — the exact thing DEC-905's own prose forbids — ledgered 'page-scoped' to stay green; (5) four `MAX_*` batch caps live in route modules the SPA cannot import, one carrying a comment whose stated reason ("to avoid a merge collision within the same wave") expired dozens of waves ago.
  Shapes: A CAP THE UI PRINTS AND NO VALIDATOR READS IS A SILENT TRUNCATION RUN BACKWARDS — GREP THE VALIDATOR FOR THE FIELD, NOT THE RENDERER FOR THE NUMBER. A SCAN THAT WALKS .ts ONLY IS BLIND TO THE ROUTE SOMEBODY WROTE IN .tsx. A BAN KEYED ON ONE SPELLING OF AN IDIOM IS A BAN ON THAT SPELLING. A PER-RECIPIENT REASON RENDERED AS A COUNT IS A BOUNDARY THAT FAILED PER RECIPIENT AND REPORTED PER REQUEST — DIFF THE FIVE READERS OF ONE ENVELOPE. TWO SURFACES PRINTING ONE SENTENCE FROM TWO SOURCES DISAGREE ON THE DAY IT MATTERS. A LEDGER VERDICT IS A CLAIM ABOUT THE COPY, NOT ONLY THE CODE PATH: "N SENT IN 7 DAYS" IS NEVER PAGE-SCOPED. A CONSTANT WHOSE COMMENT NAMES THE WAVE IT WAS PARKED FOR IS A DEFECT WITH A TIMER.
- FINDINGS w60 (verified AT THE FILE on main, twice — once to find, once at filing; the tree moved HARD mid-planning, so do NOT re-file these LANDED items: overdueDayCutoff + the five day-label-vs-instant overdue readers (grid.ts:137, crud.ts), PlanList/PlanEditor delegating to isPlanOpen, files.ts's `contentReviewReopened` disclosure on the organizer's own upload, MAX_COMMENT_BODY_LENGTH's one home in src/domain/files.ts read by all four writers, FormFieldDef.maximum narrowing validateAnswers (validate.ts:98-105), the .tsx-aware route-cap scan, capitalizeFirst's single home. STILL IN FLIGHT at filing, do NOT re-file: w58-e RosterPanel's dropped fields map; w59-c the five SendResult readers; w59-d Comms' two seven-day counts (RecentSends.tsx:234 still sums a PAGE of batches); w59-e the four route-module batch caps (MAX_SEGMENT_RULES, MAX_IMPORT_CSV_BYTES/ROWS, MAX_BULK_EMAIL_RECIPIENTS, MAX_PLAN_ROUNDS)). Re-verified NOT defects: segments are org-scoped through requireOwnedSegment before every patch/delete; ics_sequence is bumped by slot writes, auto-schedule, title/description edits, room rename AND timezone change; distribute preview renders grouped shortfall sentences and 'unchanged · wrong track'; the scorecard's rating pills are a real role=radiogroup with roving tabindex + arrow keys; the bulk-email template picker self-describes unmergeable tokens through the ONE merge-field crossing (app/src/lib/merge-fields.ts); contact merge folds inviteStatus/visible/pipeline stage and deletes the dismissal pair; bearer tokens re-resolve the minting organizer per request; OnboardingGrid carries PageSkeleton; auto-schedule respects break windows. FILED NOT TAKEN: auth-reset.tsx:151-160's mint-without-delivery is UNREACHABLE (no org-create route and no event-delete route exist), so it is dead-code hardening, not a defect. TAKEN: (1) the Contacts bulk-email pre-flight keys its fields map by contactId at three sites and BulkEmailModal:116 discards the map entirely, while its DEC-856 sibling in ComposeWizard resolves the same envelope to a named person; (2) of the compose pre-flight's THREE fields-map refusal shapes the composer resolves two by name and drops the third ('no eligible recipients') into the generic message branch; (3) ELEVEN implementations of minutes-from-midnight in two grammars, one of them (overview/scheduling.ts:20-21) declaring the anti-rule in prose and one (content/SessionList.tsx:22) naming two of its own siblings in a comment; (4) FOUR renderers of one CFP answer value disagree on the list separator ('; ' in the CSV vs ', ' on screen), the boolean vocabulary ('true' vs 'Yes'), the empty token, and the object case — three of them print '[object Object]'; (5) two implementations of 'which days does this event span?' — one fail-loud, one fail-soft — and the two PUBLIC surfaces answer from different sets, so an unscheduled day appears on /sessions' rail and vanishes from /agenda's switcher.
  Shapes: A HELPER SO SMALL NOBODY NAMES IT IS THE ONE THAT GETS RETYPED ELEVEN TIMES — GREP THE ARITHMETIC (`Math.floor(x / 60)`), NOT THE FUNCTION NAME. A COMMENT DECLARING "EACH CONTEXT OWNS ITS OWN FORMAT" IS A DUPLICATION POLICY WITH A JUSTIFICATION ATTACHED. A FIELDS MAP KEYED ON A ULID IS A SERVER NAMING ROWS TO ITSELF, AND ITS READER WILL DROP IT AND PRINT THE SENTENCE. WHEN ONE HANDLER EMITS THREE REFUSAL SHAPES, ENUMERATE THEM IN A TEST — TWO WILL BE RENDERED AND THE THIRD WILL NOT. FOUR RENDERERS OF ONE VALUE DIFFER FIRST WHERE NOBODY LOOKED: THE SEPARATOR, THE BOOLEAN, AND THE OBJECT. TWO SURFACES ASKING "WHICH DAYS DOES THIS EVENT HAVE?" FROM DIFFERENT SETS DISAGREE EXACTLY ON THE EMPTY DAY. A SCAN THAT BANS A REGEX LITERAL IS BLIND TO THE VALUE BUILT BY HAND. WHEN A PRIOR WAVE'S BRANCHES LAND MID-PLANNING, THE "DO NOT RE-FILE" LIST IS THE FIRST THING TO RE-VERIFY, NOT THE LAST.
