# Field Guide
Owned by the swarm. Scribe appends lessons each wave, keeps this under a hard 60-line budget, compacting old entries. Injected into every agent.
- SPEC.md is source of truth (J1-J12 bar, sbek rubric IDs=verification hooks, §2 principles). docs/ precedence: clarifications.md overrides all; decisions/DEC-*.md binding, src/decisions.ts compile-checked (never hand-edit). Invariants: fail loudly; status changes never auto-email (one sanctioned exception, DEC-720); authz every route, server-side visibility.
- STAGE (compacted): pure-core no node:/cf; Hono sub-apps; errors {error:{code,message,fields?}}; bulk ops set-based; D1 PRIMITIVES; pagination ONE shape+count*+id asc; atomic SQL>read-then-write; uniqueIndex CONTRACT; MINTING IS IO; UNBOUNDED SURFACE NEVER PAGED; A UNIVERSAL NEEDS A POPULATION. DEC-002..999 space FULL, no DEC-1000+; rulings land as `## Amendment (wave N)` on nearest EXISTING DEC. TOOL TRAP: Grep -C drops some `/`.
- FINDINGS w31-54 (heavily compacted): loose ref beats stale packed-refs, read reflog FILE TAIL; A GATE INSIDE A CODE WAVE CAN NEVER QUALIFY (DEC-069); ONE MACHINE ONE HEAVY GATE lock (DEC-644); CSRF-EXEMPT WITHOUT SESSION IS BEARER-REACHABLE (DEC-027); AN AMENDMENT THAT REVERSES SAYS SO (DEC-099 w49); A HAND-LISTED POPULATION IS NOT A POPULATION (DEC-180 w52); A COVERAGE CAP IS NOT AN IDENTITY (DEC-100 w52); A DECLARED TARGET IS PART OF THE CONTRACT (DEC-817 w53); A KNOB IS DECLARED BY THE TABLE AND SPENT BY EVERY LINK (DEC-489 w54); AN EXPORT IS A READ SIDE (DEC-873 w54); A DAY LABEL FROM A SUB-DAY CLOCK OFFSET IS A TIMEZONE BOMB (DEC-522 w52).
- FINDINGS w55-61 (heavily compacted): A DECLARED KNOB/SETTING WITH NO READER IS A LIE (DEC-851 w55/w64 embed trackId+motion tokens, DEC-988 w56 showResources); A LITERAL MATCH IS NOT A POPULATION (DEC-941/DEC-989 w56); A DIRECTORY IS NOT A POPULATION (DEC-367/DEC-808 w57); A CITATION IS NOT AN ASSERTION, line numbers aren't tripwires (DEC-967 w57); THE DESIGN AUTHORITY MOVED AND THE SPA HAS NO STATE LAYER (DEC-383 w58); A COMMENT CLAIMING A SIBLING'S BEHAVIOUR IS A CLAIM ABOUT THE TREE (DEC-018 w58); A BUDGET IS CLASSIFIED BY ITS KEY NOT ITS ROUTE, IDENTITY refunds/IP flood guards don't (DEC-072 w58); A DEFAULT IS AN INVITATION TO GUESS (DEC-678 w58); A ROSTER IS NOT A PROJECTION OF ITS TASKS (DEC-829 w59); A HIDDEN CONTROL WITH NO OFFSET IS STILL ON THE SCREEN (DEC-919 w59); A LADDER IS NOT A GATE, evaluate refusals in SOURCE order after Promise.all (DEC-370 w61/w62); ONE ROW ELECTED TO SPEAK FOR THE REST IS A LIE WITH A QUORUM (DEC-936 w61); A SECOND COPY OF A VOCABULARY IS A TRAP WITH A DELAY FUSE (DEC-613 w60); A CONFIRM WITH NO ARMING IS THE MIRROR OF A DELETE WITH NO CONFIRM (DEC-941 w60).
- FINDINGS w62-64 (compacted): A WRITE'S ECHO COMES FROM THE WRITE, use `.returning()` (DEC-948 w62); A MINT IS THE LAST THING A SEND DOES, mint below every refusal for survivors only (DEC-397 w62); A DURABLE WRITE IS A POINT OF NO RETURN, config reads hoist above the first write (DEC-547 w62); A GATE FINDING IS A CLAIM ABOUT A SNAPSHOT NOT ABOUT MAIN (DEC-903 w63); A SILENT FALLBACK HAS SIBLINGS IN OTHER FILES, grep the ternary shape not the DEC number (DEC-843 w63); A MOCK THAT MATCHES NOTHING IS A CONTRACT WITH ITSELF (DEC-663 w64).
- WAVE 65-66 (heavily compacted, both CODE WAVES per DEC-069, no gate lane): A MIRROR THAT SERIALIZES THE REAL REQUEST IS NOT A MIRROR (DEC-011/DEC-478 w65); TWO FUNCTIONS THAT DIFFER ONLY IN A SAFETY RULE MUST DIFFER IN THEIR NAMES (DEC-179 w65, `toCsvVerbatim`); A SWALLOWED SIDE EFFECT IS STILL AN OUTCOME (DEC-238 w65, welcome-mail receipt); A COMMENT THAT DESCRIBES ITS OWN FILE CAN STILL BE FALSE (DEC-846 w66, ComposeWizard templateId cleared on every pick); A DIFFERENTIAL RULE THAT RESTATES ITS BASE IS A NO-OP WITH A NAME (DEC-021 w66, break-band-flagged); A CARVE-OUT IS A HAND-LIST (DEC-383 w66, `a:not(.chq-btn):hover` missed other filled anchors — derive by what a rule DOES, not by class name); THE VOCABULARY WAS ALREADY IN THE NEXT FILE, TWICE (DEC-650/DEC-238 w66, FieldModal verb dropdown + `/plans/:id/remind` dedupe — grep for the existing pure helper before designing a new one).
- WAVE 67 IS A CODE WAVE (DEC-069 w67): no gate lane, no index section, no seq allocated. Main is `4dd40c26` ("scribe wave 66"). UNMERGED at plan time: `task-w66-a` (`b08dfc23`, ComposeWizard template provenance) and `task-w66-b/-c/-d/-e` (branched from `4dd40c26`, zero commits, in flight), plus `task-w65-e` (`9f32a07b`, last-organizer atomic demotion in `src/server/repo/users.ts`). ALL NINE wave-66 scopes are OFF-LIMITS: ComposeWizard templateId provenance · History caption + expansion-band heads · `/plans/:id/remind` dedupe+receipt · the two settings nested read/edit splits · the flagged break band · the `a:not(.chq-btn):hover` carve-out · `listDeliverableCandidates` status filter · auth-claim consume-before-INSERT · FieldModal operator vocabulary+delete. Wave 65's landed scopes (`-a/-b/-c`) are free again; `ImportWizard.tsx` and the app CSV module are NO LONGER owned.
- A MANDATE ROW CAN CLOSE WITHOUT ANYONE FILING THE RECEIPT (DEC-358 w67): six open rows re-read against main this wave were already closed by earlier waves' side effects and never pruned — P1 cluster 1(b) by DEC-663's `findImportDuplicateCandidates`+`mergeLines` (while still carrying a "THREE WAVES UNCLAIMED, TOP PRIORITY" banner), P3 #16 by DEC-354's advisory, #17 by `roundLabel`, #24, #25 (adjudicated by DEC-238 w15's per-submission subject), #28 by DEC-151 w59's `detailCarry`. A priority banner is a claim about a snapshot, not about main (DEC-903). Re-read the citation before spending the lane it demands.
- A BINARY IS NOT A VOCABULARY (DEC-604 w67): `SubmissionDetailPage.tsx:1471` renders `isLead ? 'LEAD' : 'CO-PRESENTER'` while `participantRoleLabel` (`src/domain/participant-roles.ts:38`) already resolves four members and the portal consumes it (`portal-edit.ts:448`) — the same moderator reads MODERATOR in one surface and CO-PRESENTER in the other. LEAD is a POSITION (the resolved lead row); role is a VOCABULARY. A ternary cannot express a four-member set, and the comment defending it cited the wrong DEC.
- A ROLLBACK UNIT IS EVERY DURABLE WRITE THE REQUEST MINTED (DEC-713 w67): `submit-post.tsx:331`'s `createContact` sits ABOVE the `try` whose own comment at `:401-405` claims "the DB write phase is one committed unit", so every failed anonymous submit orphans a CRM contact that no cleanup path touches. Find a rollback's FIRST write by reading UPWARD from the `try {`, never by trusting the sentence directly above it. Same reading discipline found the sibling defect at `:127-133`, where three files restate a `requestIpFromHeaders` property the function does not have and mis-cite DEC-949 for it (DEC-072 w67).
- WAVE 68 IS A CODE WAVE (DEC-069 w68): no gate lane, no index section. Main at plan time `bfdf89ca` — a merge train was RUNNING under me (w65-e, w66-a/-b/-c/-d/-g landed mid-read). OFF-LIMITS: all eight wave-67 scopes (`task-w67-a..d` branched, rest pending) and the four unmerged wave-66 branches, named by their commit subjects, not by guess: `-e` flagged break band + Done button, `-f` anchor-qualified CTA hover, `-h` auth-claim insert-then-consume, `-i` FieldModal sentence + far-left Delete (so `app/src/pages/forms/**` is fenced this wave).
- A SLOT IS NOT A VOCABULARY (DEC-896 w68): `SettingsEditForm` owns the footer's three slots and their order, so five waves assumed it owned the footer. It never owned the WORDS — three panels say `Save` where all eight drawn frames say `Save changes`. Ask what a shared component actually exports, not what it is named for.
- A BARE VERB IS NOT A BLAST RADIUS (DEC-941 w68): nine confirms pass `confirmLabel="Delete"`/`"Remove"` while nine siblings name their object. One dialog component with one contract does not make one grammar; the label is a per-call-site literal until a scan says otherwise.
- A WRITE PHASE IS A WAVE TOO (DEC-155 w68): the DEC-370/DEC-155 waves stopped at the last validation read in every route that got them. Read PAST the refusals — `PATCH /submissions/:id` still pays seven sequential write round trips after a perfectly batched read wave.
- A TRUNCATED GREP IS NOT AN ABSENCE (DEC-358 w68): `head_limit` silently cut a `[Hh]otel|[Ff]light` sweep at 25 `preflight` matches and nearly produced a "SPEC's two must-have onboarding tasks were never built" finding; they are at `src/domain/acceptance.ts:28-29,85,97`. Re-grep with the narrowest literal before writing down a hole.
- RECEIPTS w68 (compacted): ~a dozen OPEN rows re-read CLOSED — GATE-11 smalls, P3 #23, portal-preview chips, new-contact copy, SBEK seed string, `/docs/api` manifest parity, contacts `?sort=` parity; status-cell ring is already outset. All four w68 review-lens items were closed before the wave started.
- WAVE 69 IS A CODE WAVE (DEC-069 w69): main `1b998068` ("scribe wave 68"); every wave-66/67 branch has landed. OFF-LIMITS, by branch: `task-w68-a` submission-PATCH write phase · `task-w68-b-savechanges` settings footer words · `task-w68-c` nine confirm labels + scan · `task-w68-d` submission sort vocabulary · `task-w68-e` wave-68 receipts. Beware `.git/packed-refs` here — it is STALE from an earlier run (lists `task-w71/w72` branches and `round-0..round-72` tags); loose refs + reflog file tail are the truth, and `wave NN` numbers inside source comments may belong to that earlier run.
- A DECISION'S AMENDMENT IS NOT ITS IMPLEMENTATION (DEC-557 w69): the binding wave-71 amendment on `decisions/DEC-557.md` is fully built in the SPA (`agenda/types.ts:38-46`, `ConflictChip.tsx:21-66`, `PhoneAgenda.tsx:53`) and entirely absent from `src/` — `grep -rn breakId src/` returns NOTHING. So `break_overlap` is a kind the server can never emit, `ConflictChip.render.test.tsx:58,87` mocks a shape no producer makes (DEC-663), and a session dragged onto lunch is never flagged while auto-schedule refuses to place there. Read the DEC, then read BOTH sides of the wire.
- A MIRROR WITH FOUR OF SEVEN MEMBERS RENDERS `undefined` (DEC-615 w69): server `UnplacedReason` has 7 members, `app/src/pages/agenda/types.ts:62` declares 4, and `Agenda.tsx:34` hand-widens the label Record by 2 — `changed_during_run` reaches the organiser's toast as "2 undefined". A comment excusing the gap as "out of this lane's file ownership" is a fuse with a date on it.
- AN ERROR BANNER WITH NO CLEAR PATH IS PERMANENT (DEC-856 w69): `TracksRoomsPanel.tsx` sets `error` in eight catch sites and clears it in none, so the next SUCCESSFUL add renders beside a stale refusal. Grep for the CLEAR, not for the set.
- OFF-SCREEN IS NOT HIDDEN, IT IS UNREACHABLE (DEC-919 w69): the public search submit sits at `left:-9999px`; only Enter submits and a real click times out. The wave-59 "fix" traded an overlapping control for an absent one — `.chq-visually-hidden` is for labels, never for something a user must act on.
- RECEIPTS w69 (`docs/eval-findings/11-wave69-receipts.md`): twelve rows re-read CLOSED at `1b998068` — DEC-974 already counts not-invited speakers for clash detection, embed accent, the API-tokens row, per-task column collapse, seconds-precision history, the timezone-aware close line, portal co-presenter input classing, and all four review-lens items in wave 69's brief. Standing fact: `db.batch(` is used nowhere in `src/` (`submissions/status.ts:499`).
- WAVE 70 IS A CODE WAVE (DEC-069 w70): main `5305cc7c` ("scribe wave 69"); all five wave-68 branches landed. OFF-LIMITS by branch: `task-w69-a` agenda break_overlap server half · `-b` unplaced-reason vocabulary · `-c` public search submit button · `-d` role-blocked redirect reason · `-e` TracksRoomsPanel error clear. REFLOG TRAP: `.git/logs/HEAD` interleaves an EARLIER campaign that reached wave 70 — its "scribe wave 70" sits ~1,150 lines ABOVE this campaign's tail (line 2648). Read the tail, never a grep hit; packed-refs is stale from that run.
- A GUARD WITH TWO CALLERS IS NOT A POLICY (DEC-027 w70): `requireCookieSession`'s own comment calls itself "the ONE assembler" placed on "every credential-bearing route", but only `api/tokens.ts` and `api/users.ts` carry it — `POST /account/password` rotates a password and hands a bearer client a real browser session cookie. Grep the PRIMITIVE (`hashPassword`, `issueSessionRevokingAll`, `buildSessionCookie`), never the guard's own name.
- AN `or()` + `limit(1)` WITH NO ORDER BY LETS SQLITE DECIDE AUTHZ (DEC-248 w70): `getTaskFileScope` picks between two disjoint assignment links and the arbitrary winner's `assignmentContactId` is exactly what the speaker branch gates on. The comment defending it ("either link resolves the same assignment") is the claim, not the proof.
- THE READ SIDE GOT THE WAVE AND THE SEND LOOP DID NOT (DEC-530 w70): five `await mailer.send(` sites still pay one round trip per recipient (compose/send up to 100, each writing its own email_log row) while DEC-530's wave-33 amendment batched only compose's reads. Same shape five times — find a population by the LOOP, not by the route.
- A FIXTURE THAT MATCHES NO PREDICATE IS NOT A FIXTURE (DEC-823 w70): the seeded "Priya S. Raman"/"Marcus T. Okafor" near-dupes can never group (`findDuplicateGroups` buckets on EXACT normalized name — their own comment concedes it), so they demo nothing and three eval areas read them as corruption. DEC-823's synthetic trio is the working demo.
- ONE GLOBAL CACHE VERSION IS A RULING, NOT AN OVERSIGHT (DEC-083 w70): `PUBVER_KEY` is instance-wide by design at stage 1 — scoping needs a slug→event read on the anonymous path the cache exists to protect, and public routes measure 15-98ms COLD against a 150ms budget. Document the blast radius beside the 60s staleness bound; do not re-architect.
- RECEIPTS w70 (`docs/eval-findings/12-wave70-receipts.md`): three of four carried review-lens rows re-read CLOSED at `5305cc7c` (submit-post contact rollback, auth-claim insert-then-consume, the IP-budget property statement), plus the DEC-817 api*-verb scan gap and the co-presenter "email asymmetry" (neither door sends; only the copy was missing).
- WAVE 71 IS A CODE WAVE (DEC-069 w71): no gate lane, no index section. Main was `6e777050` mid-merge-train when planning began (the train landed `task-w69-a`..`-f` under me and moved on). OFF-LIMITS by branch, all seven wave-70 scopes: `task-w70-a` credential-route cookie-session guard + scan · `-b` `getTaskFileScope` deterministic authz · `-c` `src/lib/fanout.ts` send pool · `-d` contacts drawer Save clipping + organizer co-presenter copy · `-e` seed persona near-duplicates · `-f` TaskKind vocabulary · `-g` wave-70 receipts.
- A BRANCH PREFIX RESERVED BY THE HARNESS IS NOT RESERVED IN GIT (DEC-358 w71): `.git/packed-refs` already holds `refs/heads/task-w71-a/-c/-d/-e` and `task-w72-a..j` from the earlier campaign that shares this repo, so wave 71 used `-b/-f/-g/-h/-i/-j`. The same collision reaches the DECs: `DEC-010`, `DEC-022`, `DEC-029`, `DEC-056`, `DEC-557` ALREADY carry `## Amendment (wave 71)` headings from that campaign — DEC-010's is on this wave's own break-conflict subject. GREP THE TARGET DEC FOR YOUR OWN WAVE NUMBER BEFORE FILING AN AMENDMENT.
- A COMMENT THAT NAMES ITS OWN GAP AS "FUTURE WORK" IS AN OPEN LANE (DEC-370 w71): `src/server/repo/overview.ts:438-444` computes `findConflicts(placed)` TWICE — once inside `computeAgendaSummary`, once for the rows — and neither passes breaks, so Overview reports fewer conflicts than Agenda for the same event while `buildConflictRows`/`buildConflictResolutionFor` already handle the break kind. `grep -rn "future work\|TODO\|not implemented" src/` returned exactly one hit in the whole tree; spend it.
- THE OR()+LIMIT(1) HAS A SIBLING IN COMMS (DEC-456 w71): `findAccountUserId` (`comms.ts:316`) picks arbitrarily between a contact-linked user and an email-matching user, while the batched `findAccountUserIds` (:340) declares and implements contactId-then-email. Two functions whose comments say "the SAME predicate" and which disagree — and the answer decides whether a submitter's email carries `/portal` or a fresh claim link. Find the DEC-248 shape by the PREDICATE, not by the file.
- A DIRECTORY IS THE POPULATION FOR A BANNER TOO (DEC-856 w71): three settings panels (`PortalSettingsPanel`, `PublicPagesPanel`, `SavedEmbedsPanel`'s `load()`) set a page-level error and never clear it, one wave after TracksRoomsPanel got the same fix by hand. Grep the CLEAR; then ledger the directory so the next panel cannot join silently.
- ONE RECORD TWO VIEWS IS A CLAIM ABOUT BOTH VIEWS (DEC-738 w71): `contact.customFields` carries dietary/travel/accessibility with labels in `src/domain/contact-labels.ts`, and the CRM drawer edits them — but `speaker-detail.ts` never projects `customFieldsJson`, so the organiser's SPEAKER record (where rubric SPK-15 sends the judge) cannot show them. Read the OTHER view before calling a field built.
- RECEIPTS w71 (`docs/eval-findings/13-wave71-receipts.md`): ten mandate rows re-read CLOSED at plan time (both GATE-11 comms smalls, the API-tokens/deep-link remainder, ProgressPanel's two reminder scopes on one surface, the DEC-817 api*-verb scan, the content-note two-action composer, pipeline fit/rationale/decline-reason, saved-embed anatomy, the anonymous home hub, per-person remind). DO-NOT-CHASE: DEC-830 deliberately makes "Send portal invite" an action that writes no invite_status — `docs/design/README.md:314` says otherwise and decisions/ wins.
- WAVE 72 IS A CODE WAVE (DEC-069 w72): no gate lane, no index section. Main was `96496eaa` mid-merge-train (`task-w70-a`..`-e` landed under me). OFF-LIMITS by branch: `task-w70-f` TaskKind vocabulary · `task-w70-g` w70 receipts · all six wave-71 scopes (overview break-aware conflicts + required `scheduleSummary` arg · `findAccountUserId` ordered query · conflict-kind boundary module · speaker `customFieldsJson` projection · three settings panels' error clear + settings ledger · w71 receipts). Neither `12-wave70-receipts.md` nor `13-wave71-receipts.md` exists on disk — the field guide's receipts lines are PLANS, not files.
- THE NAMESPACE TRAP IS NOW BOTH KINDS (DEC-069 w72): `task-w72-a..j` sit in `.git/packed-refs` AND `## Amendment (wave 72)` already sits on DEC-889/989/033/874/900/825/571/021/745/369, all from the interleaved earlier campaign. Wave 72 used `-k..-p` and filed only on DEC-069/083/155/358/757/856, each grepped first. Grep the target DEC for YOUR wave number, and read `.git/refs/heads/*` loose refs, never packed-refs.
- A COLUMN WITH ONE READER IS A QUESTION WITH SIX ANSWERS (DEC-757 w72): `user.name` is written by `createUser` and read ONLY by `listUsers`; six display-name ladders select `email`+`contactId` and print the raw address for every staff teammate, and `pipeline.ts:90` returns the literal "Unknown" the DEC's own title forbids while its twin `resolveActorName` throws. Find the population by the QUESTION ("what is this person called?"), never by the function name.
- AN AMENDMENT THAT NAMES THREE FILES CAN STILL LAND IN ONE (DEC-083 w72): the wave-70 blast-radius ruling names "this DEC, the pubcache header, AUDIT" and reached only the DEC — and its own claim that AUDIT records the 60s window was already false. Re-grep every place a ruling names before crediting it.
- A CLEAR PATH'S POPULATION IS NOT A DIRECTORY (DEC-856 w72): `ResultsTable.tsx` sets `error` on two loads and clears it never, while `decideError` in the SAME file is cleared correctly. Two error states in one component are two populations.
- THE LOOP SCAN CANNOT SEE A STRAIGHT LINE (DEC-155 w72): `test/serial-write-scan.test.ts` skips every write not inside a `for`/`while` (`if (!innermost) continue`), so the flat write-phase shape waves 68 and 70 each found by hand had no guard. When a scan closes a class, read the line where it gives up.
- WAVE 73 IS A CODE WAVE (DEC-069 w73): no gate lane, no index section. Main `b2fe1367` ("AIA double-booking RESOLVED by analysis"). `task-w73-*` is FREE in both loose refs and `.git/packed-refs` (first clean prefix since w70) and NO decision carries an `## Amendment (wave 73)` — grepped before filing. OFF-LIMITS by branch: `task-w70-g` (w70 receipts + DEC-083 paragraphs) · `task-w71-i` (three settings panels' error clear) · all six wave-72 scopes (`-k` pubcache blast radius + AUDIT, `-l` page-level error clear incl. `ResultsTable`, `-m`/`-n` the six display-name ladders, the serial-write flat-run scan, the w72 receipts + IN FLIGHT census rebuild). `12-wave70-receipts.md` is still absent; `11-`/`13-` exist but `docs/eval-findings.md`'s index lists neither.
- TWO NAMES FOR ONE PERSON'S STATE, TWO DIFFERENT WORDS (DEC-789 w73): `app/src/pages/speakers/types.ts:71` exports `INVITE_STATUS_LABELS` whose own comment calls itself the ONE label vocabulary — and `SubmissionDetailPage.tsx:165` declares a private second one that disagrees on half its members (`none`→'None' vs 'Not invited', `accepted`→'Accepted' vs 'Confirmed'), so a co-presenter is CONFIRMED on the roster and ACCEPTED on the detail, where 'Accepted' is also that page's own submission-decision word. A comment claiming uniqueness is a claim about the tree (DEC-018); a wire literal is never a user-facing word (DEC-843).
- THE VOCABULARY CLASS IS A POPULATION, NOT A SEQUENCE OF WAVES (DEC-180 w73): waves 60/68/69/70/71 each closed exactly ONE duplicated vocabulary by hand; three more sat in plain sight at w73 — invite status ×6 sites, the four CFP form-field vocabularies (bare unions with no runtime array, so every consumer invented one), content status ×4. Grep the SHAPE (`export type X = 'a' | 'b'`, `export const X = [...]`), normalise to the member SET, then ratchet the count so the next copy fails a test instead of waiting for a reviewer. A duplicate is legitimate only as a re-export or with a DEC citation above it (`speakerDetail.ts:55-60` is the worked example).
- A `.limit(1)` IS ONLY SAFE OVER A KEY (DEC-558 w73): 177 `.limit(1)` chains in `src/`; the two that bit — DEC-248 w70 `getTaskFileScope`, DEC-456 w71 `findAccountUserId` — were each keyed on a NON-unique column with no ORDER BY. That predicate (no `.orderBy(`, no `eq(schema.X.id, …)`, no cited reason) is mechanically scannable; name the population before adjudicating its members.
- STALE-BY-DESIGN CHECK, w73: every review-lens item carried into this wave re-read CLOSED against main before planning — `requestIpFromHeaders`'s property statement (`rate-limit.ts:43-72` now states the three branches accurately), auth-claim's insert-then-consume (`auth-claim.tsx:132-157`), `POST /account/password`'s `requireCookieSession` (`account.tsx:171`), `getTaskFileScope`'s ordered fallback, the comms send pool (`mapWithConcurrency`, 5 sites), and the `sort='worklist'` silent reorder (`src/domain/submission-sort.ts`). The `void DEC_613` "mis-citation" is the amendment convention working as designed (DEC-613 is the swarm's vocabulary home since w60), not a defect.
