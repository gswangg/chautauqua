# Field Guide
Owned by the swarm. Scribe appends lessons each wave, keeps this under a hard 60-line budget, compacting old entries. Injected into every agent.
- SPEC.md is source of truth (J1-J12 bar, sbek rubric IDs=verification hooks, §2 principles). docs/ precedence: clarifications.md overrides all; decisions/DEC-*.md binding, src/decisions.ts compile-checked (never hand-edit). Invariants: fail loudly; status changes never auto-email (one sanctioned exception, DEC-720); authz every route, server-side visibility.
- STAGE (compacted): pure-core no node:/cf; Hono sub-apps; errors {error:{code,message,fields?}}; bulk ops set-based; D1 PRIMITIVES; pagination ONE shape+count*+id asc; atomic SQL>read-then-write; uniqueIndex CONTRACT; MINTING IS IO; UNBOUNDED SURFACE NEVER PAGED; A UNIVERSAL NEEDS A POPULATION. DEC-002..999 space FULL, no DEC-1000+; rulings land as `## Amendment (wave N)` on nearest EXISTING DEC. TOOL TRAP: Grep -C drops some `/`.
- FINDINGS w31-54 (heavily compacted): loose ref beats stale packed-refs, read reflog FILE TAIL; A GATE INSIDE A CODE WAVE CAN NEVER QUALIFY (DEC-069); ONE MACHINE ONE HEAVY GATE lock (DEC-644); verif-log sections NEW files never hand-edit; CSRF-EXEMPT WITHOUT SESSION IS BEARER-REACHABLE (DEC-027); ONE FILE ONE OWNER PER WAVE (DEC-358, REVERSED w44); AN AMENDMENT THAT REVERSES SAYS SO (DEC-099 w49); TWO CAMPAIGNS SHARE ONE DEC CORPUS/reflog, discriminate by re-reading cited file:line; A HAND-LISTED POPULATION IS NOT A POPULATION (DEC-180 w52) — use `test/helpers/registered-routes.ts` to enumerate; A COVERAGE CAP IS NOT AN IDENTITY (DEC-100 w52), count-then-insert is fine for caps, atomic SQL owed to identity only; A DECLARED TARGET IS PART OF THE CONTRACT (DEC-817 w53) — ONE resolver `resolveRegisteredRoute`, no hand-written route maps; A KNOB IS DECLARED BY THE TABLE AND SPENT BY EVERY LINK (DEC-489 w54) — honoring on arrival and emitting on departure are two different halves, check both; AN EXPORT IS A READ SIDE (DEC-873 w54), CSV must reuse the same WHERE predicate as its screen twin; A DAY LABEL FROM A SUB-DAY CLOCK OFFSET IS A TIMEZONE BOMB (DEC-522 w52), floor to UTC midnight and offset whole days.
- WAVE 55 IS A CODE WAVE (DEC-069 w55): no gate lane, no index section, no seq allocated. Main `76c947db` ("scribe wave 54"); `task-w53-a` (`34dfb8ba`) and `task-w53-e` (`96ebfe2f`) are COMMITTED-UNMERGED; `task-w54-a/-b/-d/-e` sit AT main's tip (cut, still running) and `-c` is `01b62a5f`. **No `task-w54-f` ref exists** — the day-switching half of the w54 embed-knob fix may never have been cut, so re-verify `agenda-controls.tsx` before assuming it landed.
- A DECLARED KNOB WITH NO READER IS A LIE (DEC-851 w55): `EMBED_KNOB_TABLE.schedule` lists `trackId`, but `ScheduleContent` documents the highlight control as dropped (`public/agenda.tsx:358-362`, body derives `days` from `byDay`) and the feed twin threads only day/q (`public/index.tsx:515-516`). The declaration is still spent: the builder offers the knob, `saved-embed.tsx:86` serializes it, and `deleteTrack` (`events.ts:522-547`) refuses a track because a saved embed names it. Honoring-on-arrival and emitting-on-departure are two different halves; this is the third — DECLARING.
- NINE PROBES THAT FOUND NOTHING, recorded so they are not re-spent (w55): pubcache's two closed lists are source-scanned and complete (`pubcache.ts:260-401`); DEC-318's range bound reaches breaks too (`public/agenda.ts:317`) and every public schedule_slot read; compose's 100 cap is enforced on EXPANDED recipients (`domain/compose.ts:66-86`), not on the selection; `CONTACT_FK_TABLES` has a schema-derived tripwire (DEC-282) and merge deletes dismissals (`merge.ts:635`); revision restore appends its own attributed revision (`submissions.ts:542-552`); `deleteTrack`/`deleteRoom` blocker lists are complete against every contact/track/room-referencing column; `schedule.ics?ids=` enforces `MAX_ITINERARY_IDS` server-side (`index.tsx:374-377`); airtable sync is watermarked, org-scoped and capped; `parseSessionListFields` is now byte-identical to `parseCardFields` (dead duplication, not a divergence).
- WAVE 56 IS A CODE WAVE (DEC-069 w56): no gate lane, no index section, no seq allocated. THE REFLOG HOLDS THREE CAMPAIGNS — grepping `scribe wave 55` returns three hits (lines ~1361, ~1944, ~2502); rank by TIMESTAMP and read the FILE TAIL, not the first match. Current campaign: main `1245e700` ("merge-train repair: fast tier excludes w53-e's jsdom-pinned api-mutation-bump scan"). Committed-unmerged: `task-w54-a` (`ebc2862a`), `task-w54-f` (`f102938c`). Wave 55 IN FLIGHT: `-a/-b/-d` sit at `e7c479f4`, `-e` at main's tip (all cut, zero commits), `-c` is `89c490f6`. Off-limits, owned by w55: the EMBED_KNOB_TABLE `trackId` removal, the SPA-mutator refusal-text ledger (TracksRoomsPanel wordings), the TIER-1 re-derivation, the cap-array population, the J7 portal-column enumeration.
- A LITERAL MATCH IS NOT A POPULATION, TWICE (DEC-941/DEC-989 w56): `delete-confirm.scan.test.ts:45` gates on `source.includes('apiDelete(')`, so `apiDelete<T>(` walks past it — two irreversible deletes ship unconfirmed (SubmissionDetailPage.tsx:704, Agenda.tsx:208) and a third hides in a stale exemption (TracksRoomsPanel). `page-measure.test.ts:226` matches only `className="chq-page..."` STATIC literals, so ProgressPanel.tsx:118's object-property root renders unclamped at 1532. Same shape as the prod P1's ROUTE_MODULE_MAP. A scan whose blindness is not itself asserted by a negative control is not evidence; an exemption list carries STRUCTURAL reasons only, never a branch note.
- A SETTING WITH NO READER IS A LIE, AND THE PORTAL HAS ONE (DEC-988 w56): `showResources` is written, validated, defaulted, merged and threaded into PortalBranding (`repo/portal/data.ts:168`) — and `grep showResources src/routes/portal` returns ZERO. Both entry points and the route itself render unconditionally. Same doctrine DEC-851 w55 applied to embed knobs, one surface over. Ruled: OFF suppresses the links AND 404s the route AND the organiser preview shows what the speaker sees.
- A DRAWING WIDTH IS NOT A MEASURE — ADJUDICATED NOT-A-DEFECT, do not re-file (DEC-989 w56): gate-9's "CFP builder 756 @ x422" and "12-home 820 vs 732" both ask a landed 820 to move to a ruler laid on a frame. `docs/design/README.md:409` says the frames' width IS a drawing width; `:413/:423/:425` fix reading at 820 and name the CFP builder as an editor that sits there. FormsPage already conforms (`.chq-forms-content` clamps to `var(--chq-measure)` centred; the chq-measure-table ROOT is the tested full-bleed-chrome split, `forms-header-measure.test.ts`). Also closed by measurement this wave: the auth h1 is 28px at both `auth.css.ts:65` and `bare-page.css.ts:37`; DEC-505's PATCH sibling-rule re-check landed in full at `forms.ts:361-397`.
- WAVE 57 IS A CODE WAVE (DEC-069 w57): no gate lane, no index section, no seq allocated. Main `1e423e0c` ("merge-train w55 repair: DEC-078 allowlist + schedule mock call sequence") — read the reflog FILE TAIL, three campaigns share it. Wave 56 IN FLIGHT: `-a/-b/-d/-e` sit at `9a41565b` (cut, zero commits), `-c` is `584f41c4`. Off-limits, owned by w56: the generic-aware delete-confirm matcher, the portal co-presenter V10 frames, the `showResources` reader, the History-tab chrome + `emailLogConditions` template predicate, the page-measure scan + ProgressPanel clamp + plan-editor 400 field messages.
- A DIRECTORY IS NOT A POPULATION (DEC-367/DEC-808 w57): `public-tap-floor-scope.scan.test.ts:20` sweeps `src/routes/public/**/*.css.ts` and `no-fitted-geometry.scan.test.ts:21` sweeps `src/routes/**` for `.css`/`.css.ts` — so `src/views/theme.ts`, the ~500-line THEME_CSS that OWNS the phone tap floor, is invisible to both, and seven base-scope `min-height:44px` rules ship at desktop width (auth.css.ts:93,236; portal.css.ts:53,101,110,213,294). The tell was already in the source: auth.css.ts:97-103 fights its own 44px box back with `align-items:flex-start` + `margin-bottom:-13px`. A population is a PROPERTY (exports a `*_CSS` template literal), never a path prefix plus a filename suffix.
- FOUR REVIEW-LENS FINDINGS, ALL ALREADY FIXED (DEC-358 w57): evaluations-CSV drafts, embed `fields`/`accent` carry, the anonymous draft `trackIds` cap and the `.zip` accept-vs-hint split were re-filed after landing in w54. Re-run the falsifying check before planning a lane off a review finding; a reviewer reads a diff range, not the tip.
- A CITATION IS NOT AN ASSERTION (DEC-967 w57): `turn-diet-honesty.render.test.tsx:191-195` covers two of its five named sites with `expect(true).toBe(true)` and a comment holding other files' line numbers. Line numbers in a comment are not a tripwire — they pass forever after the cited lines move. Same for a grader's blind spots: `spa-mutation-contract.scan.test.ts:396` drops a non-literal path with no record while carefully ledgering its skipped BODY keys.
- 820 + 300 + 60 = 1180 (DEC-939 w57): the scorecard's frame gutter is arithmetic, not a ruler — `--chq-measure-wide` is 1180px and `gap:36px` left 24px of unexplained slack. Before treating a frame number as a ruler reading (DEC-989 w56 forbids chasing those), check whether it makes a declared token come out exact.
- WAVE 58 IS A CODE WAVE (DEC-069 w58): no gate lane, no index section, no seq allocated. Main `50c3fcc4` ("merge-train w56 repair: portal-edit mock passthrough + Review frame-citation drift") — read the reflog FILE TAIL (`.git/logs/refs/heads/main`, last lines), three campaigns share it. Wave 56 partly landed: `-b`/`-c` merged (reflog 2514-2515); `-a` (`72ecc759`), `-d` (`0259ef82`), `-e` (`b581dd8a`) are COMMITTED-UNMERGED. Wave 57 IN FLIGHT: `-a`/`-c`/`-d` sit at main's tip (cut, zero commits), `-b` is `2304bbd6`, and **no `task-w57-e` ref exists** — the mandate-hygiene lane may never have been cut, so `docs/eval-findings.md`'s header is still pinned at wave 50 / `87cee8b9`. OFF-LIMITS, owned by w56: the generic-aware delete-confirm matcher + the two DEC-941 dialogs (SubmissionDetailPage.tsx:704, Agenda.tsx:208), the page-measure scan + ProgressPanel clamp + plan-editor 400 field messages, the History-tab chrome. OFF-LIMITS, owned by w57: `src/views/theme.ts` + every `src/routes/**/*.css.ts` phone floor, the tap-floor/fitted-geometry scans, `spa-mutation-contract.scan.test.ts`'s skippedPaths ledger, `turn-diet-honesty.render.test.tsx` + `autoAppliedTemplateName` + the tautology scan, the scorecard `820+300+60=1180` gutter.
- THE DESIGN AUTHORITY MOVED AND THE SPA HAS NO STATE LAYER AT ALL (DEC-383 w58): V11 (153 frames, `docs/design/DESIGN-RULINGS.md:108-177`) rules four button hover/active tiers, three motion durations, two easings, input hover, touch and reduced-motion. Measured on main: `src/**` has ONE `transition` (timezone.ts, unrelated) and `app/src/**` has four, none of them a state transition; there is no `:active` anywhere, no secondary hover, no `-webkit-tap-highlight-color` and no `prefers-reduced-motion` block. This is NET-NEW, not a re-skin. `transition: all` is banned (it catches the focus ring, which must be instant); `ease-in`/`ease-in-out` are banned, which retires `.chq-loading`'s `120ms ease-in` at styles.css:1066.
- A COMMENT THAT CLAIMS A SIBLING'S BEHAVIOUR IS A CLAIM ABOUT THE TREE (DEC-018 w58): `review/reviewer.ts:328-332` says "Organizers are exempt (same as the queue)" and the queue at :154 has no such exemption — a producer opening a closed plan gets zero rows while the detail route behind them loads. When two routes are described as agreeing, read BOTH; and when the fix could be made by flipping a flag, check who reads the flag first (`open` has two readers, `Review.tsx:628-632` and `ReviewerQueue.tsx:410`, so the envelope grows `viewerIsOrganizer` instead).
- THE WRITE SIDE OF A READ-SIDE RULING DRIFTS TOO (DEC-962 w58): DEC-962 put scope in the WHERE for batched READS; `files-content-status.ts:39-44` mutates on submission id alone while its bulk twin at :56-91 re-scopes on eventId in both statements. It is safe only because ONE of its two callers checks ownership first. When a ruling names a side, enumerate the other side before assuming it was covered.
- A BUDGET IS CLASSIFIED BY ITS KEY, NOT BY ITS ROUTE (DEC-072 w58): `submit-post.tsx:285` spends the per-email budget atomically and neither the R2 fan-out rethrow (:380) nor the DB-write catch (:426) returns it — ten transient failures lock a real speaker out for an hour, while the login door refunds on exactly this reasoning (`auth-login.tsx:213`). IDENTITY-keyed budgets refund on server-side failure; IP-keyed flood guards are FAILURE budgets and never refund. A refund must land in the SAME window bucket, so a helper that calls `Date.now()` internally cannot be refunded correctly.
- A DEFAULT IS AN INVITATION TO GUESS (DEC-678 w58): `PageSkeleton.tsx:83`'s `rows = 6` is a default, and `SubmissionsTable.tsx:405` overrides it with 4 — the ruling says ALWAYS six because a skeleton that guesses lies twice. When a frame says ALWAYS, the number is a contract and the prop is deleted, not documented.
- WAVE 59 IS A CODE WAVE (DEC-069 w59): no gate lane, no index section, no seq allocated. THE LOOSE REF READ STALE AGAIN — `.git/refs/heads/main` said `f673cdea` while the reflog tail (`.git/logs/refs/heads/main`, lines 2528-2531) had already advanced to `3b7c9e01` through four merges (task-w57-d/-e/-a, task-w58-c). Read the FILE TAIL, and remember three campaigns share this reflog (rank `scribe wave N` hits by timestamp). Wave 58 lanes `-a/-b/-d/-e` sit at `161cbba6` with zero commits (running); `-f/-g/-h` were planned and are STILL IN THE SHARED QUEUE (lowWater 8, 4 lanes) — a planned-but-uncut lane is OWNED, not free. OFF-LIMITS, all eight w58 scopes: the B8 interaction-state tokens + CSS scan, ConfirmDialog's irreversible weight, FieldModal's conditional sentence, `reviewer.ts`'s closed-plan organiser gate, `files-content-status.ts` scoping, `submit-post.tsx`'s email-budget refund, the invite-flow copy, `PageSkeleton`'s `rows` default.
- A ROSTER IS NOT A PROJECTION OF ITS TASKS (DEC-829 w59): `tasks/grid.ts:200-202` returns zero rows the instant an event has zero task rows — so a speaker pushed onto a fresh event is invisible on /admin/speakers even though the push worked, the count query reads the roster directly, and the cell query is already guarded for the empty case. The graded "added but not findable" defect was never in the write path. When a surface early-returns on a SECONDARY axis being empty, ask what the PRIMARY axis would have said.
- A HISTORY OF POST-EDIT SNAPSHOTS CANNOT RESTORE ANYTHING (DEC-158 w59): `revisions.ts:28` stores the state AFTER each edit, so the submitted text is never stored (the first edit destroys it) and the newest revision always equals the live row — making the only Restore a once-edited submission offers a guaranteed silent no-op that returns 200. A control that changes nothing and says nothing is broken whichever half is at fault; the fix is a baseline row plus a loud refusal, not a better label.
- FOUR FINDINGS RE-CHECKED AND ALREADY CLOSED, do not re-file (w59): CSV import's name+company duplicate warning EXISTS end to end (`findImportDuplicateCandidates` at `duplicates.ts:170` → `planImportRows` `possibleDuplicates` at `import.ts:557-571` → rendered at `ImportWizard.tsx:754-758`); add-to-event's duplicate guard already relabels its primary "Add another session" with a counted advisory (DEC-795, `AddToEventModal.tsx:79,122,185`); publish already reports `{placed, public, heldBack}` with the reason (`Agenda.tsx:267-272`); README already states `/dev/mailbox` is deliberately not mounted in production (`:33-37`). The `SBEK-PORTAL-BIO-01` seed marker is not in `scripts/seed.ts` at all.
- A HIDDEN CONTROL WITH NO OFFSET IS STILL ON THE SCREEN (DEC-919 w59): `.chq-visually-hidden` (`public/css/chrome.css.ts:22-32`) is `position:absolute` with no `left`, so it keeps its STATIC position — the hidden search submit lands one pixel inside the input's right edge and intercepts every pointer aimed at it. Clipping an element to 1px does not move it.
- WAVE 60 IS A CODE WAVE (DEC-069 w60): no gate lane, no index section, no seq allocated. Main is `b214b999` ("Prod-like load test filed") — the loose ref read stale AGAIN; the reflog FILE TAIL (`.git/logs/refs/heads/main`, lines 2532-2540) had advanced nine entries past wave 59's read point `3b7c9e01`, landing the Gate-10 sweep verdicts, the scribe, five w58 merges and the prod-like load test. Wave 58 is now FULLY merged except `-e`/`-f` (refs exist, unmerged). Wave 59 lanes `-a`/`-b`/`-c` are cut; `-d` through `-i` are STILL IN THE SHARED QUEUE — a planned-but-uncut lane is OWNED. OFF-LIMITS, all nine w59 scopes: `tasks/grid.ts`'s roster early-return + the add-to-event success link, the import wizard's event coupling, the task `contactIds` wire (both halves), the submission-revision baseline + Restore UI (both halves), the disabled-embed body, `public/css/chrome.css.ts`'s visually-hidden offset, the speakers per-task filter collapse, and the public drill-in day/filter carry. Plus w58's unmerged `-e`/`-f`.
- A CONFIRM WITH NO ARMING IS THE MIRROR OF A DELETE WITH NO CONFIRM (DEC-941 w60): `ViewTabs.tsx` has the state (:218), the dialog (:311-320) and the real `apiDelete` (:253) — and `setPendingDelete` is called at exactly two sites, both passing `null`. A saved view can never be deleted, and the DEC-941 scan, every render test and the type-checker all pass. When a scan asserts that a control IS guarded, ask separately whether the guard can ever OPEN.
- A SECOND COPY OF A VOCABULARY IS A TRAP WITH A DELAY FUSE (DEC-613 w60): `SessionboardImportPanel.tsx:47-52` re-declares the import target fields WITHOUT `externalId`, which `sessionboard.ts:76-81` declares first and `planSessionboardRows` refuses every row without (:275). The server auto-maps only when the posted mapping is EMPTY (`import.ts:90-91`), so touching one pill arms the trap for the whole file. Same shape as DEC-817's route map and DEC-851's unread knob — the domain already owned the list.
- THE PROMISE AND THE RECEIPT ARE COMPUTED BY DIFFERENT DOORS (DEC-238 w60): `send.ts:120-164` dedupes in two stages; `preview.ts` does not dedupe at all; so `ComposeWizard.tsx:1244` promises `preview.length` and `:1265` reports `sent of sent+failed`, a denominator with no room for a skip. 36 → 6. When two routes render the same batch, check whether they run the same arithmetic, not just the same query.
- FOUR MORE FINDINGS CHECKED AND ALREADY CLOSED OR NOT-A-DEFECT, do not re-file (w60): the closed-plan organiser queue gate LANDED (`reviewer.ts:161-167`); the CFP deadline day-boundary is already timezone-correct end to end (`root.tsx:130-133` → `formatEventCloseDateLabel` + `daysUntilCalendarDay`, DEC-918); the evaluation/participant/view caps stay count-then-insert by DEC-100's own ruling (a cap is not an identity); and the "speaker headshots render as blank navy rectangles" defect is THE FIXTURE — `docs/fixtures/headshot.png` is a solid #4A5A8C square, the serve route and `<img>` path are correct. Read the bytes before root-causing a renderer.
- A POPULATION DEFINED BY A DIRECTORY MISSES THE OTHER HALF OF THE PRODUCT, TWICE (DEC-383 w60): `interaction-states.scan.test.ts:11-16` sweeps `app/src/**/*.css` and calls itself complete by construction — and it is, for the SPA. The server-rendered half (`src/views/theme.ts`, every `src/routes/**/*.css.ts`) has zero interaction states and is invisible to it, exactly as DEC-367/DEC-808 found for the tap floor one wave earlier. The tell was a live bug: `theme.ts:172`'s global `a:hover` repaints the public primary CTA's LABEL to the hover brand on the un-hovered fill.
- WAVE 61 IS A CODE WAVE (DEC-069 w61): no gate lane, no index section, no seq allocated. Main `30f4e7a9` ("scribe wave 60"); read the reflog FILE TAIL (`.git/logs/refs/heads/main`), three campaigns share it. Wave 60 lanes `-a`..`-f` are cut (refs exist, zero commits); `-g`..`-j` were planned and are STILL IN THE SHARED QUEUE — a planned-but-uncut lane is OWNED. OFF-LIMITS, all ten w60 scopes: `ViewTabs.tsx`'s saved-view delete + the DEC-941 scan, the Sessionboard mapping picker, `src/views/theme.ts` + `src/routes/**/*.css.ts` interaction states + the `a:hover` primary, the submission PATCH read wave, the compose dedupe planner behind preview AND send, the settings People header grid, the portal co-presenter input widths, `/portal/preview`'s not-shown chips, `ProgressPanel`'s two reminder scopes, the agenda header numbers + conflict badge. Also off-limits: all nine w59 scopes and w58's unmerged `-e`/`-f` (`files-content-status.ts` scoping, `submit-post.tsx`'s budget refund).
- A LADDER IS NOT A GATE (DEC-370 w61): three routes serialize independent statements only because an EARLIER one can refuse — PUT slot (4 waves, 450ms), `review/submissions/:id` (8 awaits, 6 independent), `files-library.ts` (8 awaits, zero `Promise.all`). Over real D1 the thrift costs a round trip per link and saves nothing. Issue the wave, then evaluate refusals in SOURCE order; a discarded read is cheaper than a serialized one. Two shapes recur: an argument passed only to be compared in SQL (`roomBelongsToEvent(db, roomId, eventId)`) is a fake dependency — read the room's own eventId and compare in JS; and a lookup whose only caller feeds it the previous lookup's key (`getEventInfo(ownership.eventId)`) is a join wearing two function names. LEFT JOIN, so "submission missing" and "event missing" stay two refusals.
- A DENOMINATOR MADE OF ROWS THAT EXIST CANNOT COUNT WHAT IS LATE (DEC-596 w61): `SubmissionDetailPage.tsx:1147` divides by `evaluations.length`, so one assigned-but-unscored reviewer reads "Reviews · 0 of 0 in" — the screen says nobody is expected exactly when somebody is overdue. Every sibling surface already separates assigned from completed (`assignedExcludingRecused`, "Open · N of M reviews in"); this one screen invented a second arithmetic. When a progress fraction is built client-side from a list, ask what the list is a list OF.
- ONE ROW ELECTED TO SPEAK FOR THE REST IS A LIE WITH A QUORUM (DEC-936 w61): `speaker-detail.ts:307` returns a singular `participation` triple from an elected `primary` while the roster beside it renders a chip per submission — a two-session speaker gets a header contradicting its own rows. No precedence ladder fixes it; "confirmed here, not invited there" is a real state. The rollup says `'mixed'` and names each ref, and the elected row stays only because it is the WRITE target.
- A REFUSAL CAN BE DISCREET WITHOUT BEING USELESS (DEC-211 w61): `reviewer.ts:359/:444` answer a reviewer who is already assigned to the plan — and therefore already knows the event and its queue size — with the same blank "Submission not found" that hides a stranger's org. Existence-hiding is scoped to what the caller cannot already infer: inside the plan they passed `requireAssignedPlan` on, name the queue; outside its event, stay blank. The STATUS CODE never moves, only the sentence.
- WAVE 62 IS A CODE WAVE (DEC-069 w62): no gate lane, no index section, no seq allocated. Main `80a3eac3` ("scribe wave 61") — read the reflog FILE TAIL (`.git/logs/refs/heads/main`, lines 2543-2557), three campaigns share it. Wave 59 is FULLY merged; wave 60 `-a`/`-b`/`-f` merged, `-c`/`-d`/`-e`/`-g`/`-h`/`-i`/`-j` refs exist UNMERGED; wave 61 `-a`..`-e` are cut and `-f`..`-j` were planned but never cut — a planned-but-uncut lane is OWNED. OFF-LIMITS: all ten w60 scopes and all ten w61 scopes (agenda slot PUT, reviewer.ts scorecard+score PUT+404 sentence, the reviews denominator, breaks editor, embed Turn-off confirm + contact-drawer labels, reviewer scope layering, history changed-fields, scorecard a11y groups, speaker-detail rollup, files-library batching).
- A LADDER IS A PAGE, NOT A QUERY (DEC-370 w62): four more read paths measured at plan time are ladders whose dependency graph is 2-3 levels deep — `tasks/grid.ts` (9 statements), `portal/data.ts` (4 + 5), `submissions/list.ts` (4 head + SIX per-page hydration loops = ~10 RTT on the organiser's most-used screen), `contacts/crud.ts`. One Promise.all per LEVEL; per-page hydration is ONE level, chunk loops included; keep the array in the sequential order (overview.ts:62-64 says why: call-order mocks); refusals evaluate in SOURCE order after the wave.
- A WRITE'S ECHO COMES FROM THE WRITE (DEC-948 w62): `tasks/crud.ts:472-490` updates then SELECTs the same row — two RTT on the highest-frequency organiser write and an echo that can report someone else's value. `.returning()` on the same statement; an empty array IS not-found and keeps the loud throw. Five sites already do it (rate-limit:80, files-content-status:133, participants:126, profile:240, recusal:54).
- A MINT IS THE LAST THING A SEND DOES (DEC-397 w62): `send.ts:96` mints above BOTH the templateId 400 (:174-183) and the dedupe (:128-164); `bulk-email.ts:207` above its own (:224-250). `claim.ts:81-96` demotes the prior grant to 48h, so a `skipped` recipient loses a link that was already delivered and never gets the replacement. Mint below every refusal, for survivors only; dedupe on the UNMINTED render (a minted subject carries a unique token and can never match a prior send).
- A DURABLE WRITE IS A POINT OF NO RETURN (DEC-547 w62): `content-notes.ts:110` commits the unpublish, then `resolveBaseUrl` (throws without PUBLIC_BASE_URL outside dev) and `resolvePortalLinks` (KV) can throw. Config reads hoist ABOVE the first write; side-effecting IO stays below it but inside the {sent, failed, recipients} envelope. Fail-loud is preserved either way — the throw just moves to where nothing is half-done.
- SIX PROBES THAT FOUND NOTHING, recorded so they are not re-spent (w62): session auth is ALREADY one joined query (`middleware.ts:82-105`, DEC-276) — there is no per-request auth ladder to fix; `plans-progress.ts` and `review/shared.ts` rank/hydrate are already Promise.all'd; `parseResultsSort` and `parseOnboardingGridQuery` are already loud (only `parseListQuery`'s `sort` was not); the seed has NO duplicate submission title (`synthTitle(i)` is 1:1 over 27 distinct topics, no collision with the three fixture titles) so Gate-8 item 30 does not reproduce; the deliverable-kind/headshot gap is a deliberate DEC-773 split plus `completeProfileTaskForContact` (`profile.ts:207`); the v11 ConfirmDialog irreversible population is complete and the missing third site (event delete) has no SPA control at all, recorded at `ConfirmDialog.tsx:20-25`.
- `db.batch` IS USED NOWHERE IN src (only named in a comment at `submissions/status.ts:499`). One D1 batch is one round trip for N statements and would collapse every write fan-out the load test flagged — but it changes atomicity and needs mock-db support, so it is a decision + its own lane, never a drive-by.
- WAVE 63 IS A CODE WAVE (DEC-069 w63): no gate lane, no index section, no seq allocated. Main is `60c22e16` ("thunderdome medic: green main -- cross-org ledgers, verdict ratchet, fixed-table width, npm-run silence") — the reflog FILE TAIL (`.git/logs/refs/heads/main`, lines 2543-2560) ran nineteen entries past wave 62's read point, landing all of wave 59, w58 `-e`/`-f`, w60 `-a`/`-b`/`-f`, two scribes and a megabatch. Refs that exist UNMERGED: w60 `-c/-d/-e/-g/-h/-i/-j`, w61 `-a`..`-j`, w62 `-a`..`-e`; w62 `-f`..`-j` were planned and never cut — a planned-but-uncut lane is OWNED. OFF-LIMITS: all remaining w60 scopes, all ten w61 scopes, all ten w62 scopes (onboarding grid / portal home+detail / submissions list / contacts-crud batching, the task-write echo, the send mint ordering, the content-note config hoist, the `?sort=` refusal in `submissions/query.ts`, the SPA-mutation scan exemptions, the mandate re-pin).
- A GATE FINDING IS A CLAIM ABOUT A SNAPSHOT, NOT ABOUT MAIN (DEC-903 w63): gate-10's "armed click-to-place cannot land on an OCCUPIED agenda slot by mouse" does NOT reproduce — `DayGrid.tsx:256-263` already places when armed, and `SessionCard.tsx:84` wires the click. What is actually wrong is smaller and was invisible to the gate: the placed card's `aria-label` still says "click to select, then choose a new slot" while the cell button in the SAME slot says "will clash with N sessions". Two controls stacked on one slot must not describe one write two ways. Also verified absent: the whole B8 drag vocabulary (no dragging opacity, no origin well) — the free-slot half shipped, the other two never did.
- FIVE MANDATE ITEMS VERIFIED CLOSED ON MAIN, do not re-file (w63): both gate-10 P0 BROKENs (`ViewTabs.tsx:293` now arms `setPendingDelete(view)`; `SessionboardImportPanel.tsx:41-52` now derives its pills from `SB_TARGET_FIELDS` including `externalId`), the ConfirmDialog irreversible weight (`ConfirmDialog.tsx:52-58`, throws when a phrase is missing), the CFP per-email budget refund (`submit-post.tsx:396,446` via `refundEmailBudget` with ONE hoisted `budgetNow`), and `updateContentStatus`'s eventId re-scoping (`files-content-status.ts:54`). Plus `parseRoundQuery` (`review/shared.ts:335`) and the public `?trackId/format/day/limit/accent` parsers are deliberately, documentedly loud-or-degrading — not DEC-843 candidates.
- A SILENT FALLBACK HAS SIBLINGS IN OTHER FILES (DEC-843 w63): the submissions `sort` swallow has an exact twin at `contacts/query.ts:35`, and its CSV export inherits it through `exports.ts:113`. Both doors ALREADY convert a thrown parser error into a 400, so the fix is one reader in the pure module and nothing at either route. When a doctrine names one parser, grep for the ternary shape, not for the decision number.
- FOUR PROBES THAT FOUND NOTHING, recorded so they are not re-spent (w63): DEC-547's "config read above the first durable write" is clean everywhere except the one route already owned (`tasks.ts:653/681` evaluate `resolveBaseUrl` as an argument, `portal-invites.ts:49` sits below every refusal and above the mint); the A4 first-paint spec is fully enforced (`loading-first-paint.scan.test.ts:88` bans `@keyframes` in the skeleton sheet); SPA code-splitting AND hover/focus chunk prefetch both exist (`App.tsx:36-50,113,185-186`) — the only unbuilt half of SPEC §7 is prefetching route DATA, which needs a request cache and would break the call-order mocks; and headshots are served with a deliberately reasoned scope-aware `Cache-Control` (`portal/profile.tsx:444-463`), not a missing one.
- WAVE 64 IS A CODE WAVE (DEC-069 w64): no gate lane, no index section, no seq allocated. Main is `1dbe7e44` ("merge task-w62-f") — the reflog FILE TAIL (`.git/logs/refs/heads/main`, lines 2561-2584) ran twenty-four entries past wave 63's read point, landing w60 `-c`..`-j`, all ten of w61, w62 `-b/-c/-d/-f` and two megabatches. Refs that exist UNMERGED: w62 `-a/-e/-g/-h/-i/-j`; w63 `-a`..`-e` are cut and `-f` was planned but never cut — a planned-but-uncut lane is OWNED. OFF-LIMITS, all six w63 scopes: the contacts `?sort=` refusal + its CSV export, the portal task-assignment `contactId` predicate + the portal-reachable-mutator scan, the four slow-op pending buttons, the agenda drag vocabulary + the placed-card aria parity, the saved-embed card anatomy, and the "Round 1 of 1" suppression. Plus w62's unmerged scopes.
- A MOCK THAT MATCHES NOTHING IS A CONTRACT WITH ITSELF (DEC-663 w64): `contacts/import.ts:412` ships `possibleDuplicates: ContactRecord[]` (`{id, firstName, …}`) while the only reader — `types.ts:150`, `ImportWizard.tsx:799` — prints `dup.contactId`/`dup.name`, so the graded P1-1b warning renders "May be a duplicate of  ()" under `key="dup-undefined"`, and the render test is green because its fixture hand-writes the shape the server never sends. The mandate item was open for the WIRE, not the feature. When a finding says a feature "does not surface", diff the emitter's type against the reader's type before rebuilding either.
- A DECLARED DURATION WITH NO READER IS DEC-851 IN CSS (DEC-851 w64): both sheets declare six `--chq-motion-*` tokens and a reduced-motion rebind for all six; only `--chq-motion-color` has a reader. B8's 180ms appear and 220ms geometry are documented, tokenised, reduced-motion-safe and never happen. Spend the two enter tokens, DELETE the three `-exit` tokens from both sheets in one commit (symmetry keeps DEC-372 parity), and make the scan require a reader per declared token — a token block is a population like any other.
- FIVE PROBES THAT FOUND NOTHING, recorded so they are not re-spent (w64): the reviewer queue is already two Promise.all waves plus post-slice hydration (no DEC-370 ladder left in it); the hotel-stay and flight-reimbursement onboarding forms that `docs/clarifications.md:36` calls must-have are in `acceptance.ts`'s `DEFAULT_ONBOARDING_TASKS`; ConfirmDialog now guards tracks Remove (`TracksRoomsPanel.tsx:789`), participant Remove (`SubmissionDetailPage.tsx:1848`) and the saved-view delete; `FieldModal` already carries the blast-radius header, the hidden-never-required rule and the conditional sentence; and B8's touch rules (`-webkit-tap-highlight-color`, `:active`) exist in BOTH sheets. Also adjudicated NOT-A-DEFECT and closed: Gate-8 item 14 co-presenter identity (DEC-866 w64) — lists render names, the two relationship surfaces name the role, and `participant-roles.ts` is already the single vocabulary.
