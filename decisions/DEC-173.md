# DEC-173: Walkthrough selectors assert semantics and tolerate markup wrappers; harness-repair lanes validate by running the full six-module walkthrough locally

task-w5-c's walkthrough FAIL @ 64ec7de was entirely harness-side: scripts/walkthrough/public.ts:440 and scripts/walkthrough/speaker.ts:915 extract speaker names with /<strong>\s*([^<]+?)\s*<\/strong>/ while the live pages render <strong><a href=...>Name</a></strong>, so the extractors match zero names on pages that are visibly correct. Rule: walkthrough selectors must tolerate an optional anchor (or other inline wrapper) inside the asserted element — they verify semantics (names present, ordered), never exact markup. A harness-repair lane must prove its fix by running the complete six-module walkthrough against a freshly seeded local wrangler dev, fixing any FURTHER stale selectors it flushes out in scripts/walkthrough/** only; if a check fails because of PRODUCT behavior, the lane reports it and never touches src/ or app/.
