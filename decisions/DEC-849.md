# DEC-849: A committed conflict marker is a build break, not a formatting nit — the guard is a scan, and the saved-embed resolution checks DISABLED the moment the row is found

VERIFIED on main, not inherited: src/routes/public/saved-embed.tsx carries THREE unresolved hunks (`<<<<<<< HEAD` at lines 1, 36, 51, `>>>>>>> task-w9-c`) and test/saved-embed-route.test.ts carries two (lines 128, 142). The file cannot typecheck, so every dev-green claim made since that merge is worthless for this route. Nothing in the repo scans for this class, which is why it survived two waves. Decision, two halves. (a) The resolution: keep the w9-c header wording, keep `import { DEC_822, DEC_839 }`, DELETE the dead `StoredEmbedOptions`/`parseStoredOptions` (DEC-839 already re-hydrates options in src/server/repo/embeds.ts — a second parser is the drift it was minted to remove), and order the handler `getEmbedById` -> `!embed` 404 -> `!embed.enabled` empty 200 -> `!isSurface` 404. DISABLED is checked the instant the row is found, BEFORE any further validity test, because 'the organiser switched it off' is a state the organiser chose and must not be overridden by a data problem the visitor cannot see or fix; the empty 200 keeps setCacheHeaders' headers (never publicNotFound's no-store) so a CDN cannot serve a stale enabled render. (b) The guard: a scan test enumerating src/, app/src/, test/, scripts/, migrations/, decisions/ and failing by file:line on any conflict marker. Same family as every other scan in test/ — a rule enforced by habit is a rule that will be broken by the next merge under time pressure.
