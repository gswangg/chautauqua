# DEC-348: DEC-201/DEC-202 REAFFIRMED against the wave-20 review lens: the global public-cache bump stays stage-1-accepted; and three lens findings are recorded as verified STALE at the symbol

The wave-20 review lens raised the global cache purge as its top finding: src/server/pubcache.ts:105 bumpIfMutating gates only on HTTP method and status, mounted globally at src/server/app.ts:33, so logins, reviewer rating PUTs, task check-offs and CFP draft autosaves all rotate chq:pubver. That is a true description of the code and it is ALREADY ADJUDICATED — DEC-201 accepted it explicitly for stage 1 ('correctness-safe: public pages can never be stale... a route-by-route purge predicate risks serving stale public data for zero stage-1 benefit — it belongs with stage-2 production cache/perf validation') and DEC-333 reaffirmed it ('201/202 stage-2-only stay CLOSED'). Nothing in the tree has changed that calculus: the local perf gate passes every public-surface budget (5.6-90.0ms raw at 2k scale, w18-d), Miniflare's caches.default is not an edge cache, and SPEC §7's cache-hit TTFB budget is a production measurement deferred to stage 2 by SPEC §0's own staging rule. Re-opening it would spend a lane manufacturing a new staleness risk class for zero stage-1 observable benefit, against DEC-315. It stays CLOSED; a future planner reading the same lens finding should read this decision and stop. Recorded in the same breath, verified live at the symbol per DEC-328 so they are not re-derived: (a) 'admin agenda and public agenda disagree after a date edit' is STALE — src/server/repo/public/agenda.ts:40 and :120 both apply slotWithinEventRange(event), as do public/sessions.ts:238 and public/detail.ts:43, so every public surface classifies an out-of-range slot exactly as the former src/server/repo/agenda.ts:280 (now split across src/server/repo/agenda/) does (DEC-277); (b) 'bulk onboarding remind is uncapped and un-deduped' is STALE — src/server/repo/tasks.ts:701 routes through planManualReminders and returns skipped/remaining (DEC-319); (c) 'submissions list abandons SQL pagination above 100 filter matches' is STALE — src/server/repo/submissions/list.ts:71-105 is one paginated statement with correlated EXISTS (DEC-335); (d) 'Overview hard-crashes at scale on Math.max' is STALE — src/server/repo/overview.ts:191 is a single count/max aggregate (DEC-334).
