# DEC-145: Seed is the grader package, so it must exercise every flow at 'now': plan window opens 2026-01-01Z; the demo speaker gets an accepted session, onboarding tasks, a two-version deliverable with a comment thread; several contacts get real headshots; near-duplicate contacts stay

The seeded evaluation plan opens 2027-04-01, i.e. in the future at any realistic grading time — the reviewer queue is correctly-but-uselessly empty, which the eval read as a product bug. Fix deterministically (no Date.now in seed values): open_date Date.UTC(2026,0,1), close_date unchanged (2027-05-20), so the window spans the entire grading period. Per mandate Section E, Priya Raman (sbek-speaker@example.com) additionally gets: at least one ACCEPTED submission with its participant row, 2-3 task_assignments including a form/file_request task with due dates; one submission file with a second version chained via previous_file_id and an organizer<->speaker file_comment thread (R2 objects from docs/fixtures/slides.pdf via scripts/seed-r2.ts); several contacts get headshotUrl backed by docs/fixtures/headshot.png R2 objects so the public speakers/gallery show real images. The seeded near-duplicate contacts are the dedupe test vector and must be preserved. All ids stay deterministic via seedId so re-seeding never invalidates saved itineraries or eval transcripts.
