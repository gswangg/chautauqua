# DEC-313: Wave 13 is the CLOSING battery: five live sections re-run (a build-test, b walkthrough, c perf-smoke, d render-sweep, g fresh-clone) plus two closure lanes (i citation-delta, j eval-findings §D) — because wave 12 changed exactly what those sections measure

I verified against refs/heads/main rather than inheriting DEC-307's picture: at my read main is fa37629 and the ENTIRE wave-12 set has landed (task-w12-a/b/c/d/e merged at .git/logs/HEAD:122-126) plus task-custodian-w12-2 at :127. All five wave-12 anchors are live on main: package.json:20 `"drizzle-orm": "^0.45.2"` with no drizzle-kit (DEC-308), scripts/perf-smoke-lib.ts:18 PERF_CLASS_BUDGET_MS + :80 gradePerfCheck and scripts/perf-smoke.ts:336 `cls: "public"` on the schedule.ics check (DEC-309), src/server/repo/public.ts:768 getPublicAgendaByIds called from src/routes/public/index.tsx:197 with test/schedule-ics-id-scoped.test.ts (DEC-310), src/routes/dev/mailbox.tsx:44,92 viewport meta + src/routes/docs.tsx:242 overflow wrapper + scripts/render-sweep.ts:80-81 manifest rows (DEC-311), src/server/repo/tasks.ts inArray(...ACTIVE_INVITE_STATUSES) (DEC-312). So the three RED sections' causes are all fixed and nothing in the fix set is still in flight — the only thing missing is green evidence at ONE sha. Two independent facts make this more than ceremony. First, drizzle-orm went 0.36.4 -> 0.45.2: nine minor versions of an ORM whose runtime SQL generation is exercised ONLY against real D1, while `tsc --noEmit` and the fake-db unit tests cannot see a generated-SQL change at all. The live-D1 lanes (b walkthrough, c perf at the 2k corpus, d render-sweep, g fresh clone) ARE the acceptance test for that upgrade; that is why b and g are re-run even though they were PASS at 84e2c04, and why any 500 or SQL error must be logged verbatim rather than retried past. Second, task-custodian-w12-2 decomposed the 832-line src/routes/review.ts into src/routes/review/{index,plans,reviewer,recusals,shared}.ts — a weight-20 area (J4) refactored to compose three Hono sub-apps in a new registration order, landing AFTER every wave-11 section froze. Sections e and h stand on their wave-11 PASS per DEC-280 (substance, not sha-identity), but h's evidence column cites `src/routes/review.ts:247/:358/:403/:569/:609/:785/:815/:636-640` for fourteen rubric rows and that path no longer exists — dead citations are section i's job, not a reason to re-run h wholesale.
