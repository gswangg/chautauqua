# DEC-166: CI render-sweep job shape: npm ci, playwright chromium install, npm run gate:render-sweep — no manual migrate/seed/dev-server steps; ci.yml edits are code-bearing and land only in the preflight lane

Eval-findings Section F.1 (binding for exit per DEC-139) requires the render-sweep 'wired into CI as a third gate alongside walkthrough + perf-smoke', but .github/workflows/ci.yml has no such job — the only genuine code-bearing gap left. scripts/render-sweep.ts is self-bootstrapping (applies migrations, seeds D1+R2, boots wrangler dev on a self-selected free port, kills it after), so the CI job must NOT duplicate the walkthrough job's migrate/seed/health-poll steps: it is exactly checkout, setup-node 20 + npm cache, npm ci --no-audit --no-fund, npx playwright install --with-deps chromium, npm run gate:render-sweep. Because workflow config counts as code-bearing under DEC-069/DEC-114 (every prior gate treated 'config' as invalidating), this edit must merge before the battery sha freezes — hence it lives only in task-w5-a and no gate lane may touch ci.yml.
