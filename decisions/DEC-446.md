# DEC-446: SPEC §10 #2 (decision-meeting view) and #4 (resubmit-with-guidance) close as out-of-stage-1-scope; DEC-441's 'waits exactly one more wave' resolves to 'not in stage 1'

DEC-441 postponed #2 one wave for a specific, now-expired reason: its honest implementation calls buildResults, which task-w15-b was rewriting. That rewrite has landed (src/routes/review/shared.ts:259 now passes `{ withTrackIds: false }`), so the collision is gone and the promise comes due. I am resolving it as a close, not a build, for three reasons that are about scope rather than appetite. (1) This run's stage-1 charter is 'the entire product surface SPEC.md jobs J1-J12'; SPEC.md:390 gates §10 as nice-to-haves after J1-J12 are green, and both the w14-h and w15-f ledgers score every J-row VERIFIED with zero unowned FAILs -- §10 items are outside the completion definition I was given, not inside it. (2) #2's honest implementation is not a lens, it is schema: 'live slot countdown by track' requires a per-track target (the former src/db/schema.ts:384-398 (now split across src/db/schema/) track has id/eventId/name/color/position and no capacity), and 'accept-with-condition note' requires a column on submission. A new migration is the single change category that invalidates the 18-migration set, the 2k-row perf seed, and the render-sweep route manifest simultaneously, on a tree whose mobile and type-floor gates only just became blocking. (3) #4's cost is unchanged from DEC-437: it widens DEC-003's frozen five-literal status set (src/domain/status.ts:5-18), which reaches status filters, exports, public gates, the SPA, and the API surface at once. Both are recorded here as stage-1 non-goals and README roadmap items; every completion ledger from here scores them DEFERRED-BY-DECISION -- never GAP, never PENDING-OWNED.
