# DEC-316: docs/eval-findings.md §D (P3 polish) is CLOSED by live verification this wave, and graded functionally: a row is an OPEN ITEM only if the user action actually fails; latency/'occasionally' observations from campaign-1's deployed run are out of stage-1 local scope

task-w11-f-c3-triage-closure.md PASSED but self-flagged the one gap in the whole battery: the §D rows were spot-checked by static file:line reading because 'this lane was not instructed to open port 8794 speculatively'. That is precisely the self-declared scope narrowing a reviewer flagged as a process risk, and leaving it unresolved would let the campaign exit with an acknowledged blind spot. So it gets closed by instrumentation, not by argument — lane j boots the app and drives each row. But the grading rule is a planner decision, not a worker judgement call, because §D was written against a LIVE production deployment over real network latency and half its rows are irreducibly about feel: a row is CLOSED when the action works and its result is observable after a reload (with file:line evidence), an OPEN ITEM when the action silently fails, 500s, or its result never appears, and OUT-OF-SCOPE when the observation is purely latency/intermittency that cannot be reproduced against local wrangler dev ('first login click occasionally no-ops ~3s', 'Files rendered empty then showed rows ~10 min later'). Missing accessible names count as an OPEN ITEM only when a control has NO <label>, no aria-label and no adjacent visible text — SPEC §9 has the sbek agent driving this UI, so a genuinely nameless control is functional breakage, while a merely-unstyled one is not. Each row is timeboxed to about a minute of probing; a row neither reproduced nor refuted is recorded UNRESOLVED with what was tried, never guessed.
