# DEC-834: At an irreversible choice, a column is headed by the RECORD, not just by the person

VERIFIED MergePage.tsx:216-220 heads the compare table with the keeper's first+last name and the discard label, while the pick list two elements above (MergePage.tsx:203-205) already renders 'name — email — company — title'. For a same-name duplicate pair — the exact pair DEC-800's 'Same name, different company' reason exists to surface, and the pair DEC-823's seed is about to ship — both columns then print the identical string at the moment of a merge that cannot be undone. Decision: whenever the compared records' display names collide, both column heads carry the same disambiguator the pick list already shows (email, else company), so the reader is never asked to choose between two labels that read identically; and the merge screen states the second rule it obeys, 'Labels combine, notes are appended' (DEC-266/DEC-738), beside the label footnote it already prints — the rules that are NOT 'pick a side' must be visible before committing.
