# DEC-190: Adjudications: late-drained wave-11 battery is historical-only; w11-f's FAIL was a sibling merge race; the AIRTABLE_TOKEN open item is CLOSED for stage-1 by operator commit 629d57e

Verified against the ledger tail and reflog, correcting DEC-188's dead-lane record: wave-11's battery DID fully drain after the wave-12 planning snapshot — task-w11-a/b/c/d/e all RESULT: PASS @ 7561cc1 (including render-sweep 31/31), merged as 5f5a5fc/431a1c0/8d8888/9fc982c/0e84870, and task-w11-f — triage-closure @ 7561cc1 recorded 'FAIL — precondition' solely because it read the ledger before task-w11-d's section merged (its own checks were all green). Rulings: (1) all @ 7561cc1 sections remain historical-only and void for exit purposes because code-bearing 629d57e postdates them (DEC-188 stands); w11 lane names stay dead; the w11-f FAIL is a procedure lesson (hence DEC-189's sibling-wait rule), not a product defect — no fix task exists for it. (2) task-w11-b's OPEN ITEM — commit bdc472b ('scribe wave 11') added an AIRTABLE_TOKEN line to the then-tracked .dev.vars — is CLOSED for stage-1: operator commit 629d57e untracked .dev.vars, .gitignore re-excludes it, and the tracked .dev.vars.example carries only DEV_MODE=1, so no secret is required or shipped by the stage-1 tree (zero-secret invariant restored at head). Rotating the leaked token and purging it from git history are operator-scope remediations outside stage-1; workers must NEVER rewrite git history, and per DEC-188(6) never read or print the local .dev.vars. The wave-13 triage-closure cites this decision when it encounters the w11-b open item during its closure scan, rather than re-raising it.
