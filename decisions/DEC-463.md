# DEC-463: The react-router v6 advisories are closed as NOT-APPLICABLE to stage 1, with citations, rather than left drifting unowned

task-w14-e-build-test-stage1.md flagged GHSA-wrjc-x8rr-h8h6, GHSA-337j-9hxr-rhxg and GHSA-jjmj-jmhj-qwj2 against react-router-dom@^6.28.0 (package.json:35) as "PENDING-OWNED... no branch assigned; flagged for planner triage", and task-w17-e carried it forward as open item 2 across four waves with no decision. All three are React Router framework/SSR-mode issues (pre-render data spoofing, SSR cache poisoning). The admin SPA is client-only: app/src/App.tsx:2 imports BrowserRouter/Navigate/NavLink/Route/Routes/useLocation from react-router-dom, there is no createBrowserRouter or RouterProvider anywhere under app/src (the `loader:` keys at app/src/App.tsx:44-64 are the app's own prefetch functions hanging off its own nav table, not react-router data-route loaders), and nothing server-renders react-router — the SSR surfaces under src/routes/ are Hono JSX. The vulnerable code paths are not reachable in this product. No v7 migration in stage 1: it would churn every page component and invalidate the render-sweep evidence for a non-applicable advisory. A stage-2 wave may bump it when it can re-run the sweep.
