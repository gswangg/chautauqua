# DEC-202: KV read-then-write rate limiting is ACCEPTED for stage-1; atomic counters are stage-2 platform wiring

checkAndIncrement/incrementScopedLimit (src/lib/rate-limit.ts) do non-atomic get→put on the KVStore port. The lost-update and eventual-consistency bypass only exists across multiple PoPs on Cloudflare's deployed edge; Miniflare's local KV is single-instance, where the SPEC §6 caps (login/claim/submit) function correctly and are covered by tests. The limiter already sits behind the KVStore port, so swapping to a Durable Object or D1 atomic counter is a contained stage-2 deploy task; stage-1 must not add Cloudflare-provisioned primitives.
