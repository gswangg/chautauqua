# DEC-174: Seed override guarantees the demo speaker keeps one pending general-kind task; global mod-3 completion formula and the DEC-172 pin unchanged

scripts/seed.ts:975's isComplete = (contactIdx + taskIdx) % 3 !== 0 marks both general-kind onboarding tasks (DEFAULT_ONBOARDING_TASKS indices 2 and 4) complete for contactIdx 0 — the sbek-speaker demo persona — so the speaker walkthrough's mark-complete round-trip can never pass and human graders cannot demo task completion (eval-findings E requires the demo speaker have exercisable tasks). Fix: keep the formula for realistic mixed state, add an explicit override forcing the contactIdx-0 assignment for 'Announce participation' (taskIdx 4, kind general) to status pending with completed_at/completed_by null. This cannot disturb DEC-172's routeManifest pin: seed_task_assignment_0001 is contactIdx 0 / taskIdx 0 ('Hotel stay requirement form', form-kind, pending under the formula) and the override touches only taskIdx 4.
