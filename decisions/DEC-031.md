# DEC-031: Saved views are server rows scoped to the event; Submissions toolbar gains Views dropdown, New submission modal, Clone row action

J3 names saved views and 5-15 concurrent team members share them, so localStorage is wrong: saved_view table per DEC-025, config_json = { q, status: string[], trackId, sort, columns: string[] } matching the landed w2-f filter/column state shapes exactly. API: GET/POST /api/v1/events/:eventId/views, DELETE /api/v1/views/:id (organizer + csrfJson; name required, non-empty). SPA: a Views dropdown at the left of the Submissions toolbar — applying a view replaces current filters/columns/sort; 'Save current as view…' prompts for a name and POSTs the live state; saved views list with per-view delete. Manual creation (J3 'manual session creation and cloning for invited talks'): a 'New submission' button opens a modal (title required, description, optional speaker email + first/last) posting to the landed POST /api/v1/events/:eventId/submissions; a per-row Clone action posts to the landed POST /api/v1/submissions/:id/clone; both navigate/refresh to show the new row. No new status semantics — created rows are 'pending' per the existing repo.

## Amendment: a saved view's config is bounded in every direction, and views per event are capped (wave 2)

DEC-031 fixed the saved-view SHAPE; nothing bounds its size. Verified in src/server/repo/views.ts: isValidSavedViewConfig accepts `q` of any length, a `columns` array of any cardinality with strings of any length, and a `trackId` of any length; src/routes/api/views.ts bounds only the view NAME and never limits how many views one organiser may create per event -- and every one of them is read back on the submissions page's tab strip, an unbounded surface that is never paged. Ruling: MAX_SAVED_VIEWS_PER_EVENT and MAX_SAVED_VIEW_COLUMNS live in a new pure-core src/domain/saved-views.ts; the config's string bounds REUSE the existing MAX_TEXT_LENGTH/MAX_NAME_LENGTH rather than minting new *_LENGTH names (which would land in the sibling text-cap scan's population for no benefit); and the 'Save this view' dialog states the count against the cap, because a cap the control does not disclose is the defect this swarm keeps re-filing.
