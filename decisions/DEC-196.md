# DEC-196: S'''' exit battery binds at 1033d45 ('merge task-w14-c'): fix-marker preconditions, lane-letter/gate-type mapping chosen against dead-campaign w15 homonyms, sixth-generation full-heading guard

Verified live mid-planning: task-w14-b (64a4687), task-w14-a (a8c8c69), and task-w14-c (1033d45) all merged; main is quiescent with zero task branches; all four DEC-191..194 fixes plus tests (test/email-log-null-contact.test.ts, test/form-render-rules.test.ts, app/src/pages/submissions/bulk.ts+bulk.test.ts) are on the tree; every other review-lens finding re-verified stale (DEC-179/180/181/182/183/187 surfaces all present). DEC-195's binding condition is therefore met. Bindings: (1) S'''' = newest code-bearing first-parent commit per DEC-114 at gate start, EXPECTED 1033d45; `git merge-base --is-ancestor 2dd2f33 S''''` and `--is-ancestor 7f7477e S''''` mandatory. (2) Precondition markers at S'''', any miss => 'RESULT: FAIL — precondition' and stop: 'DEC-191' AND 'contactId: null' in both src/routes/api/users.ts and src/routes/review.ts; 'data-required' in src/views/form-render.tsx; 'chunkSelection' and '/tracks' in app/src/pages/submissions/SubmissionsTable.tsx; the four new test/helper files above present in `git ls-tree -r S'''' --name-only`, which must also list .dev.vars.example and NOT .dev.vars. (3) DEC-189 cooperative semantics carry over verbatim: dedupe-by-citation only against a same-gate-type section whose FULL heading ends '@ 1033d45' with RESULT: PASS; exactly one appended ledger section per lane (DEC-068); triage re-reads the ledger every ~2 min up to 10 min before declaring a sibling missing; walkthrough port 8951, perf-smoke 8952, render-sweep self-allocates; no lane ever reads or prints a local .dev.vars. (4) SIXTH-generation homonym hazard: the ledger already holds dead-campaign sections named task-w15-a..k (@ 0ba550c, 7c4101c, ce451d9, 675219f) including same-gate-type near-twins, plus current-campaign w12/w13 sections @ 7f7477e; wave-15 lane letters are deliberately mapped (a=build+test, b=walkthrough, c=perf-smoke, d=render-sweep, e=triage-closure, f=spec-audit) so no lane-name+gate-type pair collides with any dead section, and every ledger search must still match the full heading including '@ 1033d45'. (5) Six PASS sections at S'''' — five gate types plus triage-closure with OPEN ITEMS: 0 — satisfy DEC-069/DEC-139/DEC-195; wave 16 verifies sha-validity per DEC-114 and declares stage-1 complete with zero tasks. Any later code-bearing merge voids the battery per DEC-069.
