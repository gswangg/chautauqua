# DEC-279: Wave 6 is a defect-fix wave; the eight-section exit battery moves to wave 7, the first wave whose planner emits zero product tasks (re-points DEC-270's wave number, keeps its declaration protocol verbatim)

Verified directly, not inherited: refs/heads/main is ade5aa7928925a72f289a0d421da450c87afb4a9 and .git/logs/HEAD line 71 shows the newest merge is 'merge task-w5-a' -- wave 5's remaining lanes have not landed. The working tree confirms it is mid-flight: app/src/pages/review/Recusal.render.test.tsx, Scorecard.tsx and ReviewerQueue.tsx already carry DEC-271's recusal wiring, while migrations/0017 does not exist and a case-insensitive grep for 'recus' across src/ still matches only src/decisions.ts -- the server half of DEC-271 is unlanded. Naming a frozen sha today would freeze a tree that is provably about to move, reproducing for a fourth consecutive round the failure DEC-270 was written to end. Independently, four review-lens findings were re-verified as REAL at HEAD and are fixed this wave (DEC-274..DEC-278), so by DEC-262's own binding rule -- 'the eight-section battery may only be scheduled in a wave whose planner emits ZERO product-touching tasks' -- wave 6 cannot be it. DEC-270 is amended in exactly one respect, the wave number: the battery runs in the first zero-product-task wave (expected: wave 7), its planner reads refs/heads/main after wave 6 merges and writes the 40-char literal into all eight task descriptions, and DEC-270's steps (detached worktree, ancestor check, post-S allow-list check, verbatim FROZEN SHA line, unanimity + OPEN ITEMS: 0) are unchanged and still binding. Stage 1 is declared there and nowhere else.
