# DEC-375: Settings stays one route; the section rail is client state and becomes a drill-in index at phone width

The mock shows a 196px sticky section rail plus seven separate phone subscreens, which reads like seven routes. Adding them would change app/src/routeManifest.ts and therefore the render sweep and docs route coverage, all frozen contracts under DEC-366. Settings keeps exactly one route, /admin/settings, with the active section held as component state in SettingsPage. At >=700px the rail renders as `.chq-rail` with the h1 above it and the active link marked; below 700px the rail renders as a full-width list of section links, and choosing one replaces the list with that panel plus a tertiary back control that clears the selection — the mock's phone 'subscreens' are that swap, not navigation. No URL, no history entry, no route-manifest change; deep-linking to a section is deliberately not added, because no requirement asks for it and it would widen a frozen contract.

## Amendment (wave 86): Settings at 390 is a group INDEX or a drilled section, never the rail stacked above its content — and the switch belongs to exactly one lane

Verified on main bfe5d43c: settings.css:158's phone block only sets `.chq-settings-layout{display:block}` and gives `.chq-settings-rail-link` a 44px height, so at 390 the rail list and the section content both render, one above the other. The pack draws something else. The landing (Chautauqua Settings.dc.html:275, extent →313) is a GROUP INDEX: 18px-rhythm rows of `{{ g.label }}` + an olive `Open` on the baseline, a 13px detail line beneath, a hairline between, a 27px H1, and a closing hint row (`Embed codes are easier to copy on a laptop`) that is copy, not a row. Every one of the eight drills (:316, :347, :393, :444, :481, :510, :552, :578) opens with the same head: a 44px `‹ Settings` link over a 26px title (the cluster-local register DEC-643's wave-83 amendment already ruled, carried by settings.css and never a third global token). RULING: at 390 Settings renders the index OR one drilled section, never both; the existing `?section=` URL state (DEC-728 wave-13/wave-49 amendments) is the selector — no new state, no new route. OWNERSHIP, so two lanes never decide one question: the index/drill switch and app/src/pages/Settings.tsx belong to the landing lane (v12m-w4-e) alone; the drill lanes assert only body anatomy (field rows, list rows, 44px inline actions, 2px section rules) inside settings.css's trailing phone block, which is independently testable without the switch. Forecloses: a per-panel `isPhone` branch in each of the eight panels, which would fork the back-link and title register eight ways.
