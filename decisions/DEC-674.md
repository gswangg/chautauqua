# DEC-674: The organiser Review landing is the three-region worklist reached without leaving the page

docs/eval-findings.md item 10b (user-confirmed against the mock) calls for plans list with progress bars + Export CSV, an INLINE reviewer-progress table with the remind action, and INLINE ranked results with Accept/Decline. app/src/pages/Review.tsx:57-66 still routes those as three separate pages and PlanList.tsx renders region one only, with 'Progress'/'Results' links out. Both other regions are already complete components -- ProgressPanel has 'Remind laggards (N)' rendered through describeSendResult (DEC-664, landed) and round advance; ResultsTable has ranked rows, per-row Accept/Decline (DEC-632) and its CSV export -- so this is composition, not new capability, and it must stay composition: the two components gain an optional planId prop and keep working standalone at their existing routes (deep links must not break). The landing selects a plan (default: the first plan whose window is open, else the first) and renders both regions beneath the list. A worklist whose work lives one navigation away is a report, not a worklist (SPEC §2 principle 3).

## Amendment (wave 58): the admin SPA never re-implements the plan window — it reads isPlanOpen through the plan's own timezone

Verified on main: app/src/pages/review/PlanList.tsx:28 and :40 and app/src/pages/review/PlanEditor.tsx:150 compare a day-label openDate/closeDate directly against Date.now(), while the server's src/domain/evaluation.ts:407-417 isPlanOpen expands openDate through dayLabelStartInstant and closeDate through dayLabelEndInstant in the event's zone. app/src/pages/review/ReviewerQueue.tsx:42 already does it correctly, so the SPA disagrees with ITSELF as well as with the server: on a plan's close day the producer's Review landing renders 'Closed' and picks a different default plan while reviewers are still legitimately scoring and PUT /evaluations still accepts writes. DEC-674 defined the window rule; this amendment names its single implementation. RULING: isPlanOpen (src/domain/evaluation.ts) is the only implementation of 'is this plan's window open'. PlanList's planState/isWindowOpen and PlanEditor's isPlanOpenNow delegate to it, passing `plan.timezone` — already on the wire (src/server/repo/review/plans.ts:41, app/src/pages/review/types.ts:54) for both the list and the detail response, so no new fetch. The 'Opens N' branch uses the same call, not a second comparison. A raw day-label-vs-Date.now() compare anywhere under app/src/pages/review is a defect, and a test pins that.
