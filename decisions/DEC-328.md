# DEC-328: All four wave-16 review-lens findings are STALE and are closed by anchor; a finding whose cited line no longer holds its cited symbol must be re-verified at the SYMBOL before it can become a task

The harness re-injects the last 12 accumulated review findings into every planner, so a stale finding is re-litigated forever unless it is closed by anchor. I checked all four at the symbol, live on main, and every one is already fixed: (1) 'unbounded auto-schedule params' cites src/routes/agenda.ts:126 numberOrDefault, a symbol that exists NOWHERE in the tree — DEC-298 landed AUTO_SCHEDULE_BOUNDS + parseBoundedInt at src/routes/agenda.ts:129-153 (gridMin 1..480, dayEndMin 1..1440, non-integer/out-of-range yields a fields[] 400) plus an independent module-level guard at src/domain/schedule.ts:117-127 that throws on gridMin<=0 or dayEndMin<=dayStartMin regardless of caller; (2) 'every public-CFP speaker renders with blank title/company' — DEC-299 landed src/server/repo/attribution.ts backfillNullAttribution (NULL-only, blank/whitespace is a no-op), called from src/server/repo/profile.ts:138 and src/server/repo/contacts/crud.ts:109, and DEC-321 landed the CFP's own optional locked job_title/company/bio so src/routes/public/submit.tsx:568-569 writes resolvedTitle/resolvedCompany instead of null; (3) 'deleting a form field silently breaks dependents' — DEC-300 landed src/server/repo/forms.ts:276-317 (describeFieldDependents + deleteFieldCascade clearing dependent ruleJson and deleting orphan answers) behind a 409 at src/routes/api/forms.ts:216-227 that names the dependents and answer count unless ?cascade=1; (4) 'a newly created event ships an unsubmittable public CFP' — DEC-301 landed a default 'General' track at src/routes/api/events.ts:223 AND made validateTrackChoice return ok for a zero-track form at src/lib/submit-core.ts:49-51, so the dead end cannot recur even if a producer deletes every track. The general rule this establishes: review lenses read snapshots and diffs, so their file:line citations age out within a wave or two; before any finding becomes a task the planner must grep for the cited SYMBOL, and if the symbol is absent or the line now holds the fix, the finding is closed, not scheduled. The same rule retires task-w13-b-c3-walkthrough.md's OPEN ITEM 2 as a probe defect rather than a product defect: the embed-snippet builder now lives at app/src/pages/settings/embedSnippet.ts with its own unit test, while scripts/walkthrough/public.ts:518 still reads app/src/pages/Settings.tsx — DEC-326's 'cite the module that OWNS the behaviour' case, already assigned to task-w15-b. Wave 16 therefore returns ZERO tasks: main carries only 'merge task-w15-a', no docs/verification-log/task-w15-*-c3-*.md exists on main or in the task-w15-{b,c,d,e} worktrees, and those worktrees' live .wrangler/state shows the lanes still running — precisely the position in which DEC-315/320/327 require waiting rather than padding.
