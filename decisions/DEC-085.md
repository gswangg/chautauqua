# DEC-085: Findings-file closures needing no code: DEC-054 five-status pipeline upheld (SPEC §10 item 4 explicitly deferred with it); submittedAt ≡ createdAt is correct under DEC-014

Two review notes are decided, not implemented, so no future wave re-litigates them. (1) DEC-054's deferral of SPEC §5 "(+ custom)" statuses stands: swyx's stated minimum is fully covered, every consumer is built on the DEC-003 literals, and the review itself called the deferral defensible; recording here that §10 item 4 ("resubmit with guidance" status) is knowingly deferred with it under §10's own only-after-J1-J12 gate (DEC-061/062 closed §10), both on the post-competition roadmap. (2) The "submittedAt is just createdAt" note is not a defect: per DEC-014 drafts live in KV and never create submission rows, so a submission row's createdAt IS its submit time; the unused submission.submitted_at column stays as-is (dropping it is a migration churn with zero behavior value). No task ships for either.
