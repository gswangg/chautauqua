# DEC-304: The stage-1 exit predicate for the -c3- battery: eight green sections, substance not sha-identity; the wave-12 planner declares stage 1 at refs/heads/main and returns goalComplete

DEC-287's predicate required an identical FROZEN SHA literal across all eight logs. DEC-303 removes the literal, so the predicate must be restated or wave 12 will die in the same gap between 'the sections were green' and 'someone decided that counted' that killed four prior attempts. Restate it now, before the evidence exists: stage 1 is DECLARED iff all eight files docs/verification-log/task-w11-{a..h}-c3-*.md exist on main, and each one contains verbatim, each on its own line, `WAVE-10 GATE: PASS`, `OPEN ITEMS: 0`, `RESULT: PASS`, and either `RECHECK SHA: n/a` or a `RECHECK SHA: <40-char>` whose re-check section records every delta-touched claim as green — and each file's `FROZEN SHA:` value is an ancestor of main. Sha identity across sections is explicitly NOT required; DEC-280 already moved the criterion to substance, and DEC-303 makes per-lane shas expected. The declared sha for stage 1 is refs/heads/main as the wave-12 planner reads it, since wave 11 adds nothing but verification logs. If the predicate passes, the wave-12 planner returns ZERO tasks and goalComplete: true — it does not commission a fresh survey, and 'I could look harder' is not grounds to defer. If any section reds, DEC-287's narrowing applies verbatim: wave 12 fixes ONLY the file:line defects the failing sections cite, and wave 13 re-runs ONLY the failing sections.
