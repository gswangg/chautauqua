# DEC-671: An export is the SAME where clause as the list it sits beside -- the contacts directory included

DEC-649 fixed this for submissions (SubmissionsTable.tsx:40 threads the live q/status/trackId/sort into the export href; src/routes/api/exports.ts:75-91 re-uses parseListQuery). The contacts directory is the product's other server-filtered list -- q, segmentId and rules all travel to GET /api/v1/contacts (app/src/pages/contacts/ContactsApp.tsx:65-81) -- yet its Export CSV is a static href with no params (ContactsApp.tsx:103) and exportContacts (src/server/repo/exports/contacts.ts:26) takes nothing but orgId. An organizer who searches "Raman", sees 2 rows, and clicks Export CSV downloads every contact in the org. Same defect, same fix shape: the filter travels with the download or the file lies. The export must reuse the directory's own predicate path (including the MAX_CONTACT_DIRECTORY_SCAN guard, which must fail loudly rather than be quietly skipped for exports) -- never a second implementation of the q tokenizer or matchesSegment.
