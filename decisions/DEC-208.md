# DEC-208: Wave-21 completion battery: repaired w20-a/w20-c count as green; four missing gates run as task-w21-* lanes at FROZEN 6807b67; wave-22 declaration is a grep

DEC-114 derivation on today's main: d1c13d2 and 78bb286 touch only ledger/decision/field-guide files (plus pure string-constant appends to src/decisions.ts), so the newest code-bearing sha remains 6807b67 — the DEC-206 frozen binding holds and no rebinding is needed. The battery is 2/6 on main: build+test (w20-a) and perf-smoke (w20-c), valid once DEC-207 lands. The missing four gates run as fresh lanes bound to FROZEN 6807b67 with the DEC-206 drift-stop (a DEC-114 derivation yielding any other sha means 'RESULT: FAIL — sha drift' and stop, no rebinding) and the DEC-203 precondition greps: task-w21-b walkthrough (port 8951, scripted walkthrough plus the DEC-200 live /account/password checks; the tracked detail file docs/verification-log/task-w20-b-walkthrough.md proves the gate passed once at this sha but is corroboration only — its ledger section never reached main, so the lane re-runs the scripted gate), task-w21-c render-sweep (DEC-144, self-allocated port), task-w21-d spec-audit (static, SPEC §8/§9), task-w21-e triage-closure (gated on the DEC-207 repair so it can honestly report a marker-free ledger). Confirm-else-run carve-out (DEC-128 pattern): if a late-draining full-heading 'task-w20-<x> — <scope> @ 6807b67' PASS section is on main's ledger when a lane starts, the lane may confirm and cite it instead of re-running. Exit predicate for wave 22: docs/verification-log.md on main is conflict-marker-free and contains, all '@ 6807b67', well-formed PASS sections for build+test, perf-smoke, walkthrough, render-sweep, and spec-audit plus a triage-closure section ending 'OPEN ITEMS: 0' — whether under task-w20-* or task-w21-* names — with the newest code-bearing sha still 6807b67; then wave 22 declares stage-1 complete with zero tasks. Lanes remain verification-only: they never modify product code, and real failures are recorded for the next wave to fix.
