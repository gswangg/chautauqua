# DEC-164: Wave-3 consolidation conflict rules: one render-test file per page (keep the more thorough of w3-d/w3-e duplicates); migration filename collisions renumber to the next free prefix preserving relative order (the 0011 gap is fine); ContactsApp tab strip is the union with Pipeline last

task-w3-d and task-w3-e retrofit render smokes across overlapping app page dirs, task-w3-a adds a fourth ContactsApp tab that existing tab-strip render tests observe, and w3-a/b were assigned migration prefixes 0012/0013 while main's max is 0010 with 0011 unassigned. Without one rule the merge agent and task-w4-a could resolve these differently. Wrangler applies migrations in lexical filename order, so gaps are harmless but prefix collisions are not; stage-1 D1 state is disposable local Miniflare, so renumbering not-yet-canonical files breaks nothing and DEC-015 append-only is preserved. DEC-161's one-render-test-per-top-level-page remains the invariant when deduping.
