# DEC-649: An export is the SAME where clause as the list it sits beside — the filter travels with the download or the file lies

GET /api/v1/events/:id/export/submissions takes no filters (src/routes/api/exports.ts:58, exportSubmissions(db, eventId) at src/server/repo/exports/submissions.ts:90), so a mock-specified `Export CSV` button next to a filtered table would hand the producer every row while the screen shows twelve. That is DEC-634's defect (items and total from one where clause) committed to a file instead of a page. The list's own conditions move out of listSubmissions into an exported builder and BOTH readers call it; a test asserts the exported row count equals the list `total` for identical params. Same semantics claims end in the same helper.
