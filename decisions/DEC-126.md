# DEC-126: Wave-14 structure: six parallel code-bearing fix lanes named task-w14-i..n (a..h burned by round-0); landing any of them voids every 3b7ed3d gate section per DEC-069/114; wave 15 is a full five-gate re-run at the new sha

Round-0 history already contains 'merge task-w14-a' through 'merge task-w14-h' — the exact burned-name pathology that silently killed the wave-12 and wave-13 lanes (task-w13-a/b/c refs exist unmerged at planning time; task-w12-e likewise) — so wave-14 lanes start at suffix 'i', preserving the reserved task-w14-* prefix while avoiding every name any epoch has used. All six lanes are independent single-owner-per-file fixes with no dependsOn, maximizing width. Consequences ruled now so no gate worker has to decide them: (1) each fix commit is code-bearing per DEC-114, so once the first one merges, ALL existing 3b7ed3d gate sections (and any section a stray w12-e/w13-a/b/c lane appends mid-wave citing 3b7ed3d or older) are void for exit under DEC-069 sha-scoping — this is expected, not a defect; (2) wave 15 re-runs all five DEC-069 gates (build+test, walkthrough, perf-smoke, spec-audit, triage-closure) at the post-wave-14 DEC-114-derived sha on fresh 88xx ports, with triage-closure expected to reach OPEN ITEMS: 0 by citing the DEC-125 fix plus a green perf-smoke; (3) the five product-fix lanes each append one export const DEC_1NN string to src/decisions.ts and void-reference it at the landing site — same-file append overlap in src/decisions.ts across lanes is expected and the merge agent resolves it; the scripts-only lane (task-w14-i) touches nothing but scripts/perf-seed.ts and leaves the DEC_125 constant to the scribe.
