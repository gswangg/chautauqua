# DEC-177: Wave-7 lanes never executed; the DEC-176 battery protocol rebinds verbatim to wave 8 with S = the task-w8-a merge commit and gate preconditions extended to grep the DEC-173/174/175 artifacts

Verified against the tree, not summaries: after the six wave-6 merges (77b76a9) the only commit on main is "scribe wave 7" (466f45b); task-w7-a exists as a zero-commit branch stub at main, there are no worktrees, and docs/verification-log.md contains no wave-7 sections (the docs/verification-log/task-w7-*.md files are first-campaign relics per DEC-176's own naming disambiguator). The DEC-173/174/175 harness fixes are grep-confirmed absent (scripts/walkthrough/public.ts:440 and speaker.ts:915 strict extractors, scripts/seed.ts:975 mod-3 formula, zero DEC-175 probes). Therefore DEC-176 applies unchanged with these substitutions: the sole code-bearing lane is task-w8-a (scripts/** only, each change tagged with its literal decision id in a comment); the frozen sha S is the "merge task-w8-a" commit derived per DEC-114 first-parent walk; gate sections are titled task-w8-*; every gate precondition-greps the six w6 fixes (DEC-167 in src/domain/contacts.ts, ICS_ORGANIZER_EMAIL in src/mail/ics.ts, "unknown track id" in src/routes/api/forms.ts, "anonymized === false" in src/server/repo/files.ts, openDate in app/src/pages/review/PlanEditor.tsx, FORM_TASK_FIELD_SPECS in scripts/seed.ts) PLUS the harness closure ("DEC-174" in scripts/seed.ts, "DEC-173" in scripts/walkthrough/public.ts and speaker.ts, "DEC-175" in scripts/walkthrough/producer.ts, speaker.ts, and review.ts), failing "RESULT: FAIL — precondition" on any miss. The stale task-w7-a branch is dead and must never be reused or merged. All six sections PASS at one S with OPEN ITEMS 0 remains the DEC-069/DEC-139 stage-1 exit condition.
