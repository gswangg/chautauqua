# DEC-349: Wave 20 runs only the build/test gate; the full walkthrough and the authoritative perf run move to wave 21, whose three-item exit set is fixed now

DEC-343 assigned wave 20 three re-runs: build+test+tripwires, the full six-module walkthrough, and perf:smoke. Two of the three cannot honestly be run in wave 20, for the reason DEC-343 itself gave about wave 19: task-w20-a/b/c change the files-library envelope, the results envelope and the reviewer queue's load path, all of which the walkthrough and perf harness exercise, and every wave-20 lane branches from the same pre-merge tip. So: (1) the build/test/tripwire/fresh-migration gate DOES run in wave 20 (task-w20-e) because it is the cheapest, highest-signal proof that wave 19's five lanes — which rewrote the submissions list, the onboarding grid, the content worklist and the perf harness — actually compose, and it must state in its FROZEN SHA section which of task-w19-b/c/d/e are present in that sha, logging absent ones as POST-S DELTA per DEC-280 rather than stopping; (2) the perf run happens as a BASELINE only, under DEC-347; (3) the full six-module walkthrough does NOT run in wave 20 — task-w19-b's green run stands as the record that closes task-w17-e's FAILED, and re-running it against a tip that predates this wave's product lanes would add a second stale artifact. Wave 21's exit set is exactly: npm run build + npm test + the four test/ tripwires; the full six-module walkthrough orchestrator; npm run perf:smoke including the two DEC-347 checks — all at a single sha containing every merged wave-19 and wave-20 lane. NOT re-run: spec-audit, rubric-coverage, fresh-clone, render-sweep, since no wave-20 lane changes public-surface SSR markup or audited prose (the Files and Results screens are admin SPA). goalComplete belongs to the first planner that can read task-w19-b/c/d/e merged, task-w19-c and task-w20-e at OPEN ITEMS: 0 / RESULT: PASS, task-w20-a/b/c/d merged, and that three-item set green at one sha. A FAIL yields a narrow wave over the cited file:line only; DEC-315's anti-padding rule survives intact.
