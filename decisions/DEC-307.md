# DEC-307: The battery verdict is read ONE WAVE LATE: wave 12 is the narrowing-fix wave, not the exit wave — the wave-11 predicate is already provably FAILED

DEC-304 instructed the wave-12 planner to read eight -c3- logs and declare. That is unsatisfiable by construction, and I proved why rather than inferring it: the harness replans at `lowWater: 8` (/Users/wednesdayniemeyer/Documents/gniemeyer/Projects/chautauqua-harness/.claude/workflows/chautauqua-build.js:45 and :274 `if (!force && queue.length >= C.lowWater) return`), so the planner for wave N+1 is invoked when wave N's tasks are DISPATCHED, not merged. At my read refs/heads/main was 84e2c04de087310f39877140cb6e239fab018e6c ('scribe wave 11'); .git/logs/HEAD line 113 shows exactly one wave-11 merge (task-w11-a), and live worktrees still existed for task-w11-b/c/d/f/g. The planner will ALWAYS be one wave ahead of the merge train; wave 13 is the first planner that can read all eight logs. But no waiting is required to reach a verdict, because I read the evidence that exists: task-w11-a-c3-build-test.md (merged) RESULT: FAIL, OPEN ITEMS: 1 — `npm audit --omit=dev` reports drizzle-orm@0.36.4 < 0.45.2, GHSA-gpj5-g38j-94v9, HIGH, a package that ships in the Worker bundle; task-w11-c-c3-perf-smoke.md (worktree) RESULT: FAIL, OPEN ITEMS: 1 — schedule.ics 150 ids p95 51.9ms vs SPEC §7's 50ms admin-read figure; task-w11-d-c3-render-sweep.md (worktree) RESULT: FAIL, OPEN ITEMS: 2 — /docs/api 371px and /dev/mailbox 96px horizontal overflow at 390x844; task-w11-e-c3-spec-audit.md (merged) PASS 0; task-w11-f-c3-triage-closure.md (worktree) PASS 0; b/g/h unresolved. Three reds means DEC-304's predicate has already failed, so DEC-287's narrowing branch fires NOW, on evidence, not on a timer: wave 12 fixes ONLY the four cited defects. Wave 13's planner reads all eight -c3- logs and re-runs ONLY the sections that red (a, c, d, plus whichever of b/g/h red) at a fresh S. Sections e and f STAND (DEC-280: substance, not sha-identity) and are re-checked only if a wave-12 commit changed a LINE they cite — note the one near-miss to check rather than assume: section f cites src/routes/public/index.tsx:48-55 (publicNotFound) and wave-12 task c edits :182-196 of that same file. Wave-11 lanes b/g/h still in flight will now see a non-empty POST-S DELTA; per DEC-280 that is never a STOP — they record RECHECK SHA and re-check delta-touched claims only.
