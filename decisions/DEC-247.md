# DEC-247: GET /api/v1/submissions/:id/files returns a flat {items: DeliverableFile[]} envelope; worklist deliverable counts = version-chain roots

w2-e CNT-07b (docs/verification-log/task-w2-e-findings-closure.md) proved the worklist deliverable counts are always 0: the server route (src/routes/files.ts:146-151) ships {files: FilesByKind} while BOTH SPA consumers (app/src/pages/content/ContentApp.tsx:43-48 and DeliverableDetail.tsx:47-49) read .items, and a per-row silent .catch hides the crash. Per DEC-246 the SPA type file is the contract of record, and app/src/pages/content/types.ts:39-52 documents flat rows — so the SERVER conforms: the route flattens listSubmissionFiles output (repo function unchanged, its grouped return has direct repo tests) into {items: [{id, submissionId, kind, filename, sizeBytes, contentType, previousFileId, uploadedByContactId, createdAt}]}; uploaderName stays optional-absent. Worklist counts = files with previousFileId===null per kind (distinct deliverables, matching FilesLibrary's chain semantics; version-inclusive counts would inflate after every replacement). ContentApp's silent per-row .catch(() => item) is deleted — a failed load rejects the surrounding Promise.all and surfaces through the existing worklist error state (fail loudly). No back-compat 'files' key (house rule: no compatibility shims).
