# DEC-481: Re-grade every open decision from the tree at wave start; a doc comment inside src/ is narration too

w22 planned five source lanes against DEC-475..480. At this wave's read of main only DEC-476 is actually in (src/domain/schedule.ts MINUTES_PER_DAY, the former src/server/repo/agenda.ts:372 (now split across src/server/repo/agenda/)/377, test/slot-minute-bounds.test.ts). DEC-475 is absent (src/server/repo/submit.ts:89 still normalizes only `id`, :97 passes `rule` through raw). DEC-477 is absent (src/server/repo/public/bounds.ts:9 still 600, src/routes/public/query.ts:10 still 50). DEC-478 is absent (src/routes/api/contacts/import.ts:24 still 10_000 against the repo's 2000). DEC-480 is absent (three clamp copies alive at src/routes/tasks.ts:105-117, src/server/repo/contacts/query.ts:17-35, src/server/repo/submissions/query.ts:21-49; no enumeration test). And DEC-479 is HALF in, which is the worst state of all: src/server/repo/contacts/merge.ts:50-74 now documents a step (f) that 'set[s] the surviving user row's email to merged.email', its pre-checks at :81-94 and :99-114 landed, and the code at :202-203 still sets only contactId. DEC-472 taught grading from file:line rather than from decisions/ or the field guide; this wave adds the corollary that a comment inside the source file is narration too and cannot be evidence about the lines below it. w23 therefore re-derives DEC-475/477/478/479/480 from current file:line and re-issues them, and cuts NO ledger (DEC-452/470); w24 owns the ledger against a sha that contains them.
