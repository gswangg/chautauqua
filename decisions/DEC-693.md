# DEC-693: Where a test contradicts the design pack, THE TEST is wrong; and a not-found surface is a designed page on both sides of the wire

Mandate item 29, verified verbatim: app/src/pages/NotFound.tsx:6-9 carries a comment stating the heading "stays 'Page not found' (App.render.test.tsx asserts on it) rather than the design doc's 'That page isn't here' copy" — an assertion promoted to the source of truth over the vendored design pack, which inverts docs/README.md's precedence and freezes a copy defect behind a green suite. The rule must be stated once and enforced: a render assertion is a record of intent, and when intent moved, the assertion moves with it. Second, the same page family is inconsistent across the wire — src/routes/public/index.tsx:56 answers every public 404 with `c.text(...)`, a bare unstyled string, on the most judge-visible surface in the product, while the SPA renders a designed panel. One not-found treatment, both sides, with the event-name eyebrow the header context already holds.
