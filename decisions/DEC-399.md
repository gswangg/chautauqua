# DEC-399: The global public-cache bump stays CLOSED -- third and final reaffirmation of DEC-201/333/348

The wave-5 review lens raised it again as a top finding, in the same terms as wave 20: src/server/app.ts:33 mounts bumpPublicVersionMiddleware on '*' and src/server/pubcache.ts:103 rewrites the single chq:pubver key after any successful non-GET, so a POST /login purges every org's /e/* and /embed/* cache. The description is accurate and the disposition is unchanged: DEC-201 accepted it explicitly for stage 1 (correctness-safe -- public pages can never be stale -- while a route-by-route purge predicate manufactures a real staleness risk class whose only payoff is a production edge-cache TTFB number), DEC-333 and DEC-348 reaffirmed it, and DEC-366 names it by number as something that 'is never an open item and never a task'. I re-verified the code and the calculus: an allowlist that misses a publish-affecting write serves a stale public page for up to the stored copy's 86400s, which is a correctness regression, whereas the current over-purge costs at most one uncached SSR against SPEC's own 150ms budget, and Miniflare's caches.default is not an edge cache so stage 1 cannot even measure the difference. Narrowing belongs to the stage-2 swarm that can measure it. Recorded a third time so the next planner reading the next lens report stops here rather than spending a lane.
