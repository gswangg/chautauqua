# DEC-063: SPEC §9 persona walkthroughs are CI-enforced: a third 'walkthrough' job boots seeded wrangler dev and runs `npm run walkthrough`

SPEC §7's own language — budgets that aren't enforced are decoration — applies equally to §9's acceptance bar. The walkthrough job mirrors the existing perf-smoke job shape exactly (npm ci, db:migrate, seed, background `npx wrangler dev --port 8787`, poll /health for 60s, run the check, dump wrangler-dev.log on failure) minus perf:seed, so every push re-walks J1->J12 as all five personas against the standard seed. package.json gains a single `walkthrough` script (`tsx scripts/walkthrough.ts`, default --url http://localhost:8787) so evaluators can run the same gate locally with one command documented in README.

## Amendment (wave 66): the producer walkthrough walks the break LIFECYCLE, because a route that answers 200 proved nothing about a feature nobody could reach

Wave 63 shipped schema + repo + API + mount + docs for breaks and zero UI; the swarm only noticed two waves later, and the field guide already records the lesson ('THE WAY IN IS PART OF THE FEATURE'). The reason it survived is that nothing walked it end to end: the API had unit tests, the public renderer had unit tests, and no persona ever created a break and then went looking for it. DEC-063 makes SPEC §9 persona walkthroughs CI-enforced, and SPEC §9 calls them 'the real bar'. RULING: the producer walkthrough gains a J9 break-lifecycle section — create through the organiser API, see it day-scoped in the list, fetch the public agenda ANONYMOUSLY and find the label, delete, fetch again and find it gone, plus the loud-refusal case (a day outside the event range is a 400 naming `day`). It asserts on the break's LABEL TEXT only, never on a CSS class or DOM shape, because wave 64-b is restructuring that page underneath it; and it cleans up after itself so the seed is unchanged. As a by-product it exercises the DEC-627 pubcache classification of the breaks routes on a real request.

## Amendment (wave 68): the J10 persona walkthrough visits every no-login surface the event actually has

SPEC section 9 ranks persona walkthroughs above the eval harness as "the real bar". scripts/walkthrough/public.ts drives J10 off a hardcoded SURFACES = [sessions, speakers, agenda, schedule, gallery], written before wave 65 added two more public no-login GETs: the printable programme (/e/:slug/programme) and the anonymous event hub (GET /). Neither is walked. The render sweep visits both (app/src/routeManifest.ts) but the sweep only proves a page renders; the walkthrough is where status, cache-control, key content and the role redirects are asserted -- and GET / carries a rule no other surface has (DEC-582: organiser/reviewer -> /admin, speaker -> /portal, anonymous -> the hub), which nothing in the harness exercises today. Same precedent as w66-d, which extended this walkthrough with the break lifecycle and put its pure helpers in scripts/walkthrough-lib.ts.
