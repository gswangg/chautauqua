# DEC-423: Wave 11's three verification lanes are log-only, must use non-colliding filenames, and the spec-audit lane emits the stage-1 completion ledger

Three things settled together so no lane guesses. (i) Log-only per DEC-384/DEC-411/DEC-419: each lane writes exactly one file under docs/verification-log/ plus one appended section in docs/verification-log.md, reports what it finds, fixes nothing it names, flips no blocking flag, and never writes an instrument failure down as a number. (ii) FILENAMES: docs/verification-log/ already contains task-w11-a-build-test.md, task-w11-b-walkthrough.md, task-w11-c-perf-smoke.md, task-w11-d-render-sweep.md, task-w11-e-spec-audit.md and task-w11-f-triage-closure.md from the first campaign (DEC-129 homonyms — wave numbering restarted for the redesign). A wave-11 lane that writes the obvious filename SILENTLY DESTROYS another campaign's evidence, so each lane must use the distinct filename named in its own task text and no other. (iii) The spec-audit lane's output is not prose: it is a per-job J1-J12 table plus per-clause tables for SPEC §5 invariants, §6, §7 and §8, where every row reads VERIFIED (with a file:line or verbatim command output), GAP (naming what is missing), or STAGE-2 (deferred by SPEC §0's staging rule), ending in an OPEN ITEMS count. The next planner's only remaining decision is whether stage 1 is complete; that decision must rest on citations against this tree, never on a wave summary.
