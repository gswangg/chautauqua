# DEC-103: Wave-8 verify-or-run gate protocol: w7 lanes live mid-planning (task-w7-a build+test PASS @ d12eb25 merged during planning); remaining scopes re-issued as idempotent verify-or-run lanes on alternate ports 8811/8813; duplicate green sections resolve to a brief confirmation, never a dropped gate

Verified against main's reflog during planning: task-w7-a merged as 52b9ead with a green '## task-w7-a — build+test @ d12eb25' section (bookkeeping-only merge, so d12eb25 'merge task-w6-d' remains the newest code-bearing sha), and task-w7-b/c branch refs moved to 52b9ead — those lanes are executing now, but walkthrough/perf-smoke/spec-audit/triage sections do not yet exist at d12eb25 and three prior gate waves stalled without merging. Waiting is not an option and blind re-runs would collide: wave-8 therefore issues one idempotent lane per missing scope. Each lane first re-derives the newest code-bearing sha per DEC-091, then greps docs/verification-log.md for an existing same-scope section with RESULT: PASS at that sha: if found it spot-checks the section's claims and appends a short '## <date> task-w8-x — <scope> confirm @ <sha>' section ending RESULT: PASS (per DEC-093's straggler rule); otherwise it runs the full gate and appends the full section. Because w7 runtime lanes may hold ports 8801/8803, w8 runtime lanes bind 8811 (walkthrough) and 8813 (perf-smoke) — the scripts take --url/PERF_URL so no code change is needed and lanes stay code-frozen per DEC-077. Build+test is NOT re-issued: its section is green at d12eb25 and the triage-closure lane verifies every post-d12eb25 merge is DEC-090-exempt (any code-bearing gate merge resets the predicate and is recorded honestly). Triage-closure alone chains behind the w8 walkthrough lane (DEC-093/102). The wave-9 planner evaluates exit purely by the DEC-069 grep: four RESULT: PASS gate sections plus triage OPEN ITEMS: 0, all at the single newest code-bearing sha.
