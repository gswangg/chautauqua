# DEC-089: DEC-086 probe placement: five perf-smoke checks (incl. 301-id 400 assertion) and a sixth walkthrough area "scale" (bulk accept + purge refresh)

Splits DEC-086's mandated probes between the two harnesses so each stays in its existing files. perf-smoke (port 8803, perf seed) adds: (1) GET /e/perf-2k/sessions timed, (2) GET /e/perf-2k/agenda timed, (3) GET /e/perf-2k/schedule.ics?ids=<150 accepted ids fetched via the organizer submissions API> timed, asserting 200 and BEGIN:VEVENT present, (4) one-shot untimed request with 301 ids asserting status exactly 400 (DEC-080 cap), (5) GET /api/v1/plans/seed_perf_plan_0001/progress timed (12 reviewers, DEC-081 set-based path), and (6) a rating PUT timed: login as perf.reviewer.1@example-perf.test / "PerfReviewer!2027", PUT /api/v1/review/plans/seed_perf_plan_0001/evaluations/<one fixed accepted submission id> with scores {"overall":4} each iteration — the keyboard-fast keystroke path. All timed checks reuse PERF_P95_BUDGET_MS=150. Walkthrough (port 8801, demo seed) appends area "scale" after "data" in WALKTHROUGH_AREAS (runner derives everything from that list): create 110 FRESH contacts + 110 submissions + one speaker participant each via the organizer API (fresh contacts are required — the DEC-009 planner is idempotent per (contact, task-title), so reused seed contacts would show no new assignments), ONE bulk POST /api/v1/events/:eventId/submissions/status with all 110 ids -> accepted, assert onboarding task_assignments exist for sampled fresh contacts, re-POST the identical request and assert assignment counts unchanged (DEC-079 exactly-once), assert the dev mailbox count is unchanged by the status change (DEC-009 no auto-email), then a publish-affecting write (PATCH an accepted submission's title) immediately followed by GET /e/<slug>/sessions asserting the new title appears (DEC-083 purge observable in Miniflare's caches.default).
