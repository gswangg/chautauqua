# DEC-112: Walkthrough gains invite-visibility and form-task runtime probes; the w10-e lane verifies wiring by observing the expected pre-fix failures

All four DEC-108..111 defects evaded the six-module walkthrough, npm test, and the spec-audit — the same blind-spot pattern DEC-105 fixed for perf. Amend the walkthrough: public module asserts that on an accepted+approved submission a pending invitee and a declined invitee are ABSENT from the public sessions and speakers surfaces while an accepted invitee is PRESENT (reusing the existing DEC-070 invite + accept/decline steps); speaker module opens each of the two DEC-111 tasks at /portal/tasks/:assignmentId/form expecting 200, POSTs valid answers (dropdown 'Yes' + optional fields), and asserts the assignment reaches status complete. Because these probes assert post-fix behavior, they MUST fail against pre-w10 main (declined speaker still listed; form task 400) — the w10-e worker proves wiring by observing exactly those failures locally on port 8825 and keeps the module compiling green; the wave-11 walkthrough gate at the post-w10 sha is the green runtime proof. No existing walkthrough step is removed or weakened.
