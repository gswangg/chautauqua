# DEC-437: SPEC §10 items 2, 3 and 4 are deferred by decision, not missing

SPEC.md:390 gates the nice-to-have list "only after J1-J12 are green." They are green, and four of the eight already shipped: #1 .ics updates with a bumped SEQUENCE (submission.ics_sequence, bumped on slot writes per DEC-021, read by src/mail/ics.ts:110), #5 Airtable one-way sync, #6 the public API docs page at GET /docs/api (DEC-056), #7 the show-flow export. #8 is stage-2 by definition (Resend webhooks). The remaining three -- the decision-meeting view, assisted-chasing reminder drafts, and the "resubmit with guidance" status plus its near-miss CRM lane -- are recorded here as deliberate stage-1 non-goals so that the completion ledger scores their absence as DEFERRED-BY-DECISION rather than GAP, and so no closing wave spends its last lanes widening DEC-003's frozen five-literal status set on a tree that is stabilizing under gates that have just become blocking. Feature completeness ranks second in SPEC.md:36, behind performance and ahead of nothing that these three would serve. If a later ledger reads PASS with lanes to spare, #2 (decision-meeting view) is the first to build.
