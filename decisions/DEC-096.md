# DEC-096: Wave-5 gate protocol: task-w5-a is the single DEC-076 closeout barrier landing both scripts-only fixes; all four gates re-run chained behind it (code-frozen per DEC-077), triage-closure behind the walkthrough gate, and it must close the w3-c/w3-d OPEN ITEMS

The DEC-094/095 fixes are code-bearing (scripts/), so under DEC-069 they invalidate every green section at 3878d4f — all five sections must be re-recorded at the new newest code-bearing sha, which will be task-w5-a's merge (re-derived independently by each gate per DEC-091, skipping DEC-090-exempt bookkeeping). Running any gate before the fixes land would only re-record the known FAILs and be immediately invalidated, so chaining here is genuinely sequential, not lane-starvation. Both fixes go in ONE task because DEC-076 mandates exactly one closeout barrier and dependsOn can only name one prerequisite; both root causes are fully diagnosed with exact file/line fixes, keeping the combined task in normal size. Gate lanes remain code-frozen per DEC-077: a gate that finds a defect records RESULT: FAIL and never commits code. Triage-closure chains behind the walkthrough gate so it can cite the scale-area PASS as runtime evidence when dispositioning the two OPEN ITEMS from the w3-c/w3-d FAIL sections (closed by DEC-094/095); it must end OPEN ITEMS: 0. Exit predicate is evaluated by the wave-6 planner: build+test, walkthrough, perf-smoke, spec-audit sections each RESULT: PASS at task-w5-a's code-bearing sha, plus triage-closure OPEN ITEMS: 0 — then wave 6 returns zero tasks and declares stage-1 complete.
