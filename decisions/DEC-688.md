# DEC-688: The speaker portal home IS the worklist: header, waiting-on-you, your session, done, identity footer — no pipe nav, no submissions table

Mandate item 41 (r2-Public/Portal) says the portal home must REPLACE the old page, not accrete onto it, and main confirms it never did: src/routes/portal/index.tsx:63-70 still renders a `<a>Dashboard</a> | <a>Profile</a> | ...` pipe nav, :211-245 still renders the letter-wrapping My Submissions `<table>`, and :247-277 keeps separate Sessions and Resources stubs below it. The mock (docs/design/Chautauqua Public and Portal.dc.html:165-218) designs one screen: a fixed header carrying the wordmark, the speaker's name, the DEC-590 "N things to do" hero and a one-line event/slot subline; a scrolling body of exactly three sections — Waiting on you, Your session, Done; and a fixed footer carrying the speaker's identity plus a Profile link. The submissions table and the sessions list are the SAME facts the session card states better, so they are deleted rather than kept beside it — a surface that shows the same fact twice teaches the reader that neither is authoritative. The DEC-590 count rule is untouched: n is exactly the rows rendered under Waiting on you.
