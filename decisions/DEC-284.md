# DEC-284: A pre-battery mechanical gate lane runs in wave 7: it reports red, it never fixes red

Wave 8's eight lanes are expensive, and three product merges (DEC-274/276/277) are landing between now and then. A single cheap lane that runs npm ci / build / test / bundle:check / gate:render-sweep at whatever main is when it starts, and writes the sha plus a GREEN|RED verdict to docs/verification-log/, means wave 8's planner declares S over evidence instead of hope, and a mechanical break gets caught by one lane rather than by eight. It is deliberately NOT one of the DEC-256 sections: no rubric mapping, no persona walkthrough, no scoping matrix. Hard constraint: it edits exactly one file, its own log; it never touches product code, decisions/, field-guide/ or docs/eval-findings.md, and a red result is reported with file:line and failing test names for wave 8 to schedule, never patched in-lane where it would collide with the in-flight defect branches.
