# DEC-206: Seventh-generation battery binds at FROZEN sha 6807b67 with a hard drift-stop; homonym dead-set now includes task-w19-*/task-w20-* sections '@ 8c7f479'; green 6/6 at 6807b67 satisfies DEC-069/DEC-139 and wave 21 declares with zero tasks

Six lanes task-w20-a..f append sections whose full headings end '@ 6807b67'. The sha is frozen at planning rather than lane-derived: no task refs exist, and the only commits this wave are ledger/bookkeeping appends (DEC-114 exclusion set), so if any lane's DEC-114 derivation at gate start yields a sha other than 6807b67 it records 'RESULT: FAIL — sha drift' and STOPS (no rebinding — prevents a split-brain battery; the next wave rebinds). Preconditions each lane greps before running (from DEC-203, all verified at planning): toLowerCase in src/routes/api/users.ts, lower( in src/server/repo/users.ts, accountRoutes mounted in src/index.ts, welcome-email string in users.ts free of any password interpolation and mentioning /account/password; any miss is 'RESULT: FAIL — precondition'. Homonym guard, seventh generation: docs/verification-log.md already holds dead-campaign sections literally named task-w19-a..e, task-w20-a and task-w20-b '@ 8c7f479' (plus everything '@ 1033d45' and older) — every ledger search matches the FULL heading including '@ 6807b67', never a lane name alone (DEC-129). Carried forward: DEC-189(2) dedupe-by-full-heading, DEC-189(5) ports (walkthrough 8951, perf-smoke 8952, render-sweep self-allocates), DEC-187/188(6) never read or print .dev.vars, DEC-068 append-only ledger (one new section per lane plus optional detail file under docs/verification-log/), and the DEC-197 auth-flake disposition tightened: the known test/auth.test.ts login-rate-limit full-suite flake counts as PASS only if that file passes when re-run solo. Gate lanes are verification-only — they never modify product code; a real failure is recorded for the next wave to fix. Six RESULT: PASS sections at 6807b67 covering build+test, walkthrough, perf-smoke, render-sweep, spec-audit, triage-closure satisfy the DEC-069/DEC-139 exit predicate for the post-fix tree; wave 21 verifies the ledger and declares stage-1 complete with zero tasks.
