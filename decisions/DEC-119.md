# DEC-119: Wave-13 structure: three parallel verify-or-run gates — build+test, walkthrough@8841, perf-smoke@8843 — plus triage-closure chained behind perf-smoke; all cite the DEC-114-derived sha (expected 3b7ed3d); fresh ports because stray w11-d/w12-a/w12-b lanes may hold 8821-8833

With spec-audit green at 3b7ed3d (DEC-118), exactly four DEC-069 scopes are missing at the newest code-bearing sha and zero product code is outstanding, so per DEC-117's own logic no barrier exists: build+test, walkthrough, and perf-smoke start in parallel, each idempotent verify-or-run per DEC-103 (first grep docs/verification-log.md for a green same-scope section at the freshly derived sha — live stray lanes task-w11-d/task-w12-a/task-w12-b may merge one mid-wave; a green duplicate resolves to a brief confirm, never a dropped gate; a section citing 3543f09 or older is void and counts as absent). Triage-closure carries DEC-117's amendment forward (chained behind perf-smoke, not walkthrough) because the single standing OPEN ITEM — task-w7-c/task-w8-b's overview.ts fan-out FAIL — can only be dispositioned CLOSED by citing a green perf-smoke section at the new sha alongside the landed DEC-104 fix (verified in-tree: chunkIds import at src/server/repo/overview.ts:11, chunked loop at :170-177). Ports: 8841 walkthrough, 8843 perf-smoke; 8801-8833 are burned or possibly held by stray lanes; 8787 never. All four lanes are code-frozen per DEC-077 and touch only docs/verification-log.md plus their own docs/verification-log/<task>.md detail file; same-file append overlap is expected and the merge agent resolves it. Round-0 namesake note: the log already contains round-0 sections headed 'task-w13-*' at old shas (0ee30dd/d4ebf7f); DEC-069 sha-scoping disambiguates — wave-13 workers append new sections at the derived sha and ignore the namesakes.
