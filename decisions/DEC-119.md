# DEC-119: Wave-13 structure: three parallel verify-or-run gates — build+test, walkthrough@8841, perf-smoke@8843 — plus triage-closure chained behind perf-smoke; all cite the DEC-114-derived sha (expected 3b7ed3d); fresh ports because stray w11-d/w12-a/w12-b lanes may hold 8821-8833

With spec-audit green at 3b7ed3d (DEC-118), exactly four DEC-069 scopes are missing at the newest code-bearing sha and zero product code is outstanding, so per DEC-117's own logic no barrier exists: build+test, walkthrough, and perf-smoke start in parallel, each idempotent verify-or-run per DEC-103 (first grep docs/verification-log.md for a green same-scope section at the freshly derived sha — live stray lanes task-w11-d/task-w12-a/task-w12-b may merge one mid-wave; a green duplicate resolves to a brief confirm, never a dropped gate; a section citing 3543f09 or older is void and counts as absent). Triage-closure carries DEC-117's amendment forward (chained behind perf-smoke, not walkthrough) because the single standing OPEN ITEM — task-w7-c/task-w8-b's overview.ts fan-out FAIL — can only be dispositioned CLOSED by citing a green perf-smoke section at the new sha alongside the landed DEC-104 fix (verified in-tree: chunkIds import at src/server/repo/overview.ts:11, chunked loop at :170-177). Ports: 8841 walkthrough, 8843 perf-smoke; 8801-8833 are burned or possibly held by stray lanes; 8787 never. All four lanes are code-frozen per DEC-077 and touch only docs/verification-log.md plus their own docs/verification-log/<task>.md detail file; same-file append overlap is expected and the merge agent resolves it. Round-0 namesake note: the log already contains round-0 sections headed 'task-w13-*' at old shas (0ee30dd/d4ebf7f); DEC-069 sha-scoping disambiguates — wave-13 workers append new sections at the derived sha and ignore the namesakes.

## Amendment (wave 17): harness port allocation 8871/8873, self-booting sweep, and exactly one full-suite run per wave

DEC-119 already fixes "fresh ports because stray lanes may hold the old ones" and the field guide records TWO HARNESS LANES SHARING A PORT MEASURE EACH OTHER. Wave 17 allocates: task-w17-a owns 8871 (walkthrough, via --url), task-w17-b owns 8873 (perf-smoke, via the PERF_URL env var it actually reads at scripts/perf-smoke.ts:47 — there is no --url flag), task-w17-d boots its OWN wrangler dev on a self-selected free port (scripts/render-sweep.ts already does this, so it must NOT be given a fixed port), and task-w17-c runs no server at all. The hard test policy stands: every lane runs TARGETED tests only, and task-w17-c is the single designated runner of the FULL suite, through `sh scripts/with-test-lock.sh vitest run`. No other lane may invoke the full suite.

## Amendment (wave 18): the runtime battery is not a worker lane — four lanes across two waves produced four refs equal to their base

REF TRUTH read from .git at main 62685d2e: task-w16-c and task-w16-d both point at c557cff9 (main's ancestor) and task-w17-a and task-w17-b both point at 5fc3db38 ("scribe wave 17", main's parent) — four measurement lanes, zero commits, while every code+test lane in waves 14-16 landed. Whatever the cause (boot time, port contention, budget), planning the same shape a fifth time is planning a known-zero. RULE for this wave: no worker lane boots a server. Regression measurement is the merge train's own full-suite + build pass through scripts/with-test-lock.sh (which exists at scripts/with-test-lock.sh — the mechanical task that owed it has landed); the walkthrough/perf-smoke/render-sweep scripts stay in the tree for the operator. A branch whose ref equals its base measured nothing, and a plan that keeps re-buying that measurement is paying for archaeology twice.

## Amendment (wave 25): the instrument lane may boot a server again, because the receipt is now committed either way

The wave-18 amendment banned server-booting worker lanes after four measurement lanes across two waves produced refs equal to their base. The diagnosis was right and the remedy was too broad: the swarm then spent six waves on code-read micro-fixes, five of which were phantoms, while a real 14-item measured backlog sat unread in docs/verification-log/task-w17-d-render-sweep.md. RULING: a lane whose scope is the render-sweep instrument MAY run `npm run gate:render-sweep`, which boots and tears down its own wrangler dev on a free port it selects itself — never pass it a fixed port and never pre-start a server for it. The zero-commit failure mode is closed by construction instead: every such lane commits its code + unit tests FIRST and its verification-log detail file regardless of whether the run completes, so a ref can never equal its base; if the sweep cannot boot, the log says so plainly. No lane runs the full suite this wave (targeted vitest only); the merge train remains the single full-suite runner via scripts/with-test-lock.sh.
