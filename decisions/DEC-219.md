# DEC-219: The exit battery is planned only after the planner verifies every task-w* ref is merged or VOID, and each battery task carries the literal frozen sha in its task text

Waves 16-22 repeatedly voided batteries because workers derived 'newest code-bearing sha' while lanes were still draining — reviewers correctly called this 'guessing at merge state'. The one battery that stuck (w20, '@6807b67') was planned with a known literal sha. Codify that pattern: wave 23 is fixes-only; at wave-24 planning time the planner confirms zero unmerged code-bearing task branches remain in .git/refs/heads, derives the newest code-bearing sha per DEC-114 once, and embeds it verbatim in all six battery task descriptions. Sha drift during the battery = FAIL-stop (DEC-208), never rebind.
