# DEC-683: The public sessions page is list + rail, the rail never renders inside /embed, and "Save" is the ONE itinerary vocabulary

Verified: src/routes/public/sessions.tsx is still the pre-redesign surface — a bare search form, track pills, "N of M session(s)" and stacked cards — with no right rail and no per-row save, exactly what mandate item 27 (user QA, strengthens the fidelity PARTIAL) describes; SessionCard's own `itinerary` prop (src/routes/public/cards.tsx:123) is passed by nobody and is dead. The mock (docs/design/Chautauqua Public and Portal.dc.html:47-110) is unambiguous: a 1fr/300px grid whose aside carries Your schedule (count + Download .ics), a day index, and a Call for papers card, with a Save/Saved control on every row. The itinerary machinery already exists and is correct (src/lib/itinerary.ts + ItineraryScript's #chq-ics-link/#chq-ics-count contract, DEC-555/584) — the sessions page must REUSE it, never grow a second store or a second script, and the Save control stays a real .chq-itinerary-toggle checkbox styled as the mock's pill so it keeps its accessible name and needs no new JS. Two boundaries are load-bearing: the rail links out to /submit/<slug> and /e/<slug>/..., so inside /embed it must not render at all (DEC-672: a chromeless surface is closed both ways, proved by enumerating hrefs), and "is the CFP open" goes through submit-core.ts's formWindowState with the default form's window — the same resolver the home hub uses — never a second date comparison.

## Amendment (wave 65): the printable programme is a real public route, not a rail label

DEC-683 built the public sessions page as list + rail and fixed the rail's contract (chromeless-closed, one itinerary vocabulary, formWindowState as the single CFP-window reader). The v7 handoff's agenda section names a third rail item this repo has never built — "a printable programme" — and the surrounding copy makes clear what it is: a one-page, all-days rendering, the thing a producer actually hands to a venue and an attendee actually prints. Verified absent: no route, no module, no `Printable` string anywhere in src. Ruling: `GET /e/:slug/programme` is a public, no-login, print-first surface rendering every day of the published programme in one sequence — sessions merged with break rows in start order, day headings, no nav, no rail, no itinerary controls. It is a PROJECTION, never a new query: the same `getPublicAgenda` visibility gate every other public surface reads, plus `getPublicBreaksByDay`, so an unapproved or hidden session can no more leak here than it can on /sessions. Its CSS lives in its own module with a real `@media print` block rather than growing public.css.ts, because print is a different medium with different rules (no fills, no page-breaks inside a day). Its link lives in the sessions rail for now (DEC-672/683: `!embed` only — the rail links out to /e/... and must not render inside an iframe); when the agenda gains its designed rail, that rail carries the same link to the same route, never a second implementation.

## Amendment (wave 67): the agenda gets its own rail, and the rail is what makes the block's Save control real

Two things collide here. (1) `src/routes/public/sessions.tsx:104-110` names the open work in a code comment: the printable-programme link "lives here for now ... until the agenda gains its own designed rail carrying the same link to the same route", and `measureClassForSurface` (`src/routes/public/shell.tsx:44-46`) still returns `canvas` for agenda on the rationale "its lane count is the event's room count" — a rationale wave 64 deleted when it replaced the room-lane matrix with the row sequence. (2) The real defect: `AgendaDayGrid` renders an `ItineraryToggle` on every block (`agenda.tsx:172`), which is a bare `.chq-itinerary-toggle` checkbox (`cards.tsx:195-204`) — and `AgendaContent` never renders `ItineraryScript`. `ScheduleContent` does (`agenda.tsx:778`), `SessionsContent` does (`sessions.tsx:301`), `SessionDetailContent` does (`detail.tsx:136`); the agenda does not. So on `/e/<slug>/agenda` the Save/Saved control writes nothing to `chq_itinerary_<slug>`, survives no reload, and has no `#chq-ics-link` to update — EMB-10/EMB-11 dead on the surface the design makes primary. RULING: agenda moves to the `wide` 1180 pair and gains the designed rail — Your schedule (`#chq-ics-count` + `#chq-ics-link` Download .ics, reusing the EXACT ids `ItineraryScript` already drives), Rooms in use today, Printable programme › — and `AgendaContent` renders `ItineraryScript` when `!embed`, exactly as its three sibling surfaces do. The rail's room rows are IN-PAGE ANCHORS to that room's first block of the day, never filters: room was dropped from the agenda bar deliberately (a schedule's columns already are rooms), so the rail must not smuggle the facet back in.
