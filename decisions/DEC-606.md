# DEC-606: An export the browser performs silently still reports itself, and no screen prints a total it did not count

CNT-14 passes only if the UI confirms generation; today the ZIP downloads with no in-page trace, so a judge cannot tell success from a dead button. The action gets a live region naming what it built. The same honesty rule bars the tempting companion: a library-wide size total computed from the loaded page would be a cap the UI cannot see, so the library shows the size of each row it can actually hand over and no aggregate it never queried.
