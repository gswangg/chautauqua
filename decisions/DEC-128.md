# DEC-128: Wave-16 contingency mirror gate battery: confirm-else-run semantics, fresh ports 8861/8862/8863, duplicates harmless, completion stays a planner grep

At wave-16 planning time the wave-15 battery is in flight but unlanded: branches task-w15-g..j point exactly at main tip 21ea856 with zero commits and task-w15-k does not exist, so the DEC-069 predicate is not grep-true (no gate section cites 675219f or later; newest code-bearing sha per DEC-114 is 675219f since 21ea856 "scribe wave 15" is bookkeeping-only). Planned gate lanes have died unrun before (DEC-117) while others were merely slow (task-w14-k), so wave 16 runs a mirror battery task-w16-a..e that guarantees predicate progress in either world: each lane FIRST re-derives the newest code-bearing sha per DEC-114, then checks main's docs/verification-log.md for an existing section covering its gate at/after that sha. If a valid PASS exists (header cites the sha AND the DEC-127 six-marker preflight is recorded), the lane appends a short confirm section ending "RESULT: PASS" and stops without starting servers. If a FAIL section exists, the lane independently reproduces it and appends its own honest section — fixes are wave-17 planning material, never a gate lane's job. If no section exists, the lane runs the full gate under DEC-127 rules (code-frozen per DEC-077 — only docs/verification-log.md may change, append-only per DEC-068; six-marker preflight mandatory before any PASS). Server-bearing mirrors use ports never touched by any wave — walkthrough 8861, perf-smoke 8862, triage spot-checks 8863 — so they cannot collide with possibly-live w15 servers on 8851-8853. Duplicate sections for the same gate at the same sha are harmless under DEC-068/069: the exit grep needs at least one valid section per gate, not exactly one. Stage-1 completion itself remains the planner's DEC-069 grep at the next planning invocation; no worker declares exit.
