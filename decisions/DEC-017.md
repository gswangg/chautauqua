# DEC-017: Wave-3 schema additions in one migration; legacy track columns frozen

Several wave-3 features need columns DEC-003 omitted, and parallel workers must not race on migration numbering (DEC-015). Exactly one task (w3-a) owns the next migration file (migrations/0002_wave3.sql, following 0001's file + meta/_journal.json pattern; take the next free number at branch time; never edit committed migrations): ALTER TABLE plan_reviewer ADD COLUMN submission_id text (+ index plan_reviewer_submission_id_idx) — plan_reviewer scope semantics: track_id set = all plan submissions in that track; submission_id set = that single submission; both null = all submissions matching plan filters; a reviewer's assignment is the union of their rows. ALTER TABLE task_assignment ADD COLUMN response_json text (form-task answers, JSON map fieldId->value), file_id text (file_request completion link), last_reminded_at integer ms (reminder dedupe). Matching append-only edits to src/db/schema.ts; other wave-3 tasks needing these columns may append IDENTICAL definitions if not yet on main (declared overlap; merge dedupes). Track membership: all wave-3+ code reads/writes ONLY the submission_track join (landed in migration 0001 per DEC-015); submission.track_id and additional_track_ids_json are frozen legacy — never read, never written, dropped in a later cleanup wave; the seed switches to submission_track rows.
