# DEC-809: A saved view re-saved under its own name is one view, and the directory headline states every figure it computes

The CRM P3 batch reports that saving a segment under an existing name creates a second identical segment — two rows with one name is a directory of ghosts, and the organiser's only repair is to delete the one they cannot tell apart. Separately VERIFIED src/server/repo/contacts/stats.ts computes eventCount and returningSpeakers, app/src/pages/contacts/types.ts declares them, and ContactsApp.tsx:195 renders only total/speakerCount/duplicateCount — the two figures that actually answer 'how deep is our speaker bench' are computed on every page load and shown to nobody. Decision: POST of a segment whose name already exists in this org UPDATES that segment's rules (a re-save is a save, not a twin) and the response says which id it wrote; deleting a saved view asks first, because a view is somebody's saved work. The directory headline states returning speakers and event reach alongside the counts already there, each with a word naming what it is — a number the API computes and the UI hides is a fact the product refuses to say. A database-level unique index on (orgId, name) is deliberately deferred: existing rows may already collide, and a dedupe migration is its own piece of work.

## Amendment (wave 38): a uniqueness rule the product states in prose is a uniqueIndex plus an atomic upsert

DEC-809 says a re-save under an existing name is one record, not two, and src/server/repo/contacts/segments.ts:59-73 implements it for segments as a read-then-write (findSegmentByNameForOrg, then insert or patch) while its own comment confesses the gap: "No DB unique index on (orgId, name) exists yet — pre-existing rows may already collide on name, so adding one here would break on migration; that dedupe is separate work." A confession in a comment is a defect with a comment. Two concurrent saves still twin, and nothing stops a direct writer. House invariants say uniqueIndex is the CONTRACT and atomic SQL beats read-then-write. Ruling: a migration deduplicates existing (org_id, name) collisions deterministically before creating the unique index, the schema declares it, and the writer becomes one onConflictDoUpdate. The named blocker is the work, not a reason to defer it.

## Amendment (wave 39): the stats API states bench depth unconditionally; the directory headline states it conditionally-and-quiet

DEC-809 ruled "The directory headline states returning speakers and event reach alongside the counts already there ... a number the API computes and the UI hides is a fact the product refuses to say", and the deletion wave's counter-argument was that "0 returning speakers" reads as a defect on a single-event demo org. Both are right about different orgs. The v9 Contacts frame (docs/design/Chautauqua Contacts.dc.html:57) shows exactly three headline clauses. Ruling that satisfies all three authorities: the endpoint always states both figures (DEC-432 amendment above); the headline renders `N people · M speakers · [R returning] · [across E events] · K possible duplicates`, with the returning clause present only when returningSpeakers > 0 and the reach clause only when eventCount > 1 — so the seeded single-event org renders exactly the frame's three clauses and a real multi-event org is told what its bench is worth. This is the pack's own conditional-and-quiet grammar (a control/figure appears only when it says something), not an improvised fourth stat band.
