# DEC-217: app/src/routeManifest.ts gains authenticated /account/password entries for organizer, reviewer, and speaker so the render-sweep gate covers DEC-200's account routes

The task-w20-d render-sweep PASSed 31/31 but recorded OPEN ITEMS: 1: the hand-enumerated manifest has no /account* entry, so the DEC-200 self-service password page's authenticated render is never exercised by the automated gate (only an anonymous-302 curl spot-check exists). The page is role-agnostic (any authenticated user), so one entry per role exercises it under each seeded identity the sweep already logs in as. This closes the only recommendation the w20 battery carried forward and makes the wave-23 render-sweep strictly stronger than its predecessor.
