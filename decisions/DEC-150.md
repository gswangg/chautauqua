# DEC-150: CRM bulk-email parity + dashboard: BulkEmailModal gains an event-template picker and a server-rendered merge preview via new POST /api/v1/contacts/bulk-email/preview (shared render helper with send, first 5 recipients); StatsStrip becomes a dashboard with KPIs plus a click-through top-companies widget

CRM-11: the event Comms path already has templates and preview; the CRM path must reach parity, and the preview must use the exact merge-render code path as the send (extracted shared helper) so previews can never lie. Loading GET /events/:eventId/templates reuses the existing template store rather than inventing a second one. CRM-12: the stats repo already computes totals, returning speakers, and top companies — the gap is presentation; upgrading StatsStrip to a dashboard panel with KPI counters and a top-companies widget whose click applies a {field:'company',op:'eq'} DEC-149 rule satisfies the rubric's drill-through bonus with data we already have.
