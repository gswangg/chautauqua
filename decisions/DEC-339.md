# DEC-339: Wave-18 product commits supersede task-w17-c's build/test evidence; wave 19 re-runs exactly the gates whose covered code moved, and nothing else

DEC-330 set the stage-1 exit predicate as DEC-327 plus task-w17-a/b/c/d/e green, with task-w17-c supplying build+test+audit+tripwires at a post-decomposition sha. Wave 18 lands product changes in src/server/repo/{overview.ts,submissions/list.ts,contacts/crud.ts}, src/domain/contacts.ts, src/db/schema.ts, migrations/ and scripts/perf-*.ts, so that build/test evidence is stale by construction the moment any w18 lane merges — recording otherwise would repeat the DEC-195/DEC-203 pattern of a battery declared green at a sha the tree has left. The wave-19 re-run set is exactly: (1) npm run build + npm test + the four test/ tripwires at the post-w18 tip; (2) scripts/walkthrough/producer.ts (submissions list filters/paging) and scripts/walkthrough/data.ts (contacts directory, exports, email log) — the only two walkthrough modules whose covered code this wave touches; (3) npm run perf:smoke against the extended perf seed. NOT re-run: spec-audit, rubric-coverage, fresh-clone, render-sweep, and the speaker/review/public/scale walkthrough modules, whose covered code wave 18 does not touch. goalComplete belongs to the first planner that can read task-w17-a/b/c/d/e green AND this re-run set green at a sha containing every merged w18 lane; a FAIL yields a narrow wave over the cited file:line only. DEC-315's anti-padding rule survives intact.
