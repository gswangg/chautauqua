# DEC-362: The stage-1 exit predicate, restated for the next planner: six wave-23 logs at OPEN ITEMS: 0 / RESULT: PASS, then goalComplete — supersedes DEC-358's three-log predicate

Mechanical, so the next planner evaluates it without re-litigating scope (DEC-315's pattern). goalComplete: true iff all six of docs/verification-log/task-w23-{a,b,c,d,e,f}-c3-*.md exist — on main OR readable in their chautauqua-wt worktree, exactly as DEC-307/DEC-315 permitted for unmerged logs — and every one states OPEN ITEMS: 0 and RESULT: PASS. Any FAIL yields a narrow wave addressing ONLY the cited file:line defects; settled scope is never reopened (DEC-287's narrowing branch). Stage-2 items — Cloudflare provisioning, wrangler deploy, real Resend delivery/webhooks, Airtable sync, DNS, CI deploy, production edge-cache/perf validation — are never open items and never block this predicate. The global public-cache purge in src/server/pubcache.ts remains CLOSED (DEC-201, reaffirmed by DEC-333, DEC-348 and DEC-358): a planner or auditor who rediscovers it should read those four decisions and stop. Likewise DEC-342's files-library deferral is DISCHARGED by DEC-344. A planner invoked while wave-23 lanes are still running and logs are incomplete returns ZERO tasks and waits — padding is a worse failure than waiting.
