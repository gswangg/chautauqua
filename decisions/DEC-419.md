# DEC-419: Wave 10's three gate lanes re-measure build/test, perf, and the phone sweep against the redesigned tree, log-only

docs/verification-log holds exactly one redesign-phase build-test log (task-w5-h) and zero redesign-phase perf-smoke logs, while waves 6-9 re-skinned every surface, added co-located page stylesheets, and landed three source conformance guards (table-class, focus-ring, and DEC-410's control-class). SPEC.md:356-357 says budgets that aren't enforced are decoration, and SPEC.md:325-326 says the numbers are measured at scale-target row counts -- a perf reading taken from the pre-redesign tree is not evidence about this tree. The render-sweep lane exists to check the one claim wave 9 makes loudest: that DEC-411's raw-string addInitScript shim turned 35 rows of `ReferenceError: __name is not defined` into real measurements. That lane must first confirm the shim is actually present in scripts/render-sweep.ts and, if it is not, report the run as instrument-blocked rather than transcribing zeros as measurements -- reporting an instrument failure as a number is the exact error four waves of 'mobile pass' figures made. All three lanes stay LOG-ONLY per DEC-384/DEC-411: each writes exactly one file under docs/verification-log/ and touches nothing else, never flips ADMIN_MOBILE_PASS_BLOCKING, and never fixes what it names.
