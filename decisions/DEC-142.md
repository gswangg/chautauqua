# DEC-142: Speaker-edited profile surfaces on the organizer's contact record: ContactDrawer shows and edits bio, social links, and headshot from the same contact row the portal writes

Portal profile edits already write the one true contact row (src/server/repo/profile.ts, DEC-028) and GET /contacts/:id already serializes bio/headshotUrl/socialLinks — the SPK-08/CNT-10 gap is purely display: no organizer view renders those fields. Fix in the contacts drawer (one record, two views — no shadow model, no new endpoint): render headshot via the authenticated /files route URL stored in headshotUrl, show bio and social links, and make bio organizer-editable through the existing PATCH /contacts/:id (repo patchContact already accepts bio). This closes the round-trip both directions with zero schema change.
