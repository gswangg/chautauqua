# DEC-039: Reviewer-surface org scoping: organizers resolve plans only via getPlanForOrg

Cross-org IDOR confirmed: requireAssignedPlan (review.ts:344) returns any plan to any organizer via un-scoped getPlanById, and the organizer branch of GET /api/v1/review/plans (review.ts:328) lists plans for an arbitrary eventId — violating SPEC §6 object-level ownership. Binding rule: on /api/v1/review/*, organizers resolve plans exclusively through repo.getPlanForOrg(db, planId, auth.orgId) (the same helper the producer endpoints already use); the plans list requires an eventId verified to belong to auth.orgId before listing; reviewers continue via plan_reviewer assignment with not_found on miss. Un-scoped getPlanById must not appear in any route handler.

## Amendment (wave 37): the reviewer surface's org scoping is proven over real rows, not only over mocks

test/review-idor.test.ts mocks getPlanForOrg (org-scoped) and getPlanById (deliberately unscoped) to encode the very rule the route is supposed to depend on. That is a real and worth-keeping proof of guard ORDER — it shows the handler never reaches the unscoped helper for an access decision — but it cannot show that the scoped resolver's predicate actually filters, because the predicate is the mock. The reviewer probe re-asks the question against real rows across the whole /api/v1/review/* population, including the planId query-param precondition that test/role-refusal-probe.test.ts had to ledger rather than exercise. Both stay: mocks for order, real rows for the predicate. Neither is allowed to be cited as covering the other.
