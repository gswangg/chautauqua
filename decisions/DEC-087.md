# DEC-087: Review-rounds migration renumbered to migrations/0009_review_rounds.sql (journal idx 9); DEC-082 repo/API details pinned

DEC-082 named migrations/0005_review_rounds.sql, a name superseded below by this decision, but idx 5 / prefix 0005 is already occupied by 0005_w4_segment.sql in migrations/meta/_journal.json (entries run 0..8; later migrations follow the journal-entry-only pattern, no snapshot files past 0004). A duplicate prefix would corrupt journal ordering. Correction: file migrations/0009_review_rounds.sql, journal entry idx 9 / tag "0009_review_rounds", SQL `ALTER TABLE evaluation_plan ADD COLUMN current_round integer NOT NULL DEFAULT 1;`. Also pinned so no worker re-decides them: listEvaluationsForPlan(db, planId) gains a REQUIRED third param `round` and every call site (queue, progress, results, remind) passes one; POST /api/v1/plans/:id/advance-round (requireOrganizer + csrfJson) returns the updated plan JSON, or 409 {error:{code:"conflict"}} when currentRound >= rounds; ?round= on progress/results must be an integer in 1..plan.rounds (default plan.currentRound), otherwise {error:{code:"invalid"}}; plan create rejects non-integer or <1 rounds, PATCH additionally rejects rounds < currentRound. All other semantics per DEC-082.
