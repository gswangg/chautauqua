# DEC-231: CRM-02 'no true multi-criteria filter / lossy segment-from-search' is CLOSED as implemented; the w24-f open item was a prose-grep false negative

task-w24-f's STEP 2 grepped for the literal prose strings 'multi-criteria filter' and 'segment-from-search', which cannot match code, and flagged CRM-02 as unresolved (open item 2). Planner verified the implementation on the tree: src/domain/contacts.ts:224-288 defines SegmentRule[] with AND semantics across rules (matchesSegment: rules.every) and a dedicated field === "any" rule so a saved free-text search matches across fields rather than collapsing to a lossy firstName-only rule; the admin UI ships FilterRulesPanel.tsx/SegmentsPanel.tsx/segments.ts with unit tests (app/src/pages/contacts/segments.test.ts) — matching the field-guide record 'CRM=SegmentRule[]+any'. w24-f open item 1 (sha drift) was already resolved by DEC-223's freeze re-derivation. The wave-27 triage-closure lane cites this decision to close both items; workers never edit docs/eval-findings.md themselves.
