# DEC-836: The seed demonstrates the reads a grader performs: two plans open at once, and a worklist with work in it

VERIFIED the seed satisfies the schema and starves two surfaces. scripts/seed.ts:1236-1266 makes plan 2 CLOSED on purpose (both bounds before SEED_NOW) so exactly one plan is ever open — which means the multi-plan reviewer landing, the per-plan scoping DEC-831 is about, and 'which queue am I in' can never be seen; and the only content the file pipeline seeds is one v1→v2 chain on a single accepted submission (seed.ts:1828-1918) plus per-task deliverables, so the Content worklist demos 28 of 30 rows as 'No files yet' and its three chips read near zero. Separately, ADDITIONAL_EMAIL_TEMPLATES (scripts/seed-lib.ts:314) puts the multi-line {task_list} block in a SUBJECT line, which renders paragraph subjects and a double period on the empty case. Decision: the seed carries a SECOND plan that is open at SEED_NOW with distinct criterion weights (docs/design/README.md:204 — weighted must visibly differ from naive) and at least one reviewer scoped to both open plans; deliverable files with a spread of content statuses across enough accepted sessions that each worklist chip has a non-zero count under the product's OWN predicate; and a reminder subject that is a subject. Proof is by ENUMERATION in the seed test — the product's predicates counted over the seeded rows — never a hand-listed expectation, and no two tasks in one event share a title.
