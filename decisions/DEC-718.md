# DEC-718: A validator rejects what the persistence layer cannot carry: number answers must be finite, and every accepted answer survives a JSON round trip

src/forms/validate.ts:112 rejects only NaN, so 'Infinity', '-Infinity' and '1e400' validate as ok and are then written by JSON.stringify at src/server/repo/submit.ts:320 as the literal string 'null'. A public CFP submitter sees the success confirmation while a REQUIRED number answer has silently vanished — an unhandled input class on the one unauthenticated write path (SPEC §6: the endpoint validates against the server's form schema). Patching the number branch alone repeats the pattern that produced it. The invariant that actually holds is broader and testable per-kind: whatever validateAnswers accepts must deep-equal itself after JSON.parse(JSON.stringify(...)). Enumerated over the FieldKind union rather than a hand-listed set, that test outlives the next new field kind.

## Amendment (wave 11, sha 85d8b9ac): submission_answer.value_json gets the read-side owner its write-side contract always implied

DEC-718 gave this column a write-side contract (src/server/repo/submit.ts:51-60 assertJsonRoundTrips, which throws NAMING the field id) and no read-side one. Six readers in six files hand-parse it with an unvalidated cast — portal-edit.ts:157, submissions/list.ts:473, submissions/detail.ts:224, review/submissions.ts:949, exports/submissions.ts:222, portal/data.ts:373 — while every sibling JSON column closed in waves 79-81 got exactly one validated parser module (forms/field-json.ts, forms/form-tracks.ts, forms/task-response.ts, domain/evaluation/plan-json.ts, domain/evaluation/scores-json.ts). It is the highest-traffic JSON column in the schema and the last one with no owner. RULING: one pure-core parser, src/forms/answer-json.ts, modelled on field-json.ts; its accepted shape is DERIVED from what the writers can actually store (submit.ts, portal-edit's save path, portal/tasks.tsx, submit-core.ts) and what src/forms/validate.ts admits — never invented, so a shape a writer can produce must parse. Two readers stay outside it and say why in the new module's header: form-roles.ts:45 roleAnswerLabel (DEC-592's separate role-answer resolver, whose own header forbids conflation) and forms.ts:506 (option-usage counting, whose shape tolerance is deliberate).
