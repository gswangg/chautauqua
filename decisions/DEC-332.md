# DEC-332: The barrel decomposition of src/server/repo/public.ts is ratified; cite the submodule that owns the behaviour, and the DEC_258 compile-checked marker belongs in every module that reads the frozen attribution snapshot

task-custodian-w16-3 split src/server/repo/public.ts into a pure re-export barrel over ./public/{gates,event,sessions,speakers,detail,agenda}.ts. I checked the split at the symbol rather than trusting the commit message: all three DEC-274 gates plus slotWithinEventRange live in public/gates.ts (:65), the four DEC-318-bounded schedule_slot reads survive at public/sessions.ts:232, public/detail.ts:43, public/agenda.ts:40 and public/agenda.ts:120, getPublicAgendaByIds (DEC-310) is in public/agenda.ts, and every previously-exported symbol is still exported from the barrel, so imports of '../repo/public' keep working. The layout is ratified as canonical. Two consequences. First, per DEC-328's symbol rule, every citation in task-w15-c-c3-build-test.md's Part B that names a pre-split public.ts line number is stale-by-refactor and must be re-anchored at the submodule — and citation staleness alone never re-opens the six wave-14 findings, which task-w15-d confirmed LIVE. Second, the split dropped a house invariant: the pre-split file carried `import { DEC_258 }` and `void DEC_258;` guarding every read of participant.title_at_time/org_at_time, and only public/detail.ts:8,18 kept it, while public/sessions.ts:185-186 and public/speakers.ts:43-44 read the same frozen-snapshot columns unguarded. A compile-checked marker that covers one of three call sites is worse than none, because it reads as coverage. Restore it in both files. Decomposition may move code; it may not silently shed the guards that were attached to it.
