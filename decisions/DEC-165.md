# DEC-165: Wave-4 battery lanes never executed; consolidation is already complete on main via the merge train; the battery re-issues as wave 5 with task-w5-a as preflight and the frozen sha defined by the w5-a merge

Main's reflog shows no task-w4-* merges after 'scribe wave 4' (f357477) — the wave-4 lanes were created but never ran. Meanwhile DEC-163's premise dissolved: task-w3-a/b/c/d all merged before scribe wave 4, and I verified their deliverables in-tree (pipeline routes/repo + migration 0012, submission_revision repo + migration 0013, files routes + src/lib/zip.ts, render smokes, gate scripts, DEC-145 seed enrichment). The DEC-162 battery therefore re-issues unchanged in structure: exactly one code-bearing preflight lane (task-w5-a), six gate lanes chained on it running in parallel. The frozen battery sha is the newest code-bearing first-parent main sha after task-w5-a merges — self-verifying: it must contain the ci.yml render-sweep job (DEC-166) and pass git merge-base --is-ancestor 2dd2f33 <sha> (DEC-129/139). Per DEC-069/163, the gates' own sequential docs-only verification-log merges and scribe commits never invalidate it. Homonym warning: campaign-1/2 sections literally named 'task-w5-*' already exist in docs/verification-log.md; only the sha, never the branch name, identifies a citable section.
