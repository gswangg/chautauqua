# DEC-315: The stage-1 exit predicate: seven wave-13 '-c3-' logs at OPEN ITEMS: 0 / RESULT: PASS; a planner that cannot see all seven returns ZERO tasks rather than padding

DEC-307 proved the planner runs ahead of the merge train (harness lowWater=8, chautauqua-build.js:274, and lane() calls maybeReplan the moment a lane goes idle at :473/:520), so this planner cannot be the one that declares. Make the predicate mechanical so whoever runs next can evaluate it without re-litigating scope: goalComplete iff docs/verification-log/task-w13-{a,b,c,d,g,i,j}-c3-*.md all exist — on main OR readable in their chautauqua-wt worktree, exactly as DEC-307 read wave-11's unmerged logs — and every one states OPEN ITEMS: 0 and RESULT: PASS. Any FAIL yields a narrow fix wave addressing ONLY the cited file:line defects, never a re-opening of settled scope (DEC-287's narrowing branch). A planner invoked while lanes are still running and logs are incomplete must return ZERO tasks: padding with invented work is a worse failure than waiting, and the harness's own end-of-run final-tag (build+test+round-final) and fresh-eyes codebase review at chautauqua-build.js:530-535 close the run honestly even if the empty-replan counter converges first. Stage-2 items (deploy, real Resend, Airtable, DNS, CDN/edge cache measurement) are never open items and never block this predicate.
