# DEC-518: A cross-file manifest is DERIVED in a test, never hand-mirrored — including the test that guards a manifest

Third instance of the DEC-503/511/513 shape, and the first one where the guard itself is the desync risk. `test/docs-route-coverage.test.ts:33-58` protects the judge-facing `/docs/api` page by hand-mirroring `src/index.ts`'s `app.route()` calls in a local `buildApiApp()`, with a comment admitting it mirrors rather than imports; a new sub-app mounted in index.ts and forgotten here would leave its endpoints undocumented AND leave the test green — the exact failure the test exists to prevent. Separately, `app/src/pages/settings/ExportsPanel.tsx:7-13` hand-copies the five export kinds that `the former src/server/repo/exports.ts:25 (now split across src/server/repo/exports/)` already exports as `EXPORT_KINDS`; they agree today, with nothing making them agree tomorrow. Both are verified as currently CORRECT — this decision buys the invariant, not a bug fix, which is precisely the standing field-guide rule that enumeration means a re-runnable artifact scanning the source, never a hand-picked sample. Decision: the docs coverage test derives its mount list by parsing `src/index.ts`'s `app.route(<prefix>, <ident>)` calls and resolving each identifier through that file's imports, failing loudly and by name on any sub-app it cannot resolve (no allowlist that can silently absorb a new module); and a second test asserts ExportsPanel's kind list equals `EXPORT_KINDS` in both directions.

## Amendment (wave 43): SPA fetch-failure honesty — a `.catch` in app/src/pages|components either surfaces its outcome or is ledgered with a reason

The server already has this rule: test/mail-swallow-honesty.scan.test.ts (DEC-006 wave-51) makes every catch around mailer.send prove it surfaces or carry a ledgered reason, with the DEC-518 negative-control convention. The SPA has no such rule and has drifted: most page fetches route their failure into a setError-family channel, but app/src/pages/Comms.tsx's recent-batches read (`?groupBy=batch`, ~line 79) and its DEC-905 seven-day sent/failed figures (~line 108) both end in `.catch(() => undefined)`, leaving `batchesLoaded`/`sentLast7DaysLoaded` false forever — on the one page whose entire job is the send audit trail, a failed read renders as a permanent loading state and a permanent "0 sent", with nothing on screen saying so. Other silent catches on the same surface ARE legitimate and already carry written reasons in place (Scorecard.tsx's DEC-831 tracks and DEC-939 queue-progress reads; Comms.tsx's templatesById courtesy annotation). RULING: the distinction is not "never swallow" but "a swallow is a recorded decision" — the population is re-derived from source at test time, classification is by whether the handler body names a setError-family setter or rethrows, and every non-surfacing handler must appear in a closed, two-directional ledger keyed by a stable marker (the fetch's URL template, never a line number) with a one-line reason. Field-guide shape: A CATCH RETURNING A DEFAULT IS NOT A GUARD — AND A SILENT DEFAULT ON THE AUDIT SURFACE IS A BLANK PAGE.

## Amendment (wave 32): SPA module reachability is enumerated, not assumed — a component whose only importer is its own test is a finding

The field guide records the shape A SHARED COMPONENT WITH ONLY ITS OWN TEST AS CONSUMER MARKS THE SHAPE NOBODY BUILT: a wave built a shared surface, wrote its render test, and never wired it into a page, and nothing failed. DEC-518's parse-source/resolve-imports technique (already reused by test/ssr-link-targets-scan.test.ts and by DEC-550's mount enumeration) is exactly the tool that makes recurrence impossible. Scoped deliberately to app/src/** — one entry point, a bounded population, and the only place the shape has actually occurred. Ruling: a scan builds the app/src import graph from source text and fails on any non-test module unreachable from the SPA entry; the ENTRY/allowlist is two-directional. An orphan is deleted (house rule: no dead code, no compat shims) or wired; it is never left silent.

## Amendment (wave 33): a decision's own file-path references are ENUMERATED against the tree — a path that no longer exists is either corrected or marked historical in the prose

DEC-518's rule is that a cross-file claim is derived by a re-runnable scan, never trusted; its wave-32 amendment applied that to SPA module reachability. The same technique applies to the decision corpus itself, which is the repo's primary index of intent: 1000 decision files carry file:line references written across dozens of refactors, and at least two monoliths have since been split into directories (the former src/routes/api/contacts.ts (now split across src/routes/api/contacts/) -> contacts/, src/routes/portal/tasks.tsx -> tasks/), so decision prose written before those splits now points at files that are gone. A reader who greps decisions/ first (the field guide's standing instruction) is then handed a dead pointer. RULING: a scan asserts every src/, app/src/, scripts/, migrations/ and test/ path token in decisions/*.md either exists on disk or sits in a sentence explicitly marking it historical (former/formerly/was at/superseded/deleted/renamed/moved to/replaced by). Deliberately no allowlist array: an exemption must be a WORD IN THE PROSE, where the next reader of that decision will see it, rather than a line in a test file they will never open. The scan also asserts a non-trivial population so a broken regex cannot pass vacuously.

## Amendment (wave 35): every source-text scan ships a NEGATIVE CONTROL — a proof nobody has ever shown a violation to is not a proof

Twice now a scan that reads as rigorous has turned out to be unfalsifiable: email-shell-sweep's "every send site is accounted for" test asserts the identical predicate in both arms of its branch (six files declared against eight real send sites), and the route-authz enumeration accepts a CSRF guard as an authorization guard. Both passed continuously while the invariant they claim to hold was false. DEC-518 already rules that a cross-file manifest is DERIVED in a test rather than hand-mirrored, including the test that guards a manifest; this amendment extends that to the predicate itself. RULING: a scan's detection logic must be a pure function over source text (fileText, repoRelPath) -> violations, and the scan file must contain at least one test that feeds it a synthetic VIOLATING snippet and asserts the violation is reported, plus one compliant snippet asserting silence. Where a scan compares a declared list against a discovered population, BOTH directions are asserted — declared-but-absent and discovered-but-undeclared — and two branches asserting the same predicate is a bug, not a style. A scan without a negative control is treated as unproven.

## Amendment (wave 36): the rubric-coverage claim is a DERIVED, two-directional ledger, not sha-pinned prose

docs/verification-log/task-w13-g-rubric-coverage-stage1.md tables all 116 `- id:` rows of docs/eval-rubric/*.yaml with a file:line + test citation each, RESULT: PASS — and it is frozen at sha 0578511. It is the single largest CURRENT-tense claim about the tree in the repo and nothing checks it; its own prose already records that src/routes/review.ts (formerly a monolith, now decomposed) and src/server/repo/public.ts (formerly a monolith, now decomposed) were split under it, which is precisely the drift DEC-518's decision-path scan was written to catch (test/decision-path-references.scan.test.ts, scoped to decisions/*.md only). DEC-459 set the precedent for exactly this move — replacing pinned route-authz prose with a scan that re-derives the population at test time. Same shape here: population re-derived from the yaml, ledger in the test file, exact in both directions, every cited artifact path must resolve on disk, waivers must name a DEC. A frozen evidence log stays frozen (historical documents may point at moved files); what becomes executable is the coverage claim, carried forward into a live artifact.

## Amendment (wave 39): a rendered whole-set count comes off the envelope — an enumerated, two-directional SPA ledger with named exclusions

Two independent surfaces this wave and last were caught reporting a page as the whole: ReviewerQueue.tsx derives "N left to score" from res.items after the server clamped to 200, and MergePage.tsx reports res.items.length as the pair total. Fixing instances leaves the family. DEC-518's own discipline (a claim becomes a scan that re-derives its population at test time, in both directions, shipping a negative control) is the right closure: every non-test module under app/src/pages/** that reads a paged list endpoint carries exactly one ledger verdict — `envelope` (counts read from res.total/res.count), `page-scoped` (the number is deliberately about the visible rows and the source says so, e.g. FilesLibrary's "Download all N"), or `unpaged` (the endpoint returns the whole set by contract) — and every ledger row names a live file. Exclusions are named with their owning task in the file header, never silently omitted, so the ledger is honest rather than short.

## Amendment (wave 41): the count-source population is every reader of a PAGINATED ENVELOPE, whichever helper fetched it

test/spa-count-source-ledger.scan.test.ts derives its population as 'app/src/pages/** modules calling apiList', and states in prose that MergePage.tsx 'is the one place this codebase calls apiGet against a paginated list route'. That claim is false on the current tree: app/src/pages/content/FilesLibrary.tsx calls apiGet<EventFilesEnvelope>('/events/:id/files?...') against a page/perPage route (src/routes/files.ts:241-244) — it happens to behave correctly, which is precisely why nobody noticed the population is blind to that whole family. A scan whose own honesty prose is untrue is the same class of defect as a decision with no code. Ruling: the ledger population is derived two-sidedly — paginated envelope routes enumerated from src/routes/** (handlers returning {items, total, ...}), SPA modules enumerated by the URL literals they fetch with EITHER helper — so a future count lie told through apiGet is caught, and the false 'one place' sentence is corrected rather than left standing.

## Amendment (wave 42) — the /docs/api endpoint table is a cross-file manifest of the router and must be DERIVED-CHECKED in both directions, not hand-mirrored

J12 sells the REST API as the premise ('No lock-in is not a feature of this product; it is the premise'), and src/routes/docs.tsx hand-types ~90 endpoint rows (180 method:/path: lines) while test/docs-page.test.ts only greps a handful of strings. test/helpers/registered-routes.ts already enumerates every registration under src/routes/** + src/index.ts resolved to its full mounted path, so the population exists and the doc table is exactly the hand-mirrored manifest DEC-518 bans. RULING: the endpoint rows move to a JSX-free module src/routes/docs-endpoints.ts exporting `API_DOC_ENDPOINTS: { method: string; path: string; role: string }[]`, docs.tsx renders from that single source, and a scan asserts both directions against the derived /api/v1 population — every documented row names a live registration, every live /api/v1 registration is documented or carries a reasoned row in a small DOCS_OMITTED ledger. Drift found by the scan is fixed in the data, never by loosening the scan.

## Amendment (wave 41): docs/clarifications.md — the top of the precedence chain — gets the same derived, two-directional ledger the rubric corpus already has

`docs/README.md` puts `docs/clarifications.md` above everything: it overrides the brief, the Sessionboard reference and (where they differ) SPEC.md itself, so an unhonored clarification is a correctness bug by definition. `docs/eval-rubric/*.yaml` has `test/rubric-coverage-enumeration.scan.test.ts`, which re-derives its 116-row population at test time and asserts a citation ledger in both directions; `SPEC.md` §9's four named invariants have `test/spec9-invariants.test.ts`; `docs/AUDIT.md`'s route and absence claims have `test/audit-claims.test.ts`. `docs/clarifications.md` has NO instrument at all — verified by grepping `test/` for `clarifications`, which returns only three files that cite one bullet each in passing. Same shape as this DEC's original ruling (derive the manifest, never hand-mirror it): every bullet under `## Scope reductions` and `## Scope confirmations` becomes a population member re-derived at test time, each carrying a resolvable citation, and — critically for the SCOPE REDUCTIONS half — an ABSENCE citation in the style `test/audit-claims.test.ts` already uses for DEC-642, so "skip Accelevents" and "ticketing not wanted" are proved by the artifact NOT existing rather than asserted in prose.

## Amendment (wave 41): the ledger is built — 14/14 clarifications resolve, zero gaps

`test/clarifications-ledger.scan.test.ts` (task w41-a) implements the above
ruling. Population: 14 bullets (9 under Scope reductions, 5 under Scope
confirmations) whose bold lead-in opens the bullet line; the one Scope
confirmations bullet whose bold span does not open the line ("Calendar
invites: **no video link...**") is deliberately excluded — its content rides
along with the sibling ".ics is sufficient" row's citation. 12 rows verdict
`honored` (file exists, contains the cited literal, test file exists,
e.g. calendar-invites → `src/server/repo/ics-sequence.ts`'s
`bumpIcsSequences` + `test/ics-sequence-bump.test.ts`; category-routing →
`src/lib/submit-core.ts`'s `validateTrackChoice`; acceptance auto-creates →
`src/domain/acceptance.ts`'s `planAcceptance`). 2 rows verdict
`absent-by-design`: "skip Accelevents" (term `accelevent` absent from
`src/**`/`app/src/**`) and the ticketing/registration half of the
compound "open source is not a hard requirement... Ticketing/registration:
not wanted" bullet (term `ticket` absent — note "registration" itself is
NOT usable as the absence term, since the codebase's own route-registration
prose pollutes it; `ticket` is the clean, zero-hit term). `KNOWN_GAPS` is
`[]` — nothing found unhonored this wave; see
`docs/mandates/w41-clarifications-ledger.md` for detail and the ratchet's
shrink-only rule going forward. Both directions asserted (every derived
bullet ↔ exactly one ledger row), plus 7 negative-control tests proving the
scan can fail in each direction (population→ledger, ledger→population,
missing-file, missing-literal, present-when-should-be-absent,
gap-ratchet-under and gap-ratchet-over), per DEC-099.

## Amendment (wave 76): "unreviewed — filed for a later wave" is not a reason — every ledgered swallow states what the user SEES when that fetch fails, and the ledger's scope is all of `app/src`

`app/src/spa-fetch-swallow-honesty.scan.test.ts` exists precisely to make every non-surfacing `.catch(` prove it is honest, and eight of its `KNOWN_SWALLOWS` entries carry the literal reason `'unreviewed — filed for a later wave.'` (`:234, :257, :280, :285, :290, :312, :334, :339`). A ledger row whose reason is "we have not looked" converts a fail-loudly guard into a parking space — the same shape wave 75 named for the `.limit(1)` scanner: A SCAN THAT NAMES A POPULATION AND FIXES NOTHING IS AN OPEN LANE WITH A NUMBER ON IT. SPEC §2 principle 6 ("Fail loudly … no defensive defaults that hide bugs") makes each of these a product claim, not a bookkeeping detail: a swallowed read leaves a count, a picker, or a disposition on screen with nothing saying it is absent. RULING: an entry's reason must name (a) the exact degraded rendering the user gets on failure and (b) why that rendering is honest — i.e. why it can never be read as a real value. An entry that cannot say that is not exempt: the handler surfaces the failure instead (feed the page's existing error state, or render the area's named absent state). Separately, `SCAN_DIRS = ['pages','components']` is a directory, not a population — the scan walks all of `app/src` (excluding `*.test.*`), which brings `app/src/lib/useNavExceptions.ts:71`, `useCurrentEvent.ts:110` and `useMe.ts` under the same rule; each already carries a written reason in a code comment, which moves into the ledger verbatim rather than being invented.

## Amendment: docs article content is JSX-free data modules with a derived registry, and figure slots are proved against the shoot manifest

DEC-518 already moved the /docs/api endpoint rows out of the JSX file into `src/routes/docs-endpoints.ts` so a test could DERIVE the manifest instead of hand-mirroring it; the same rule governs the new user-facing docs site. Ruling: article content lives in `src/routes/docs-content/` as pure data — no JSX, no route imports — so whichever shell renders it (one is in flight on `task-w82-g`) can never fork the content, and two branches editing docs never collide on a route file. The shape is fixed here so parallel workers cannot each invent one: `DOCS_GROUPS = ['getting-started','running-an-event','your-contacts','for-reviewers','for-speakers','running-the-software'] as const`; `DocsBlock = {kind:'heading',text} | {kind:'prose',text} | {kind:'list',items:string[]} | {kind:'figure',shotId:string,caption:string}`; `DocsArticle = {slug, group, title, standfirst, blocks}`; `DOCS_ARTICLES: readonly DocsArticle[]` exported from `src/routes/docs-content/index.ts`. Figure slots name a shot by STRING id in the grammar `<group>-<article-slug>-<nn>` — never an import — so content and shoot are independently buildable and a manifest test proves the two sets agree once both have landed.

## Amendment (wave 2): the docs article set is closed at nine, and this ruling fixes every remaining slug, group and export name so two parallel content branches cannot collide

task-w1-c landed the registry (src/routes/docs-content/{types,index}.ts + four articles) and test/docs-content-manifest.test.ts, which derives the population from the directory. Five of the v12 intake's seven area families are still unwritten, and two branches will write them in parallel. If either branch invents its own slug or export name the registry desyncs at merge, and if either picks a different group the role IA silently gains a seventh bucket. So the allocation is decided here, once: speakers-tasks-and-content / agenda-and-publishing / embeds-and-public-pages all sit in `running-an-event`; contacts-pipeline-and-comms is the sole `your-contacts` article; running-the-software is the sole `running-the-software` article. Each file exports exactly one camelCase const named after its slug, and index.ts's DOCS_ARTICLES stays hand-listed with the manifest test as the derived check. The shotId grammar `<group>-<slug>-<NN>` is already machine-checked at test:94-108 and binds; no branch may invent a second one. After both tranches land the article set is nine and CLOSED — a tenth article is a new design decision, not a content task.

## Amendment (wave 3): the shoot manifest is a READER of the article registry's figure slots, and a figure caption has exactly one owner

Wave 1 split one contract across two branches. task-w1-c's registry declares seven figure shotIds under the grammar `<group>-<article-slug>-NN`; task-w1-b's scripts/docs-shots-lib.ts declares eleven shot ids under a DIFFERENT slug vocabulary (running-an-event-cfp-and-submissions vs the article's call-for-papers-and-submissions). The two sets overlap in ZERO members, so the docs site would render seven placeholder frames the shoot never fills while the shoot produces eleven images nothing references — and both files are green because test/docs-content-manifest.test.ts:94-108 checks only the registry's own grammar and test/docs-shots-manifest.test.ts:54 checks only its own captions. RULING: the ARTICLE's figure block is the declaration; DOCS_SHOTS is its reader. DocsShotEntry becomes {id, route, group} — `caption` is DELETED, because the caption that renders is the article's and a second copy is DEC-613's trap with a delay fuse. The id sets must be asserted EQUAL in both directions with a negative control, and a row's group must equal the owning article's group. When a wave splits a contract across two branches, the cross-check is its own task.

## Amendment (wave 4): docs figure availability is a GENERATED manifest read by the shell, never a runtime asset probe

scripts/docs-shots.ts writes public/docs/shots/<shotId>.png, wrangler.jsonc's run_worker_first is /admin only so /docs/shots/*.png resolves from the assets binding before the worker — but the shell renders a text placeholder and never an <img>, so the freeze-time shoot would produce files nothing reads. A worker cannot stat the filesystem and per-request ASSETS.fetch() probing is IO on a read path. So the shoot emits src/routes/docs-content/shots-available.ts (DOCS_SHOTS_AVAILABLE: readonly string[]), the figure renderer branches on it, and a test asserts every listed id is a declared figure slot in DOCS_ARTICLES. Derived-manifest discipline, same as DOCS_ARTICLES itself: no hand-mirroring, both directions machine-checked, and an empty manifest degrades to today's named placeholder rather than a broken image.
