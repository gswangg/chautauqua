# DEC-753: A list envelope's KEYS do not depend on its contents, and a seeded reviewer is a named person

Verified on main at src/routes/review/reviewer.ts:74: the closed-plan early return emits {items,total,page,perPage,open:false} while the open path (line 138) also emits `recused`. The reviewer landing fetches every plan's queue and reads recused.length, so one closed plan crashes the only page a reviewer's nav points at — the fifth cycle of the same functional lockout, with a new cause each time. The rule that ends the class: an envelope's shape is fixed by its route, never by whether the branch had anything to say; the empty case emits the empty array. Second, seeded reviewer users carry contact_id: null (scripts/seed.ts:600, 613-633) and user has no name column, so resolveReviewerIdentity falls back to email everywhere an organiser reads — Progress, submission detail, the evaluations export — and the ABS scenario that screenshots "Sam Whitfield" on the progress dashboard cannot pass. Reviewer users are therefore linked to real contacts named from the fixture and the design pack (Sam Whitfield, Ana Petrov, Devin Cole, Ines Duarte), and the seed test asserts it by enumeration over every reviewer row, never by sampling one.
