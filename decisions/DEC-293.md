# DEC-293: The deferred DEC-287 exit grep, run at wave 10: PREDICATE FAILS (f/g/h are RESULT: FAIL); the two baseline-count doc-typo rows are WAIVED and must never be counted OPEN again

DEC-288 deferred DEC-287's mechanical grep to the first planner invoked after all eight -c2- logs land. They have all landed (docs/verification-log/task-w8-{a..h}-c2-*.md all exist on main at 2eb80f81; the reflog shows `merge task-w8-a` through `merge task-w8-h` plus task-custodian-w8-1). I ran the grep verbatim. FROZEN SHA: 80b811d250285de0d37417ddc12f65445ce27f96 appears in all eight. a (build-test), b (walkthrough), c (perf-smoke), d (render-sweep), e (spec-audit) each carry OPEN ITEMS: 0 + RESULT: PASS + RECHECK SHA 50354380d2. But f (triage-closure) has `OPEN ITEMS: 7` / `RESULT: FAIL`, g (fresh-clone) `OPEN ITEMS: 2` / `RESULT: FAIL`, h (rubric-coverage) `OPEN ITEMS: 1` / `RESULT: FAIL`. The predicate therefore FAILS; stage 1 is NOT declared at the wave-8 sha, exactly as DEC-288 anticipated. Disposition of the twelve cited rows, each read against the tree, not the prose: h:1 = SPK-03 (wave-9 tasks a/b, in flight); f rows = SPK-03, SPK-15, EMB-15 (wave-9 a/b, c/d, e/f/g), the public-404 cache header (src/routes/public/index.tsx:97 calls setCacheHeaders before the not-found branch — CONFIRMED still present), the emailed off-origin claim link (src/server/origin.ts:98 returns the poisoned requestOrigin — CONFIRMED), plus TWO restatements of one stale file/test count in docs/verification-log/task-w1-a-origin-walkthrough.md (185f/1584t vs 184f/1573t); g rows = the /admin intermittent 500 (src/routes/root.tsx:38 treats a 304 as not-ok — CONFIRMED) and the same off-origin link. The doc-typo pair is WAIVED here in the manner of DEC-272: it is a historical count inside a verification log, not product code, no PASS/FAIL depends on it, and editing a landed log to chase a count is worse than the count. It must not appear as an OPEN ITEM in any future section. Every other cited row is assigned to a wave-9 or wave-10 branch below.
