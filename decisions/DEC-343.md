# DEC-343: Wave-19 gates freeze before the wave-19 product lanes; w17-e's FAILED closes only by a green six-module orchestrator; the wave-20 re-run set is fixed now

DEC-330's exit predicate requires task-w17-a/b/c/d/e green. Verified on main: w17-a PASS (exit 0, every check ok), w17-b PASS (6/6 steps), w17-c PASS, w17-d PASS — but task-w17-e-c3-walkthrough-full.md is RESULT: FAILED, aborted at scale step 3, with its own log recording the cause as task-w17-b's then-unmerged probe repair. That repair is now on main, so the predicate's last open element is a full-orchestrator run: task-w19-b closes it, and nothing less does — five-of-six modules is not a pass, and DEC-339(2)'s producer+data requirement is subsumed by it. Simultaneously, task-w19-d and task-w19-e change wire contracts (the onboarding grid envelope; the submissions list payload, contentStatus filter and worklist sort) that the wave-19 gates exercise, so w19-a/b/c evidence is stale BY CONSTRUCTION for those endpoints the moment d or e merges — recording otherwise would repeat the DEC-195/DEC-203 pattern DEC-339 named. The wave-20 re-run set is therefore exactly: (1) npm run build + npm test + the four test/ tripwires at the post-w19 tip; (2) the full six-module walkthrough orchestrator; (3) npm run perf:smoke against the extended perf seed. NOT re-run: spec-audit, rubric-coverage, fresh-clone, render-sweep (no lane this wave changes public-surface markup; the Speakers and Content screens are admin SPA). goalComplete belongs to the first planner that can read w17-a/b/c/d/e resolved (w17-e via w19-b), w19-a/c at OPEN ITEMS: 0 / RESULT: PASS, w19-d/e merged, and that three-item re-run set green at a sha containing every merged w19 lane. A FAIL yields a narrow wave over the cited file:line only; DEC-315's anti-padding rule survives intact.
