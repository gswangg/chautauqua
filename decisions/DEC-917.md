# DEC-917: One optionality grammar for every form row in the product: no asterisks, '· optional' on the skippable, and a prop that renders nothing is deleted

DEC-909 fixed this for src/views/form-render.tsx (public CFP, /portal/edit, /portal/tasks): required fields carry NO marker, optional fields append ' · optional', because a submitter's real question is what they may skip. The admin half of the product never got the rule and is worse than unmarked -- app/src/components/ModalFrame.tsx:52 destructures a `required` prop and never uses it, so seven call sites (EventSwitcher.tsx:97/107/121/132/144, NewSubmissionModal.tsx:117, PipelineBoard.tsx:306) pass a flag that renders literally nothing, while the genuinely skippable rows beside them (Venue, Due date, Why them, Speaker name/email, Company, Bio) say nothing either. A decision with no code is a lie, and a dead prop is a decision with no code that seven callers still believe in. Meanwhile Scorecard.tsx:387 prints a bare ' *' on required text criteria -- an asterisk is a legend the page never prints. Rule: FormRowProps.required is DELETED (no shim, no deprecation); FormRow takes `optional?: boolean` and renders the identical suffix string form-render.tsx uses, so the two renderers cannot drift; each call site declares optionality from what its OWN submit handler actually accepts as blank, read not guessed; and no surface in the product marks a field with an asterisk.

## Amendment (wave 48)

**The optionality scan binds the GRAMMAR, not just the dead asterisk, and an uppercasing container may never swallow the suffix.** DEC-917 says required rows carry no marker and optional rows append the shared OPTIONAL_SUFFIX (' · optional', src/domain/form-copy.ts:5). The scan that guards it (app/src/optionality-grammar-scan.test.ts) only bans the literal ' *' — so app/src/pages/review/PlanEditor.tsx:1374 ships its own literal 'Guidance for reviewers · Optional ' (capital O, trailing space) inside .chq-review-criteria-head-cell, which sets text-transform: uppercase (review.css:828-835) and renders '… · OPTIONAL'. That is the mandate's 'uppercase · OPTIONAL persists' finding, and rulings A7/A21 say lowercase. RULING: (1) every optionality marker in the SPA renders OPTIONAL_SUFFIX — no per-surface literal; (2) the class that carries the suffix must not inherit text-transform/letter-spacing from a micro-label container; (3) the scan is extended to fail on any of `· Optional`, `· OPTIONAL`, `(optional)`, `(Optional)` outside src/domain/form-copy.ts. A status COLUMN VALUE ('Required'/'Optional' in FieldList's builder row, FormsPage's chip) is not a field suffix and is exempt by name, with the reason recorded in the scan.

## Amendment (wave 3): the optional suffix PAINTS lowercase — a grammar policed only at the literal is a grammar half-enforced

DEC-917 established one optionality grammar and app/src/optionality-grammar-scan.test.ts bans every hand-typed variant of the literal, so the string in the source is correct everywhere. It still renders as '· OPTIONAL' across the new-submission, add-to-pipeline and contacts modals, because `.chq-form-row-optional` (app/src/components/modal-frame.css:121) is a `<span>` inside `.chq-form-row-label`, which declares `text-transform: uppercase` (line 105) — an inherited property the child rule never resets. eval-findings records this as a ruling-needed conflict between DEC-917's 'match the label' and rulings A7/A21. It is not a conflict: DESIGN-RULINGS item 7 says 'Use `· optional` as a lowercase suffix on the label ... not uppercase' and item 21 repeats it verbatim, and DESIGN-RULINGS is the vendored authority. The frames that show caps in the criteria context are the outlier, and one context is not a second grammar. RULING: `.chq-form-row-optional` (and every sibling class that renders OPTIONAL_SUFFIX inside an uppercased label, SPA and SSR alike) declares `text-transform: none`; the criteria editor follows the same rule rather than keeping its own case. Guard: extend the existing optionality scan from the LITERAL to the PAINT — assert every rule that styles the suffix declares `text-transform: none`. The general shape, for the field guide: A SCAN THAT POLICES THE STRING IS BLIND TO THE STYLESHEET THAT REWRITES IT.
