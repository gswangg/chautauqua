# DEC-104: Complete the DEC-078 chunk sweep: every remaining unbounded inArray site iterates chunkIds batches; bounded sites enumerated as exempt; each fixed file gains a source-scan guard test

task-w7-c's perf-smoke gate failed at d12eb25 with D1_ERROR "too many SQL variables" from src/server/repo/overview.ts:167-177's single-shot inArray over ~300 placed submission ids, and a tree sweep shows the identical latent defect wherever an id list scales with submissions/speakers: agenda.ts loadAcceptedSessions (lines 166/178/189, all accepted ids), exports.ts exportSubmissions (161/173), exportAgenda (339/350) and the showflow builder (525/530/540/549) over ALL event submissions (2,000 at DEC-088 scale), tasks.ts grid fetch (144), createTaskAssignments existing-check (244, fed by listAcceptedContactIds = all accepted speakers) and reminder-stamp update (512), review.ts getUsersByIds (640), contacts.ts findContactsForOrg (668), and files.ts comment-author hydration (409/419). All MUST adopt the canonical house pattern already used in comms.ts:137-168: for (const batch of chunkIds(ids)) accumulate rows (for UPDATEs, issue the update per batch); semantics stay byte-identical. Explicitly EXEMPT as bounded by construction: review.ts:302/385/408 (track-filter lists bounded by event tracks), submissions/list.ts:166 (guarded by the allowedIds.size > ID_CHUNK_SIZE branch at :150), public.ts:405 and comms.ts:254 (room ids bounded by event rooms), submissions/list.ts:89 (status enum), and every site already iterating chunkIds (public.ts, comms.ts, submissions/status.ts, contacts.ts:511). Because the test suite has no D1 harness and local SQLite's variable ceiling is far above D1's (~100), unit tests cannot reproduce the 500; each fixed file therefore gains a dedicated source-scan guard test (read the repo file with node:fs, assert the named functions contain chunkIds( loops and the file imports chunkIds from src/lib/chunk) — the runtime proof is the wave-10 perf-smoke gate plus DEC-105's new checks.
