# DEC-127: Wave-15 five-gate battery at the post-wave-14 sha: lanes task-w15-g..k (a-f burned by round-0), code-frozen log-only, fresh ports 8851/8852/8853, six-fix preflight mandatory, triage chains behind perf-smoke

All six wave-14 fix lanes (task-w14-i..n) are verified merged at main 675219f ('merge task-w14-k' last) with their tests in-tree; the empty-looking task-w14-k ref during planning was a worker still pushing, not a dead lane, so no re-implementation lane is needed and any duplicate DEC-121 work would now be harmful. Consequences ruled here so no gate worker decides them: (1) every gate section must cite the DEC-114-derived newest code-bearing sha (expected 675219f or later) and must FIRST preflight all six fix markers (tasks.ts DEC-120 block, portal-edit.ts LOCKED_SPEAKER_FIELDS prefill, comms.ts requireFullMatch, review.ts DEC-123 conflict guard, validate.ts MAX_TEXT_LENGTH, perf-seed.ts kind:"rating") — a PASS recorded against a tree missing any marker is invalid; (2) gates are code-frozen per DEC-077: the ONLY file a gate lane may modify is docs/verification-log.md (append-only per DEC-068), so gate merges are non-code-bearing under DEC-114 and cannot void each other; (3) server-bearing gates use fresh ports never used by earlier waves — walkthrough 8851, perf-smoke 8852, triage spot-checks 8853 — to dodge zombie wrangler processes; (4) triage-closure chains behind perf-smoke (the historically decisive gate) and, when a sibling gate section has not yet merged to main, independently spot-verifies that scope itself instead of counting an unmerged section as an open item — OPEN ITEMS counts unresolved product defects, not merge-ordering races; (5) w15 suffixes a-f are burned (round-0 merged a/b/c/e/f and a task-w15-d log section exists), so lanes are g,h,i,j,k per DEC-126's never-reuse rule.
