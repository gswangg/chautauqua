# DEC-431: DEC-387's flip fires for the two passes that have now read all-PASS; contrast stays advisory one more wave

docs/verification-log/task-w12-a-render-sweep-overflow.md Reading 2 records, in one gate:render-sweep run against the corrected DEC-424 instrument, admin-mobile 20/20 PASS (up from wave 10's 14/20) and type-floor 83/83 PASS. DEC-387's rule is that an advisory pass becomes blocking in the wave AFTER it first reads all-PASS; this is that wave, so ADMIN_MOBILE_PASS_BLOCKING and FONT_FLOOR_BLOCKING go true. The flip is made only inside the lane that re-confirms all-PASS on the current tree in its own run — a flag is never flipped on the strength of a previous wave's transcript, which is the failure mode that let five non-defects be scored as defects for three waves. CONTRAST_BLOCKING stays false: DEC-426's pass has never read all-PASS, and its first all-PASS reading is what this wave produces, so its flip belongs to the next wave. A gate that stays advisory forever is decoration, which SPEC.md:356-357 explicitly rejects.
