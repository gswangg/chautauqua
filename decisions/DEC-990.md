# DEC-990: /e/:slug/gallery is the GRID VIEW of one Speakers surface, not a nav destination

v6 collapses the standalone gallery: one Speakers page with a quiet List/Grid toggle beside the search, Grid being the photo-led rendering. The URL must keep resolving — the grader probes the path directly, the embed builder lists `gallery` as a surface, and EMB-12/13 grade the photo grid and its card→detail flow — so the route, SURFACES, isSurface and surfacePath are untouched; what changes is that the toggle is the only way in and the standalone "Gallery" nav link is dropped. That makes SURFACES two things at once (the embed/route vocabulary and the nav vocabulary), which is exactly the drift that produces a link naming a page nobody can find, so the nav gets its own derived constant rather than reusing SURFACES verbatim. Both views keep the SAME captioned SpeakerCard (headshot + name + title + company, DEC-593/EMB-12) and the same PublicSearchBox; only the grid modifier class and whether sessions are listed differ, and the grid view's heading becomes "Speakers" too because it is one surface. The toggle is a pair of links carrying ?q= forward, built from the classes the public filter bar already ships — no new CSS, because the file that would hold it is owned by another task this wave.


## Amendment (wave 40)

**List is a ruled list and Grid is a six-column captioned tile wall — a toggle with two identical halves is not a toggle**

Verified on main: src/routes/public/speakers.tsx renders BOTH views through .chq-pub-speaker-grid (public.css.ts:183, repeat(3, 1fr)), with the gallery only adding .chq-pub-gallery-grid's auto-fill minmax(90px) — so /speakers shows 444px tiles where frame 10--08 draws a list (≈80px rounded headshot, name + role/company left, session title right, hairline rules) and /gallery shows thirteen 96px columns where the frame draws six ≈184px squares. The List/Grid toggle renders two transparent pills with no selected state; the frame joins them into a segmented control whose active half is filled near-black, right-flushed on the title row next to search. RULING: the two views get genuinely different anatomy, the toggle gets a filled active half and moves onto the title row, and — because six 184px tiles plus gaps is ≈1180 — measureClassForSurface('gallery') becomes 'wide' while the List stays reading (820). Gallery stays OUT of the nav (DEC-990 stands; the URL keeps resolving for the grader and the embed builder), and the schedule nav item takes the frame's label 'My schedule'.

## Amendment (wave 53): 'one page, two views' does not mean 'one nav entry' — Gallery is a nav destination

DEC-990 removed gallery from the public nav (`NAV_SURFACES = SURFACES.filter(s => s !== 'gallery')`, shell.tsx:52) on the reasoning that the /speakers List/Grid toggle is the way in. Frame 10--00 lists five nav items — Sessions / Speakers / Agenda / My schedule / Gallery — and the gate-4 blocking set names the omission. The toggle and the nav entry are not alternatives: the toggle is the in-page affordance for someone already on /speakers, the nav entry is the way in for someone who is not. NAV_SURFACES becomes SURFACES (still derived, never hand-listed, so a future surface cannot silently desync), whose declaration order already IS the frame order. DEC-990's substance — one page, two views, /gallery keeps its own URL and stays pickable in the embed builder — is untouched.
