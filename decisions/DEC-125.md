# DEC-125: scripts/perf-seed.ts criteria literal gains kind: 'rating', unblocking the perf-smoke rating PUT check and the DEC-069 exit predicate

task-w11-d's perf-smoke at 3b7ed3d posted RESULT: FAIL solely because scripts/perf-seed.ts:269 seeds criteria_json as [{ id: 'overall', label: 'Overall', weight: 1 }], omitting the kind: 'rating' discriminant that src/domain/evaluation.ts's EvaluationCriterionDef union (RatingCriterionDef requires kind: 'rating'; verified at evaluation.ts:121-127) demands — validateEvaluationScores falls into the dropdown branch and 400s every rating PUT, aborting the harness before its p95 table. The fix is the one-line literal change to [{ id: 'overall', label: 'Overall', kind: 'rating', weight: 1 }]. This is a script-only, product-code-untouched change, but it is still code-bearing per DEC-114's mechanical rule (scripts/** is outside the bookkeeping set), consistent with how 3b7ed3d itself was classified.
