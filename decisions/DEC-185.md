# DEC-185: Wave-11 battery rebinding: six gates at frozen S' containing all wave-10 fix markers

Wave 10 is code-bearing (DEC-179..183), so per DEC-069/139 the full six-gate battery (build+test, walkthrough incl. DEC-175 probes, perf-smoke, render-sweep expect 31/31, spec-audit, triage-closure incl. eval-findings A-F citation closure) must re-run at one frozen post-fix sha. The w8-b/w8-c/w8-e PASS results at 38860f9 are historical confirmations that the DEC-167..175 line is sound; they do not satisfy the exit predicate for the post-DEC-179 tree. S' = newest first-parent commit on main whose tree greps ALL DEC-177 anchors PLUS: 'DEC-179' in src/lib/csv.ts (moved to src/domain/csv.ts), 'DEC-180' in src/lib/rate-limit.ts, 'DEC-181' in src/server/middleware.ts, 'DEC-182' in src/server/http.ts, 'DEC-183' in wrangler.jsonc. Every wave-10 fix therefore MUST tag its change with its DEC id in a comment. Gates started before all fixes merge precondition-FAIL cheaply, per the DEC-176 mechanism that has now proven itself across two late-draining waves.
