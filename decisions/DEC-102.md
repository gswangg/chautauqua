# DEC-102: Wave-7 gate protocol: all four wave-6 fix lanes verified merged (tip d12eb25), the DEC-076 barrier is vacuous, four DEC-069 gates run parallel and code-frozen with triage-closure behind walkthrough; every pre-wave-7 gate section is void for exit

Verified against main's reflog and tree during planning: task-w6-c (pubcache, 347df55), task-w6-b (submit claim, 771e06c), task-w6-e (merge repoints, de8d492), and task-w6-d (seq, d12eb25) all merged mid-planning, and each implementation conforms to DEC-098/099/100/101 (submit.tsx ConfirmationState three-state render; pubcache.ts CLIENT_CACHE_CONTROL restored on hits; repo/submissions/seq.ts submissionSeqSubquery with both SELECT-then-INSERT helpers deleted; contacts.ts dedupe-delete -> six repoints -> delete). With no code outstanding, DEC-076's single closeout barrier degenerates to zero exactly as in DEC-077/DEC-093, so chaining gates would only starve lanes: the four gates (build+test, J1-J12 walkthrough @ 8801, perf-smoke @ 8803, spec-audit) run in parallel from main at or after d12eb25, each re-deriving the newest code-bearing short-sha per DEC-091 (skipping DEC-090-exempt bookkeeping commits) and recording honestly if a code-bearing merge lands mid-run; triage-closure alone chains behind the walkthrough gate (DEC-093) to cite its runtime evidence. Because the task-w6-* merges are code-bearing, the late-w5 sections (task-w5-b build+test @ b638f75, task-w5-e spec-audit @ 3d1e838, task-w5-c walkthrough @ b638f75, and any straggler task-w5-d section) are void for exit under DEC-069. The unmerged task-w6-a-retry branch duplicates the already-landed b638f75 scripts fixes; if it ever merges it must be verify/log-only — any code it carries resets the predicate and the observing gate records the invalidation. The wave-8 planner evaluates exit: five sections green at one sha (four RESULT: PASS + triage OPEN ITEMS: 0) at or after the newest code-bearing sha.
