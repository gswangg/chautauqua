# DEC-162: Six-gate battery runs next wave as parallel same-sha gate lanes; gate-runner tasks are the sole exception to the campaign-3 verification-log freeze, appending only their own section

DEC-139 requires the DEC-069 battery plus the DEC-144 render-sweep (six sections: build+test, walkthrough, perf-smoke, spec-audit, triage-closure, render-sweep) at the newest code-bearing sha, but also says workers never edit docs/verification-log.md — read literally that makes the exit predicate unwritable. Resolution: gate-runner tasks (and only they) may append exactly one section — their own '## <date> task-wN-x — <gate> @ <sha>' block ending in RESULT:/OPEN ITEMS: — to docs/verification-log.md, append-only per DEC-068, touching nothing else in the file; docs/eval-findings.md, decisions/, and src/decisions.ts stay scribe/planner-only for everyone. Scheduling: the battery runs NEXT wave, after wave-2 in-flight branches, task-w1-i, and wave-3 all merge, as parallel gate lanes at one frozen sha (the wave-19 pattern that produced the five-PASS battery @ 8c7f479); running it this wave would burn the battery on a sha that in-flight merges immediately invalidate per DEC-069 sha-scoping. DEC-129's homonym guard applies: cited shas must descend from 2dd2f33.
