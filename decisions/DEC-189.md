# DEC-189: Wave-13 cooperative exit battery: S''' = merge of task-w12-a (7f7477e), dedupe against concurrently-draining task-w12-* gates, fourth-generation homonym guard, sibling-wait rule

Verified on the tree mid-planning: task-w12-a merged as 7f7477e ('merge task-w12-a', first-parent child of 35eef1d 'scribe wave 12') and the full DEC-187 surface is present and conformant (scripts/ensure-dev-vars.ts tagged DEC-187; package.json predev; scripts/render-sweep.ts:40,168; ci.yml:38,69; test/wrangler-config.test.ts retargeted to .dev.vars.example with .gitignore guard and ensureDevVars unit tests; .gitignore:9 '.dev.vars'; .dev.vars.example = DEV_MODE=1 only). Branch ref task-w12-e exists, so DEC-188's w12-b..g gate lanes may drain concurrently with wave 13 — this campaign has seen batteries drain late twice (waves 6 and 11) and lanes die unrun twice (waves 7 and 11-as-first-observed). Bindings: (1) S''' = newest code-bearing (DEC-114 first-parent rule) commit on main at gate start, expected 7f7477e; `git merge-base --is-ancestor 2dd2f33 S'''` mandatory; if the DEC-188 21-marker precondition set (17 prior markers + DEC-187 grep in scripts/ensure-dev-vars.ts, DEC-187 grep in test/wrangler-config.test.ts, 'ensure-dev-vars' in package.json, `git ls-files` listing .dev.vars.example and NOT .dev.vars) misses at the derived sha, record 'RESULT: FAIL — precondition' and stop. (2) Dedupe: before running its gate, each w13 lane searches docs/verification-log.md for a section of the SAME gate type whose full heading ends '@ <S''' short sha>' with RESULT: PASS (lane name task-w12-* or task-w13-* — this campaign only); if found, append a short section citing that heading verbatim as its evidence with 'RESULT: PASS — duplicate of <heading>' and do not re-run the gate; otherwise run the full gate. Either way each lane appends exactly one new ledger section (plus optional detail file under docs/verification-log/), never editing existing content, per DEC-068/DEC-188(5). (3) Triage-closure equivalence: the gate-of-gates requires, for each of the five gate types (build+test, walkthrough, perf-smoke, render-sweep, spec-audit), at least one RESULT: PASS section at S''' from EITHER battery, full-heading matched; before concluding a sibling type missing it must re-read the ledger every ~2 minutes for up to 10 minutes (w11-f burned a lane on exactly this merge race). (4) Fourth-generation homonyms: the ledger already holds task-w13-a/b/c/d/e sections from two dead campaigns (@ 0ee30dd and d4ebf7f; @ 3b7ed3d — none descend from 2dd2f33): every search matches the full heading including '@ <sha>', never the lane name alone. (5) Ports: the w13 walkthrough lane boots wrangler dev on 8951 and the perf-smoke lane on 8952 to avoid colliding with concurrently-running w12 lanes (which use 8787/8845); render-sweep self-allocates a free port. (6) No lane may ever read or print a local untracked .dev.vars (DEC-188(6)). Six PASS(-or-duplicate-PASS) sections at one S''' — five gate types plus a green triage-closure — satisfy DEC-069/DEC-139 for the post-fix tree; the next wave verifies the ledger and declares stage-1 complete with zero tasks.
