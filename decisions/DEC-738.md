# DEC-738: Labels ARE customFields minus the reserved travel key, formatted once server-side as `key value` chips in stable key order

DEC-726 settled that a contact's Labels render customFields ("role speaker · year 2027") and retired DEC-712's derived participation roles, but the code still ships DEC-712: src/routes/api/contacts/crud.ts calls repo.fetchContactLabels over `participant`. Two details DEC-726 left open, decided here so directory, drawer and merge cannot drift: (1) `travel_logistics` (customFields.TRAVEL_KEY) is NOT a label — it is prose with its own labelled field, and as a chip it would blow out every row; (2) the format is computed ONCE, server-side, as `${key} ${value}` per entry in stable key order, so the list payload, the drawer and any future consumer render the identical string instead of three formatters. The derived-role query and its repo helper are deleted, not kept behind a flag.

## Amendment (wave 4): labels render as plain small-caps meta text; the server-side `key value` formatting is unchanged

DEC-738 fixed WHERE labels are formatted (once, server-side, stable key order). The directory column then wrapped each one in a bordered chip, which competes visually with status pills that mean something actionable. Ruling: the LABELS cell prints the same server-formatted strings as plain small-caps meta joined by ' - ' (ROLE SPEAKER - YEAR 2027). No formatting logic moves to the client; only the chip chrome goes.


## Amendment (wave 71): the organiser's speaker record carries the person's org-wide logistics facts, editable in place through the one contacts writer

`contact.customFields` already has three reserved keys with labels (`src/domain/contact-labels.ts` — dietary, travel, accessibility) and the CRM drawer edits them. The organiser's SPEAKER record renders company, title, Contact and Notes and nothing else: `src/server/repo/tasks/speaker-detail.ts` projects `notes` (:143, :331) but never `customFieldsJson`, so `app/src/pages/speakers/SpeakerDetailPage.tsx` cannot show them. SPK-S3 step 8 sends the judge to the speaker record looking for travel/logistics/custom fields (SPK-15), and SPEC J7's "one record, two views" says the same fact must not be reachable from only one of them. The write goes through the EXISTING contacts writer (`PATCH /api/v1/contacts/:id` with `customFields`, validated at src/routes/api/contacts/crud.ts:334-353) and the EXISTING pure helpers (`toRows`/`fromRows`/`reservedValue` in app/src/pages/contacts/customFields.ts) so unowned custom keys survive the round trip — no second writer, no second normalization. Scope copy states the truth: these are the person's org-wide facts, not event-scoped ones.

## Amendment (wave 75): the DEC-930 speaker-detail wire contract carries every field the portal profile can write — bio and social links join customFields

DEC-738's wave-71 amendment closed ONE member of a population: `contact.customFieldsJson` was projected into `SpeakerDetailContact` so the organizer's SPEAKER record was "not a second, poorer view of the same person". Verified at my own runtime, the population has two more members and they are the two the rubric names. `src/server/repo/tasks/speaker-detail.ts:23-43` declares `SpeakerDetailContact` with id/name/email/company/title/hasAccount/phone/notes/customFields/headshotFileId and NO `bio`, NO `socialLinks`; `grep -n 'bio\|social' app/src/pages/speakers/speakerDetail.ts app/src/pages/speakers/SpeakerDetailPage.tsx` returns nothing. Meanwhile `src/server/repo/profile.ts:93,95,139,140` shows the portal writes exactly `bio` and `socialLinksJson` alongside title/company/headshot, and `src/server/repo/exports/speakers.ts:29-34` already exports both. SPK-08 (`docs/eval-rubric/03-speaker-management.yaml:283-286`, weight 3, in the lowest-scoring area) requires that "the organizer's view of Priya's record later shows the same bio text and headshot without manual re-entry", and SPK-S3 step 2 sends the judge to precisely this page to check the `SBEK-PORTAL-BIO-01` sentinel. RULING: `SpeakerDetailContact` gains `bio: string | null` and `socialLinks: { label: string; url: string }[]`, the latter built through the SAME fixed Twitter/LinkedIn/GitHub/Website order and the SAME `safeExternalUrl` boundary the public detail already uses (`src/server/repo/public/detail.ts:150-161`) rather than a second link vocabulary. The projection is READ-ONLY on the speaker record: the CRM contact drawer stays the one editor of a person's org-wide record (it already edits bio and uploads headshots), so no second writer is created; the record links to the drawer instead. Additive to a normative wire contract, so no existing consumer breaks.

## Amendment (wave 77): contact.custom_fields_json gets one declared parser beside its declared serializer, and `customFields` is one present object on every wire

VERIFIED at main. The column has a declared SERIALIZER, customFieldsJsonOf (src/server/repo/contacts/crud.ts:58), spent by crud.ts:94,149 and import.ts:102,128 — and bypassed by src/server/repo/contacts/merge.ts:456, which hand-rolls `JSON.stringify(merged.customFields)`. It has NO declared parser at all: eight independent hand-parses give THREE different answers to "what are this person's custom fields" when the column is null — `null` at src/routes/api/contacts/shared.ts:51 (the CRM drawer's wire), `{}` at src/routes/api/contacts/crud.ts:88, src/server/repo/tasks/speaker-detail.ts:372 and src/server/repo/exports/contacts.ts:80,112, and THE KEY IS ABSENT at src/server/repo/contacts/rows.ts:73 and crud.ts:411. This is the same shape wave 76 found one column over in social_links_json (DEC-152 w76), with a bigger population and a live serializer bypass on top. It is not cosmetic: docs/eval-rubric/07-speaker-crm.yaml CRM-04 grades "custom fields or tags that persist on the profile" and 03-speaker-management.yaml SPK-15 grades a travel/logistics custom field surviving save and reload — the two surfaces a judge compares are two of the three answers. RULING: parseContactCustomFields(json) is declared next to customFieldsJsonOf and exported through src/server/repo/contacts.ts; it returns Record<string,string>, `{}` for null/undefined/empty, and JSON.parse with NO try/catch (a malformed column is a violated invariant — fail loudly, house rule), asserting a non-null non-array object. Every one of the eight readers spends it; merge.ts writes through customFieldsJsonOf. The wire contract is now single-valued: `customFields` is ALWAYS present and ALWAYS an object, so ContactRow.customFields becomes required and the SPA's `Record<string,string> | null` type loses its null arm along with any render mock asserting a value the server can no longer emit. DERIVE THE POPULATION FROM THE COLUMN, NOT FROM THE ROUTE.
