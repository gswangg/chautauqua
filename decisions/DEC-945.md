# DEC-945: One 404 card, three renderers — and it never prints the path it failed to match

The product has three not-found renderers and they disagree. src/server/not-found.tsx (no route matched) already draws the design's card through AUTH_CSS. src/routes/public/not-found.tsx — the one /e/<bad-slug>, an unknown session and an unknown embed surface all land on — draws a bare `<main style="max-width:520px">` with hand-written inline styles and no card at all. app/src/pages/NotFound.tsx (the SPA catch-all) draws neither: a full-width `.chq-measure` 820 column, a body sentence that inlines `<code>{location.pathname}</code>`, and two `.chq-btn-tertiary` links separated by a literal `{' '}` so they jam together. The frame is one card: eyebrow, 28px title 'That page isn''t here', a 15px/24.5 body line, and a row of ‹-suffixed links — and it does NOT restate the URL, because a person who mistyped a link learns nothing from being shown their own typo and an evaluating agent reads the links instead. Rule: all three renderers draw the SAME card at the same numbers — 520 max-width, content-hugging (never viewport-stretched), 35px padding, 28px title, 15px/1.63 body, links in one flex row. The two server-side renderers reach it by importing AUTH_CSS (one string, no second ad hoc card layout); the SPA page owns a co-located app/src/pages/not-found.css that repeats the numbers and names this decision so any drift is visible in a diff. The attempted pathname is not rendered anywhere. A link is still the route it lands on (Overview and Submissions for the SPA, the hub for the public surface).


## Amendment (wave 40)

**The v6 auth measures supersede 520/450/640, the card hugs its content, and there is no card border**

Verified on main: src/routes/auth.css.ts:28 sets body { display: flex; justify-content: center } with the default align-items: stretch, so every SSR auth card renders exactly viewport-tall (227-521px of dead space under the content); .chq-auth-card caps at 640 with a 1px --chq-rule border and 6px radius, .chq-auth-card-narrow at 520, and the control column at 450. The v6 frames measure the login form column at 732 (the 820 reading column inset 44px) and the password/not-found surfaces at the full 820, and draw NO card border on any 11-account frame — the border plus the stretch is what makes the dead space read as an empty rectangle. RULING: align-items: flex-start on the auth body; the border and radius come off; .chq-auth-card (the form card) is 732 and .chq-auth-card-narrow (the plain card: password, 404) is 820, with the 450px control-column cap deleted; ‹ Back and the h1 sit flush at one x (the 36px margin-left on .chq-auth-titlerow .chq-auth-title goes). The modifier keeps its now-inaccurate name for one wave and carries a comment saying so: renaming it would touch five call sites that two lanes are editing this wave, and a rename is not worth a merge conflict in a security-adjacent route file.

## Amendment (wave 53): the designed not-found card gains its admin renderer

DEC-945 established one 404 card with three renderers (src/server/not-found.tsx, src/routes/public/not-found.tsx, app/src/pages/NotFound.tsx). The admin surface was the gap: it renders the SPA's copy of the card inside admin chrome at status 200, so the one designed card exists but the admin path never reaches it. The Worker's /admin/* handler routes an unmatched path through src/server/not-found.tsx at status 404 — the SPA catch-all stays as an in-session safety net for a client-side navigation, but the URL-entry/refresh path now gets the real thing.


## Amendment (wave 1)

auth.css.ts applies the design's CONTENT column to the card BOX and then pads inward: `.chq-auth-card` is max-width 732 with 44px padding (content 644 against the frame's 732) and `.chq-auth-card-narrow` is max-width 820 with 35px padding (content 750 against 818), so every left edge on /login, /account/password and the not-found card sits 44/35px inboard. A measure names what you read, never what surrounds it. Ruling: the box is stated as column + 2x padding — 820 for the login card, 888 for the narrow variant — so the content columns land on 732 and 818 exactly; and the 404 block's rhythm comes off the uniform 26px card gap onto the frame's tighter values (h1->body ~19, body->links ~26), with the UA `<p>` margin zeroed and the footer links' 44px tap target achieved without adding leading above the text.


## Amendment (wave 6): an unknown path under a role shell is a 404 for EVERY role — /portal/* never redirects an organizer into /admin

DEC-945's wave-53 amendment already ruled that an unknown /admin path is a real 404 (chromeless card, HTTP 404, no route leak) rather than a 200 with the SPA's catch-all. Its mirror never landed: gate-4 measured an organizer at /portal/nope getting HTTP 200 on /admin/overview, because src/routes/portal/shared.tsx:41 redirects every non-speaker to /admin before anything asks whether the path exists. A speaker at the same URL correctly gets the designed 404 card, so the product currently answers the same question two ways depending on who asks — and the organizer's answer invents a page. Ruling: path existence is decided before role redirection on the portal shell exactly as it is on the admin shell; an unknown /portal path returns the shared NotFoundDocument at HTTP 404 for every role, and the speaker path stays byte-identical. Corollary already in the mandate: the admin-404 card's links are role-aware (`Overview ›` + `Submissions ›` for a signed-in organizer), never a hardcoded homepage/Log-in pair that sends a signed-in producer to the wrong door.

## Amendment (wave 25)

**The auth card is 460 wide and its actions are a footer row, not a full-column bar**

The V8 intake redrew 11-account--00 ("a card, not a stretched phone") and explicitly SUPERSEDES the pair-6 box-math ruling that trued the app up to 732 content / 820 box. DESIGN-RULINGS' governing corollary names the exact defect we shipped: full-column buttons and one-field-per-row stacking are phone anatomy, and a centred card is not a desktop design just because it is centred. So: `.chq-auth-card` max-width 460px, 1px rule + paper fill + 8px radius, padding 36/34/32, gap 22; `.chq-auth-card-narrow` 520px; the submit button loses `width:100%` and becomes an intrinsic-width olive control (min-height 46, padding 0 22px) inside a footer ROW that is space-between when a tertiary link sits at its left and flex-end otherwise. Every measure test pinning 732/820/818/888 for the auth card is now WRONG and gets updated with the change (field-guide rule: where a test contradicts the current design authority, the test is wrong).

## Amendment (wave 48)

**One bare reading page (820, no card) for every non-credential SSR dead-end; the 460 card is credentials only.** DEC-945 recorded the v9 sign-in redraw: .chq-auth-card is a 460px bordered card. Its sibling .chq-auth-card-narrow (auth.css.ts:46, 520px with the same 1px border + 8px radius) is being used for three pages that are not credential entry — /account/password (account.tsx:85), the /admin/* not-found (not-found.tsx:95) and the expired-claim dead-end (auth-views.tsx:194) — and the CFP-closed page draws its own .chq-cfp-closed card frame. docs/design/README.md §Widths is explicit on both halves: 'Reading measure 820px, centred' and 'Chrome is always full bleed … a centred card floating on a wide background would be a different design language from the one this set uses everywhere else.' RULING: ONE new shell class .chq-bare-page — max-width 820 centred, NO border, NO background fill, NO radius — owns those four surfaces. /login, /forgot, /reset keep .chq-auth-card at 460 (a credential prompt is deliberately a small card). .chq-auth-card-narrow is deleted, not left as an unused rule.

## Amendment (wave 3): the bare reading page holds its 820 measure inside a flex body, its title is its own register, and one rule sizes the auth submit on BOTH shells

The wave-48 amendment gave .chq-bare-page `max-width: 820px; margin: 0 auto` (src/views/bare-page.css.ts) but AUTH_CSS's very first rule is `body { display: flex; justify-content: center; align-items: flex-start }` (src/routes/auth.css.ts:43). A flex item with `max-width` and no `width`/`flex` shrink-wraps to its content, so /account/password and the /admin 404 render a min-content column, not an 820 one — the fleet measured 188px password fields and a 553px 404. `margin: 0 auto` is dead inside a flex line. Second half, same page: `.chq-auth-actions button[type=submit] { width: auto; padding: 0 20px }` (auth.css.ts:201) zeroes vertical padding, and the only rule that supplies a control height — `.chq-auth-card button[type=submit] { min-height: 46px }` — is scoped to the 460 CARD, which the bare page deliberately does not wear. Result: a 19px-tall 'Change it' with its label overflowing the fill. RULING: (a) .chq-bare-page declares `width: 100%` so its stated measure is the measure it renders at; (b) the auth submit's size comes from ONE selector list covering both `.chq-auth-card` and `.chq-auth-actions` — a control's height is not a property of which shell contains it; (c) the bare page's h1 is its own register (`.chq-bare-page .chq-auth-title { font-size: 36px }`), because 28px is the 460-card's title size and an 820 reading column is a different measure; (d) src/routes/account.tsx stops wearing `.chq-phone-actionbar` on a desktop-visible row — that row is currently visible only because `.chq-auth-actions { display: flex }` happens to be declared later than theme.ts's `.chq-phone-actionbar { display: none }`, and a layout that depends on source order between two unrelated stylesheets is a coincidence, not a design.

## Amendment (wave 65): a portal refusal renders the portal; `c.text` is not a surface

DEC-945 already ruled that an unmatched `/portal/*` path renders the shared `NotFoundDocument` card rather than a redirect (`src/routes/portal/shared.tsx:52-73`), and `/portal/preview` renders the same card three times (`preview.tsx:141-194`). But six IN-ROUTE refusals never migrated: `portal/edit.tsx:385,415,496`, `portal/index.tsx:585`, `portal/tasks/resources.tsx:27,46` still answer `c.text("Not found", 404)`. A signed-in speaker following a stale submission link therefore gets an unstyled `text/plain` page inside a branded portal with no way back — the exact shape DEC-367/DEC-808 and DEC-383 found twice already: a rule adopted by the GATE and never by the routes behind it. RULING: one exported helper `portalNotFound(c, body?)` in `portal/shared.tsx` renders `NotFoundDocument` with `resolveNotFoundEyebrow` and a SPEAKER link set whose labels come from the existing `PORTAL_BACK_LINKS` vocabulary (DEC-914: a link's label must agree with the route it lands on), and every portal refusal goes through it. The STATUS CODE never moves (404, existence-hiding preserved); only the surface does. The population is asserted by a scan derived from globbing `src/routes/portal/**`, never a hand list.
