# DEC-188: Wave-12 battery binding: wave-11 battery voided (only w11-a ran; 629d57e is code-bearing), S'' = merge of task-w12-a, third-generation homonym guard, dead-lane and orphan-report declarations

Verified against refs and the ledger: task-w11-b/c/d/e branch refs are parked at bdc472b with zero commits and task-w11-f was never created — those lanes died unrun and their branch names are dead, never reuse. task-w11-a's PASS @7561cc1 and the orphan on-disk docs/verification-log/task-w11-e-spec-audit.md (PASS @7561cc1, never committed or merged, no ledger section) are historical only: operator commit 629d57e is code-bearing under DEC-114 (touches .gitignore/.dev.vars/.dev.vars.example, all outside the bookkeeping set), so DEC-069 sha-scoping voids both for exit purposes; gates must not cite the orphan report as evidence. Bindings: (1) S'' = newest code-bearing first-parent commit on main after task-w12-a merges — expected the 'merge task-w12-a' commit itself; if a gate's walk lands on 629d57e instead (w12-a not merged), record 'RESULT: FAIL — precondition' and stop. `git merge-base --is-ancestor 2dd2f33 S''` stays mandatory. (2) Preconditions per gate at S'': the 17 prior markers (12 DEC-177 anchors + 5 DEC-185 markers; DEC-183's marker is the wrangler.jsonc comment, unaffected by 629d57e) PLUS 'DEC-187' greps in scripts/ensure-dev-vars.ts and test/wrangler-config.test.ts, 'ensure-dev-vars' present in package.json, `git ls-files` listing .dev.vars.example and NOT .dev.vars. (3) Lane map: w12-b build+test(+bundle:check), w12-c walkthrough 6/6 incl. DEC-175 probes, w12-d perf-smoke, w12-e render-sweep 31/31, w12-f spec-audit of 38860f9..S'', w12-g triage-closure gate-of-gates (dependsOn w12-c); w12-b..f dependOn w12-a since no gate can PASS at an S'' that does not exist yet — this is genuine sequencing, not convenience. (4) Third-generation homonyms: the ledger already holds task-w12-* sections from TWO earlier campaigns (@01c6ace/f6e3422 and @3b7ed3d/3543f09) — every sibling search and exit-predicate grep must match the full heading including '@ <sha>', never the lane name alone. (5) Gate lanes append exactly one new ledger section each (plus an optional new detail file under docs/verification-log/); no edits to existing ledger content, decisions/, src/decisions.ts, or docs/eval-findings.md. (6) No worker may ever read or print the local untracked .dev.vars — it may contain a real secret. Six PASS at one S'' with OPEN ITEMS 0 satisfies DEC-069/139 for the post-fix tree and wave 13 declares stage-1 complete.
