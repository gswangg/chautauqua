# DEC-175: The review-lens authz probe set becomes permanent walkthrough assertions with existence-hiding semantics (404 for out-of-scope, 403 for wrong role, 401/302 for unauthenticated)

The reviewer file-scope defect (DEC-170) proved role-scoping regressions slip past unit tests. Codify the probes as walkthrough assertions: unauthenticated GET /admin → 302, unauthenticated GET /api/v1/contacts, /api/v1/review/plans, /files/:id → 401; speaker2 GET /portal/submissions/<speaker1's id> → 404, speaker2 on speaker1's task-assignment form/complete → 403, speaker2 GET speaker1's uploaded file → 403; a speaker session on organizer APIs (event submissions list, /api/v1/contacts, email-log) → 403; a reviewer assigned only to a track-scoped plan gets 404 — never 403, which would confirm existence — from GET /api/v1/review/submissions/<out-of-scope id>?planId=... and PUT .../evaluations/<same>. The product already implements the reviewer semantics (the former src/routes/review.ts:570-573 (now split across src/routes/review/),609-612). Probes live in scripts/walkthrough/{producer,speaker,review}.ts; if the product fails a probe the walkthrough gate FAILs and the fix is a later planner-issued lane, never part of a gate or harness lane.
