# DEC-092: Ratify scale.ts's purge-probe deviation: no organizer PATCH-title endpoint exists or will be added; the portal-edit write path is the sanctioned DEC-086 publish-affecting write

DEC-089's text assumed an organizer JSON PATCH-title endpoint on /api/v1/submissions/:id, but no such route exists in the DEC-005 route map and scale.ts's GAP NOTE flagged it honestly. The script instead drives the real title write path (public submit -> claim -> bulk accept -> content-status approve -> speaker portal edit, DEC-041) and asserts the new title appears on /e/<slug>/sessions with no sleep. This fully satisfies DEC-086's requirement of "a publish-affecting write observably refreshing a public page" because DEC-083's version bump fires on ANY successful mutation regardless of route. Adding a new endpoint now would be code-bearing and pointlessly invalidate the gate wave. The GAP NOTE is resolved-by-ruling: triage-closure must record it as closed, not as an open PLANNER item.
