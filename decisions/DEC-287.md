# DEC-287: The wave-9 exit predicate is a mechanical planner-only grep over the eight -c2- logs; stage 1 is declared at the greatest RECHECK SHA

DEC-280 made the exit criterion substance rather than sha-uniformity, and DEC-281 made wave 8 the battery, but neither wrote down the literal check the next planner performs — and every previous exit attempt died in the gap between 'the sections were green' and 'someone decided that counted'. Fix it now, before the evidence exists, so the verdict cannot be re-litigated by whoever reads it: stage 1 is DECLARED iff all eight files docs/verification-log/task-w8-{a..h}-c2-*.md exist on main, each contains the line `FROZEN SHA: 80b811d250285de0d37417ddc12f65445ce27f96` verbatim, each contains `OPEN ITEMS: 0`, each contains `RESULT: PASS`, and each contains either `RECHECK SHA: n/a` (empty POST-S DELTA) or a `RECHECK SHA: <40-char>` whose re-check section records every delta-touched claim as green. The declared sha for stage 1 is the greatest RECHECK SHA across the eight sections (ancestor-ordered via git merge-base --is-ancestor), per DEC-280's 'the tip sha the last section observed'. If the predicate fails, wave 9 fixes ONLY the file:line defects the failing sections cite and wave 10 re-runs ONLY the failing sections at a new planner-declared literal — never the whole battery, never a fresh survey.
