# DEC-359: Wave 23's stage-1 exit set is SIX gate lanes, not DEC-358's three: the browser render-sweep, the SPEC/rubric audit, and the zero-secret fresh-clone proof are all stale by many waves or have never run

DEC-358 fixed wave 23's exit at three lanes (build/test, walkthrough, perf:smoke) on the reasoning that those are the gates wave-22's bulk-path lanes can invalidate. That reasoning is correct as far as it goes but it is not the whole exit predicate, and I verified the gap by reading docs/verification-log/ rather than inheriting it. Within this cycle (the "-c3-" logs; task-w21-a-c3-build-test.md's FROZEN SHA c84d8ec matches .git/logs/HEAD, so c3 is current): the last browser render-sweep is task-w15-e-c3-render-sweep.md at f0d56ce, the last SPEC/rubric audit is task-w11-e-c3-spec-audit.md at 84e2c04, and the last fresh-clone proof is task-w11-g-c3-fresh-clone.md. Every admin-SPA data flow was rewritten AFTER those runs: DEC-340 (J6 grid server-paged), DEC-341 (J8 SQL filter/sort/page, client fan-out deleted), DEC-344 (files library server-paged), DEC-345 (results ranked server-side, resultsSort.ts deleted), DEC-350 (J5 picker server-paged), plus wave-19's server-driven worklist that made deliverableCounts a required envelope field. DEC-352 itself argues the walkthrough never loads the admin bundle (its sole /admin assertion is an unauthenticated 302) and perf:smoke times API/SSR routes only — which means NO gate has loaded the rewritten admin SPA in a real browser since before the rewrites. scripts/render-sweep.ts is exactly that gate (every app/src/routeManifest.ts route: 200 + non-empty #root + zero console error/pageerror, then the DEC-253 390x844 mobile pass) and it is one npm script away. Likewise SPEC §0's hard rule — "a fresh checkout plus npm ci plus one dev command is a fully working app" with zero secrets — and its claim that Miniflare emulates cron offline (wrangler.jsonc triggers.crons '*/15 * * * *' -> src/index.ts -> src/server/scheduled.ts:17 -> runDueReminders at src/routes/tasks.ts:419) have unit tests but no end-to-end proof at any tip in this cycle. Six lanes, not three; each is evidence the goalComplete claim actually rests on, so none of it is DEC-315 padding.
