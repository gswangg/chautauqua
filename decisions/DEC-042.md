# DEC-042: Root README.md is the evaluator entrypoint

SPEC §8 requires a 'For evaluators' README with URLs and credentials verbatim, and no root README exists. Structure: project blurb + MIT; quickstart (npm i, npm run db:migrate, npm run seed, npm run dev, plus dev:app watch and npm test); For-evaluators section listing guessable routes (/admin, /portal, /submit/<slug>, /e/<slug>/*, /embed, /dev/mailbox) and the four persona logins exactly as seeded in scripts/seed-lib.ts; a J1-J12 tour table mapping each job to its screens; stage-2 section naming the deferred platform wiring; roadmap noting the Sessionboard-API importer (SPEC §3 migration path). No other top-level doc files.
