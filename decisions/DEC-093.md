# DEC-093: Wave-4 gate protocol: wave-3 gate tasks never executed; the DEC-076 barrier is vacuously satisfied, so all four verification gates run in parallel at code-bearing sha 3878d4f, with triage-closure chained behind the walkthrough gate only

Main's reflog shows no reopened-run task-w3-* merges — history ends at "merge task-w2-d" (3878d4f) followed by the non-code-bearing "scribe wave 3" commit (f9a33fd, touches only decisions/, field-guide, docs, src/decisions.ts string append — bookkeeping per DEC-090). All wave-1/2 code including the scale walkthrough area and the DEC-089 perf-smoke checks is verified in-tree, so no closeout barrier task is needed and chaining the gates would only starve lanes. Per DEC-091 every gate section cites the newest code-bearing short-sha, expected 3878d4f; each worker independently re-derives it by walking git log and skipping DEC-090-exempt commits, and records honestly if a code-bearing merge lands mid-run. Triage-closure alone chains behind the walkthrough gate because pruning docs/eval-findings.md (non-code-bearing per DEC-090) should cite the scale-area probe PASSes as runtime evidence for the DEC-079/083 closures. If a duplicate reopened-run section for the same scope at the same sha appears mid-wave (late wave-3 stragglers), workers verify it and append a brief confirmation instead of duplicating. Exit predicate is evaluated by the wave-5 planner: five sections PASS at 3878d4f plus OPEN ITEMS: 0.
