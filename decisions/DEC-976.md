# DEC-976: The app's CSS contract gains its second direction: a chq-* class in the SPA's markup must have a rule

DEC-937's css-contract.scan.test.ts proves two things about app/src CSS — a bare var(--chq-…) resolves, and a button face declares its own font-family — and both are one-directional checks on the STYLESHEET. Nothing checks the markup side, which is where this product has repeatedly lost frames: the participation pill rendered invisible three times because a className resolved to a reset rule, and the public frame's meta grammar sat unbuilt behind .chq-pub-session-tag. DEC-970 is adding exactly this direction for the SSR stylesheets; the admin SPA — 33 routes, the bulk of the surface — has no equivalent, so a renamed or typo'd className there still fails silently at runtime with no build signal. Rule: app/src/css-contract.scan.test.ts gains invariant (C): every chq-* token appearing in a class position in app/src/**/*.tsx (comments stripped, *.test.tsx excluded) has at least one matching class selector in the union of the enumerated app/src/**/*.css files. Both sides ENUMERATE (readdirSync recursive), never hand-list. Violations are FIXED — the missing rule is written, or the dead className is deleted; a class that genuinely carries no style (a pure query/test hook) gets a NAMED exception with its reason beside it in the source, never an open allowlist.

## Amendment (wave 23): a frame citation must quote the line it cites

w21-e's landed criteria rule cites docs/design/Chautauqua Review.dc.html:132/:62 and then declares 150px/70px — the citation looks like evidence and is actually a paraphrase nobody re-read. A LINE NUMBER IS NOT AN IDENTITY, and a citation is not a transcription. RULING: every `docs/design/*.dc.html:<line>` reference in an app/src or src comment must be immediately followed, in the same comment, by the cited line's literal declaration in backticks (e.g. `grid-template-columns:190px 1fr auto`). The quote is the receipt: a reader comparing the rule below to the quote beside it needs no second file open, and a frame edit that moves a line breaks the quote instead of silently rotting the number. The scan asserts quote PRESENCE and that the quoted literal appears verbatim (whitespace-normalised) at that line of that frame file — it deliberately does NOT assert the neighbouring rule equals the quote, because that comparison is the auditor's judgment (a rule may legitimately differ from a frame under a recorded ruling) and because forcing equality would make the scan a rubber stamp.

## Amendment: a task-boundary exclusion in a scan is dated and expires with its wave (wave 2)

app/src/css-contract.scan.test.ts's invariant (D) skips app/src/pages/contacts/** with a comment stating it is 'dated to the current wave's task split, not a standing exemption -- a future wave should re-probe it'. That wave finished long ago; contacts CSS has been unpoliced for dead rules ever since, and the exclusion now reads like policy. Ruling: an exclusion written as temporary is honoured by DELETING it at the next wave that can afford the cleanup, and offenders are resolved by removing the dead rule -- never by adding a className to satisfy the scan, and never by an allowlist populated from the failure output.

## Amendment (wave 25): a planner's defect claim carries the same receipt as a frame citation, and a worker that cannot reproduce it stops

Read at tip 39ac22d0, five of the six defects handed to wave 24 were already closed on main: src/views/form-render.tsx sets aria-invalid + chq-field-invalid on all six FieldControl kinds (:46,64,79,93/99,113,120/125) and test/form-render-invalid-vocabulary.test.ts already enumerates them; src/mail/shell.ts:70 already emits `<html lang="en">`; SpeakerDetailPage.tsx:334-424 already wraps its link and buttons in `<span role="cell">` rather than putting the role on the control; contacts.css:530-542 already sources the phone label from `td[data-label]::before`; and submit-views.tsx:421-431 already renders the authored `form.description` as the CFP lede with the computed sentence as fallback. A wave of no-op briefs costs a worker's whole budget and risks rewriting correct code. RULING, extending DEC-976's quote-the-citation discipline from frames to defect claims: a task brief asserting a defect must quote the offending source line, and a worker who cannot reproduce the quoted defect at its own branch point STOPS, reports CLOSED-AT-TIP with the line that disproves it, and never edits working code to satisfy the brief. Corollary for planners: the newest verification-log DETAIL file outranks the newest prose — a code read invents defects, a gate finds them.

## Amendment (wave 27): the ten-wave-old TIER-1 fidelity clauses get a quoted line each or they are CLOSED-AT-TIP

DEC-976 established that a claim without a quoted source line is a rumour. `docs/eval-findings.md` TIER 1 carries roughly a dozen clauses explicitly marked VERIFIED-OPEN-NOT-RECHECKED — 'participation panel 420', 'speaker-detail grid/theads', 'write-failed banner anatomy', 'upload-reject modal anatomy', 'templates-grid overlap', 'plan-editor draft footer', 'duplicated results head', 'content-status band not full-bleed', 'active-filter ink chip', 'speakers toolbar right-cluster', 'underlined initials', 'blue avatars', 'Add-track tertiary', 'saved-embed single-card anatomy', plus the eight-item Settings remainder — none re-measured since a research-repo gate whose renders are not vendored here. The field guide's own wave-25 lesson is that waves 18-24 spent five lanes fixing already-fixed defects sourced from exactly this kind of inherited claim. RULING: each clause is walked against current `main` AND the vendored `docs/design/*.dc.html` pack, and gets one of three verdicts, each with a quoted `path:line`: OPEN (the tree contradicts a quoted frame/README line), CLOSED-AT-TIP (the tree satisfies it, or the clause names a figure the vendored README supersedes — the `12-home` 46px/732 precedent), or UNQUOTABLE (no line in the vendored pack states the rule the clause asserts, so it was never a spec claim). UNQUOTABLE clauses are deleted, not carried.

## Amendment (wave 29): a quoted line is evidence only for the surface its own enclosing SECTION HEADING names

Wave 28's sole surviving gate OPEN ITEM (docs/verification-log.md:3877-3884) quoted `docs/design/README.md:350` — `Speakers   [Search speakers…]   [All tracks ▾]   [List | Grid]` — and then searched `app/src/pages/speakers/GridFilters.tsx`, the ADMIN onboarding-grid toolbar, concluding "no view-mode toggle exists anywhere". Five lines above the quote, `docs/design/README.md:345` heads the block: "Public filter bar — one idiom, four surfaces". The specified controls are already built, twice over: `SpeakerViewToggle` renders the literal `List`/`Grid` anchors (src/routes/public/speakers.tsx:41-56) and `TrackFacetSelect` renders the `All tracks` select (`:82-101`), both mounted on `SpeakersContent` (`:235`) and its gallery twin `GalleryContent` (`:314`). DEC-976 already ruled that a claim without a quoted line is a rumour; this shows a line quoted out of its section is the same rumour wearing a citation. RULING: a fidelity citation names `path:line` AND the nearest enclosing heading; a finding whose quote and whose searched surface sit under different headings is VOID, and closing it requires a test that pins the real surface, not merely a note.
