# DEC-882: A frozen round reads as TEXT, the criteria table names its columns, and the lock card explains below what the rows already show

PlanEditor already computes activeRoundIsLocked from server truth (evaluationCountsByRound, DEC-213/676) but keeps rendering the same input rows in a disabled state. A control that cannot act must not render as a control — that is the standing affordance rule, and a disabled text input is the least honest form of read-only. The Review frame renders a locked plan's criteria as read-only text rows beneath a CRITERION | GUIDANCE | WEIGHT header row, with the lock explanation BELOW the rows so a reader sees what the criteria ARE before being told why they cannot change, and the page header reading "Open · N of M reviews in" from the plan's own progress aggregate rather than a second count derived in the component. The weight-to-percentage vocabulary ("Weight N · NN%") belongs to the scorecard decision that a concurrent lane owns; this surface prints the raw weight only, so one formatter is never written twice.


## Amendment (wave 7, sha ddc9a7d9): a frozen criteria readout must name each criterion's TYPE; the freeze caption reads "wording, type and weights"

`PlanEditor.tsx:1667` carries the comment "w5-e: KIND column dropped" and renders an empty cell where the frame draws four columns — `1fr 232px 128px 122px` = label · hint · `{{ c.typeLabel }}` · `{{ c.frozenWeight }}` (Chautauqua Review.dc.html:696-701). That removal was taken when every criterion was a Scale; under v12 it is exactly the column that tells a reader whether a locked criterion is a Choice, and it explains why that row's weight cell is blank. The same frame's eyebrow (`:693`) now says the freeze covers "wording, TYPE and weights", and DESIGN-RULINGS.md:408 states the frozen caption "now reads 'wording, type and weights fixed for this wave'" — while the app's caption at `:1507` still says "Wording, weights and the scale are fixed for the rest of this wave", and "the scale" is meaningless for a Choice criterion. RULING: restore the type column (rendering DEC-613's one map), print "Not scored" in a non-rating criterion's weight cell (frame `:1130`), and change the caption's vocabulary. The eyebrow KEEPS its review count — it is the falsifiable fact and the frame's caption text now lives in the lock card; a pre-v12 removal surviving a v12 intake is the defect, not the eyebrow.
