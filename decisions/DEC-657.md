# DEC-657: The speaker portal's file surface keeps no private upload vocabulary and ships no unlabelled byte stream; the DEC-551 nosniff guard enumerates the whole repo

src/routes/portal/tasks.tsx:222-223 hand-writes the accepted-types sentence ('pdf, ppt, pptx, key, odp, zip (25 MB max); ...') -- a fourth copy of the DEC-020 allowlist, alongside the server authority in src/domain/files.ts and the SPA mirror DEC-653 is deleting -- while src/views/form-render.tsx:8,92-96 already imports ALLOWED_UPLOAD_EXTENSIONS + uploadHintText() and does it right. Both portal file inputs (:225, :246) carry no accept attribute and the Replace-file form carries no hint at all. Separately, the three portal streams at :710, :749 and :787 return R2 bodies with only Content-Type while the organizer twin (src/routes/files.ts:421) sets nosniff -- and test/user-content-nosniff.test.ts:22-27 documents this exact gap in a NOTE, with its static guard deliberately scoped to two files. A guard that names the routes it is not guarding is a manifest that has already desynced; enumerate the repo instead.


## Amendment (wave 28): the SSR error vocabulary is hoisted to one module shared by CFP and portal, and the portal's upload refusal is a designed refusal

The vocabulary exists exactly once on the server, buried in src/routes/public/cfp.css.ts:108-119, so the speaker portal — the one surface a non-employee uses — renders its refusals under an unprefixed class="field-error" (src/routes/portal/tasks/views.tsx:186) that no stylesheet defines, with the copy 'file is required' (src/routes/portal/tasks.tsx:525). DEC-657 forbids a private portal file vocabulary; this extends it to the refusal itself. Ruling: hoist the rules into a new src/views/error-states.css.ts exporting ERROR_STATES_CSS, composed into both CFP_CSS and PORTAL_CSS the way PUBLIC_CSS already composes CHROME/CARDS/AGENDA/RAIL; rename the portal class to chq-field-error and mark the file input .chq-field-invalid + aria-invalid; and make both refusal messages sentences that name the accepted formats, why, and what survived ('Your current file is unchanged. Nothing was replaced.'), sourced from src/domain/files.ts, not re-typed.
