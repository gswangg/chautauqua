# DEC-657: The speaker portal's file surface keeps no private upload vocabulary and ships no unlabelled byte stream; the DEC-551 nosniff guard enumerates the whole repo

src/routes/portal/tasks.tsx:222-223 hand-writes the accepted-types sentence ('pdf, ppt, pptx, key, odp, zip (25 MB max); ...') -- a fourth copy of the DEC-020 allowlist, alongside the server authority in src/domain/files.ts and the SPA mirror DEC-653 is deleting -- while src/views/form-render.tsx:8,92-96 already imports ALLOWED_UPLOAD_EXTENSIONS + uploadHintText() and does it right. Both portal file inputs (:225, :246) carry no accept attribute and the Replace-file form carries no hint at all. Separately, the three portal streams at :710, :749 and :787 return R2 bodies with only Content-Type while the organizer twin (src/routes/files.ts:421) sets nosniff -- and test/user-content-nosniff.test.ts:22-27 documents this exact gap in a NOTE, with its static guard deliberately scoped to two files. A guard that names the routes it is not guarding is a manifest that has already desynced; enumerate the repo instead.
