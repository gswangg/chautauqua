# DEC-496: The closing ledger grades at file:line at its own sha, classifies a missing fix by ancestor-check on the branch that owns it, and strikes a carried open item whose own source contradicts it

Wave 25 opens with four of wave 24's five lanes (DEC-488/489/490/491) merged into main DURING this planning pass and the fifth (DEC-492, task-w24-e: agenda.ts's read-then-write `bumpIcsSequence` at the former src/server/repo/agenda.ts:426 (now split across src/server/repo/agenda/) and runAutoSchedule's unbounded per-placement loop at :500-513) still outstanding. The ledger must therefore be written to survive a moving tree: every row is graded by a fresh read at the ledger's OWN sha (DEC-448/472), and a fix that is absent is classified by mechanical ancestry -- PENDING-OWNED(task-w24-e) when a branch or merge commit owns it, FAIL-unowned ONLY when nothing does. Two carried items must also be settled with evidence rather than repetition. task-w21-f open item #1 cites docs/verification-log/task-w21-e-perf-smoke-stage1.md:103 for an UNMEASURABLE-BY-CONSTRUCTION perf row, but that file's sentence reads "Neither row is UNMEASURABLE-BY-CONSTRUCTION at this sha." -- the open item is a misread of its own source and is STRUCK with the quote, not carried a fourth wave. task-w21-f open item #2 (src/routes/api/forms.ts:259, bare `{items}` echo, no field ceiling) is CLOSED by DEC-488 and must be re-verified at file:line (src/server/repo/forms.ts:250, src/routes/api/forms.ts:154-156 and :264) rather than credited from this decision doc.
