# DEC-358: Stage-1 exit accounting for wave 22: the global public-cache purge stays CLOSED, and wave 23's exit set is build/test/tripwires/fresh-migration + FULL walkthrough + perf:smoke at one tip

Three things a later planner should not re-derive. (1) The review lens's top finding — pubcache.ts's bumpIfMutating rotating chq:pubver on every successful non-GET, so logins and rating PUTs cold-start the public cache — is a true description of the code and is ADJUDICATED CLOSED by DEC-201, reaffirmed by DEC-333 and again by DEC-348, on the ground that SPEC §0 defers production edge-cache validation to stage 2, Miniflare's caches.default is not an edge cache, and a route-by-route purge predicate manufactures a real staleness-risk class for zero stage-1 observable benefit. I re-read the code and the three decisions and concur: do not open a lane for it. (2) DEC-342's second deferral — the files library's whole-event scan and its quadratic lead-speaker find-in-loop — was DISCHARGED by DEC-344: src/server/repo/files-library.ts:97-243 is one paginated statement over chain roots with a single grouped lead-speaker pass, and task-w20-d's run timed it at 47.4ms adjusted against the 50ms read budget (a watch item at wave 21's authoritative run, not an open item). (3) DEC-352's rule applied to this wave: all five wave-22 lanes change routes the six-module walkthrough exercises directly (scripts/walkthrough/scale.ts:265/335/399/499 and public/producer/speaker bulk-accept steps; scripts/walkthrough/data.ts:214 CSV import) and change bulk write paths, so the walkthrough gate DOES die to them and perf:smoke is re-run at the same tip rather than reasoned about. Wave 23's exit set is therefore exactly three lanes — build + test + the four test/ tripwires + a fresh-migration/seed proof; the full six-module walkthrough; the authoritative perf:smoke — at one tip containing task-w21-a..e and task-w22-a..e, and the planner that can read those three at RESULT: PASS / OPEN ITEMS: 0 (together with task-w21-a/b/c green) returns zero tasks with goalComplete: true. A FAIL yields a narrow wave over the cited file:line only; DEC-315's anti-padding rule survives intact.

## Amendment (wave 17): docs/eval-findings.md is rebased onto the named tip, per-item cited, and dismissals are recorded rather than deleted

docs/eval-findings.md currently declares itself "Verified against main sha 541688f5" and its IN FLIGHT section triages the w8-w11 range; main is 9b21309c and the whole w12-w16 range has landed since. The file is the mandate every wave reads first, so a stale mandate misdirects every lane that obeys it — and this run's four reviewer-lens findings (bulk-email dedupe, createUser TOCTOU, breaks field accumulation, send.ts ics_sequence) were all already closed on main, which is exactly the waste a stale mandate produces. RULE, extending DEC-358's exit accounting: the findings file names the tip sha it was verified against in its header; every TIER entry carries either a file:line against THAT tip or an exercised-check citation; a dismissed item keeps its citation in a DO-NOT-RE-FILE block rather than being deleted (deletion invites rediscovery); and the IN FLIGHT block is written from .git ref state per the DEC-069 amendment above, never from a task brief's restated claim.

## Amendment (wave 27): the eval-findings rebase is a COMPACTION with per-item citations kept, not another prepended header

`docs/eval-findings.md` is 1,076 lines carrying four campaigns' sediment: a wave-24 header superseding a wave-18 header, four stacked REF TRUTH blocks (wave 24, wave 18, wave 17, wave 16) each kept "for its own boundary citations", and an IN FLIGHT section listing `task-w24-*` lanes that merged three waves ago (main's reflog shows `merge task-w24-a..f` at 1786810048-1786810437, and no `task-w24-*` ref survives). Each successive rebase has PREPENDED a header rather than compacting, so the file now costs more to read than it saves and the standing rule 'a mandate item moves tier only on a citation against current main' is unenforceable against it. RULING: the rebase re-derives REF TRUTH from `.git` at its OWN runtime (never from this brief), collapses every superseded REF TRUTH block to a single dated line plus its file path, deletes nothing that is a per-item citation, and moves any item whose owning branch is now merged into a TIER-0 CLOSED entry naming the merge. Target: under 400 lines with strictly more live information than the 1,076 it replaces.

## Amendment (wave 31): a recorded ruling is not a landed fix — the rebased mandate carries a RULED-NOT-IMPLEMENTED tier, keyed to ref evidence

docs/eval-findings.md still declares itself verified against `ceda66f2` while main is `dbac66d1`, four merges and three waves later, and its IN FLIGHT block triages `task-w26-*`/`task-w27-*` refs that merged long ago — the exact staleness DEC-358's wave-17 amendment exists to forbid and its wave-27 amendment last compacted. This wave supplies the reason the rule needs one more clause. DEC-773's wave-29 amendment is a fully argued, binding, file:line-cited ruling on the files-library defect, written into `decisions/` and compiled into `src/decisions.ts` — and for a full wave nothing in `src/server/repo/files-library.ts` moved, because the lane that owned it (`task-w29-b`) produced zero commits. A reader of `decisions/` alone would conclude the 481ms row was fixed. The same trap is live for `task-w29-e`. RULING: the rebase carries an explicit RULED-NOT-IMPLEMENTED section listing every DEC amendment whose named file is unchanged since the amendment landed, each with the ref evidence that proves it (branch tip vs merge-base) and the current file:line showing the old shape still present. A decision document is a mandate, never a receipt; only a diff or an exercised check closes an item. Also settled here so no future lane re-litigates it: the review-lens claim that "DEC-358 is cited with no visible finding backing it" is DISMISSED — `decisions/DEC-358.md:9-11` carries the wave-27 compaction amendment verbatim, and a decision file is its own backing.

## Amendment (wave 35): a findings rebase re-derives REF TRUTH *and* re-globs every existence claim — an absence is a measurement, not a note to carry

`docs/eval-findings.md` is rebased at wave 31 against `6dbf7117` and is materially false at `c9532d9a`, nine merges later. Three failure modes, all present: (1) its TIER 0 OWNED section still assigns the two surviving perf rows to `task-w31-a/-b/-c` as zero-commit owners, when `task-w32-a`/`task-w32-b` have since landed and merged fixes for both; (2) its IN FLIGHT section enumerates 29 refs of which every wave-31/32/33 entry has been deleted post-merge, so its own "do not re-file" fences point at nothing; (3) its TIER-1 pointer asserts that `docs/verification-log/task-w27-g-fidelity-recheck-*.md` "does not exist" — `docs/verification-log/task-w27-g-fidelity-recheck-ceda66f2.md` is in the tree. The first two are already governed by DEC-069's wave-17 REF TRUTH rule. The third is a new class and the reason for this amendment: a claim that a FILE IS ABSENT decays exactly like a claim that a defect is present, and this one has been carried across at least four waves unre-globbed. RULING: a rebase re-derives IN FLIGHT from `.git` at its own runtime, and additionally re-globs every claim of the form "file X does not exist" before carrying it; an existence claim that survives a rebase without being re-globbed is DELETED rather than carried. Dismissals are still never deleted — only re-homed and re-dated.

## Amendment (wave 37): a CLOSED finding must name the exercised check that would fail if its fix were reverted, or be relabelled UNFALSIFIABLE with an owner

DEC-426's wave-36 finding generalizes past its own selector. `.chq-participation-menu-caret` was FAILed by task-w28-d, credited fixed by task-w29-d, credited PASS by task-w29-c — and task-w35-b's fresh full-gate run proved the contrast check contains no row for that selector at all. Three waves of receipts rested on a code-shape citation that no instrument could ever contradict. This is the same decay DEC-358's wave-31 amendment named (a recorded ruling is not a landed fix) and its wave-35 amendment named again (an absence is a measurement), one level up: a closure with no falsifying check is not evidence, it is a belief with a citation. RULING, binding on every eval-findings rebase from wave 37 on: each TIER 0 CLOSED row carries the exercised check that would go red if the fix were reverted — a test file path, a named gate row, or a walkthrough assertion. A row whose only backing is a code read is relabelled UNFALSIFIABLE with a named owner and KEPT, never deleted (dismissals are recorded, never deleted). This costs the rebase nothing it was not already reading and makes the next instrument gap loud at write time instead of three waves later.
