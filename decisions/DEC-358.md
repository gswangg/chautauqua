# DEC-358: Stage-1 exit accounting for wave 22: the global public-cache purge stays CLOSED, and wave 23's exit set is build/test/tripwires/fresh-migration + FULL walkthrough + perf:smoke at one tip

Three things a later planner should not re-derive. (1) The review lens's top finding — pubcache.ts's bumpIfMutating rotating chq:pubver on every successful non-GET, so logins and rating PUTs cold-start the public cache — is a true description of the code and is ADJUDICATED CLOSED by DEC-201, reaffirmed by DEC-333 and again by DEC-348, on the ground that SPEC §0 defers production edge-cache validation to stage 2, Miniflare's caches.default is not an edge cache, and a route-by-route purge predicate manufactures a real staleness-risk class for zero stage-1 observable benefit. I re-read the code and the three decisions and concur: do not open a lane for it. (2) DEC-342's second deferral — the files library's whole-event scan and its quadratic lead-speaker find-in-loop — was DISCHARGED by DEC-344: src/server/repo/files-library.ts:97-243 is one paginated statement over chain roots with a single grouped lead-speaker pass, and task-w20-d's run timed it at 47.4ms adjusted against the 50ms read budget (a watch item at wave 21's authoritative run, not an open item). (3) DEC-352's rule applied to this wave: all five wave-22 lanes change routes the six-module walkthrough exercises directly (scripts/walkthrough/scale.ts:265/335/399/499 and public/producer/speaker bulk-accept steps; scripts/walkthrough/data.ts:214 CSV import) and change bulk write paths, so the walkthrough gate DOES die to them and perf:smoke is re-run at the same tip rather than reasoned about. Wave 23's exit set is therefore exactly three lanes — build + test + the four test/ tripwires + a fresh-migration/seed proof; the full six-module walkthrough; the authoritative perf:smoke — at one tip containing task-w21-a..e and task-w22-a..e, and the planner that can read those three at RESULT: PASS / OPEN ITEMS: 0 (together with task-w21-a/b/c green) returns zero tasks with goalComplete: true. A FAIL yields a narrow wave over the cited file:line only; DEC-315's anti-padding rule survives intact.

## Amendment (wave 17): docs/eval-findings.md is rebased onto the named tip, per-item cited, and dismissals are recorded rather than deleted

docs/eval-findings.md currently declares itself "Verified against main sha 541688f5" and its IN FLIGHT section triages the w8-w11 range; main is 9b21309c and the whole w12-w16 range has landed since. The file is the mandate every wave reads first, so a stale mandate misdirects every lane that obeys it — and this run's four reviewer-lens findings (bulk-email dedupe, createUser TOCTOU, breaks field accumulation, send.ts ics_sequence) were all already closed on main, which is exactly the waste a stale mandate produces. RULE, extending DEC-358's exit accounting: the findings file names the tip sha it was verified against in its header; every TIER entry carries either a file:line against THAT tip or an exercised-check citation; a dismissed item keeps its citation in a DO-NOT-RE-FILE block rather than being deleted (deletion invites rediscovery); and the IN FLIGHT block is written from .git ref state per the DEC-069 amendment above, never from a task brief's restated claim.

## Amendment (wave 27): the eval-findings rebase is a COMPACTION with per-item citations kept, not another prepended header

`docs/eval-findings.md` is 1,076 lines carrying four campaigns' sediment: a wave-24 header superseding a wave-18 header, four stacked REF TRUTH blocks (wave 24, wave 18, wave 17, wave 16) each kept "for its own boundary citations", and an IN FLIGHT section listing `task-w24-*` lanes that merged three waves ago (main's reflog shows `merge task-w24-a..f` at 1786810048-1786810437, and no `task-w24-*` ref survives). Each successive rebase has PREPENDED a header rather than compacting, so the file now costs more to read than it saves and the standing rule 'a mandate item moves tier only on a citation against current main' is unenforceable against it. RULING: the rebase re-derives REF TRUTH from `.git` at its OWN runtime (never from this brief), collapses every superseded REF TRUTH block to a single dated line plus its file path, deletes nothing that is a per-item citation, and moves any item whose owning branch is now merged into a TIER-0 CLOSED entry naming the merge. Target: under 400 lines with strictly more live information than the 1,076 it replaces.

## Amendment (wave 31): a recorded ruling is not a landed fix — the rebased mandate carries a RULED-NOT-IMPLEMENTED tier, keyed to ref evidence

docs/eval-findings.md still declares itself verified against `ceda66f2` while main is `dbac66d1`, four merges and three waves later, and its IN FLIGHT block triages `task-w26-*`/`task-w27-*` refs that merged long ago — the exact staleness DEC-358's wave-17 amendment exists to forbid and its wave-27 amendment last compacted. This wave supplies the reason the rule needs one more clause. DEC-773's wave-29 amendment is a fully argued, binding, file:line-cited ruling on the files-library defect, written into `decisions/` and compiled into `src/decisions.ts` — and for a full wave nothing in `src/server/repo/files-library.ts` moved, because the lane that owned it (`task-w29-b`) produced zero commits. A reader of `decisions/` alone would conclude the 481ms row was fixed. The same trap is live for `task-w29-e`. RULING: the rebase carries an explicit RULED-NOT-IMPLEMENTED section listing every DEC amendment whose named file is unchanged since the amendment landed, each with the ref evidence that proves it (branch tip vs merge-base) and the current file:line showing the old shape still present. A decision document is a mandate, never a receipt; only a diff or an exercised check closes an item. Also settled here so no future lane re-litigates it: the review-lens claim that "DEC-358 is cited with no visible finding backing it" is DISMISSED — `decisions/DEC-358.md:9-11` carries the wave-27 compaction amendment verbatim, and a decision file is its own backing.

## Amendment (wave 35): a findings rebase re-derives REF TRUTH *and* re-globs every existence claim — an absence is a measurement, not a note to carry

`docs/eval-findings.md` is rebased at wave 31 against `6dbf7117` and is materially false at `c9532d9a`, nine merges later. Three failure modes, all present: (1) its TIER 0 OWNED section still assigns the two surviving perf rows to `task-w31-a/-b/-c` as zero-commit owners, when `task-w32-a`/`task-w32-b` have since landed and merged fixes for both; (2) its IN FLIGHT section enumerates 29 refs of which every wave-31/32/33 entry has been deleted post-merge, so its own "do not re-file" fences point at nothing; (3) its TIER-1 pointer asserts that `docs/verification-log/task-w27-g-fidelity-recheck-*.md` "does not exist" — `docs/verification-log/task-w27-g-fidelity-recheck-ceda66f2.md` is in the tree. The first two are already governed by DEC-069's wave-17 REF TRUTH rule. The third is a new class and the reason for this amendment: a claim that a FILE IS ABSENT decays exactly like a claim that a defect is present, and this one has been carried across at least four waves unre-globbed. RULING: a rebase re-derives IN FLIGHT from `.git` at its own runtime, and additionally re-globs every claim of the form "file X does not exist" before carrying it; an existence claim that survives a rebase without being re-globbed is DELETED rather than carried. Dismissals are still never deleted — only re-homed and re-dated.

## Amendment (wave 37): a CLOSED finding must name the exercised check that would fail if its fix were reverted, or be relabelled UNFALSIFIABLE with an owner

DEC-426's wave-36 finding generalizes past its own selector. `.chq-participation-menu-caret` was FAILed by task-w28-d, credited fixed by task-w29-d, credited PASS by task-w29-c — and task-w35-b's fresh full-gate run proved the contrast check contains no row for that selector at all. Three waves of receipts rested on a code-shape citation that no instrument could ever contradict. This is the same decay DEC-358's wave-31 amendment named (a recorded ruling is not a landed fix) and its wave-35 amendment named again (an absence is a measurement), one level up: a closure with no falsifying check is not evidence, it is a belief with a citation. RULING, binding on every eval-findings rebase from wave 37 on: each TIER 0 CLOSED row carries the exercised check that would go red if the fix were reverted — a test file path, a named gate row, or a walkthrough assertion. A row whose only backing is a code read is relabelled UNFALSIFIABLE with a named owner and KEPT, never deleted (dismissals are recorded, never deleted). This costs the rebase nothing it was not already reading and makes the next instrument gap loud at write time instead of three waves later.

## Amendment (wave 39): an UNFALSIFIABLE label is an owner, not an annotation — and it is discharged by READING the check, never by grepping for a filename

The wave-37 rebase stamped ~15 TIER-0 rows `UNFALSIFIABLE — owner: wave-38 lane`; wave 38 spent all five lanes on the security/correctness defects and claimed none of them, so the label has already rolled once unclaimed. Worse, I spot-checked four of the rows myself at this wave's runtime and the label is WRONG on all four: `clearSessionCookie` appending `Secure` is exercised by `test/cookie-flags.test.ts`, the onboarding-grid status-token refusal by `test/onboarding-grid-query.test.ts`, the two review scan caps by `test/review-plan-submissions-scan.test.ts` / `test/review-repo-aggregates.test.ts`, and the two "render-sweep items closed in the tree" CSS rows by the sweep's own generic assertions (`scripts/render-sweep.ts:17-20`: page-level horizontal overflow <= innerWidth + 1px, and a >= 40px minimum control height on the mobile pass). The rebase labelled them unfalsifiable without looking. RULING: the label is discharged only by opening the candidate check and confirming it would FAIL if the cited behavior were reverted; a row with no such check gets one written, and a row whose check exists gets the label replaced by that check's path plus what it asserts. A stale UNFALSIFIABLE label is the same defect class as a stale "file X does not exist" claim the wave-35 amendment already outlawed — an absence is a measurement, and so is a presence.

## Amendment (wave 41): within one wave docs/eval-findings.md has exactly ONE editing owner; discharge lanes write their evidence into their own docs/mandates/ file

The mandate doc is simultaneously the thing a rebase lane must rewrite wholesale (header, IN FLIGHT, stale-claim corrections) and the thing every falsifiability lane wants to annotate — three lanes in one file per wave, at the same tail, is the contention shape the wave-28 structure custodian already decomposed for `docs/verification-log.md`. It is also an honesty problem: a rebase lane that relabels an UNFALSIFIABLE batch as discharged is claiming a verification it did not perform, and a discharge lane that rewrites the header is naming a boundary it did not measure. Ruling: the rebase lane owns `docs/eval-findings.md` and may change a batch's OWNER label (a plan fact) but never its discharge status (a verification fact); each discharge lane owns one test file plus one new `docs/mandates/w41-falsifiability-batch-<x>.md` carrying its per-item `FALSIFYING CHECK:` lines, which the NEXT wave's rebase folds in after reading them. Unchanged and reaffirmed: an item closes on an exercised check, never a code read; a lane that finds a real product defect FILES it with file:line and does not fix it (DEC-453).

## Amendment (wave 42): ONE SECTION, ONE OWNER — the triage-closure population is split by source section, and no wave-42 lane touches docs/eval-findings.md

Two lanes adjudicating the same open item concurrently produce two verdicts and no owner. The wave-42 population is therefore split by SOURCE SECTION, single-owner: `task-w42-a` owns index sections 0174 (7), 0175 (3), 0188 (1), 0189 (1), 0192 (1), 0193 (4), 0196 (5) — every OPEN ITEMS > 0 section from 0174 to EOF except two; `task-w42-e` exclusively owns 0195 (task-w36-f aie-scale, OPEN ITEMS: 3, including the `autoSchedule320` FAIL) and 0197 (task-w37-d walkthrough-preflight, OPEN ITEMS: 1). `task-w42-a` records those two as EXCLUDED-BY-POPULATION-SPLIT with the owning branch named, and states in its own body that its count does not speak for them — a split that is declared is auditable; a split that is silent is gaming. Review-lens claims are a third, separate population living in `task-w42-c`/`-d`'s advisory sections. Finally: `docs/eval-findings.md` is owned this wave by the wave-41 rebase lane (`task-w41-*`, three refs created from `d7cfe08d` at my runtime, still at base = RUNNING per this DEC's sibling rule); NO wave-42 lane edits it. Adjudication verdicts land in verification-log index entries instead, which is where a verdict with a sha belongs anyway.

## Amendment (wave 44): the triage-closure population is NOT split — one lane reconciles every open row in docs/verification-log/index/

Wave 42 split the triage population: `0210` defined its scope as sections 0174..0201 "except 0195 and 0197 (excluded-by-population-split, owned by task-w42-e)", and `0213` took the excluded pair. That split is precisely how two false rows survived a whole wave with no single owner reconciling them against each other — `0210` recorded `perf-seed.ts` CLOSED on a live re-grep (`PERF_SPEAKER` count 13) while its sibling `0213` recorded the same file as a CONFIRMED-DEFECT, and no lane owned the contradiction. A population split also let `0210` re-file an item (`admin Speakers [List | Grid]`) that `docs/eval-findings.md` had already ruled VOID with a DO-NOT-RE-FILE line, because the lane that held the eval-findings ruling was not the lane holding that row.

RULING: wave 44's triage-closure lane takes the WHOLE population, with no exclusion clause — every `OPEN ITEMS: <n>` line with n>0 in `docs/verification-log/index/`, from `0174` through the highest sequence present at that lane's own runtime, which today means at least 0174, 0175, 0188, 0189, 0192, 0193, 0195, 0196, 0197, 0210, 0213, 0215, 0216, plus anything a wave-43 or wave-44 sibling adds before it measures. The wave-42 dedupe rule is KEPT (one underlying defect reconciled once, citing every section that raises it) because dedupe collapses duplicates within one owner's view; a population split hands duplicates to different owners, which is the opposite. Standing evidence rules bind unchanged: a row closes only on a file:line re-read or a named exercised check taken at the lane's OWN runtime (never inheriting a prior wave's verdict, DEC-069 wave-17); a row that cannot be closed is COUNTED, named with file:line, and given a wave-45 owner; a code-shape citation alone is labelled UNFALSIFIABLE and kept rather than deleted (DEC-358 wave-37). Where a section's row contradicts another section's row, the lane adjudicates the contradiction explicitly rather than counting both.


## Amendment (wave 45): the adjudication population is SPEC's OWN J-areas, one owner per area, no exclusion clause and no split

DEC-358's wave-44 amendment named the failure exactly — A SPLIT POPULATION HAS NO OWNER FOR THE CONTRADICTION: `0210` called the perf-seed row CLOSED while `0213` called it CONFIRMED-DEFECT, and because wave 42 split the 0174..0201 population between two lanes, nobody owned the clash. Wave 45 must not reproduce that shape in a new dimension.

Wave 45's population is therefore NOT the ledger and NOT the verification-log corpus (wave 44 owns that, un-split). It is SPEC.md's own job narratives, partitioned into six disjoint areas with exactly one owning lane each, every clause traceable to a SPEC line range: J4 review/evaluation (SPEC.md:114-121) → `task-w45-a`; J5 comms/send/email-log (SPEC.md:123-127 + §2 principle 4 at :199-201) → `task-w45-b`; J6/J7 onboarding tasks, cron reminders, portal scope (SPEC.md:129-144) → `task-w45-c`; J9/J10 agenda, auto-schedule, .ics publication (SPEC.md:153-169) → `task-w45-d`; J11 CRM directory, import, merge, segments (SPEC.md:171-175) → `task-w45-e`; J8 content approval and file versioning (SPEC.md:146-151) → `task-w45-h`. No area appears in two lanes; no lane carries an "except" clause. The three areas wave 42 already adjudicated (contacts-integrity `0211`, content-lifecycle `0212`, gate-gap `0213`) are NOT re-opened wholesale — where a wave-45 area touches them, the lane's named claim is a NEW claim (e.g. merge PREVIEW/EXECUTE parity after wave 43's all-or-nothing fix), not a re-litigation of a settled row.

EVIDENCE STANDARD, unchanged and binding: a claim closes only on runtime evidence — a targeted test the lane actually ran, quoted with its pass/fail counts — or on a file:line quote PLUS a named existing test that would fail if the behavior were reverted. A code-read alone is labelled `UNFALSIFIABLE` and KEPT, never upgraded to CLOSED. A claim that cannot be adjudicated is labelled `UNRECHECKED` with the reason, never given a fabricated citation (the honest shape `0214:42-43` used).

COUNT AND LIST SHARE ONE POPULATION (DEC-615's wave-43 rule, restated because it is the rule most often broken): each section's `OPEN ITEMS: <n>` equals exactly the number of rows in its own table marked CONFIRMED-DEFECT — not "issues noticed", not "claims examined".


## Amendment (wave 46): the batch-A falsifiability remainder has a live owner again — four items discharge with exercised checks, the rest re-point to wave 47+

`docs/eval-findings.md` carries two lists labelled "STILL UNFALSIFIABLE ... owner: a wave-45 lane, not re-pointed at wave-41 any longer (that lane closed)". Measured at my own runtime, no wave-45 branch covers either list: `task-w45-a` is at its base and `-b`/`-c`/`-d` carry commits scoped to J4, J5, J6+J7 and J9+J10 adjudication, while `-e` through `-h` have no ref at all. So the named owner does not exist and both lists are unowned — the exact failure this DEC's wave-31 amendment named (a recorded ruling is not a landed fix) applied to ownership itself: an owner line that names a wave rather than a branch decays the moment that wave ends. RULING: (a) an owner line names a BRANCH, and when its wave closes without that branch existing the items return to unowned and must be re-pointed by the next planner, not silently carried; (b) wave 46 discharges exactly four batch-A items, each by opening the cited file at the lane's own runtime and writing a real assertion against the real symbol — saved embeds (`src/db/schema/embed.ts`, `src/routes/public/saved-embed.tsx`), `/account/password`'s real Cancel and 820px bare-page column (`src/routes/account.tsx:139-144`), Overview §01's skips-last-hour caption (`app/src/pages/Overview.tsx:325`), and the Home footer media rule (`src/routes/public/home.css.ts:72-76`); (c) bounded scope is the point — four discharged with real checks beats nine discharged with tautologies, and this DEC's wave-37 amendment already rules that a code-shape citation is not a closure; (d) if any of the four claims is FALSE on re-reading, it is recorded as a finding with file:line and NOT converted into a green test, because a test written to match whatever the code happens to do is worth less than the open item it replaced.

## Amendment (wave 48): a branch that PRODUCED A COMMIT but was never merged stays OWNED and must be named in the wave's census

`.git/refs/heads/task-w46-g` = `74d682d3f01b17d631bfbb24dd4d03e237430243` ("DEC-020: scan-lock the upload accepted-type/size-cap vocabulary (J8 ENUMERATION)"), committed at 1786838556 — BEFORE the wave-46 merge train ran (`merge task-w46-d` at 1786838581 through `merge task-w46-f` at 1786838713) — and it is NOT an ancestor of `main`. Real, test-only work that the train simply did not pick up. DEC-069's wave-17 rule covers only the zero-commit case ("a branch whose ref equals its base PRODUCED NOTHING and its scope returns to unowned"); its converse has never been written down, and the failure mode is the opposite and worse one: either a later wave re-files scope that already exists on a branch (duplicate work, guaranteed merge conflict), or the work rots invisibly and nobody re-derives it. RULING: a live ref carrying a non-ancestor commit is OWNED — never re-filed as a new task — AND is named in that wave's triage-closure census by branch, sha and one-line scope, so the merge train inherits a standing list of unlanded work instead of the swarm rediscovering it three waves later. Applied this wave: `task-w46-g` is NOT re-filed, and `task-w48-g`'s census must name it.

**Correction (`task-w48-e`, same wave, later runtime):** the paragraph above was written from a `.git` reading taken before `task-w46-g` actually merged. At `task-w48-e`'s own runtime, `git merge-base --is-ancestor 74d682d3f01b17d631bfbb24dd4d03e237430243 main` returns true, and `git log --ancestry-path 74d682d3..main` shows `merge task-w46-g` (`185fa8d7`), landed before the wave-47 merges on `main`'s first-parent line. `task-w46-g` is therefore CLOSED, not stranded, as of `243b3094`. The custody RULE stated above is unaffected and still binds — a live ref carrying a non-ancestor commit stays OWNED, is never re-filed, and must be named in the wave's census — but `task-w46-g` is no longer a live example of it. Per this DEC's own wave-35 amendment (an absence, or here a presence/merge status, is a measurement, not a note to carry), do not carry the STRANDED label for `task-w46-g` forward into wave 49; the next lane that finds a genuinely stranded ref should cite this rule fresh against its own runtime, not the specific instance named above.

## Amendment (wave 49): a row owned by a committed-but-unmerged branch is re-measured, never re-fixed

task-w48-a's first filed failure (`test/decision-path-references.scan.test.ts:122` — this DEC's prose references `migrations/0043_file_version_chain_unique.sql`, absent at main, which tops out at 0042) is already owned: `task-w47-g` (`9a541796`) is a real commit whose tree confirms it contains that exact migration file. Duplicating it in a wave-49 lane would produce two same-named migrations racing through the merge train — strictly worse than the red row. Adding a historical marker to this DEC's sentence to silence the scan would be false, since the plan is live rather than superseded. This generalizes the wave-46 lesson that a commit without a merge is still owned: a wave-49 lane re-measures the row at its own runtime and records the observed status with the owning branch named, and only a wave whose planning boundary shows the owner ABANDONED may re-file the scope. The same rule protects task-w47-a's DEC-023 reminder fix, which is likewise committed and unmerged.

## Amendment (wave 50): the adjudication population is the three areas that have never had an owner — J3 triage, J12 data-out, and the TIER-1 fidelity sub-clause list — one owner each, plus a sole eval-findings rebase owner

DEC-358's wave-45 amendment partitioned SPEC's J-narratives into six disjoint areas with one owner each and got seven real CONFIRMED-DEFECT rows for it. That partition covered J4, J5, J6/J7, J8, J9/J10, J11; wave 49's docs lane takes J1/J2 (forms/CFP and public submit). J3 (SPEC.md:108-112 — volume triage, saved views, any-answer-as-column, bulk status change, manual creation and cloning) and J12 (SPEC.md:177-181 — exports everywhere CSV/JSON, the bearer /api/v1 the SPA itself consumes, Airtable as stage-2) have never been an owned adjudication area in any wave. Separately, eval-findings TIER 1 item 3 carries six design-fidelity sub-clauses marked VERIFIED-OPEN-NOT-RECHECKED across at least eight waves — a standing list whose staleness is the exact decay this DEC's wave-35 and wave-39 amendments outlawed for existence claims and UNFALSIFIABLE labels. RULING: task-w50-f owns J3, task-w50-g owns J12, task-w50-h owns the six TIER-1 sub-clauses; no exclusion clauses, no overlap, and where an area touches ground a wave-45 lane already settled, the claim must be a NEW claim rather than a re-litigation. Evidence standard unchanged and binding: a clause closes on a targeted test the lane actually ran (quoted with counts) or a file:line quote PLUS a named existing test that would fail if the behavior were reverted; a code read alone is labelled UNFALSIFIABLE and kept; OPEN ITEMS equals exactly the number of CONFIRMED-DEFECT rows in that lane's own table. And per this DEC's wave-41 amendment, docs/eval-findings.md has exactly ONE editing owner this wave — task-w50-i — which may re-point OWNER labels (a plan fact) but must never change a discharge status it did not itself verify (a verification fact); it names f/g/h as IN FLIGHT rather than folding verdicts it cannot see.

## Amendment (wave 51): the falsifiability remainder gets three named owners with planner-fixed item lists, and wave-49's rulings are off-limits because a ruling on main is not a landed fix

Two rulings in one. (1) OWNERSHIP OF THE REMAINDER: `docs/eval-findings.md`'s 'STILL UNFALSIFIABLE' batch-A and batch-B remainders have carried UNOWNED since wave 47 ('an owner line names a branch, not a wave'). They now have three branches — task-w51-b (batch-A, four items), task-w51-c (batch-B server, four items), task-w51-e (batch-B SPA, four items) — with the item lists fixed BY ME so no two lanes can contend for one item and none can quietly drop one. A check must EXERCISE the behavior (render the real component, read the real exported CSS string, parse the real package.json); a comment grep or a DEC-literal assertion is not a falsifying check. (2) DO NOT RE-FILE WAVE-49 SCOPE: `.git/refs/heads` at main `87cee8b9` shows task-w48-b/d/e/g, task-w49-a..h and task-w50-a..g all live and unmerged, while wave-49's DEC amendments are already ON main because the scribe lands the planner's decisions at the START of a wave. DEC-505 (PATCH re-validates sibling rules, `?cascade=1` clears them), DEC-268 (a gate that boots a server builds the SPA first; the walkthrough asserts the authenticated /admin), DEC-489 (an unpaged feed reports the ceiling it applied), DEC-037, DEC-728, DEC-146, DEC-974, DEC-663, DEC-990, DEC-939 and DEC-891 are all committed-but-unmerged scope — 'A COMMIT WITHOUT A MERGE IS STILL OWNED'. I confirmed each of the first three against today's file:line (forms.ts:319-359 vs :398, ci.yml:57/:88, index.tsx:512) precisely so I could rule them OWNED rather than re-file them. Trap recorded: two campaigns share one DEC corpus (the earlier swarm reached wave 72), so an `## Amendment (wave N)` for N<=50 is only current-campaign if the file:line it cites still matches the tree exactly.

## Amendment (wave 52): the census names branches, and this wave's do-not-re-file list is the whole of waves 49-51's scope

An owner line names a branch, not a wave (standing). Re-derived at my own runtime rather than inherited: task-w49-b owns the forms PATCH sibling-rule 409 (DEC-505), task-w49-c owns the CI-gate SPA build (DEC-268), task-w49-d owns the unpaged agenda/schedule feed ceiling (DEC-489), task-w49-a owns the clock-dependent spec9 test, task-w49-f/-g own the batch-A/batch-B falsifiability remainders, and task-w51-a/-b/-c (running now) own the cloneSubmission file-answer defect (DEC-275 w51), the api_token mint ceiling (DEC-027 w51) and the twelve-item falsifiability remainder (DEC-358 w51). None of those are re-filed here. I also re-checked, and did not re-file, four areas that reviewers have repeatedly flagged and that I found already closed on main: session revocation on password change (src/server/auth-session.ts:77 issueSessionRevokingAll, scan-locked), the acceptance/onboarding retry-convergence obligation DEC-079's wave-26 amendment created (discharged by test/status-accepted-at-repair.test.ts case 4), the five DESIGN-RULINGS B10 consequence lines (all five present and render-tested), and the reviewer multi-plan landing (ReviewerQueue.tsx:610-638 plus its render tests). Recording the negative result is the point: four probes that found nothing is a measurement, and the next planner should spend its lanes elsewhere.

## Amendment (wave 53): census — the queue drained; wave-52's five lanes are the whole live population

Ref truth at this planner's own runtime, taken from `.git/refs/heads/*` (loose), `.git/packed-refs` and `.git/logs/refs/heads/main` rather than any prior summary: main advanced through `merge task-w48-e/b/g/d`, `task-w49-a..h`, `task-w50-a..i`, `task-w51-a..e`, then fast-forwarded the orchestrator's `ad84367d`, then `02575938` ("mandate: user-filed participant-remove P1 fixed + DEC-817 scan must cover DELETE verbs"), then `0e8787f5` (assembler regeneration). Every scope the wave-51/52 field guide listed as OWNED-BUT-UNMERGED is therefore LANDED and re-verifiable in-tree — I re-opened DEC-505's `forms.ts:361-397`, DEC-489's `public/index.tsx:431-517`, DEC-268's `ci.yml:52,85` + `producer.ts:1076-1089`, and the DEC-275 clone fix's owning branch, and none of them is a live gap. OWNED, do not re-file: the walkthrough day-label instance and class (task-w52-a), `isDayLabelMs` at the form/plan write doors (task-w52-b), the seed's floored day-label mint (task-w52-c), the unauthenticated-write rate-limit enumeration in `test/security-invariants.test.ts` (task-w52-d, still running), the content-review reopen population scan (task-w52-e). Also recorded, so the next planner does not re-spend them: three probes that found NOTHING this wave — reminder chasing already gates on `chaseableContactExists` (`src/server/repo/tasks/crud.ts:124`, composed at reminders.ts:100/202/265/741) so a removed or declined participant is never chased; `src/mail/ics.ts:138-148` already carries stable UID + caller-bumped SEQUENCE + conditional LOCATION; `src/routes/api/exports.ts` already reuses the lists' own parsers and REFUSES rather than silently truncating.

## Amendment (wave 55): a mandate row a landed verification section already closed is STALE, not OPEN — and an owned row names its branch

`docs/eval-findings/07-tier1.md` still carries six fidelity sub-clauses as VERIFIED-OPEN-NOT-RECHECKED (templates-grid overlap, Comms History-tab chrome, Content upload-reject modal, content-detail container, Review results-head, plan-editor footer) that landed section `docs/verification-log/index/0267-2026-08-15-task-w50-h-tier-1-fidelity-recheck-87cee8b9.md` re-measured and closed at boundary `87cee8b9`, and item 1 (compose-flow turn diet) is being worked right now by `task-w54-e` under the user's standing ruling. A backlog that lists closed rows as open burns a lane every wave that reads it. RULING: TIER 1 is re-derived at the re-verifying lane's own runtime — each row becomes CLOSED naming its falsifying check, OPEN with a file:line, or OWNED naming the branch (never a wave), and the two recorded forfeits (CFP-16, ABS-14) are carried verbatim.


## Amendment (wave 57) — a mandate row whose defect is closed is CLOSED WITH A RECEIPT, not carried; four review-lens items and four gate-9 rows are closed as of main 1e423e0c

This planner re-ran every current review-lens finding and every gate-9 SWARM LANE row against main at plan time. All four review-lens findings are already fixed in-tree and were re-filed anyway: evaluations CSV drafts (exports/evaluations.ts:156 now pushes `submittedEvaluationCondition()`), embed fields/accent carry (sessions.tsx:203, speakers.tsx:215/303, agenda.tsx:379, agenda-controls.tsx:56 all call `embedKnobQuery`), the anonymous draft trackIds cap (submit-draft.tsx:83-101), and the .zip accept/hint disagreement (`CFP_FILE_FIELD_KIND` at domain/files.ts:124, consumed at form-render.tsx:124 and :168). Gate-9 rows 2, 7 and part of 3 were adjudicated NOT-A-DEFECT or closed by measurement in wave 56; row 8 (.zip policy) closed in wave 54. Carrying a closed row costs a whole wave the next time someone reads the mandate as a work queue — the same decay this DEC has recorded three times ("an adjudication without an owner decays in one wave", "a commit without a merge is still owned", "an owner line names a branch, not a wave"). RULED: the mandate index re-derives its own remaining set with a file:line receipt per closure, names the wave-56 branch that owns each still-open row, and never restates a discharge status the re-deriving lane did not itself verify.
