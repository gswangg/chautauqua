# DEC-178: Waves 7 and 8 both vanished unexecuted; the DEC-176 battery protocol rebinds verbatim to wave 9 with S = the task-w9-a merge, fresh ports 8831-8833, and the dead w7/w8 branch stubs quarantined

Verified against the reflog, not summaries: after the six wave-6 merges (77b76a9) the only commits on main are 'scribe wave 7' (466f45b) and 'scribe wave 8' (5d3acae); task-w7-a and task-w8-a are both zero-commit branch stubs pointing at main and must never be reused or merged. The DEC-173/174/175 harness closure is grep-confirmed still absent (scripts/walkthrough/public.ts:440 strict extractor, speaker.ts:915 strict block regex, scripts/seed.ts:975 bare mod-3 formula, zero 'DEC-175' matches in scripts/), while all six DEC-167..172 anchors are grep-confirmed present. DEC-176/177 therefore apply unchanged with these substitutions: sole code-bearing lane task-w9-a (scripts/** only, every change tagged with its literal decision id in a comment, validated by a full local six-module walkthrough on port 8831); frozen sha S = the 'merge task-w9-a' commit per DEC-114 first-parent walk; gate sections titled task-w9-*; walkthrough gate port 8832, perf-smoke port 8833 (render-sweep self-boots on a free port); every gate keeps the DEC-139 ancestry check (git merge-base --is-ancestor 2dd2f33 <sha>) and the DEC-177 precondition greps — six w6 anchors ('DEC-167' in src/domain/contacts.ts, ICS_ORGANIZER_EMAIL in src/mail/ics.ts, 'unknown track id' in src/routes/api/forms.ts, 'anonymized === false' in src/server/repo/files.ts, openDate in app/src/pages/review/PlanEditor.tsx, FORM_TASK_FIELD_SPECS in scripts/seed.ts) plus the closure anchors ('DEC-173' in scripts/walkthrough/public.ts and speaker.ts, 'DEC-174' in scripts/seed.ts, 'DEC-175' in scripts/walkthrough/producer.ts, speaker.ts, and review.ts), any miss ending the section 'RESULT: FAIL — precondition' without running the gate. The spec-audit delta base is 64ec7de (task-w5-f's last full PASS audit). Exit condition unchanged per DEC-069/139: all six sections PASS at one S with OPEN ITEMS: 0, after which the next wave returns zero tasks and declares stage-1 complete.
