# DEC-445: The CONTRAST_BLOCKING flip passes to wave 16's build/test lane under DEC-436's rule, unchanged and unweakened

DEC-443 gave the flip to wave 15's build/test lane. That lane (task-w15-e) did its job correctly: it ran the sweep itself, read 41/42, and declined to flip -- a decline on evidence, not a miss. scripts/render-sweep-contrast.ts:25 still reads `export const CONTRAST_BLOCKING = false`. Ownership passes once more, with no relaxation: wave 16's build/test lane flips it only from its OWN run in its OWN branch reading all-PASS, never from this plan, a prior transcript, or an assumption that DEC-444's fix worked. DEC-436's corollary still binds -- the lane that flips a constant owns the test currently asserting its old value (test/render-sweep-contrast.test.ts:97-99 asserts `false`), and must update it in the same commit. The difference from wave 15 is that this wave's lane also owns the pixel fix, so for the first time the flip's precondition is something the lane can actually create rather than merely observe. A FAIL reading still costs the lane nothing but an offender table.
