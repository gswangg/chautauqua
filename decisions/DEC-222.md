# DEC-222: Wave-24 exit battery binds FROZEN sha 0a263d2e6e4dbf438f6ad9e98bffa6af527b965c (merge task-w22-e); six gates task-w24-a..f; one per-lane log file each; isolated worktrees and ports

Per DEC-219 the planner derived the newest code-bearing sha per DEC-114 exactly once at planning time: first-parent chain of main is 617679b (scribe wave 23; diff confined to decisions/, field-guide/, string-appends to src/decisions.ts — non-code-bearing) <- 0a263d2 (merge task-w22-e, code-bearing). Frozen literal: 0a263d2e6e4dbf438f6ad9e98bffa6af527b965c, short 0a263d2. Every gate lane re-derives the sha first and FAIL-stops on mismatch — never rebinds (DEC-208/219). Each lane writes exactly ONE new file, docs/verification-log/task-w24-<letter>-<gate>.md; the shared docs/verification-log.md, docs/eval-findings.md, and decisions/ remain scribe-owned (DEC-207 conflict-marker lesson; w21 per-lane-file pattern). Lanes that boot wrangler dev do so in their own detached worktree at 0a263d2 (w20-a pattern) on a dedicated port (walkthrough 8961, perf 8962; render-sweep self-allocates) so parallel lanes never share .wrangler local state or ports. Stage-1 completion is declared at wave 25 ONLY on 6/6 PASS at the frozen sha with zero new verified defects; any FAIL or new verified defect reopens fixes-first planning.
