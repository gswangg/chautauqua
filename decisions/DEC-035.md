# DEC-035: Ratify bootstrap split: src/server/app.ts + src/server/scheduled.ts

The w3 custodian extracted createBaseApp/guardDevMailbox into src/server/app.ts and the cron handler into src/server/scheduled.ts without a governing decision (review-lens flagged the gap). Ratified as binding: base-app construction (context, sessionLoader, error handler, dev-mailbox guard) lives only in src/server/app.ts, the scheduled entry only in src/server/scheduled.ts, and src/index.ts remains the sole mount site per DEC-012. No second bootstrap path may be created.
