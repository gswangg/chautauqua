# DEC-679: The route manifest is DERIVED from the composed route table, and the /embed closure proof walks every embed page including the detail routes

app/src/routeManifest.ts opens with 'Enumerated from: ...' and is exactly the hand-listed manifest the field guide says desyncs — its own body already records one prior desync (DEC-489/490: two of the five embed twins 'were missing from this manifest'). It has desynced again: w12-c added /embed/:eventSlug/sessions/:sessionId and /embed/:eventSlug/speakers/:contactId (src/routes/public/index.tsx:235, :249) and neither is in the manifest, so gate:render-sweep never visits them and test/audit-claims.test.ts — which checks docs/AUDIT.md only against that manifest — reports green while AUDIT.md's J10 route list is wrong. Separately, test/public-embed-links.test.ts loops SURFACES and renders only /embed/<slug>/<surface>, so the two detail pages DEC-672's own text calls '/embed pages' are proved closed by inspection alone, which DEC-672 explicitly forbade. All three enumerations must end in one derived list: what the app registers is what the sweep visits, what AUDIT.md documents, and what the closure test walks.
