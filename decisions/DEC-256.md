# DEC-256: Wave-2 exit battery: freeze-by-rule at the newest code-bearing commit, eight greppable sections, unanimity predicate (supersedes DEC-255's six-section formulation)

Verified by direct read of /Users/wednesdayniemeyer/Documents/gniemeyer/Projects/chautauqua/.git (not inherited from summaries): main is at e0d3379 ('merge task-w1-a'), and wave-1 lanes task-w1-b..task-w1-f are still unmerged branches created from c663cf2 with live worktrees under chautauqua-wt/. The planner therefore CANNOT name a literal frozen sha the way DEC-232/DEC-250 did, because the sha that matters does not exist yet. So the freeze is defined by RULE, and each battery lane derives it independently:

1. Before freezing, confirm every `refs/heads/task-w1-*` branch in the base repo is an ancestor of main (`git merge-base --is-ancestor`); if any is not, poll every 30s for up to 15 minutes, then proceed and record the unmerged branch names as an OPEN ITEM.
2. S = the newest commit on `git log --first-parent main` whose `git show --stat` touches any path OUTSIDE the allow-list {decisions/**, field-guide/**, docs/verification-log/**, docs/eval-findings.md, pure string-constant appends to src/decisions.ts}.
3. Print `FROZEN SHA: <40-char>` on its own line near the top of the log and head the file `# task-w2-<x> - <scope> @ <short-sha>` (DEC-129 homonym guard: first-campaign task-w2-d/task-w2-e logs already exist).
4. Re-derive S at the END of the run - if it moved, that is DRIFT: record it, set OPEN ITEMS >= 1 and RESULT: FAIL, never silently re-run against a newer tree.

Stage 1 may be declared complete only when ALL EIGHT sections - build+test, six-module J1-J12 walkthrough, perf-smoke at the 2k-row seed, render-sweep including the DEC-253 mobile pass, SPEC section6/section7 + docs/clarifications.md audit, triage-closure of every wave-1 OPEN ITEM, fresh-clone evaluator bootstrap (DEC-257), and full docs/eval-rubric ID coverage - log `OPEN ITEMS: 0` AND the identical FROZEN SHA literal.

Battery lanes are read-only with respect to the product: each commits exactly one new file docs/verification-log/task-w2-<x>-<scope>.md and touches nothing under src/, app/, test/, scripts/, migrations/, wrangler.jsonc, decisions/, field-guide/ or docs/eval-findings.md. A lane that finds a defect LOGS it; fixing it is next wave's work, because any product commit voids the freeze (DEC-069).
