# DEC-666: An internal shorthand is not public prose: a session with no room renders "To be announced" from one exported label, while the JSON/ICS payloads keep room null

src/routes/public/agenda.tsx:40 and :186 print the literal "TBD" -- an organizer-side shorthand DEC-010 introduced for a nullable room column -- verbatim to anonymous visitors, on the most judge-visible public surface. The sibling public renderers already do the honest thing by omitting the room entirely when it is null (detail.tsx:79, cards.tsx:113), so only the agenda, which must render a column header for the roomless lane, needs a word. One exported label keeps the two agenda call sites from drifting from each other. Data stays data: feeds.ts's `.json` items and the `.ics` LOCATION keep null/absent rather than gaining prose a consumer would have to parse back out.
