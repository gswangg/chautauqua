# DEC-666: An internal shorthand is not public prose: a session with no room renders "To be announced" from one exported label, while the JSON/ICS payloads keep room null

src/routes/public/agenda.tsx:40 and :186 print the literal "TBD" -- an organizer-side shorthand DEC-010 introduced for a nullable room column -- verbatim to anonymous visitors, on the most judge-visible public surface. The sibling public renderers already do the honest thing by omitting the room entirely when it is null (detail.tsx:79, cards.tsx:113), so only the agenda, which must render a column header for the roomless lane, needs a word. One exported label keeps the two agenda call sites from drifting from each other. Data stays data: feeds.ts's `.json` items and the `.ics` LOCATION keep null/absent rather than gaining prose a consumer would have to parse back out.

## Amendment (wave 56)

ROOM_TBA_LABEL/publicRoomLabel are the only room-absence vocabulary, and the SPA reaches them through one named crossing.

DEC-666 says an internal shorthand is not public prose: a session with no room renders 'To be announced' from ONE exported label. Verified on main: src/domain/schedule.ts:132 exports ROOM_TBA_LABEL and :138 publicRoomLabel — and four app-side sites re-declare or inline the same literal (app/src/pages/submissions/schedule.ts:19, app/src/pages/content/SessionList.tsx:29, app/src/pages/content/DeliverableDetail.tsx:68, app/src/pages/speakers/SpeakerDetailPage.tsx:72), while src/routes/portal/index.tsx:372 prints a FIFTH, divergent wording — 'Room to be announced' — so the speaker's own placement line says different words than the public schedule for the same fact. Ruling: the pure-core pair is the only implementation; the SPA imports it through a new named crossing app/src/lib/room-label.ts (the DEC-957/DEC-660 idiom app/src/lib/plural.ts already establishes); the portal placement line prints publicRoomLabel(roomName), i.e. 'Tue 12 May, 10:00 · To be announced'. Where an existing test asserts the old portal wording, the test is wrong and changes with the code.
