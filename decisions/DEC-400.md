# DEC-400: The overview endpoint's wire keys are DEC-370's, not the repo's private ones: `triage` is the v2 rows section and the v1 aggregate ships as `triage-counts`; a repo-level contract test pins them to the SPA type

Verified on main at read time: src/server/repo/overview.ts:104 declares `triageQueue` and :616 returns `triage` = the v1 aggregate {pending, accept_queue, decline_queue}, while app/src/pages/overview/types.ts:102/110 (the contract of record per DEC-246) and Overview.tsx:51/238 expect `triage` = {total, oldestSubmittedAt, rows} and `'triage-counts'` = the aggregate. So `payload.triage.rows.length` throws and /admin/overview renders an empty #root for every organizer — the flagship structural change of the redesign (docs/design/README.md: 'Overview stopped being a directory') has never rendered. DEC-370 states the contract verbatim ('PLUS the v1 aggregate keys triage-counts/review/speakers/content/agenda/comms RETAINED'), so the SERVER is wrong and moves; the client does not. Nothing caught it because GET /events/:id/overview is a pass-through handler and test/spa-contract-sweep.test.ts:481-514 mocks the repo, so it asserts its own fixture's keys. The guard therefore has to sit where the payload is actually built: a test that (a) type-asserts the server's OverviewPayload and the client's OverviewPayload are mutually assignable and (b) key-asserts a real getOverviewPayload() result against the client's key list. `triage-counts` stays even though only the type reads it — DEC-370 requires the retained aggregates.

## Amendment (wave 55): TriageRow.format is `string | null` and the overview payload contract asserts deep structural assignability in both directions

app/src/pages/overview/types.ts:31 declares TriageRow.format as `string`, while the server's TriageQueueRow sends `string | null` and the SIBLING UnplacedRow.format at :90 correctly says `string | null` with a DEC-895 note. test/overview-payload-contract.test.ts:16-28 explicitly downgrades itself to a top-level key-set check because of this one mismatch, calling it "out of this task's scope" — so the contract test that exists to catch client/server drift is disarmed by the drift it found. Fixing the type closes the escape hatch: the deep check goes on, and the field's nullability stops being a lie the render sites have to guess around.
