# DEC-874: A reviewer with one plan lands IN it; the queue row leads with its reference and ends with a full-width action

/review currently renders an unscoped "Your queue" hub (ReviewerQueue.tsx:282-296) that stacks a PlanSection per plan. The v5 pack has no frame for that hub — every Review screen in it is plan-scoped, headed "Review · <plan>" — and the mandate names the unscoped hub as a DECIDE item. Decision: zero plans keeps today's empty state; exactly ONE plan replaces the hub with the scoped queue (`<Navigate replace>` so Back does not bounce); two or more render a PLAN LIST (name, scope, count) whose rows link into a scoped queue — never N stacked queues, because a page that is several pages at once has no header that is true. The row takes the frame's anatomy: REF left, SCORED/NOT SCORED right, a meta line under the title, and the action as a full-width button; recused rows render INLINE in the same list, marked, with Undo, rather than in a trailing section that hides the reason the queue is short; the list ends with the footer "Scores stay hidden from other reviewers" beside the shell's existing sign-out. The externally-verified P0 reviewer path (login → review → scorecard) is a hard constraint: it must stay green, and each of the three landing branches gets its own test.

## Amendment (wave 65): the reviewer queue row's action is intrinsic-width and right-aligned, and a recused row's action IS its reason

DEC-874's own text specifies "the action as a full-width button" — written when the ONLY reviewer-queue frames in the pack were 390px phone captures, so the build graded a phone block button as frame-faithful. The REVIEW PACK vendored at wave 62 supplies the first DESKTOP frame for this surface (03--03) and the standing USER filing says the same thing: on desktop the primary is a normal intrinsic-width button right-aligned on the row, vertically centred against the title/meta stack. Verified still live at app/src/pages/review/review.css:1336-1340 (`.chq-review-queue-row-action { display:flex; width:100%; }`) and app/src/pages/review/ReviewerQueue.tsx:171-176 (both scored and unscored rows take `chq-btn-primary`). Ruling, superseding DEC-874's full-width clause for desktop: (a) the row is a two-column grid, body left, action right-aligned at intrinsic width; (b) an unscored row takes the olive primary face, a SCORED row the outlined secondary face — the action names what it offers and a row that already took the action must not shout; (c) on a recused row the RECUSAL REASON is the row action — an outlined, disabled-looking control carrying the reason (default copy "You work with this speaker"), with DEC-271's Undo demoted to a quiet link beneath, because a separate trailing Undo button competing with a bare reason line is two controls for one fact; (d) the state eyebrow reads RECUSED in the same bold caps as SCORED/NOT SCORED; (e) the queue gains the frame's title-row primary `Score the next one` (conditional-and-quiet: absent when nothing is unscored) and a footer "Showing N of M" + `Show all M` cap at 5 rendered rows, counted from the same arrays the list renders. DEC-939's wave-39 scoping rule stays binding: a page-scoped rule may set size/weight/spacing/width on a button-face element and NEVER color or background. The phone full-width treatment is deferred to the mobile round, not carried here.

## Amendment (wave 72): the queue row's eyebrow is ONE pair, a recused row keeps its meta line, and the queue owns its footer

Three gate-4 findings against frame 03--03, all verified on main. (a) REF and STATE are drawn ~8px apart as a single left eyebrow; `.chq-review-queue-row-top` declares `justify-content: space-between` (review.css:1387), so the app spreads them ~500px and they read as two unrelated columns. (b) A recused row drops the "Talk, 30 min · advanced" meta line the frame keeps, because `RecusalItem` (app/src/pages/review/types.ts:211) carries no `format` — while the actionable items do. The fix costs no query: src/routes/review/reviewer.ts already computes `formatBySubmission` over EVERY scoped id (line 140-143) and then throws the recused half away at line 167. A projection must carry its source's vocabulary. (c) The footer is the queue's own row: "Showing 5 of 18  Show all 18" as one LEFT group, and the reviewer reassurance right-aligned on that same row (see DEC-369's amendment — chrome no longer carries it). The footer row renders whenever the queue has rows; the count/Show-all group inside it stays conditional on >5.

## Amendment (wave 29): a CLOSED plan on the reviewer hub trades its unactionable count for "Read your scores"

The v9 DESIGN-RULINGS delivery table rules the reviewer hub as "shown only when they hold more than one; with a single open plan the page is skipped... Closed plans stay listed with a quiet Read your scores." Both halves of the first sentence are built (ReviewerQueue.tsx:460-487). The last clause is not: ReviewerPlanRow (:293-324) renders name + scope + "N left to score" for EVERY plan, and the string "Read your scores" appears nowhere in the app — only in docs/design. A count of work you can no longer do is noise on a closed plan, and the row's link promising a queue that will refuse writes is a dead end. Ruling: closed-ness is read off the SAME queue envelope the row already fetches (no second request, no second predicate — reuse the close-date helper at ReviewerQueue.tsx:29-39 that the scoped header already uses); a closed plan's row drops the left-to-score meta, keeps its scope meta, and its action reads "Read your scores ›" as a quiet tertiary; open rows are untouched. This is the same conditional-and-quiet rule the queue's own title-row primary follows: a control renders only when its action is possible.

## Amendment (findings wave 5): the reviewer plans hub row carries five elements, and its H1 is the work left, not the page's name

docs/design/Chautauqua Review.dc.html :695-733 (390) and :736-760 (1600, "same list at the reading measure") draw the hub as: H1 "17 left to score" + "Across two open plans", then per plan a name, a right-flushed state pill, a meta line "6 assigned · 5 left · closes in 19 days", a 5px progress bar, and ONE action per row (:937-939 gives the three action labels: "Score the next one" / "Start scoring" / "Read your scores"), closing with "With one open plan this page is skipped". main renders name + meta only (ReviewerQueue.tsx:386-392) under an H1 that just repeats the nav label. Every missing element is already on the wire: ReviewerQueueEnvelope carries total, unscoredTotal, open and closeDate (app/src/pages/review/types.ts:242-261), so this is a render decision, not a data one. RULING: the hub sums unscoredTotal across OPEN plans for its H1 (spelled counts via src/domain/count-copy.ts's spellCount/plural only — test/count-grammar.scan.test.ts forbids a second word list), hoists the existing ?perPage=1 fetch to the hub so the sum has one reader, and renders the five row elements from that one envelope. A row whose envelope fails to load still links into its queue — the decoration is optional, the destination is not.

## Amendment (wave 86): the reviewer's 390 surfaces are head band / stacked rows ending in a full-width action / the queue's own footer — and sign-out stays the shell's single control

Chautauqua Review.dc.html:793 (Your plans, →830) and :413 (Reviewer queue, →451) draw one anatomy: a head band (wordmark + identity on one row, then — on the queue — an 11px/800 uppercase eyebrow, a 25px H1 that states the WORK LEFT not the page name, a 13px meta line and a 6px progress hairline), a body of hairline-separated cards whose last element is a full-width action (the wave-65 amendment's intrinsic-width right-aligned desktop action is the desktop reflow; at 390 it spans), and a footer band carrying the hidden-scores line. DEC-874's wave-72 amendment already gives the queue its own footer, so the 390 footer is that same footer docked, not a new region. The frames also draw `Sign out` in that footer, and this exposes a real shell defect: `.chq-user-identity` (which carries Sign out per DEC-369's wave-72 amendment) is hidden at ≤700px, a reviewer has exactly one nav section and therefore no More sheet (App.tsx:269-274), so a reviewer cannot sign out on a phone at all today. RULING: the page dock carries the frame's scores line only; the sign-out control is NOT duplicated into the page — App.tsx's `signOut()` is a shell-private fetch of POST /logout and a second copy is DEC-613's delay-fused vocabulary — the shell restores the phone sign-out path (see the DEC-576 wave-86 amendment) and the frame's footer placement is satisfied by one control, not two. Forecloses: exporting `signOut` from App.tsx for a page to import, which would make a page lane an owner of a shell file.

## Amendment (wave 19): the reviewer's "Your plans" hub row is the frame's four-column desktop grid under a column-header row; the stacked card is the phone reflow only

docs/design/Chautauqua Review.dc.html:743-777 is explicitly labelled "Your plans · /review -- Same list at the reading measure" and lays the hub out at 820 as `grid-template-columns: 1fr 150px 190px auto; gap: 20px` with a real uppercase column-header row (Plan / State / Your progress / blank) closed by a 2px ink rule, rows at `align-items:center; padding:16px 0` over a 1px #E1DDCE hairline, and the action as a right-flushed 13px/700 tertiary link. app/src/pages/review/review.css:1676-1680 instead forces `.chq-reviewer-plan-row { flex-direction: column; align-items: stretch; padding: 15px 0 }` at EVERY width, and app/src/pages/review/ReviewerQueue.tsx:400-421 renders a stacked li (name-line / meta / bar / filled .chq-btn action) with no header row. The build's own comment at ReviewerQueue.tsx:662 cites "frame :925-939" -- which is the renderVals DATA block feeding the 390 card, not the 1600 desktop frame at :743 -- so this row was drawn from the phone artifact. DESIGN-RULINGS.md's governing principle is explicit that the desktop frame is the authority and the phone one derives from it, and B4 restates it for this exact surface. DEC-874's own "full-width action" clause is about the QUEUE row and is untouched.
