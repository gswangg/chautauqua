# DEC-135: Wave-19 five-gate battery at the post-wave-18 sha: lanes task-w19-a..e all-parallel, BEHAVIORAL DEC-130..133 preflight (void-marker imports alone prove nothing), fresh ports 8881/8882/8883, FAIL-and-stop on a missing fix marker

At planning time the wave-18 fix lanes were in flight: branches task-w18-a..d existed at main 7162750 with zero commits, yet the working tree already contained the `void DEC_130;`/`void DEC_131;` marker imports and two new test files BEFORE the fixes themselves. So (1) each wave-19 gate lane preflights all four fixes by BEHAVIOR, never by the void-marker line: DEC-130 = autoSchedule in src/domain/schedule.ts contains no findConflicts call and no `[...placed, candidate]` trial array (incremental room/speaker interval indexes instead); DEC-131 = escapeText in src/mail/ics.ts normalizes /\r\n/ and lone /\r/ to \n before the escape chain; DEC-132 = src/routes/public/submit.tsx skips isVisible()===false fields in the file-validation loop and gates the post-create upload loop on cleaned[field.id]==="pending"; DEC-133 = src/server/repo/submissions/status.ts throws ApiError("invalid", ...) naming unknown/foreign ids after the event-scoped SELECT and before any mutation. A missing marker means that fix lane did not land: the gate appends its section ending `RESULT: FAIL — preflight: DEC-1NN fix not present` and stops; gates are code-frozen per DEC-077 and never implement fixes. (2) Every section cites the DEC-114 first-parent newest code-bearing main sha (expected: the last `merge task-w18-*` commit or later); all 675219f-citing sections are void for exit per DEC-134; the DEC-129 guard stays — a section is exit-valid only if `git merge-base --is-ancestor 675219f <cited-sha>` passes, mechanically excluding first-campaign homonyms. (3) Server-bearing lanes use fresh never-used ports: walkthrough 8881, perf-smoke 8882, triage spot-checks 8883. (4) All five lanes run in parallel — triage-closure is NOT chained behind perf-smoke this time; per DEC-127 clause 4 it independently spot-verifies any sibling scope whose section has not merged, and OPEN ITEMS counts unresolved product defects, not merge-ordering races. (5) Exit remains a planner-only DEC-069 grep at the next invocation; the only files a lane may touch are docs/verification-log.md (append-only per DEC-068) and optionally one new detail file under docs/verification-log/.
