# DEC-105: perf-smoke gains an organizer-agenda timed check plus one-shot untimed export min-line probes (submissions CSV >= 2001 lines, showflow.csv >= 301 lines); budgets and existing checks unchanged

The w7-c FAIL proved gate coverage has a blind spot: neither npm test nor the walkthrough nor DEC-089's check list exercises the organizer agenda or the export surfaces at DEC-088's 300-accepted/2k-submission scale, which is exactly where unbounded fan-outs die on D1. Amend scripts/perf-smoke.ts: (1) add a timed check 'organizer agenda (300 accepted)' = GET /api/v1/events/PERF_EVENT_ID/agenda with organizer cookie headers, appended to the checks array after 'event overview' and held to the existing PERF_P95_BUDGET_MS=150 (it is an interactive screen, so the SPEC latency budget applies); (2) after the DEC-080 cap probe, add two one-shot UNTIMED correctness probes (bulk downloads are not latency-budgeted): GET /api/v1/events/PERF_EVENT_ID/export/submissions?format=csv must return 200 with >= 2001 newline-separated lines (header + 2,000 seeded submissions), and GET /api/v1/events/PERF_EVENT_ID/exports/showflow.csv must return 200 with >= 301 lines (every accepted perf submission is scheduled per DEC-088). Assertions live in a pure assertMinCsvLines(name, body, minLines) helper in scripts/perf-smoke-lib.ts that throws with the check name on failure (fail loudly, DEC-034 lib/test split), unit-tested in test/perf-smoke.test.ts. PERF_P95_BUDGET_MS, iteration counts, and all existing checks are unchanged.
