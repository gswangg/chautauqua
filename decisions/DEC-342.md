# DEC-342: The reviewer queue and the event files library were evaluated live and are DEFERRED with explicit triggers, not silently left

Two further admin reads scale with total rows and I am deliberately not fixing them this wave, so no future planner re-derives them as new. (1) GET /api/v1/review/plans/:id/queue (src/routes/review/reviewer.ts:49) loads every submission in the event via repo/review.ts:323 listPlanFilteredSubmissions and every evaluation in the round, then emits a fake envelope (perPage: items.length). A page slice cannot be applied naively because the ordering is global: buildReviewerQueue prioritises across the WHOLE scoped set, and app/src/pages/review/Scorecard.tsx:79 re-reads the queue to advance to the next unrated submission — slicing at 50 would silently truncate a reviewer's traversal, trading a measured non-problem for a correctness bug. Measured evidence: task-w17-d timed 'plan progress (12 reviewers)', which performs the same whole-set load at 2,000-submission scale, at 18.5ms raw against a 50ms budget. TRIGGER: if task-w18-d/task-w19-c's reviewer-queue check exceeds the DEC-309 budget, the next wave fixes it by (i) replacing the whole-round listEvaluationsForPlan load with a SQL GROUP BY count per submission, (ii) dropping `description` from the queue's load path, and only then (iii) paginating, with Scorecard's traversal changed in the same commit. (2) GET /api/v1/events/:eventId/files (src/routes/files.ts:190 -> repo/files-library.ts:74) is unpaginated, loads every submission in the event before it looks at files at all, and contains a quadratic lead-speaker scan at files-library.ts:131 (participantRows.find inside a loop over participantRows). It is not yet observable on local D1 because the perf seed writes no file rows, so DEC-333's conjunctive test is not met. TRIGGER: whichever lane next grows the perf seed's file rows owns measuring it; the quadratic scan is a defect on sight and must be replaced by a precomputed per-submission lowest-order map when that lane lands. Neither item is a wave-19 task.
