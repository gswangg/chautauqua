# DEC-192: Admin submissions track filter is populated via GET /api/v1/events/:eventId/tracks using the existing apiList<Track> pattern

SubmissionsTable.tsx:35 declares const [tracks] = useState<Track[]>([]) with no setter and no fetch, so the FilterBar track dropdown is permanently empty and J3/CFP-12 track triage is unreachable from the UI even though the list endpoint supports trackId. The fix follows the established precedent in SubmissionDetailPage.tsx:86, FormsPage.tsx:38, and TracksRoomsPanel.tsx:42: apiList<Track>(`/events/${eventId}/tracks`) in a useEffect keyed on eventId, errors surfaced through the page's existing error banner. The endpoint is organizer-only (events.ts:54), which matches the admin SPA context.
