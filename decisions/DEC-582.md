# DEC-582: GET / is an anonymous surface: signed-in visitors are redirected by role, and it is styled from the public CSS family — revising DEC-382 for / only

Today GET / never reads c.var.auth and shows an operator link farm ('Admin', 'Speaker portal', 'Dev mailbox') to everyone including strangers. The v2 handoff makes / the org's event hub — the masthead carries the ORG's name, the product is named exactly once in a footer GitHub attribution, and the only auth affordance is a quiet Sign in. It therefore only ever renders for anonymous visitors: organizer and reviewer sessions redirect to /admin, speaker sessions to /portal, matching the redirects /admin already performs. DEC-382 filed /, /docs/api and /dev/mailbox together as 'chrome, not designed screens' sharing TOOLS_CSS; that verdict is now wrong for / alone, which draws ThemeStyles + the public CSS family + its own HOME_CSS module. /docs/api and /dev/mailbox stay chrome under DEC-382 unchanged. The dev-mailbox link disappears from / rather than being auth-gated: the only role that could see it now redirects away before the page renders.

## Amendment (wave 48)

**The anonymous hub obeys 'chrome is full bleed': the 900px bordered shell is stripped, content sits at the 820 reading measure.** DEC-582 put GET / on the public CSS family. HOME_CSS then wrapped the whole page in `.chq-home-shell { max-width: 900px; border: 1px solid …; border-radius: 6px }` (home.css.ts:18) — a card floating on the paper ground, which docs/design/README.md §Widths forbids in the same sentence it defines the width system: 'Chrome is always full bleed. The header rule, the toolbar rules and section rules run edge to edge; only the content inside them is constrained.' RULING: the shell loses max-width, border, radius and its own background; the header rule, the footer rule and the footer's sunk fill run edge to edge; the CONTENT of header, body and footer sits in an 820 centred measure (the README's named reading measure — chosen over the fleet's derived 732 because a fleet measurement is not authority over the handoff's stated number). The 900px frame width is a drawing width, exactly as the README says of its own 1240 frames.

## Amendment (wave 81): the home hub's measure is read from the vendored Home frame, not measured off a picture of it

Five gates (9, 10, 11, the delta probes and the wave-78 design-agent verdict) argued 820-vs-732 from RENDERED frame pixels, and wave 56 closed it NOT-A-DEFECT by citing docs/design/README.md's §Widths table — which lists Overview, session detail, the CFP form and login, and does NOT list the hub. The vendored frame settles it in bytes: `docs/design/Chautauqua Home.dc.html:38` is `max-width:820px; margin:0 auto; width:100%; padding:36px 44px 40px` — an 820 CONTAINER whose CONTENT box is 820-2x44 = 732 — while `:33` (header `padding:15px 44px`) and `:101` (footer `padding:18px 44px`) are full-width chrome with 44px gutters, i.e. content spanning 1512 at the frame's 1600, not a centred 820. The implementation gives all three the same centred `max(34px, calc((100% - 820px)/2))` (`src/routes/public/home.css.ts:21`), which matches neither the body nor the chrome and whose 34px minimum has no source. Ruling: the hub's layout derives from the vendored Home frame; where the README's table and a surface's own frame disagree about a surface the table does not name, the frame wins; a gate's pixel measurement is evidence about a rendering, never authority over the drawing.

## Amendment: /docs (user-facing) is the second revision of DEC-382; /docs/api and /dev/mailbox stay chrome and are reached as labelled leaving links

DEC-582 revised DEC-382 for `GET /` only, moving one operator surface into the public CSS family and saying so explicitly. The v12 ruling (docs/design/DESIGN-RULINGS.md:300-306) asks for the identical move for a NEW `/docs` site and pointedly declines to restyle `/docs/api`: "that is a second revision of DEC-382 and needs saying explicitly." Ruling, said explicitly: the user-facing `/docs` site is designed content and joins the public CSS family; `/docs/api` (DEC-056) and `/dev/mailbox` remain TOOLS_CSS chrome and are reached from the docs as labelled LEAVING LINKS ("Leaves the docs — an operator surface", ↗ muted), never presented as articles. The seam is real and the design names it rather than hiding it. Measures: prose 680, figures 900 — the one sanctioned measure break in the bundle; on phone figures go edge-to-edge with the caption inset.

## Amendment (wave 11, sha 85d8b9ac): the hub's three states are one render contract, and the fresh state carries the frame's SECOND action

docs/design/Chautauqua Home.dc.html draws the fresh-deploy state's action cluster as a primary Sign in AND a tertiary "API docs ›" link at :284-287 (desktop) and :313-314 (phone); src/routes/root.tsx:349-355 renders only the primary. Every other clause of the frame is built and correct (auth redirect at :419-424, the three-group ordering at home-hub.ts:97-101, the between-cycles archive-leads layout at :330-347), so this is the one drawn control with no reader — the DEC-851 shape applied to a frame instead of a setting. RULING: add the tertiary to the fresh branch pointing at the same /docs/api target the footer already uses (root.tsx:282), and pin all three states as a render CONTRACT (one test naming the exact action set each state may show) rather than three independently-drifting branches. The 820 container and .chq-home-body content box are explicitly NOT in scope — the orchestrator owns the 732 content-box lane.

## Amendment (wave 12): DESIGN-RULINGS.md is an authority with no instrument; it gets a derived ledger with the clarifications ledger's exact shape

`docs/design/DESIGN-RULINGS.md` is part of the v2 handoff and is cited by name in ~12 shipped source files, yet nothing in `test/` reads it — the same hole DEC-518's wave-5 amendment closed for `docs/sessionboard-reference/`, and the same hole `clarifications-ledger.scan.test.ts` closed for `docs/clarifications.md`. Prose authority with no executable instrument decays silently: a ruling can be superseded, half-built, or forgotten and nothing notices. I rule it gets a ledger of the SAME shape as the clarifications one — population derived from the file's own headings at test time, one row per heading, honored rows citing file+literal+testFile that must all resolve — because a shape three vendored sources already share is cheaper to keep honest than a fourth invention. A ruling that genuinely has no implementation gets verdict `gap`, recorded, never omitted.

## Amendment (wave 12, sha a6dba5a6): the ledger lands

`test/design-rulings-ledger.scan.test.ts` derives its population from every `## `/`### ` heading in `docs/design/DESIGN-RULINGS.md` (30 headings) and transcribes one ledger row per heading, exact in both directions. 25 headings resolve `honored` against a live file+literal+testFile citation. Five resolve `gap`, held in a shrink-only `KNOWN_GAPS` ratchet: `governing-principle-applied` and `superseded-frames-formerly-owed` are prose (a cross-surface axiom and a closing status list) with no single code artifact to cite; `a-extensions-capabilities-with-no-frame`, `b-uncovered-surfaces` and `frames-delivered-against-this-brief` are section-divider headings whose content is the subsection headings this same population already derives and ledgers separately — citing one of their children again would not be a distinct verdict. No heading resolved `absent-by-design`: nothing in this file states a deliberate scope reduction the way `docs/clarifications.md`'s "Skip Accelevents" bullet does. Notably, `review-criteria-scale-or-choice` resolves `honored` against `app/src/pages/review/ResultsTable.tsx`'s `row.perDropdown[c.id]` read — the wave-82 finding (DEC-241) that this value was declared with no reader is closed as of this tree.


## Amendment (wave 84, v12 mobile campaign w2): The public hub's three phone states share one head and one footer, and the phone footer drops the desktop tagline

`Chautauqua Home.dc.html` draws the hub at 390 three times — Event hub (:116), Between cycles (:231), Fresh deploy (:302). Across all three the head (`Chautauqua Demo Org` + `Sign in`) and the footer (`Running on Chautauqua` + `API docs`) are identical; only the body changes. The 900 desktop frames of the same three states (:181, :269) carry a longer footer that ends `· open-source speaker and event-content management`, which every 390 frame drops. Ruling: the phone head and footer are declared ONCE for the surface and are not restated per state, so a fourth state cannot arrive with a fourth footer; and the tagline is dropped inside the phone block as a WIDTH decision, never by branching on which state is rendering — a footer that changes with content is a second footer. The state bodies keep the frames' copy verbatim (`No open calls right now` / `Nothing is taking submissions. Past programmes are still up.` / `Nothing here yet` / `No call for papers is open and no programme has been published.`). Per DEC-582's own thesis — GET / is styled from the public CSS family — this lives in `src/routes/public/home.css.ts`, never in TOOLS_CSS, and the hub's 820/732 content box (pinned since w81) is a desktop measure that the phone block must not disturb.
