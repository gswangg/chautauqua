# DEC-760: The Review landing states counts as facts, and the remind link is absent at zero

Verified residue on the landing page. ProgressPanel.tsx:170-177 renders "Remind the 0 not started" disabled when everyone has started — the exact affordance violation DEC-733 closed elsewhere (an action that cannot apply is absent, not greyed). PlanList.tsx:142 summarises only "N plans", where the mock's title row reads "N plans · M with evaluations in", and a plan row (line 193-205) carries name/state/window but not the mock's subtitle (track scope · reviews per talk) or its counts. All three numbers already exist client-side in the progress aggregate the page fetches, so this is reporting facts it already holds rather than a new query; where a plan's progress has not resolved yet the clause is omitted rather than guessed (DEC-377: no invented figures). Every existing capability on the row — Progress, Results, Edit, CSV export, row selection — stays, because chrome fidelity never deletes a capability.

## Amendment (wave 60): the two reminder scopes share one surface; a per-plan rounds counter names its plan

Verified at app/src/pages/review/ProgressPanel.tsx: 'Remind laggards (N)' renders only inside the `!embedded` toolbar (:153-169) and 'Remind the N not started' only inside the `embedded` section head (:177) — so the two scopes the V11 in-place edit to the 03 frames explicitly puts side by side NEVER appear together, and the standalone /plans/:id/progress page an organiser actually opens shows only one of them. DEC-760's hidden-not-disabled ruling is separately verified live and stays intact. Separately, :136 renders `Round {plan.currentRound} of {plan.rounds}` with no plan name, which a judge read as an event-level claim ('Round 1 of 1' while the event had six plans) — the counter is correct but its subject is unstated. RULING: both scopes render on both surfaces; 'Remind laggards (N)' is DISABLED when its population is zero (the population exists, it is simply satisfied) while 'Remind the N not started' stays ABSENT at zero per DEC-760; both report the standard {sent, skipped, remaining} line; and the rounds counter names its plan so it can only be read as the per-plan fact it is.
