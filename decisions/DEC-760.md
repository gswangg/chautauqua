# DEC-760: The Review landing states counts as facts, and the remind link is absent at zero

Verified residue on the landing page. ProgressPanel.tsx:170-177 renders "Remind the 0 not started" disabled when everyone has started — the exact affordance violation DEC-733 closed elsewhere (an action that cannot apply is absent, not greyed). PlanList.tsx:142 summarises only "N plans", where the mock's title row reads "N plans · M with evaluations in", and a plan row (line 193-205) carries name/state/window but not the mock's subtitle (track scope · reviews per talk) or its counts. All three numbers already exist client-side in the progress aggregate the page fetches, so this is reporting facts it already holds rather than a new query; where a plan's progress has not resolved yet the clause is omitted rather than guessed (DEC-377: no invented figures). Every existing capability on the row — Progress, Results, Edit, CSV export, row selection — stays, because chrome fidelity never deletes a capability.
