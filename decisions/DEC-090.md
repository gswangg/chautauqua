# DEC-090: Non-code-bearing extension: docs/eval-findings.md pruning and all docs/verification-log.md appends are bookkeeping; wave-16 GREEN sections are void for exit purposes

DEC-069's sha-scoping plus DEC-086's reopening creates a trap: the triage-closure gate must prune docs/eval-findings.md, but if that commit counted as code-bearing it would invalidate every sibling gate section and the exit predicate could never evaluate true. Extending DEC-076/077's rulings: commits touching only docs/verification-log.md, docs/eval-findings.md, field-guide/index.md, decisions/*.md, and string-constant appends to src/decisions.ts are NOT code-bearing. Conversely, the original run's wave-16 GREEN sections (@ 7ac6aef/5692a6d) are void per DEC-086; only reopened-run sections citing the DEC-086 probes count toward exit.
