# DEC-444: The last WCAG AA offender is remedied by re-pointing the rule to --chq-muted; --chq-disabled's value and the contrast checker are both untouchable

task-w15-e's own render-sweep run read 41/42 and named exactly one offender, unchanged since w13-a: `/admin/submissions/forms`, organizer, `td` text fg rgb(142,138,122) on bg rgb(244,241,232), ratio 3.06 vs the 4.5 normal-text minimum. task-w14-d-contrast-flip-stage1.md:117-133 already located both declaration sites precisely -- `app/src/styles.css:1147-1149` `.chq-forms-field-locked { color: var(--chq-disabled); }` (rendered as a <td> in FormsPage's field table) and `app/src/pages/forms/forms.css:130-132` `.chq-forms-settings-title`, same token, same route, same measurement set. w15-e recorded it as 'carried forward unowned for a future contrast-remediation task', which makes it the one genuine FAIL-unowned item on main and the only thing blocking DEC-426's third mandate from becoming blocking. The remedy is fixed by DEC-430 (remedies change PIXELS, never the instrument) and by w13-a's own precedent (the drag glyph was fixed by re-pointing it to --chq-muted): re-point these two rules to var(--chq-muted) (#565A4B on paper computes ~6.2:1, matching the 6.28 readings every other row in the sweep already shows). Explicitly forbidden: changing --chq-disabled's value in src/views/theme.ts:38 or app/src/styles.css:30 (it is the frozen token for genuinely disabled controls, which WCAG exempts, and redefining it re-pixels surfaces no one measured), and any edit to scripts/render-sweep-contrast.ts's thresholds, selector set, or measurement logic. If fixing this offender unmasks another (exactly what happened in w13-a), the same lane fixes it by the same re-point-to-an-existing-token move; if an offender cannot be fixed that way, the lane stops, names it with selector and declaration site, and does not flip.
