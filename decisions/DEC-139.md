# DEC-139: Stage-1 exit reopened by the 2026-08-10 browser-eval mandate; docs/eval-findings.md governs this campaign; exit requires the full gate battery at a post-fix sha plus findings closure

Main @ 2dd2f33 carries code-bearing changes after 8c7f479 (wrangler.jsonc html_handling, SubmissionsTable forms fix), and docs/eval-findings.md now holds a consolidated browser-eval mandate with P0-P2 defects — so DEC-138's exit is void per DEC-069/DEC-114 sha-scoping and correctness-beats-bookkeeping (DEC-134 precedent). This campaign (branch prefix task-w1-*, colliding with first-campaign names: a cited gate section is valid only if its sha descends from 2dd2f33 via git merge-base --is-ancestor) implements the mandate in its stated priority order (A ratify, B P1, F gates, E seed, C by weight, D optional). Re-declaring stage-1 exit requires: the DEC-069 battery re-run at the newest code-bearing sha, now INCLUDING the DEC-144 render-sweep as a sixth required section, AND eval-findings Sections A/B/E/F fully done with each Section C item either fixed or explicitly waived by planner decision. Workers never edit docs/eval-findings.md, docs/verification-log.md, decisions/, or src/decisions.ts (scribe/planner-owned).

## Amendment (findings wave 6): the mandate file may only carry findings verified against the CURRENT tree and the CURRENT frame pack

docs/eval-findings.md is 1,505 lines of the previous generation's gate-4 through gate-7 fleet verdicts, sbek loss analyses and 'V8/V9 DESIGN INTAKE' sections, all written against chautauqua-research/design-frames-v7/v8/v9 — a pack that is no longer vendored. The frame pack is now docs/design/*.dc.html, whose line references do not resolve against any of those findings (field-guide/index.md, w5 entry, records exactly this). Spot-checking confirms the drift is not theoretical: the file's own 'dates are not reconciled with the seed' family is dead (scripts/seed.ts:261-278 anchors every seeded instant to SEED_NOW under DEC-591), and its 'saved embeds do not exist' / 'per-person reminder scope does not exist' family is dead too (src/db/schema/embed.ts; DEC-694's contactIds on both remind and remind/preview at src/routes/tasks.ts:564-613). Every worker reads this file each wave. Ruling: the mandate carries only items re-verified against main with a file:line the writer checked, tiered T0/T1/T2; everything else MOVES to docs/mandates/ (archived, never deleted) with a one-line disposition. A mandate that outlives its frame pack is a map of a demolished city.
