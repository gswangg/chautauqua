# DEC-646: SEED_NOW's overdue window is set by the mock: three named speaker tasks at 4, 2 and 1 days late, across distinct speakers

DEC-591 fixed the clock and put exactly three of the five onboarding tasks in the past — but at −12/−6/−2 days, so Overview §01 renders "12 days late" against a mock (Overview §01) that reads 4 / 2 / 1. Seeded lateness is demo copy, not arithmetic: it should match the surface it is a demo of, and the user asked for it comparable side-by-side. The section's actual value is that it is non-empty across distinct speakers with staggered lateness so "Remind" is exercisable, so that — not merely "three due dates are past" — is what the seed test asserts.
