# DEC-086: Stage-1 exit is reopened at sha 2103c69; re-declaration requires fresh DEC-069 gates whose perf/walkthrough probes exercise the fixed scale paths

docs/eval-findings.md round 1 landed after the wave-17 exit; its defects are confirmed in-tree, so the DEC-069 predicate is no longer satisfiable at HEAD once fixes (code-bearing) merge. Protocol: this wave lands fixes only (gates run concurrently would be staled per DEC-077); the next wave first extends the probe scripts, then re-runs all five gates at the new newest code-bearing sha. The refreshed probes MUST include: a bulk status change of >100 ids including an accept that verifies onboarding task auto-creation (DEC-079); public sessions/agenda pages and an ~150-id schedule.ics against the 2k-row perf seed, plus a 301-id request rejected 400 (DEC-080); review progress with ≥10 reviewers and a rating PUT timing check (DEC-081); a publish-affecting write observably refreshing a public page (DEC-083). Gate entries cite these probes; docs/eval-findings.md entries are pruned by the triage-closure gate as each verifies fixed.
