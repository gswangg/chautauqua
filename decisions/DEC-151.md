# DEC-151: Public drill-ins are SSR detail routes, not client modals: GET /e/:slug/speakers/:contactId and GET /e/:slug/sessions/:sessionId in public.tsx, gated by the same visibleSubmissionConditions as list surfaces, linked from sessions cards, agenda blocks, speakers directory, and gallery; directory and gallery gain server-side ?q= name search

EMB-05/08/13 need drill-down detail with bio + per-speaker sessions and full session detail. The public surface is server-rendered JSX with no client framework, so separate SSR pages with a Back link (?from=surface) are the smallest correct implementation and keep the no-login, edge-cacheable property. Visibility must reuse visibleSubmissionConditions() verbatim (404 for non-visible ids) so drill-ins can never leak unapproved content — same invariant as the headshot serving route. Name search as a plain GET form (?q=) keeps the pages JS-free and satisfies the EMB-05/EMB-12 search requirement.
