# DEC-333: Scale-shaped admin read defects are STAGE-1 product defects (not stage-2 perf tuning); four injected lens findings are closed by anchor

Read live on main. Two of the injected findings are already fixed and are CLOSED per DEC-328's symbol rule: (a) 'admin agenda and public agenda disagree after a date edit' cites src/server/repo/public.ts:707, a file that is now a barrel — slotWithinEventRange(event) is present in getPublicAgenda at src/server/repo/public/agenda.ts:40 and in getPublicAgendaByIds at :120 (DEC-318); (b) 'bulk onboarding remind is uncapped/un-deduped' — src/server/repo/tasks.ts:680 remindNow already routes through planManualReminders + capReminderGroups (DEC-319), its own doc comment says so. A third, 'every mutation purges the public edge cache', is TRUE at src/server/pubcache.ts:103-108 + src/server/app.ts:33 but was ruled on deliberately by DEC-201 (coarse bump ACCEPTED for stage 1; narrowing is stage-2 production cache validation) — it stays closed and NO wave-18 lane touches pubcache.ts. The fourth class is real and unowned. The boundary rule this decision establishes: a defect is stage-1 when its cost is a function of total event/org row count rather than page size and is observable on wrangler dev's local D1 (SPEC §2.2 'a feature that demos well with 10 rows and dies at 2,000 is not built'; §7 'admin API reads p95 < 50 ms ... a route exceeding this is a bug, not a tuning opportunity'); it is stage-2 only when validating it requires the real edge or a real provider (DEC-201/202). Three sites qualify and are fixed this wave: src/server/repo/overview.ts:199-206 (whole email_log read + Math.max spread at :78), src/server/repo/submissions/list.ts:150-163 (fetches every matching row per page request once a q/trackId filter exceeds ID_CHUNK_SIZE, then sorts/slices in JS), src/server/repo/contacts/crud.ts:148 (whole org contact directory materialized per page request). DEC-104's exemption of submissions/list.ts:166 was about the D1 bound-parameter ceiling only and does not bless JS pagination. Small pure helpers (LIKE escaping) may be duplicated per lane this wave to keep lanes independent; deduping is a later custodian's job, not a defect.
