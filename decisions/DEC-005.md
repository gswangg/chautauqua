# DEC-005: Route map and admin nav (guessable, two clicks to anything)

Worker routes: /admin and /admin/* serve the SPA (public/admin/index.html fallback); /api/v1/* JSON API (bearer token or session cookie); /submit/:eventSlug public CFP (SSR, no login); /portal/* speaker portal as Hono JSX SSR with light progressive enhancement (speakers do simple things; SPA weight not justified); public surfaces SSR at /e/:eventSlug/sessions, /speakers, /agenda, /schedule (personal itinerary + .ics), /gallery; embeds at /embed/:eventSlug/:surface (same five surfaces, chromeless); /login, /logout shared; /claim/:token post-submission password creation; /files/:fileId authenticated file serving through the Worker (stage 1 — no presigned URLs); /dev/mailbox dev-only email sink UI (mounted only when env DEV_MODE="1", which wrangler.jsonc sets as a plain var — not a secret). Admin SPA top nav sections, in order: Overview, Submissions (incl. Forms tab), Review, Speakers (incl. onboarding grid), Content, Agenda, Comms, Contacts, Settings.
