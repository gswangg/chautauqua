# DEC-361: Every wave-23 gate verifies the ten wave-21/wave-22 lanes are ancestors of its FROZEN SHA before the expensive run; drift is a logged POST-S DELTA, never a STOP

I watched main move mid-plan: at first read src/routes/files.ts had no total-byte guard, and minutes later ARCHIVE_MAX_TOTAL_BYTES was there (.git/logs/HEAD:185 "merge task-w22-a"), while task-w22-c/d/e were still unmerged branch refs. task-w21-a's and task-w21-c's logs record the same phenomenon twice, including worktrees vanishing mid-run. So each wave-23 lane must, before spending an npm ci + migrate + seed + boot cycle: record its FROZEN SHA, confirm `git log --oneline -60` shows a merge commit for each of task-w21-a..e and task-w22-a..e, and corroborate the wave-22 five by code fact (ARCHIVE_MAX_TOTAL_BYTES in src/routes/files.ts; trackExistsInEvent in src/routes/review/plans.ts; DEC-355 in src/server/repo/submissions/status.ts; DEC-356 in src/server/repo/contacts/import.ts; DEC-357 in the former src/routes/api/contacts.ts (now split across src/routes/api/contacts/)). If any is absent, re-check every 60s for up to 15 minutes, then `git worktree prune` + re-add at main's new tip and restart from a clean state. If a lane is still absent after that wait, run anyway and name it in POST-S DELTA together with which of the lane's assertions it would have affected. Per DEC-280 a non-empty delta is logged, never a STOP. Wave-23 port table (one per lane, no sharing): b=8851, c=8852, d=8853 (manual spot-checks only — gate:render-sweep binds its own findFreePort and accepts no external URL, per task-w13-d/task-w15-e), f=8855; a and e need no server. PUBLIC_BASE_URL in .dev.vars must match the lane's port (DEC-296).
