# DEC-270: Wave 6 is the exit battery and nothing else; the PLANNER names the literal frozen sha and lanes never derive one (supersedes DEC-256 steps 1-2 and DEC-262's quiescence procedure)

DEC-256 asked each lane to derive S by rule; DEC-262 tried to save that with a quiescence poll. Both failed in practice: wave-2's eight sections produced e330aef, 1e08bc8, e002bc9, e4e7b03 and 2682c43 (the last from a prior campaign, see DEC-269), so the unanimity predicate 'all eight print the identical FROZEN SHA literal' could never be satisfied no matter how green the substance was -- and the substance WAS green (b/c/d PASS with OPEN ITEMS: 0). Replace derivation with declaration: the wave-6 planner reads refs/heads/main after wave 5 merges and writes the 40-char sha into every battery task description, so uniformity holds by construction. Each lane must (1) `git worktree add --detach <scratch> <S>` and verify only inside it, never in a worktree tracking main; (2) confirm `git merge-base --is-ancestor <S> main`; (3) check that every commit on main after S touches ONLY {decisions/**, field-guide/**, docs/verification-log/**, docs/eval-findings.md, src/decisions.ts} -- if any post-S commit touches a product path, STOP and report it as an OPEN ITEM rather than re-deriving or re-running; (4) print `FROZEN SHA: <the given 40-char literal>` verbatim. Wave 6 emits zero product tasks, so by DEC-262's own rule it is the wave the battery belongs in. Stage 1 is declared only when all eight sections log OPEN ITEMS: 0 at that one literal sha.
