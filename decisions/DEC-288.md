# DEC-288: Wave 9 is a product wave, not the DEC-287 exit grep: the eight -c2- logs do not exist yet, so the exit predicate moves to the first planner invoked after they all land, at a NEW frozen sha that includes wave 9

Verified against .git, not against prose. refs/heads/main is 5ccd4d63ea2648a6cc91ad18f301a77976514efb; the only wave-8 merge in the reflog is `merge task-w8-a` (1786468439). docs/verification-log/ contains exactly ONE campaign-2 wave-8 log (task-w8-a-c2-build-test.md). Branch refs show task-w8-b @ ac285ab, task-w8-c @ da19477, task-w8-d @ 1f68850 committed but UNMERGED, task-w8-e/f/g created from main with ZERO commits, and task-w8-h not created at all. DEC-287's predicate ('all eight files exist on main, each containing FROZEN SHA 80b811d..., OPEN ITEMS: 0, RESULT: PASS') is therefore unevaluable at wave-9 planning time; running it now would either fabricate a verdict or stall a wave. Two things follow. (1) The grep is deferred verbatim to the first planner invoked after all eight -c2- logs are on main; nothing about DEC-287's predicate text changes, only when it is run. (2) Wave 9 must not sit idle, because section (h)'s rubric sweep will re-report the three rows the wave-4 sweep recorded as OPEN (docs/verification-log/task-w4-e-c2-rubric-coverage.md lines 164/165/167: SPK-03, SPK-15, EMB-15) and DEC-287's 'OPEN ITEMS: 0' clause makes them blocking. Those three are missing FEATURES, not failed verifications: no battery section can close them, and they cost the same to build now as later. Wave 9 builds them. Consequence, recorded so it is not re-litigated: wave-9 commits land after the wave-8 sections record their RECHECK SHAs, so the stage-1 declaration sha is NOT the wave-8 greatest RECHECK SHA; the battery re-runs at a new planner-declared frozen sha that contains wave 9, and DEC-287's 're-run ONLY the failing sections' narrowing does not apply to that re-run (product code changed under all eight). ABS-12 (recusal, landed w5 per DEC-271) and ABS-14 (waived by DEC-272) stay closed; EMB-03 stays NOT-open — submission has no `format` column (src/db/schema.ts:169-200), session format is a form_field answer, and the yaml's own bar is 'at minimum Track', which src/routes/public/sessions.tsx:33 meets. Do not invent a format column to chase it.
