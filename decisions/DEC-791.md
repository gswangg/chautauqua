# DEC-791: The agenda's chrome speaks the reader's language, and the builder links to where rooms are made

VERIFIED app/src/pages/Agenda.tsx:229 renders the day pill as the bare `{day}` string — the raw ISO "2027-05-12" the AIA probe reported, while the public surface has formatted its days since DEC-768. VERIFIED :205 prints "<n> conflicts" with no singular form. AIA-02 additionally names a missing "+ Add room / track" path from the builder into Settings, and the auto-schedule's own no_rooms_configured reason proves the product already knows when that path is the user's next move. Decision: the day label is formatted by the ONE app-side date home (app/src/lib/dates.ts — enforced by date-helpers-single-home.test.ts; add the formatter there, never inline in the page), the summary counters carry singular/plural, and the toolbar carries a plain link to /settings#chq-settings-section-tracks. Chrome fidelity here deletes no capability: the pill's value is unchanged, only its rendering.
