# DEC-558: DEC-534's total-order rule extends to every LIMITed admin worklist and to the JS 'best row' reducers beside them

DEC-534 established that a LIMIT obliges an ORDER BY ending in a unique column, and it was swept through the public lists (repo/public/speakers.ts:84 tiebreaks on contact.id) and the CRM history panel (repo/contacts/history.ts:49,71). The admin worklists the rule exists for were never swept. src/server/repo/overview.ts — the first screen a producer opens, whose whole justification is SPEC §2.3 "nothing falls through the cracks" — takes five capped or arbitrary samples with no unique tiebreak: overdue tasks ordered by task.dueDate alone (:316) where dueDate is a DAY LABEL so ties are the norm, not the exception; the triage queue by submission.createdAt alone (:348), which the seed and any bulk import share to the millisecond; content approval by submission.updatedAt desc alone (:402); the unplaced-sessions slice taken from acceptedRows (:433) which has no ORDER BY at all; and two JS reducers that pick a 'best' row with a strict `>` / `<` comparison and therefore keep whichever tied row the database happened to return first — latestFileBySubmission (:420) and fetchLeadSpeakerNames (:638), the latter deciding which human's name is printed next to a session. src/server/repo/tasks/grid.ts:97-106 selects the J6 grid's task COLUMNS with no order at all, and repo/contacts/stats.ts:45-51 takes the CRM dashboard's top-5 companies with `order by count(*) desc` and no tiebreak. The result is a dashboard whose rows, and a grid whose columns, can differ between two refreshes of unchanged data — which is precisely the crack the screen exists to close. Decision: (a) every capped read in these three modules gets an ORDER BY that totally orders its row: overdue tasks (dueDate, taskAssignment.id), triage (createdAt, submission.id), content approval (updatedAt desc, submission.id), acceptedRows (submission.seq — unique per event), topCompanies (count desc, company asc — the group key). (b) the J6 grid's task columns get a legible AND total order: undated last, then due date ascending, then title, then id. (c) the two 'best row' reducers become named pure functions with an explicit id tiebreak and are unit-tested against a shuffled input, per the standing rule that a JS twin of a SQL ordering must be proven. (d) the regression bar is a repeat: the same input rows in reversed order must produce byte-identical output.

## Amendment (wave 73): a `.limit(1)` with neither a primary-key predicate nor an ORDER BY is a scanned, ratcheted population — SQLite must never be the one choosing the row

DEC-558 already extends DEC-534's total-order rule to every LIMITed worklist and every JS best-row reducer. Two authz/identity bugs in three waves were the same narrower shape and were each found by hand: DEC-248 wave-70 (getTaskFileScope's or()+limit(1) let SQLite decide which speaker may download a task file) and DEC-456 wave-71 (findAccountUserId picked arbitrarily between a contact-linked and an email-matching user, deciding whether a submitter's email carried /portal or a fresh claim link). src/ carries 177 `.limit(1)` chains across 67 files; the overwhelming majority are keyed on a primary key and are unambiguous by construction. The interesting minority is mechanically identifiable: a chain containing `.limit(1)` with no `.orderBy(`, no `eq(schema.X.id, …)` term in its WHERE, and no DEC-NNN citation in the six lines above it. RULING: that predicate becomes a scan with a ratcheted maximum seeded on the implementing branch, printing file:line for every flagged chain. This wave deliberately does NOT re-adjudicate the flagged sites — naming the population is the deliverable, and a site's resolution (add an ORDER BY that totally orders the candidate set, or write down why one row is unambiguous) is a per-site ruling a later wave makes with the list in hand.

## Amendment (wave 75): the 30-site .limit(1) population is adjudicated, not just named — order it, key it, or cite the uniqueIndex; the ratchet only falls

`test/limit-one-total-order.scan.test.ts` (landed w73-e) says of itself: "This scanner does NOT fix any of the flagged sites -- each one's resolution ... is a per-site ruling a later wave makes with this list in hand. This wave only NAMES the population and ratchets a ceiling." That is an open lane with a number on it (`MAX_UNORDERED_LIMIT_ONE = 30`), and the class has already produced two real defects — DEC-248 w70 let SQLite decide which speaker could download a task file, DEC-456 w71 let it decide whether a submitter's email carried `/portal` or a fresh claim link. I am ruling the resolution so two lanes cannot answer it differently: (1) if the `.where(...)` is already at most one row BY A DATABASE UNIQUE INDEX, keep `.limit(1)` and add a one-line comment naming that index and citing DEC-558 — the scan's own citation exemption, and the honest answer for e.g. `portal_settings_event_id_idx`; (2) otherwise add `.orderBy(...)` ending in `asc(schema.<table>.id)` as the final total-order tiebreak, so the chain is deterministic even when the leading column ties; (3) where the RIGHT row is semantically decided (newest, lowest position, accepted-before-invited), order by that column FIRST and then `asc(id)`, and say which fact decided it in the comment; (4) never resolve by widening the query to N rows and picking in JS — that trades an unspecified row order for an unbounded read (DEC-104). A site whose ambiguity turns out to be a real authz or identity decision is a DEFECT, not an annotation: fix the behaviour and say so. `MAX_UNORDERED_LIMIT_ONE` may only be lowered to a count the lowering lane itself measured; it is never raised.

## Amendment (wave 79): a cited index is machine-checked, and a bare DEC number no longer exempts an unordered .limit(1)

`test/limit-one-total-order.scan.test.ts:211` waives any unordered, non-key `.limit(1)` when ANY `DEC-\d{3}` appears in the six lines above it — a near-universal escape hatch in a tree where every comment cites a DEC. Underneath it, ~45 comments justify their `.limit(1)` by NAMING an index (`user_email_idx`, `form_event_id_title_idx`, `portal_settings_event_id_idx`, `pipeline_entry_org_id_contact_id_idx`, `schedule_slot_submission_id_idx`, `api_token_token_hash_idx`, …) and claiming it is unique. Nothing checks that the named index exists in `src/db/schema/**`, or that it is `uniqueIndex(` rather than plain `index(` — and the difference is load-bearing: `contact_email_idx` (org.ts:85) is NOT unique, which is exactly why `findContactByEmail` needed its wave-75 ordering. The existing `test/unique-index-guard-coverage.scan.test.ts` covers only the WRITE side (index → guarded write). Ruling: the READ side gets a symmetric, allowlist-free tripwire (`test/limit-one-index-citation.scan.test.ts`, task-w79-b) — every `*_idx` token appearing in a comment anywhere under `src/` (excluding `src/db/schema/**` and `*.test.ts(x)`) must resolve to a declared index, and must resolve to a `uniqueIndex(` whenever the surrounding comment asserts uniqueness; and `limit-one-total-order.scan.test.ts`'s `hasDecCitation` now only exempts a `.limit(1)` chain when its DEC citation ALSO names a declared uniqueIndex (by literal name, not restricted to the `*_idx` suffix — `file_previous_file_id_unique` is a real uniqueIndex with no such suffix) or an explicit `.orderBy(`/`ORDER BY`. A DEC number is a reference, not an argument.

Measured effect (task-w79-b, wave 79): narrowing the predicate raised the flagged count from 17 to 22 — 8 sites (`contacts/merge.ts` ×4, `files-authz.ts` ×3, `auth-reset.tsx` ×1, `files-versions-delete.ts` ×1, minus one resolved for free by the non-`_idx`-suffix uniqueIndex match) had only ever been waived by the bare-DEC-number loophole, not by a real uniqueIndex or ordering. Five were resolved honestly this wave: `auth-reset.tsx` and one `contacts/merge.ts` site got the `uniqueIndex` name they already argued for but never wrote down (`user_email_idx`, `pipeline_entry_org_id_contact_id_idx`); three `contacts/merge.ts` EXISTS-only checks got an `.orderBy(schema.user.id)` total-order tiebreak (harmless — their callers only ever read `.length > 0`, never row identity). That brought the count back to 17, matching (not lowering) the existing ceiling; the remaining 17 are the same sites wave 75 explicitly left to sibling lanes (`files-authz.ts`'s "1:1 in practice" plain-index note, `review/**`, `portal/**`, and the several genuinely-undecided lookups in `forms.ts`/`submit.ts`/`submissions/create.ts`), now re-verified honest under the narrowed rule rather than merely uncounted by it.


## Amendment (wave 5, sha ee8ceffa): the unordered .limit(1) ceiling is SPENT, not carried — the sites its comment leaves to branches from a dead numbering are resolved and the ceiling drops to the measured truth

`test/limit-one-total-order.scan.test.ts:136` holds `MAX_UNORDERED_LIMIT_ONE = 17`, and the comment above it (`:108-135`) says in the file's own words that the remaining 17 'are deliberately left to sibling lanes/files this wave did not own', naming `task-w74-a/-b/-c` and `task-w75-b`. Those branches are from the PRE-RESET numbering; the harness merge queue was reset with the counter and DEC-358's wave-3 amendment already ruled that a branch from a previous harness run is not in flight. So the exemption's stated reason expired two numberings ago, and this is the third consecutive appearance of the shape the field guide named at wave 79: a ratchet whose closing sentence is a work item that successive waves read as prose. The remaining sites sit in `src/routes/auth-login.tsx`, `src/server/middleware.ts`, `src/server/repo/contacts/**`, `src/server/repo/files*.ts`, `src/server/repo/portal/**` and `src/server/repo/review/**` — two of those are the authentication and request-authorization paths, where an arbitrary row pick over a non-key candidate set is the worst place in the product to leave nondeterminism. Ruling: each surviving site is resolved the honest way wave 79 established — add `.orderBy(asc(schema.<table>.id))` where the candidate set is genuinely existence-only, key the predicate where a key exists, or name a real `uniqueIndex(...)` declared in `src/db/schema/**` in the citation window — and the ceiling is lowered to whatever the scan then measures. The ceiling is never raised and the branch-naming comment is deleted with the sites it excused.

**Resolved (task w5-h):** re-running the scan against the tree found the actual offender list had already drifted from the one the stale comment named — none of `auth-login.tsx`, `middleware.ts`, or `contacts/**` were still flagged (their `.limit(1)` chains were already keyed, ordered, or honestly cited). The live 17 were in `files-authz.ts`, `files-versions-delete.ts`, `forms.ts`, `portal/data.ts`, `profile.ts`, `public/event.ts`, `review/evaluations.ts`, `review/plans.ts`, `review/recusal.ts`, `review/submissions.ts`, `submissions/create.ts`, and `submit.ts`. Each was resolved: five got a real declared `uniqueIndex` name in the citation window whose columns exactly cover the predicate (`evaluation_plan_submission_reviewer_round_idx`, `review_recusal_plan_submission_user_idx`, `portal_settings_event_id_idx`, `submission_event_id_seq_idx`, `file_previous_file_id_unique`); the rest got an `.orderBy(...)` total-order tiebreak, including `findOrCreateContact` (submissions/create.ts) mirroring `findContactByEmail`'s existing createdAt-asc/id-asc order over the deliberately-plain `contact_email_idx`. Three explanatory comments (`files-authz.ts` ×2, `contacts/merge.ts` ×1) were also reworded — they *mentioned* `.limit(1)` in prose with a leading dot, which the scanner's naive substring match treated as a second, phantom call site with nonsensical statement bounds; dropping the leading dot removes the false positive without touching the scan's matching rule. The scan now measures 0 unordered chains; `MAX_UNORDERED_LIMIT_ONE` is lowered to 0 and the branch-naming paragraph at the old `:108-135` is replaced with a statement of the current (empty) survivor set.
