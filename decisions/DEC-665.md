# DEC-665: A surface never asserts a fact it has not yet measured, and a worklist opens on a view that can hold work

app/src/pages/contacts/PipelineBoard.tsx renders "{total} people" (:90) and every per-stage count (:104-106) from state that is still zero while :97 simultaneously renders "Loading..." -- the board asserts "0 people" before it has asked. app/src/pages/content/ContentApp.tsx:30 hard-defaults the worklist to tab 'changes_requested', a tab that is empty in the common case, so the judge's first sight of Content is a page reading "0 submissions" while the event has dozens. The default becomes 'all' -- the one tab that cannot be empty when the event has content -- with the tab row visible so needs-decision stays one click away. Counts and empty states are withheld until the request that produces them resolves.
