# DEC-684: An irreversible merge is a PAGE at its own URL, never a modal; and a list's search box lives in the toolbar row under the title

Mandate item 24 (user QA, explicitly DOWNGRADING the fidelity pass's MINOR verdict) and item 22. Verified: app/src/pages/contacts/DuplicatesView.tsx still presents merge as a modal over the list (mergeGroup state + dialog), while the mock designs a full-screen surface for it — appropriate to an action that destroys records and cannot be undone, and the only presentation where the KEEP/DISCARD comparison has room to be read before it is committed. A modal also cannot be linked, reloaded, or returned to after an interruption, which for a destructive review step is a correctness property, not a nicety. The merge target set is already set-based (DEC-629 {keepId, mergeIds}), so the URL can carry it honestly. Second, app/src/pages/contacts/ContactsApp.tsx renders the title row and the tab strip but the directory's search input is buried inside ContactsTable below the KPI strip; every other list page in the product puts search first in the toolbar between the rules. One list-page toolbar pattern, or the pattern is not a pattern.
