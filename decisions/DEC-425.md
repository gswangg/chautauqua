# DEC-425: DEC-417's caps reach the last three uncapped free-text writes: reviewer scorecard text + comment, API-token name, upload filename

DEC-417 scoped itself to "every admin JSON mutation route" and DEC-422 added the two public/speaker SSR paths. Three writes are outside both and take unbounded client free text straight into D1, each the same SQLITE_TOOBIG-500 class the reviewers found on PATCH /api/v1/submissions/:id. (1) src/routes/review/reviewer.ts:206 writes `comment: typeof body.comment === "string" ? body.comment : null` into evaluation with no length check, on a reviewer-authenticated route at SPEC's 10-50-reviewer scale. (2) src/domain/evaluation.ts's validateEvaluationScores checks a 'text'-kind criterion only for required/non-blank (evaluation.ts:208-215) -- a 10MB criterion answer serialises into scores_json unchecked. (3) src/routes/api/tokens.ts:61-73 requires `name` to be a non-empty string and caps nothing. (4) src/domain/files.ts's validateUpload checks kind, extension and byte size but never filename length, and src/routes/files.ts:135 writes the attacker-controlled multipart file.name into file.filename. Rule: the caps are the ones already exported from src/forms/validate.ts -- MAX_LONG_TEXT_LENGTH for the scorecard comment and text-criterion values, MAX_NAME_LENGTH for the token name and the filename -- so no lane invents a fourth vocabulary; the length check for a criterion value lives in pure-core validateEvaluationScores (not the route) so the route, the SPA and the unit tests share one rule; and oversized input is a 400 naming the field in the existing envelope, never a 500.

## Amendment (wave 24)

A stored filename leaves the app through ONE HTTP door — contentDispositionAttachment in src/domain/files.ts, RFC 6266/5987, ASCII-only output. DEC-425 capped the upload filename but left its egress ungoverned. `filename: file.name` is stored raw from multipart (src/routes/files.ts:200), and FOUR independent hand-copies of `.replace(/[\r\n"]/g, "")` then interpolate it into a Content-Disposition header (src/routes/files.ts:640, src/routes/portal/tasks.tsx:673 and :713, src/routes/portal/tasks/resources.tsx:53), with a fifth private copy in src/mail/ics.ts:162. Three defects follow. (1) None strips backslash — src/mail/email-binding.ts's stripQuoteChars (wave 23) already ruled that a value riding inside a double-quoted header parameter must lose BOTH `"` and `\`; the HTTP door still gives the other answer, so one repo holds two answers to one class. (2) None strips C0 controls other than CR/LF, while every other serializer boundary in this repo now does (sanitizeIcsText, escapeXml, headerValue). (3) None emits `filename*` — a header value is an ASCII byte string, so a speaker uploading `スライド.pdf` produces a header carrying code points above 0xFF: RFC 6266 requires the `filename*=UTF-8''…` form and the plain parameter to be an ASCII fallback. Ruling: one exported owner returning the complete header VALUE, pure-ASCII by construction (it asserts that, loudly), used by every HTTP Content-Disposition in src/**; src/mail/email-binding.ts's MIME body-part header is the one ledgered exemption (a message-internal header under RFC 2231, already served by sanitizeMimeFilename) and must name that reason in the scan.

## Amendment (wave 64): a download is a Content-Type AND a filename — the pairing scan is the dual the single-source scan cannot see

test/content-disposition-single-source.scan.test.ts enumerates occurrences of the string "Content-Disposition" under src/**, so its population is precisely the set of responses that already set one; a download that never mentions the header is invisible to it. Verified: of the eight download responses on main, seven pair a downloadable Content-Type with contentDispositionAttachment(...) (exports.ts:66/67 and :198/199, files.ts:456/457, ics.ts:174/175, public/index.tsx:389/390 and :415/416), and exactly one does not — src/routes/review/plans-progress.ts:188 returns the DEC-018 plan-results CSV with `{ "Content-Type": "text/csv; charset=utf-8" }` and no disposition, so the J4 results export has no filename for any consumer that is not the SPA's `download`-attributed anchor (curl, the /docs/api manifest's readers, an embedded viewer). Rule: every response in src/** whose Content-Type is text/csv, text/calendar or application/zip must set Content-Disposition through contentDispositionAttachment, locked by a scan whose population is the CONTENT-TYPE, not the header; ledgered exemptions are src/mail/email-binding.ts (a MIME body-part header, already exempt in the sibling scan) and src/domain/files.ts (the content-type vocabulary map, not a response).
