# DEC-443: CONTRAST_BLOCKING's flip passes to wave 15's build/test lane under DEC-436's unchanged rule

DEC-436 gave the flip to wave 14 and bound it to the flipping lane's OWN all-PASS reading. At wave-15 plan time scripts/render-sweep-contrast.ts:25 still reads `export const CONTRAST_BLOCKING = false` (wave 14's e/f/g branches had not merged), so the flip either did not fire or is still in flight. Rather than let a flip that nobody owns evaporate, ownership passes to this wave's build/test lane under exactly the same rule, with no weakening: it flips only from its own contrast run in its own branch reading all-PASS, never from a prior transcript, and if the flip has already landed by then it is a no-op. DEC-436's corollary still binds -- the lane that flips a constant owns the test asserting its old value. A FAIL reading costs that lane nothing but an offender table.
