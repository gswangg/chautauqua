# DEC-652: Overview §04 carries the CONCRETE next action the server computed — "Place at 11:30" — and clicking it places; a suggestion the server cannot compute is absent, never invented

Overview.tsx:419 renders `Place it` as a Link to /agenda for every unplaced row and leaves the conflict row's action cell literally empty (`<div />` at :405), while the mock (Overview.dc.html:150-161) shows `Place at 11:30` per unplaced session and `Move DFC-047 to 11:30 · Next free slot in Room 2A` per clash. A worklist that says "go look somewhere else" is a report. The suggested slot comes from the SAME greedy candidate enumeration autoSchedule already uses (one placement truth, no second placer), is returned by the overview repo alongside the row, and the button PUTs /api/v1/submissions/:id/slot optimistically with loud rollback. When no free slot exists the row falls back to the existing link — the UI never fabricates a time it did not compute.
