# DEC-113: DEC-112 probes land entirely in scripts/walkthrough/speaker.ts: invite-visibility via the existing A/B fixtures plus a new pending C, asserted on chq-session cards and the speakers-surface block; Flight task gets the full backing-form fill, Hotel gets a pre-attach GET-200 only; probes are expected-GREEN; task-w11-a verifies-or-implements with self-test on port 8827

DEC-112 said "public module asserts" but walkthrough modules are separate processes with no shared state, and accept/decline requires the invitee's portal session — only speaker1 has one (seeded email+password login, speaker.ts:424). The DEC-070 invite fixtures A(accepts)/B(declines) already live in speaker.ts:589-690, so the probe adds a third throwaway submission C (invited, never responds), organizer flips all three to status accepted THEN contentStatus approved (approval purges pubcache, mirroring speaker.ts:861-890), and asserts unauthenticated /e/<slug>/sessions: each card div id="chq-session-<subId>" for A/B/C is present (session visible), speaker1's display name present in A's card and ABSENT from B's and C's; on /e/<slug>/speakers, speaker1's block lists A's title and NOT B's/C's (B/C titles DO appear under their owner contacts, so page-global absence is wrong — the assertion must be block-scoped). For DEC-111: completing Hotel via its backing form would kill the existing CFP-attach+fill steps' "Fill out form" link (DEC-112 forbids removing/weakening them), so the Hotel probe is a GET /portal/tasks/<aid>/form 200 inserted BEFORE the attach step (proving formId set at creation), and the Flight task — untouched by existing steps — carries the full probe: GET 200, assert "Do you need flight reimbursement?", POST field__<dropdownId>=Yes parsed from the rendered HTML, 302, assignment complete. The stale comment block at speaker.ts:469-478 is rewritten for DEC-111. Verify-first per DEC-103: the original task-w10-e branch may still re-merge; equivalent probes already on main make the lane a report-only no-op, and the merge agent resolves overlap toward either complete implementation.
