# DEC-856: A preflight names EVERY merge field each recipient is missing, in one message shape, and the banner announces itself

VERIFIED: renderTemplate (src/mail/render.ts:78-85) throws MergeFieldError on the FIRST unresolved placeholder, and preflightRender (src/domain/compose.ts:184-199) catches once per template, so a recipient missing {speaker_name} AND {talk_title} is reported with one of them. Worse, missingToFields (src/routes/comms/compose-core.ts) keys by `${contactId}:${submissionId}` into a flat Record, so the subject's entry and the body's entry for the SAME recipient overwrite each other — the organiser fixes the named field, re-runs, and is told about the next one. The DEC-793 banner that lists them (ComposeWizard.tsx:330-341) also carries no role='alert', unlike its capMessage sibling three lines below, so a screen-reader user gets no announcement of the thing that just blocked their send. Decision: shape-scanning replaces throw-on-first — `missingMergeFields(template, vars): string[]` in src/mail/render.ts returns every placeholder absent from vars in first-appearance order (renderTemplate keeps throwing, unchanged, for its single-value callers); PreflightMissingField becomes ONE entry per recipient carrying `fields: string[]` (subject's misses first, then the body's, deduped); and both routes emit the SAME message shape whether one field or five — `missing merge fields: speaker_name, talk_title` — so the client has one parser instead of a singular/plural pair that will drift. The banner takes role='alert'. A preflight exists to make a batch fixable in one pass; naming one field per round trip is a preflight that lies about how much is wrong.


## Amendment (wave 1)

The bulk-email context resolves exactly speaker_name/event_name/portal_link (BULK_EMAIL_MERGE_FIELDS, src/mail/render.ts:56; enforced at src/routes/api/contacts/bulk-email.ts:156/173/238), but BulkEmailModal offers every event template, so Acceptance / Content Reminder / Decline preview straight into a 400 that names no offending token and offers no forward path. A control whose only outcome is an error is the same defect as a control with nothing to write. Ruling: the modal computes each template's unmergeable tokens with the pure helpers that already exist (`missingMergeFields` over the three-key vocabulary) and renders those templates as DISABLED options that name the blocking token, with one forward path — Comms compose, which does resolve them, opened with the template preselected (the `?template=` parameter ComposeWizard already honours). The vocabulary is imported, never re-listed.


## Amendment (wave 60) — a pre-flight refusal names the PERSON on every surface that sends, not just the one that was measured

DEC-856 already rules that the compose pre-flight names every merge field each recipient is missing and resolves the id to the person (ComposeWizard.tsx:228-253 does exactly this). Its sibling surface does not: src/routes/api/contacts/bulk-email.ts builds the identical fields map at :158, :175 and :243 keyed by `m.contactId` — a ULID — and its only reader, app/src/pages/contacts/BulkEmailModal.tsx:116, collapses the whole envelope to `setError(err.message)`. The organizer therefore reads 'One or more recipients are missing merge fields (only speaker_name/event_name/portal_link are allowed)' with nobody named, no token named, and no forward path. RULING: (1) the three refusal sites key the fields map by the recipient's EMAIL (human-legible and unique within one send) and the value names the person plus the exact missing placeholders, resolved from the already-loaded recipient rows with NO additional D1 read; (2) the message leads with the count ('3 of 12 recipients are missing {talk_title}'); (3) BulkEmailModal renders the map as a RECIPIENT / MISSING list in the recorded error vocabulary (1px ink border, 3px ink left edge, 13px/600 ink message — no red), and never discards `err.fields`. This closes the shape 'a fields map keyed on a ULID is a server naming rows to itself'.

## Amendment (wave 63): a refusal's fields map is read by SHAPE, never dumped — a handler that emits several fields-map shapes gets a matcher per shape, and anything unmatched renders labelled by its key

app/src/pages/contacts/BulkEmailModal.tsx:122,159 does `setMissingMergeFieldLines(err.fields ? Object.values(err.fields) : null)` — it treats EVERY fields map POST /contacts/bulk-email{,/preview} can return as a list of merge-field sentences. That endpoint emits at least four shapes: bulkEmailMergeFieldError's `{ <email>: "<Name> is missing {company}" }` (the only one the bullets are for), plus the validators at src/routes/api/contacts/bulk-email.ts:47-59 — `{ subject: "required" }`, `{ subject: "Max 2000" }`, `{ bodyText: "Max 20000" }`, `{ eventId: "required" }`. Paste a long subject and the modal renders a bullet reading `Max 2000` with no field named — a server talking to itself in front of the user, and the control that caused it carries no mark. The DEC-856 sibling in ComposeWizard already does this correctly (extractIcsUnscheduledIds / extractMissingMergeFieldLines / extractNoRecipientRefs, locked by app/src/pages/comms/compose-refusal-shapes.test.ts): match on the message shape the server actually throws, resolve to a name, and never let an unmatched entry fall into an unlabelled list. The rule generalises: an entry that matches no known shape is rendered as "<field>: <message>" beside its control, never as an anonymous bullet.

## Amendment (wave 65): read-by-shape is a rule for EVERY SPA writer, not the two surfaces that were measured — and the placeholder message is never shown alone when a fields map exists

DEC-856's wave-60 and wave-63 amendments settled the CONTRACT (match on the shape the server actually throws; resolve ids to names; render an unmatched entry as "<field>: <message>" beside its control, never an anonymous bullet) but bound it to the compose/bulk-email family. Re-derived at the file on main, five more writers whose endpoints emit named-field refusals still collapse the whole envelope to err.message: app/src/pages/contacts/ContactDrawer.tsx:199 (PATCH /contacts/:id throws "Validation failed" with up to twelve keys — firstName/lastName/email/phone/company/title/bio/notes/customFields.<key>/socialLinks — src/routes/api/contacts/crud.ts:269-345), app/src/pages/settings/TracksRoomsPanel.tsx:187,203,242,261 ("Invalid track"/"Invalid room" with name/color/capacity, src/routes/api/events.ts:416-536), app/src/pages/contacts/SegmentsPanel.tsx:46 (segments.ts:104-193), app/src/pages/contacts/ImportWizard.tsx:281,311 (contacts/import.ts throws "Validation failed" with csvText/mapping/dryRun/skipLines/eventId/sessionTitle at seven sites), app/src/pages/contacts/PipelineBoard.tsx:110,565,736,862 (api/pipeline.ts throws "Validation failed" with fitScore/rationale/reason/contactId/stage/patch/body at eleven sites). Compare src/routes/api/tokens.ts:78-81, whose message stands alone ("name is too long") — those clients are fine BECAUSE the server said something. The rule, stated once so five workers do not each invent it: (1) on an ApiError with a non-empty fields map the writer NEVER renders err.message alone; (2) each key the surface owns is routed to that control's own message slot — FormRow's `error` prop where the surface uses FormRow, otherwise a `<span role="alert" className="chq-field-error">` immediately after the control, and the control's wrapper marks itself invalid the way error-states.css already expects; (3) a form with two or more controls also renders ONE ErrorSummary (app/src/components/ErrorSummary.tsx) above the fields, heading from countHeading(n, '<tail>'), one anchor per problem pointing at the real control id; (4) a key the surface does not own — a dotted key like customFields.travel_logistics, or a key added by a later route change — renders labelled as "<key>: <message>" in the summary block, never dropped and never an unlabelled bullet; (5) the refusal is announced with role=alert and the surface says what survived ("Nothing else was saved") where the server's own semantics are all-or-nothing. This closes the shape 'when six refusals share one message, `?? err.message` is not a fallback, it is silence' for the SPA's remaining write surfaces.

## Amendment (wave 67): the settings embed panels are the last three writers funnelling a named fields map into one sentence

src/routes/api/embeds.ts throws 20 fields-map refusals across 12 distinct named keys (options, trackId, sessionFormat, roomId, day, q, limit, fields, accent, name, surface, format, enabled -- :42-231). Its three SPA clients read none of them: SavedEmbedsPanel.tsx:71,95,105, plus EmbedsPanel.tsx and PublicPagesPanel.tsx, all take the `err instanceof ApiError ? err.message : '...'` branch. Every other settings panel (TracksRoomsPanel, PortalSettingsPanel, PeopleRolesPanel, EventSettingsPanel, CallForPapersPanel) already reads its map; these three are the ones in the embeds sub-tree that the wave-63/65 sweeps did not enumerate. Ruling: same DEC-856 shape as TracksRoomsPanel -- named keys route to their own controls, an unmatched key renders LABELLED rather than dropped, and the sentence branch survives only for a field-less refusal.

## Amendment (w1): a fields map is the server naming a control to the client — it never carries a raw ULID

Verified at the file: src/routes/tasks.ts:426 (`contactIds: `unknown ids: ${missing.join(", ")}``) and :443, src/routes/comms/compose-core.ts:53 (`submissionIds: `unknown ids: ...``) and src/routes/comms/portal-invites.ts:42 interpolate raw ULIDs into a fields map the SPA prints verbatim. At least two are reachable from an ordinary stale selection — a producer who leaves a page open while a colleague deletes a contact gets `unknown ids: 01J8Z...` as their explanation. That is the server talking to itself in front of the customer. This decision's own sibling shape at compose-core.ts:366 already resolves a (contactId, submissionId) envelope to a named person, which is the standard these three fell short of. Ruling: refusals name the count and, where resolvable, the people or sessions; where nothing resolves they say the selection went stale and offer the refresh. The id list never reaches the string. compose-core.ts:366's ULID-keyed map stays as-is — it is keyed for the client's own row lookup, not printed.

## Amendment (wave 69): a page-level error banner has a clear path

`app/src/pages/settings/TracksRoomsPanel.tsx` declares `error` at :106 and assigns it in EIGHT catch sites (:143,146,226,249,275,302,328,354); nothing in the file ever clears it. The banner at :401 (`{eventError || error ? <p role="alert">…</p> : null}`) is therefore permanent for the life of the panel: the next SUCCESSFUL track add renders its success beside a stale refusal, which is exactly the 'success + simultaneous error box' the eval filed against the tracks-and-rooms panel. DEC-856 already owns this panel's error grammar (a fields map is never collapsed to err.message); it gains the missing half. Ruling: a component that renders a page-level error banner clears it at the START of every write it owns (`setError(undefined)` as the first statement of the handler, the idiom 44 other files already use), so the banner can only ever describe the most recent attempt. The population is derived MECHANICALLY, never hand-listed (DEC-180): files that call `setError(` and never `setError(null)`/`setError(undefined)`.
