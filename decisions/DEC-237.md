# DEC-237: Ratify the two production hotfixes as stage-1 code with regression tests; DEC-004 amended to 100,000 PBKDF2 iterations

workerd hard-caps PBKDF2 at 100k iterations, so DEC-004's 600k value 500s every production login; the human hotfixed ITERATIONS to 100_000 (src/auth/password.ts:13) and made the public-submit confirmation email best-effort (src/routes/public/submit.tsx:553-564) because a mailer throw after the row persisted showed an error page and caused duplicate submissions. Both fixes are correct and are hereby ratified as the design; hash format is 'pbkdf2$v1$100000$<salt>$<hash>' and verification must honor the iteration count embedded in each stored hash. Each fix must gain a regression test (assert ITERATIONS <= 100_000; assert submit returns the confirmation page when mailer.send throws) so the amendment cannot silently regress.
