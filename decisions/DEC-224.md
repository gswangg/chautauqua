# DEC-224: Wave-24 battery is VOID as gate evidence; task-w24-* refs closed with LATE-DRAIN-IMMUNE accounting — only docs/verification-log/task-w25-*.md counts toward the exit predicate

The w24 battery was bound to a superseded freeze, so none of its output can serve as exit evidence: task-w24-a merged a correct FAIL-stop log (kept as historical record only); task-w24-c (tip 0a263d2) and task-w24-f (tip cde03cd) are zero-commit branches whose tips are ancestors of main — drop; task-w24-b/d/e (tips 0a8eb560, 40b1d141, 121f3980) carry unmerged commits, presumably their single stale log files — the merge policy should drop them unmerged. CRITICAL LESSON after four late-drains: correctness must not depend on drop compliance. Therefore accounting is made late-drain-immune: (1) the wave-26 completion predicate counts ONLY docs/verification-log/task-w25-<letter>-<gate>.md files — any task-w24-* log that late-merges is ignored regardless of its verdict; (2) a late merge of a task-w24-* branch touching only docs/verification-log/task-w24-*.md is allow-listed non-code-bearing and is NOT sha drift; (3) if such a merge touches anything else, every w25 lane's sha re-derivation FAIL-stops by design. No future decision may declare an unmerged branch 'will not land' as a load-bearing premise.
