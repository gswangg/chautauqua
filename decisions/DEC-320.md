# DEC-320: Wave-14 supersedes DEC-315's exit predicate: the wave-13 log battery is evidence at a PRE-FIX sha, so goalComplete additionally requires a wave-15 confirmation pass over the four surfaces wave-14 changes

Read live, not inherited: refs/heads/main carries only docs/verification-log/task-w13-a-c3-build-test.md (PASS, OPEN ITEMS: 0, frozen at f6983e6); worktrees exist for task-w13-{b,c,d,g,i,j} and only d and g have written their logs. So DEC-315's seven-log predicate is un-evaluable right now, and its instruction to a planner in that position is to return ZERO tasks rather than pad. That instruction is correct about PADDING and wrong about this wave: independent review surfaced eight cited file:line defects, four of which I confirmed present on main today (declined co-speakers holding write access, decision emails reaching decliners, public/admin agenda divergence after a date edit, an uncapped bulk send) plus two SPEC gaps I found while verifying them (the default CFP collects no title/company/bio, so J10's speakers list is structurally blank for every non-fixture speaker; portal-collected social links reach no public surface despite SPEC.md:141). Landing narrow, cited fixes is DEC-287's narrowing branch, not padding. The real cost is evidence hygiene: seven wave-13 logs at a pre-fix sha must not be readable as a green light for post-fix code. Therefore the exit predicate becomes conjunctive — (i) the seven wave-13 '-c3-' logs at OPEN ITEMS: 0 / RESULT: PASS as DEC-315 defines them, AND (ii) a wave-15 confirmation pass at a sha containing the wave-14 merges consisting of build+test+audit and a targeted live regression over exactly the four changed surfaces (portal/file authz under a declined invitation, compose recipients, public agenda vs admin after an event-date edit, capped reminder batch) plus the two added ones (CFP profile fields reaching /e/:slug/speakers, social links rendering with a rejected javascript: URL). Wave 15 is NOT a seven-lane re-run: lanes e/h/i/j audit prose and citations that wave-14 does not move. A planner that can see (i) but not (ii) returns zero tasks and waits; DEC-315's anti-padding rule survives intact.
