# DEC-503: The phone manifest covers every surface the desktop manifest covers, proven by an executable test

task-w25-d fixed app/src/routeManifest.ts to list all five `/embed/:slug/*` twins but could not touch scripts/render-sweep.ts, whose MOBILE_ROUTE_MANIFEST (lines 89-114) still lists only sessions/agenda/speakers — so the DEC-489/490 embed rework has never been rendered at 390x844 for schedule or gallery, on the viewport SPEC §9 calls the actual final exam. Two hand-maintained lists of the same five-element set desync silently, and the one that desynced was the phone pass. `SURFACES` (src/routes/public/shell.tsx:13) is the authority; a vitest test enumerates it and asserts that both ROUTE_MANIFEST and MOBILE_ROUTE_MANIFEST contain `/e/<slug>/<surface>` and `/embed/<slug>/<surface>` for every member — an enumeration artifact, not prose, so the next surface added fails a test rather than quietly escaping the sweep.

## Amendment (wave 69): the phone sweep manifest is DERIVED from the desktop manifest's public entries, with a closed reasoned exclusion list

DEC-503 made the sweep manifests derive from SURFACES so a new public SURFACE cannot be forgotten — but /e/:slug/programme is a public HTML page that is not a Surface, and it is in app/src/routeManifest.ts while missing from scripts/render-sweep.ts's MOBILE_ROUTE_MANIFEST, so the one surface a producer prints has never been rendered at 390x844. Waves 65-68 kept finding the same shape (the perf gate and the J10 walkthrough both predated two wave-65 public GETs). Deriving the phone list from the desktop list's role:"public" rows — with an explicit, reasoned exclusion list for the twins genuinely covered elsewhere — turns the last hand-maintained public-route list into a checked one.
