# DEC-366: The docs/design/ handoff is this run's binding requirement; stage-1 function is frozen and complete

Verified against the tree, not prior summaries: main carries a full product (149 src modules, 166 app modules, 18 migrations, 161 test files), decisions DEC-001..365, and DEC-363's stage-1 exit predicate satisfied by six wave-23 gate logs at FROZEN SHA e3d558e (all OPEN ITEMS: 0 / RESULT: PASS, present on main). The newest commit on main (git reflog line 200, 'Design redesign handoff: full design pack, vendored variable fonts, swarm mandate') adds docs/design/README.md, 11 *.dc.html prototypes, 12 canvas screenshots and public/fonts/*-var.woff2. That handoff is the customer's newest statement of need, so it ranks immediately below docs/clarifications.md for every visual, layout and copy question and above brief.md/sessionboard-reference/eval-rubric; SPEC.md still governs behaviour, data model, authz and performance. Two consequences bind every lane: (1) functional scope is FROZEN — no lane may change endpoints, authz, data flow, optimistic-update semantics or role gating except where a decision here explicitly says so (DEC-370); (2) re-verifying settled stage-1 items (pubcache purge DEC-201/333/348/358, ABS-14 AI triage DEC-272, stage-2 provisioning/deploy/Resend/Airtable/DNS/CI) is never an open item and never a task.

## Amendment (wave 42): a frame is drawn at demo scale — it governs anatomy, not the existence of volume/capability affordances; and where its words describe a computation we do not perform, truth wins

Successive waves keep proposing to delete the results pager, the per-row reviews disclosure, the inline edit affordances, and the anonymize/delete-plan controls because no frame drawn over ~10 rows shows them. SPEC §2 principle 2 (volume-first: server pagination, bulk actions, keyboard-fast review) and graded rubric IDs (ABS anonymity DEC-736, the CNT turn-diet directive that content edit paths be few clicks deep) require them; the frames simply never depicted the 2,000-row case. So: frames bind typography, geometry, grammar and the presence of decorative chrome — they never authorize removing a volume or capability affordance. The converse also binds: the results eyebrow may not read 'MEAN OF SUBMITTED REVIEWS' while the number underneath is a weighted blend — the frame's letterspaced treatment is adopted, its claim is corrected.
