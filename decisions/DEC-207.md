# DEC-207: Deterministic repair of the conflict-marker-corrupted verification ledger (sole authorized non-append edit)

Commit d1c13d2 ('merge task-w20-a') embedded literal git conflict markers into docs/verification-log.md at lines 7076 ('<<<<<<< HEAD'), 7130 ('======='), and 7197 ('>>>>>>> task-w20-c') because the task-w20-a branch committed an unresolved self-merge with task-w20-c; the ort merge into main kept the corrupted blob. This breaks DEC-069's format (the w20-a build+test section has no terminating RESULT line of its own — the single trailing 'OPEN ITEMS: 0'/'RESULT: PASS' belongs to the w20-c perf-smoke section) and violates the zero-conflict-marker checklist item carried since DEC-197. Repair is surgical and derivation-checked, not a rewrite: delete exactly those three marker lines (located by content, not line number) and insert 'OPEN ITEMS: 0', blank line, 'RESULT: PASS', blank line at the end of the w20-a section (immediately before the '## 2026-08-10 task-w20-c — perf-smoke @ 6807b67' heading), cross-checking against git history — the w20-a section as originally authored on the d1c13d2^2 branch lineage and the w20-c section at ref task-w20-c (edb5a52) both end with exactly that footer; abort and record FAIL if either cross-check contradicts. Afterward a repo-wide ripgrep for '^(<<<<<<<|=======|>>>>>>>)' must return zero matches. This is the single authorized exception to DEC-068 append-only, executed by lane task-w21-a, which also appends a short ledger section documenting the surgery for the audit trail. Both repaired sections then stand as valid green gates: their PASS content is intact and was committed by the lanes that ran them.
