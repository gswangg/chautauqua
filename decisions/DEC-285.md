# DEC-285: Wave-8 exit battery freezes at S=80b811d250285de0d37417ddc12f65445ce27f96 with a pre-registered KNOWN-IN-FLIGHT ledger and a second detached RECHECK worktree

Verified by direct read of .git, not inherited: refs/heads/main = 80b811d250285de0d37417ddc12f65445ce27f96 ('commit: scribe wave 7', .git/logs/HEAD line 82); wave 6 fully drained (merges task-w6-a..e at lines 77-81) and the code proves it — DEC-274's split gates are live (src/server/repo/public.ts:42 visibleSessionConditions, :53 visibleParticipantConditions, :71 the AND), DEC-276's resolveBearerAuth re-resolves the minting user (src/server/middleware.ts:151-172, no hard-coded role), DEC-277 range-checks slot days (the former src/server/repo/agenda.ts:336 (now split across src/server/repo/agenda/) isDayWithinEventRange). Wave 7 is live and unmerged: refs/heads/task-w7-a sits at main's tip with zero commits, refs/heads/task-w7-c is at 5f54097ff227ade54797b2ba621c7d21baf61407, and no task-w7-* merge exists in the reflog. So the POST-S DELTA is GUARANTEED non-empty and two defects are provably present at S: src/server/repo/contacts.ts:192/207 repoints only six contact FK tables while the former src/db/schema.ts (now split across src/db/schema/) has seven (user:38, participant:244, task_assignment:462, file:538, file_comment:554, pipeline_entry:655, email_log:718) — an orphaned pipeline_entry makes src/server/repo/pipeline.ts:161 throw for the whole org; and src/server/repo/tasks.ts:263 listAcceptedContactIds has no active-participant filter, so assignToAllAccepted re-assigns declined co-speakers. Without a pre-registered ledger, eight independent lanes each rediscover these two and the whole battery FAILs on code that is being fixed in flight — the exact bookkeeping failure DEC-280 was written to end. Therefore: each lane still verifies inside `git worktree add --detach <scratch> 80b811d…` and still prints the FROZEN SHA literal verbatim (DEC-270 steps 1/2/4 binding), and when the POST-S DELTA is non-empty it opens a SECOND detached worktree at the observed refs/heads/main sha, re-checks only its own claims whose cited file appears in the delta, and prints `RECHECK SHA: <40-char>`. The two ledger entries above, if observed at S, are logged under '## KNOWN IN-FLIGHT AT S' and counted as OPEN ITEMS only if they are still present at the RECHECK SHA or if the delta shows no fix landed at all — fail-loudly is preserved (an unlanded fix still fails the battery) without a guaranteed eight-way false FAIL.
