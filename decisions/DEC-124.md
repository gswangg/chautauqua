# DEC-124: Server-side answer length caps in validateAnswers: text-kind answers max 2,000 chars, long_text max 20,000; field-level error 'Too long (max N characters)'

src/forms/validate.ts enforces required/type/options but no maximum length, so an unauthenticated public submit persists a 2,000,000-byte description verbatim (reproduced by a reviewer as SES-031) — unbounded payload bloat per submission against SPEC §6's server-side form-schema validation posture. Ruling: exported constants MAX_TEXT_LENGTH = 2000 and MAX_LONG_TEXT_LENGTH = 20000 in src/forms/validate.ts, enforced for kinds 'text' and 'long_text' respectively after the required check, producing a per-field error 'Too long (max N characters)' in the standard errors map. Dropdown values are already bound to the options list, number/checkbox are non-string, and file answers are server-generated keys, so no cap is needed there. The caps apply uniformly to public submit and portal edit since both flow through validateAnswers; all fixtures, seeds, and walkthrough inputs are far below these bounds.

## Amendment (wave 26): there is no red in this system — an invalid field is 1px ink border + 3px ink left edge + a 13px/600 ink message, summarised at the top and repaired in place

The V9 intake records an error-and-validation-states standard with nine frames and thirteen rules, and nothing on main implements it: src/routes/public/submit-views.tsx has per-field `.chq-field-error` paragraphs and no top summary, no anchors, no 'nothing was lost', no over-count. Ruling, binding on every future error surface: errors are set in WEIGHT AND RULE, never colour — the invalid control takes a 1px ink border with a 3px ink left edge (the same vocabulary 'overdue' uses) and its message renders in ink at 13px/600. Add the classes ONCE (`.chq-field-invalid`, `.chq-field-error`, `.chq-error-summary`, `.chq-error-summary-link`) and never invent a per-surface treatment. The four rules that bind this wave's public-submit application: (1) summarise at the top — "Three things need fixing before this can be sent" with one anchor link per problem — AND repeat the message at each field; (2) say what was kept — "Nothing was lost. Everything you typed is still below."; (3) count against the limit — "1,412 of 1,200" in the counter and "212 characters over" in the message, never a bare 'too long'; (4) required means pick one — "Pick one — a talk needs a track so the right people review it" — and a field whose cost is invisible says why it matters ("we send your portal link here, so a typo means you never get it"). Credentials still fail with ONE message for both causes; that half is wave 25's.


## Amendment (wave 28): the SPA error vocabulary is ONE file and is CLASS-NAME-FIRST — surfaces emit the agreed class names, one module styles them

The wave-26 amendment named four classes (.chq-field-invalid, .chq-field-error, .chq-error-summary, .chq-error-summary-link) and shipped them only into src/routes/public/cfp.css.ts. The admin SPA has none of them: app/src/styles.css:764 styles .chq-error/.chq-error-banner as a rounded bordered box and :1054 styles .chq-field-error as an 11px uppercase micro-label, and app/src/components/modal-frame.css:148 adds a THIRD register (.chq-form-row-error). Ruling: the SPA's copy of the vocabulary lives in exactly one new file, app/src/components/error-states.css, imported once from app/src/main.tsx AFTER ./styles.css; it owns .chq-field-invalid, the .chq-error-summary family, and the RESTYLE of .chq-field-error and .chq-form-row-error to the standard's 13px/600 sentence ink. No other app/src CSS file may declare those selectors, and a scan pins that. Corollary that decides task ordering: because the vocabulary is class names plus copy, a surface can emit the markup and be tested (render tests assert classes and text, not computed style) before the CSS module lands — so applying the standard to a surface is NOT sequential on the CSS, and only a surface that imports the shared ErrorSummary component is.

## Amendment (wave 30): the plan editor's rejected save is the ninth error shape — shared ErrorSummary, the cross-field window rule mirrored client-side, and an empty-criteria card that offers the way out

Eight of the V9 standard's nine failure shapes are built and tested on main (public submit, sign-in, speakers optimistic rollback, import file-level, upload wrong-type, compose pre-flight, scorecard draft/submit, settings slug clash). The ninth — 'Plan editor, save rejected: cross-field + empty-collection', framed at docs/design/Chautauqua Review.dc.html:788-831 — is absent: PlanEditor.save() prints a single generic line ('Fix the highlighted fields before saving.'), validatePlanDraft has NO closeAt<openAt rule at all (the server's DEC-509 checkEpochOrder catches it only after a round trip), the empty list shows a bare field error instead of the framed card, and `dateFieldError` is a declared-and-rendered useState that nothing ever sets — a reader with no writer. app/src/components/ErrorSummary.tsx + countHeading already exist with tests and have ZERO consumers; this is the surface they were built for, so the fix consumes the shared component rather than hand-rolling a fourth summary. No red, no new colour literal: the error vocabulary stays in app/src/components/error-states.css.

## Amendment (wave 48)

**The auth surface family composes ERROR_STATES_CSS; a rejected credential marks BOTH fields and names neither.** DEC-124's no-red error vocabulary was hoisted to src/views/error-states.css.ts precisely so no surface would re-invent it, and CFP_CSS (cfp.css.ts:188) and PORTAL_CSS (portal.css.ts:440) compose it. AUTH_CSS does not: it declares a bespoke .chq-auth-error (auth.css.ts:171-175) that is one bold 14px line, and no auth input ever receives .chq-field-invalid. A component that exists and is never called is a rule that is not enforced. DESIGN-RULINGS 'Error and validation states' rule 7 is explicit: 'Credentials fail with one message for both causes … Both fields take the error treatment, since the design does not know which was wrong.' RULING: AUTH_CSS composes ERROR_STATES_CSS at its tail; .chq-auth-error is deleted; a rejected sign-in renders the shared .chq-error-summary block (no anchors — there is nothing to jump to) and puts .chq-field-invalid on BOTH the email and the password input. The message stays single and non-enumerating (DEC-004). No new tone is introduced — the vendored ruling states the treatment as '1px ink border with a 3px ink left edge and its message in ink at 13px/600', which .chq-field-invalid/.chq-error-summary already are; a fleet-measured band fill is not authority over that stated spec.

## Amendment (wave 50): ONE error vocabulary, two bundles — the SSR module and the SPA stylesheet are scan-bound, and the leading '!' marker is dropped

DEC-124 declares the no-red error vocabulary ONCE and reuses it by class name. It is now declared twice: src/views/error-states.css.ts (ERROR_STATES_CSS, composed by AUTH_CSS, PORTAL_CSS and CFP_CSS) and app/src/components/error-states.css (the admin SPA). Verified at the file on main, they disagree on the message rule: the SSR module declares `.chq-field-error { font-size:13px; font-weight:600; margin:0; color:ink }` PLUS `.chq-field-error::before { content: "! " }`, while the SPA declares the same three properties for `.chq-field-error, .chq-form-row-error` and no marker at all — so the identical class renders with a leading '!' on the portal and CFP and without it in the admin, and the SPA's own header comment claims 'weight + a leading marker/border' it never declares. The vendored ERROR & VALIDATION STATES STANDARD names exactly three properties for an invalid field — '1px ink border + 3px ink left edge + 13px/600 ink message' — and no marker glyph. RULING: the frames are the authority, so the '!' pseudo-element is dropped from ERROR_STATES_CSS rather than added to the SPA; the non-colour signal the standard actually relies on (the 3px ink left edge, plus role="alert"/aria-describedby, both already present on both sides) is unchanged, so nothing regresses for assistive tech. The two bundles are then bound by a scan test that extracts the shared declarations (.chq-field-invalid, .chq-error-summary and its children, .chq-field-error) from both files and fails naming any property whose value differs — the vocabulary is one contract or it is two grammars, and a comment is not a binding.

## Amendment (wave 56)

Every key in an ApiError.fields map reaches its own control — a one-field branch is a dropped map.

DEC-124 is the top-of-form error summary plus fix-in-place, and the v9 error standard makes 'summarize at top + fix in place (anchors)' rule 1 of nine. Four SPA panels already capture the whole envelope map (CallForPapersPanel.tsx:189/213, PeopleRolesPanel.tsx:127, BreaksPanel.tsx:145/171, FormsPage.tsx:197). app/src/pages/settings/EventSettingsPanel.tsx:186 is the outlier: it reads err.fields?.slug and routes EVERYTHING else into one generic banner. PATCH /api/v1/events/:id can refuse name, slug, startDate, endDate, timezone, location and branding (src/routes/api/events.ts:317-355), and the most reachable of those — endDate 'Must be on or after startDate' — currently surfaces to the producer as the bare message 'Invalid event' with no field marked. That is a parsed result discarded. Ruling: the panel keeps the whole fields map, renders each message under its own control, and renders one ErrorSummary at the top of the edit view with one anchor per offending control id. Two refusals get non-blaming copy (slug already claimed; end date before start date); every other key renders the server's own message rather than being dropped.

## Amendment (wave 59): a printed character budget is an enforced cap — FormFieldDef.maximum NARROWS validateAnswers' kind cap, and the locked abstract's 1,200 is set by the one projection

src/routes/public/submit-views.tsx:36 declares CFP_ABSTRACT_MAX_LENGTH=1200 and :384 applies it as a DISPLAY-ONLY `maximum` on the locked description field; src/views/form-render.tsx:138 turns that into the visible "412 / 1,200" counter. src/forms/validate.ts:98 computes its cap from `field.kind` alone and never reads `field.maximum` — so the public CFP shows every submitter a 1,200-character budget the server does not enforce until 20,000. submit-views.tsx:26-35 admits this in a comment and defers it to "a follow-up decision"; this is that decision. The design side is unambiguous: docs/design/Chautauqua Public and Portal.dc.html:1198 frames the OVER-LIMIT state as "1,412 of 1,200", DESIGN-RULINGS.md:204 requires "212 characters over" in the message, and docs/design/Chautauqua Submissions.dc.html:694 gives the Abstract field the hint "Up to 1,200 characters". RULING: `FormFieldDef.maximum` is an enforced per-field cap, not a display hint. validateAnswers takes `min(field.maximum, kindCap)` — a field maximum may only NARROW, never widen, so no form definition can raise a text answer past MAX_TEXT_LENGTH/MAX_LONG_TEXT_LENGTH. The 1,200 stops being a view-layer override and becomes a property of the locked `description` field, set inside projectFieldForAnswers (DEC-486's ONE projection, called by src/server/repo/submit.ts:142 and src/server/repo/portal-edit.ts:135), so the public CFP and the speaker's portal edit render the same counter and refuse at the same number. The refusal string stays byte-identical ("Too long (max N characters)") so src/routes/public/submit.tsx:134's overLengthErrorMessage keeps producing the counting copy the ruling asks for. A stored abstract longer than 1,200 is now unsaveable until trimmed; that is the intended meaning of a stated budget, and any seeded row over it is the seed being wrong, not the cap.

## Amendment (wave 61): a locked session field's character budget is a property of the field, not of the role writing it: ONE constant per locked field, read by every writer and declared by every control

submission.title and submission.description each carry TWO caps today, split by role, and each split produces a dead end. description: the speaker path (public CFP + portal edit) enforces LOCKED_ABSTRACT_MAX_LENGTH=1,200 via projectFieldForAnswers -> validate.ts:102, while src/routes/api/submissions.ts:210/290 lets an organizer POST/PATCH 20,000 — so an organizer can MINT an abstract its own speaker can never re-save (validate.ts refuses the untouched answer at 1,201). title: the mirror image — validate.ts caps the speaker's locked `text` title at MAX_TEXT_LENGTH=2,000 while submissions.ts:208/286 caps the organizer at MAX_NAME_LENGTH=200, so an organizer cannot open 'Edit title and abstract' and save ANY change to a submission whose speaker typed a 500-character title. And app/src/pages/forms/FieldList.tsx:160 prints 'Up to 20,000 characters' on the Abstract row whose own doc-comment claims it shows 'the real imported length cap' — the design frame (docs/design/Chautauqua Submissions.dc.html:694) reads 'Up to 1,200 characters'. Ruling: the tighter, human-scaled number wins in both directions — title 200, abstract 1,200 — declared once in src/forms/types.ts, stamped by projectFieldForAnswers for both locked session fields, imported by the organizer routes, and printed by every control that edits them. Restoring a stored revision (submissions.ts:409) deliberately stays unbounded: it replays a value the product already accepted.

## Amendment (w1): the control declares the cap the validator enforces — one resolver, read by validate.ts and the SSR form renderer

Verified at the file: src/forms/validate.ts:101-102 computes `cap = field.maximum !== undefined ? Math.min(field.maximum, kindCap) : kindCap` and refuses past it, while src/views/form-render.tsx's FieldControl renders text (l.37-48), long_text (l.55-66) and number (l.100-109) with NO maxlength/max at all. Worse, the DEC-909 counter's population is `field.kind === 'long_text'` (l.138), so the locked title (LOCKED_TITLE_MAX_LENGTH=200) and first_name/last_name/job_title/company (LOCKED_CONTACT_TEXT_MAX_LENGTH=200, stamped by projectFieldForAnswers) reach the submitter with no disclosure whatsoever and are enforced only after they press Submit. This is the public CFP — J2's single highest-traffic surface. Ruling: the cap resolution becomes ONE exported function in src/forms/validate.ts; the validator and the renderer both call it; text and long_text controls carry maxlength from it and both kinds carry the counter. A parser that refuses a value the control was happy to produce is the surface lying by omission. Note for the record: FormFieldDef.maximum has exactly one writer (projectFieldForAnswers, six locked fields) — no schema column, no builder control — so its `def.maximum ??` fallbacks are dead branches today; do not build an organizer-facing budget control in this wave.

## Amendment: FormFieldDef.maximum is a TEXT-length budget only; no control renders a bound the validator does not read (wave 2)

w1-a wired src/views/form-render.tsx's text/long_text controls to resolvedTextCap, the same function validate.ts refuses by -- and left a NOTE in the number branch admitting the opposite: `max={field.maximum}` is rendered although validate.ts's number branch never reads field.maximum and no writer (projectFieldForAnswers stamps only locked text/long_text fields) ever sets it. That is w1-a's own defect class inverted: a control declaring a bound the server does not enforce. Ruling: `maximum` is defined as a text-length budget, its only reader is resolvedTextCap, and the unbacked HTML `max` attribute is DELETED rather than back-filled with a server check nobody asked for -- SPEC J1 lists number fields with required flags and help text, never a configurable numeric ceiling, so inventing an enforcement path would be building configurability no requirement names.

## Amendment (findings wave 6): an error band is a {headline, detail?} PAIR, and the sign-in rejection uses the frame's two lines

src/routes/auth-views.tsx:84-89 renders the login band as a headline supplied by the caller plus a hard-coded second line, 'Check your email and password, then try again.' Two failures follow. First, the copy is off-frame: docs/design/Chautauqua Account.dc.html:302-305 draws 'That email and password do not match' over 'Check both, or reset your password.', while the app says 'Invalid email or password.' (src/routes/auth-login.tsx:200). Second, and worse, the fixed second line also paints under RATE_LIMIT_ERROR (auth-login.tsx:131/:153/:176), where 'check your email and password' is false advice — the credentials may be perfect; the request was refused for rate. A band whose detail line is written once and reused under every headline is a sentence that is true only for the error it was written for. Ruling: band copy travels as a pair (AuthBandCopy {headline, detail?}); the rejection carries the frame's two lines; the rate-limit band carries a headline only. The plain-string `error` prop stays on ClaimPage/PasswordPage/ForgotPasswordPage/ResetPasswordPage — they render one line and need no churn.

## Amendment (findings wave 14): the invalid-control rule must OUTRANK the base control rule — declared at (0,2,0) in both error-vocabulary bundles

`.chq-field-invalid { border: 1px solid var(--chq-ink); border-left: 3px solid var(--chq-ink); padding-left: calc(0.6rem - 3px) }` (src/views/error-states.css.ts:36) has CSS specificity (0,1,0). THEME_CSS's base control group `input[type=search], input[type=text], input[type=email], input[type=tel], input[type=url], input[type=password], select, textarea { ... border: 1px solid var(--chq-border); border-radius: 4px; padding: 0.4rem 0.6rem }` (src/views/theme.ts:175-186, and a second copy at :495 inside the phone media query) has specificity (0,1,1). Source order cannot save the class — (0,1,1) beats (0,1,0) outright. Therefore on EVERY SSR surface that composes ERROR_STATES_CSS (AUTH_CSS/PORTAL_CSS/CFP_CSS: /login and /account/password via src/routes/auth-views.tsx:100, the public CFP and speaker portal via src/views/form-render.tsx:30) an invalid <input> renders with the ordinary hairline and no 3px ink left edge. The <select>/<textarea>/<fieldset> arms happen to work, because bare element selectors are only (0,0,1) — so the no-red vocabulary is HALF DEAD in a way no source scan can see, and every existing test that asserts the class is present still passes. RULING: the invalid-control declaration is written `.chq-field-invalid.chq-field-invalid` — an order-independent (0,2,0) that outranks any type-attribute base rule, present and future — declared identically in src/views/error-states.css.ts and app/src/components/error-states.css so test/error-vocabulary-parity.scan.test.ts keeps binding them with NO new allowlist entry. Not !important (DEC-409 already establishes that these stylesheets argue by specificity, never by force) and not a change to THEME_CSS's base group, whose job is to style controls carrying no class at all. `fieldset.chq-field-invalid` keeps its own selector; the SPA's `.chq-form-row[data-invalid='true'] ...` descendant arms are already higher than (0,1,1) and are untouched.

## Amendment (wave 24): the invalid vocabulary is uniform across every control kind the SSR form renderer emits

`src/views/form-render.tsx:27-31` states the invalid state is a class plus a real `aria-invalid`, "never re-invented per control kind" -- but only three of the six kinds apply it. `number` (:100-112) uses a bare `class="chq-input"` and sets no `aria-invalid`; `checkbox` (:89-99) and `file` (:113-122) set neither. So a required number or file field that fails validation renders its `role="alert"` message while the control the error-summary link lands on carries no invalid state at all -- the error is visible in prose and invisible in the accessibility tree, on the public CFP form and the portal edit form that share this renderer. Ruling: every branch of FieldControl runs its class through `invalidClass()` and sets `aria-invalid` from the same `error` prop, reusing the existing `.chq-field-invalid` rule (`src/views/error-states.css.ts:46`) exactly as `src/routes/portal/tasks/views.tsx:278` already does on a non-text control -- no new geometry, no new fitted numbers (DEC-369). A test enumerates all six kinds so a seventh cannot be added without one. DEC-194's four required-capable controls stand unchanged: this amendment is about the INVALID vocabulary, not about `data-required`.

## Amendment (wave 56): a 400 that names fields must render those fields — the plan editor's save catch feeds the ErrorSummary the file already owns

VERIFIED. src/routes/review/plans-crud.ts:88 and :151 both throw `new ApiError("invalid", "Invalid plan", errors)` with a per-field map that can name name, instructions, scale, criteria, rounds, roundCriteria, roundMeta, maxEvaluations, openDate and closeDate (:67-88, :118-151) — including DEC-417's 'N of M' over-cap sentences and DEC-517's cross-field order check. app/src/pages/review/PlanEditor.tsx:669-673 catches it and calls `setError(err.message)`, discarding `err.fields` entirely, so the producer is told 'Invalid plan' and nothing else. The same file already does this correctly three times for reviewer assignment (:982, :1007, :1044) through `reviewerAssignProblems` at :103-108, whose comment says it 'Walks the WHOLE err.fields map (never a single named key)', and already mounts the V9 ErrorSummary at :2075. This is DESIGN-RULINGS.md:202 rule 1 ('Summarise at the top, fix in place') violated on the one screen with the most cross-field rules. RULING: commitSave's catch walks the whole `err.fields` map into the ErrorSummary already mounted on this page, one anchored problem per key; `err.message` stays the summary sentence and is never the only thing rendered. No new banner component and no second, less specific message underneath — the w28-b/DEC-124 'ONE rejection message' rule at :628-631 still binds.
