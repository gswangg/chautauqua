# DEC-280: Exit-battery execution amended: post-S product commits are a recorded POST-S DELTA to re-check, never a STOP (supersedes DEC-270 step 3, keeps steps 1/2/4)

Verified at refs/heads/main = 1a4d55bc75a9433e62485be9cb2cb833e3105948: .git/logs/HEAD line 77 shows the newest merge is 'merge task-w6-b', and reading the code proves DEC-275 (src/server/repo/submissions/create.ts:194-216 copies active participants, resets invite to 'none') and DEC-278 (src/server/repo/submissions/status.ts:104-135 isActiveParticipant gate; call sites at src/routes/api/submissions.ts:301 and src/routes/portal/index.tsx:291) HAVE landed while DEC-274 (src/server/repo/public.ts:38 still returns one collapsed predicate), DEC-276 (src/server/middleware.ts:155 still hard-codes role:'organizer') and DEC-277 (the former src/server/repo/agenda.ts:353 (now split across src/server/repo/agenda/) isValidSlotInput still range-free) have NOT. This is the fourth consecutive round in which main is provably mid-flight at planning time, because the harness's merge train lags roughly one wave: DEC-270's step 3 ('if any post-S commit touches a product path, STOP') can therefore never be satisfied, and it is the only reason three substantively-green battery runs were discarded. Amend exactly that step: each lane still verifies inside `git worktree add --detach <scratch> <S>` at the planner-declared 40-char literal and still prints `FROZEN SHA: <S>` verbatim (DEC-270 steps 1,2,4 unchanged and binding), but it finishes by running `git log --oneline <S>..refs/heads/main -- src app migrations scripts test` and recording the output verbatim under a `POST-S DELTA` heading. Empty delta: the section's verdict stands. Non-empty: the lane re-checks only those of its own claims whose cited file appears in the delta, at the tip, and records the re-check. Exit criterion becomes substance, not bookkeeping: all eight sections log OPEN ITEMS: 0 and every POST-S DELTA is empty or re-checked green. Stage 1 is declared at the tip sha the last section observed.
