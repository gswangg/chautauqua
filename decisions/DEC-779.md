# DEC-779: A separator earns its place only when both sides exist

VERIFIED at app/src/pages/Overview.tsx:331: `{row.speakerName} · {row.trackName ?? row.format} · {row.ref} · …` prints "Marcus Okafor · · SES-002 · waiting 120 days" whenever a triage row has neither a track nor a format -- and src/server/repo/overview/types.ts:59 documents that `format` is ALWAYS null today, so the empty slot is the normal case, not an edge. The same shape produces "· min ·" on unplaced rows (types.ts:115, durationMin always null). Decision: ONE pure joiner in app/src/pages/overview/rows.ts -- `joinSegments(parts: (string | null | undefined)[]): string` dropping empty/blank segments -- owns every dot-joined caption on the Overview, and no row template concatenates its own separators. Punctuation is a claim that there is something on both sides of it.
