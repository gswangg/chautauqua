# DEC-229: deleteTrack extends its 409-never-cascades guard to form.tracks_json, evaluation_plan filters, and plan_reviewer.track_id references

Review-lens verified live: src/server/repo/events.ts:265-288 refuses deletion only for submission references, so deleting an offered-but-unsubmitted-to track leaves resolveOfferedTrackIds returning stale ids (src/lib/submit-core.ts:58-66) — /submit/<event> can render zero selectable tracks while validateTrackChoice still demands one — and reviewer track scopes silently empty. Consistent with the existing fail-loudly '409 conflict (never cascades)' contract, deleteTrack must ALSO throw ApiError('conflict', ...) when the track id appears in (a) any of the event's form.tracks_json offered lists, (b) any of the event's evaluation_plan.filters_json track filters — parsed with the same canonical helpers the review scope logic uses, never an ad-hoc re-parse, or (c) any plan_reviewer.track_id scope row for the event's plans. Never cascade and never silently clean references: the organizer must detach the track from forms/plans/reviewer scopes first. Route layer unchanged — ApiError conflict already maps to 409.
